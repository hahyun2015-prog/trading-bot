# KOSPI200 선물 일봉 과거 데이터 확보 가능성 조사

작성일: 2026-08-13
범위: **조사 및 가능성 확인만.** 대량 수집·계좌 API·시세 API 실호출 **전부 미실행**. DB 쓰기 없음. 프로세스 미접촉.

전제 문서: `일봉스윙_신호스크리닝_20260813.md`

---

## 0. 결론 요약 (먼저)

| 항목 | 판정 |
|---|---|
| 10년치 선물 일봉 확보 가능성 | **가능성 높음. 단 미검증** — 키움 `opt50030` TR이 존재하는 것은 확인, 소급 깊이는 **실호출 전까지 확인 불가** |
| 가장 현실적인 경로 | 키움 OpenAPI+ `opt50030`(선물옵션일차트요청), 대표코드 `10100000`/`10500000` |
| 예상 소요 | 호출 3~10회 · 실행 시간 10초 미만 · **매매 영향 사실상 없음** |
| 최대 리스크 | 키움 대표코드의 일봉이 **몇 년까지 소급되는지 불명**. 실측이 유일한 확인 방법 |
| 지수(KOSPI200 현물) 대체안 | **성립함. 단 보조 수단으로만.** 신호 검증엔 충분, 체결·비용 가정은 별도 |
| 현 DB 5분봉으로 연속월물 구성 | **불가.** 개별 월물 union이 195 거래일뿐 |

**핵심**: 이 작업은 난이도가 낮고 비용이 거의 없습니다. 어려운 것은 확보가 아니라, 확보 후 "10년이 정말 서로 다른 레짐을 포함하는가"를 검증하는 일입니다.

---

## 1. 현재 시스템의 데이터 수집 경로 (코드 근거)

### 1.1 경로는 둘. 둘 다 분봉 전용.

| 경로 | 파일 | TR/엔드포인트 | 주기 | 계좌 필요성 |
|---|---|---|---|---|
| 키움 OpenAPI+ | `era/night_futures_data_collector.py` | `opt50029` (선물옵션분차트요청) | **5분봉** | 로그인 필요, 계좌조회 불필요 |
| KIS REST | `kis/kis_night_futures_collector.py` → `kis/inquire_time_fuopchartprice.py` | `FHKIF03020200` `/uapi/domestic-futureoption/v1/quotations/inquire-time-fuopchartprice` | **1분봉** | appkey/secret 토큰만, **계좌번호 불요** |

근거:
- `era/night_futures_data_collector.py:174-179` — `SetInputValue("시간단위","5")` 후 `CommRqData(..., "opt50029", ...)`
- `kis/inquire_time_fuopchartprice.py:79` — `tr_id = "FHKIF03020200"`, 주석에 *"실전계좌의 경우 한 번의 호출에 최대 102건"*
- `kis/kis_night_futures_collector.py:194-215` — `fid_hour_cls_code="60"`(1분), `fid_pw_data_incu_yn="Y"`(과거 포함), 시각을 뒤로 옮기며 반복

**일봉 조회 함수는 코드베이스에 존재하지 않습니다.** 유일한 유사 코드가 하나 있으나 오류입니다 ↓

### 1.2 발견된 버그 — `opt50028`은 일봉이 아닙니다

`ai_trader/data_collector.py:56`:
```python
api.req_historical_data(future_code, "opt50028", "선물일봉차트조회")
```

키움 TR 목록상 실제 매핑:

| TR | 실제 의미 |
|---|---|
| OPT50028 | 선물옵션**틱**차트요청 |
| OPT50029 | 선물옵션**분**차트요청 |
| OPT50030 | 선물옵션**일**차트요청 |

즉 `ai_trader/data_collector.py`는 **일봉을 요청한다고 이름만 붙여 놓고 틱차트를 요청**합니다. 이 스크립트로 뽑은 CSV가 과거에 쓰인 적이 있다면 그 결과는 재확인이 필요합니다. (수정하지 않았습니다 — 이번 세션은 읽기 전용)

출처: [키움증권 open api 호출 TR 리스트](http://blog.hippochart.com/2016/12/open-api-tr.html)

### 1.3 KIS 야간수집기 동작 방식 — 일봉에 그대로 쓸 수 있는가

현재 동작 (`backfill_history`, `kis/kis_night_futures_collector.py:188`):
1. `ka.auth(svr="vps" if use_mock else "prod")` — **기본값이 모의투자(vps)**
2. 현재 시각부터 1시간씩 뒤로 이동하며 `inquire_time_fuopchartprice` 반복 호출
3. 수신분 중 가장 이른 시각 -1초를 다음 기준점으로 삼아 페이징
4. `time.sleep(0.3)` 고정 대기
5. 3회 연속 0건이면 중단

**일봉에 재사용 가능합니다.** 같은 페이징 골격에 엔드포인트만 바꾸면 됩니다. 다만:
- 일봉 엔드포인트는 `inquire-daily-fuopchartprice` (tr_id `FHKIF03020100`)로 추정되나 **로컬 코드에 없고 공개 문서로 확정 못 했습니다 → 확인 필요**
- 모의투자(vps) 서버는 시세 API 지원 범위가 실전과 다를 수 있습니다 → **확인 필요**

---

## 2. 조회 가능 범위 (실호출 없음 · 코드/문서 기반)

### 2.1 키움 OpenAPI+ — 1순위 경로

| 항목 | 값 | 근거 |
|---|---|---|
| TR | `opt50030` 선물옵션일차트요청 | TR 목록 (위 출처) |
| 1페이지 수신량 | **900건** (실측) | `era/night_data_collector.log` — `opt50029` 응답 25회가 전부 "900개 캔들" |
| 연속조회 | `sPrevNext=2`로 지원 | `era/night_futures_data_collector.py:215` |
| 현 코드 페이지 상한 | `MAX_SYNC_PAGES = 10` | 같은 파일 :78 (설정값일 뿐, API 제약 아님) |
| 호출 빈도 제한 | 초당 약 5회 수준 권장 | `ai_trader/kiwoom_api.py:150` 주석 *"1초에 5회 제한"* + 알고랩 가이드 |
| 계좌 필요성 | **로그인만 필요.** 계좌조회 TR 불필요 | `era/night_futures_data_collector.py` 헤더 주석 — 주문/체결 콜백 없이 시세만 수집하는 설계가 이미 검증됨 |

**추정 소급 능력**: 일봉도 900건/페이지라면 1페이지 = **약 3.6년**. 10년치는 **3페이지**. 10페이지면 36년으로 상장 이래 전체를 덮습니다.

⚠️ **이것은 `opt50029`(분봉) 실측을 `opt50030`(일봉)에 유추한 값입니다. 확인 필요.**
⚠️ 더 큰 불확실성: **대표코드 `10100000`이 얼마나 과거까지 일봉을 갖고 있는지**. 키움 서버가 대표코드에 대해 보관하는 이력 깊이는 문서에서 확인하지 못했습니다. 3년일 수도, 20년일 수도 있습니다. **실호출 1회면 즉시 판명됩니다.**

### 2.2 KIS REST — 2순위 경로

| 항목 | 값 | 근거 |
|---|---|---|
| 분봉 엔드포인트(확인됨) | `FHKIF03020200`, **1회 최대 102건** | `kis/inquire_time_fuopchartprice.py` 주석 |
| 일봉 엔드포인트 | `inquire-daily-fuopchartprice` / `FHKIF03020100` 추정 | **확인 필요** — 로컬 코드 없음, 검색으로 확정 실패 |
| 유량 제한 | 실전 **초당 20건**, 모의 **초당 2건** | 알고랩·hky035 자료 |
| 계좌 필요성 | **시세 API는 appkey/appsecret 토큰만.** 계좌번호 불요 | `kis/kis_auth.py:232-275` — `auth()`는 appkey로 토큰만 발급 |

KIS의 **결정적 약점**: KIS 국내선물옵션 시세는 종목코드가 **개별 월물**(예: `101W09`)입니다. 대표코드 개념이 없어 10년치를 만들려면 **월물 40개를 각각 받아 직접 이어붙여야** 합니다. 키움 대표코드보다 작업량이 훨씬 큽니다.

또한 KIS는 **상장 폐지된 과거 월물의 시세를 제공하지 않을 가능성**이 있습니다 → **확인 필요**. 만약 제공하지 않는다면 KIS로는 장기 이력 확보가 원천적으로 불가능합니다.

### 2.3 계좌 로그인 필요성 정리

| | 시세 전용 가능? |
|---|---|
| 키움 | **불가.** OpenAPI+는 영웅문 로그인(공동인증/ID)이 전제. 다만 **계좌 API를 호출하지 않는 시세 전용 프로세스는 이미 구현·검증돼 있음**(`night_futures_data_collector.py`) |
| KIS | **가능.** appkey/appsecret으로 토큰 발급 → 시세 엔드포인트 호출. 계좌번호 미사용 |

---

## 3. 대안 데이터 소스

### 3.1 KOSPI200 **지수**(현물) 일봉 — 매우 쉬움

| 소스 | 소급 | 비고 |
|---|---|---|
| pykrx (`stock.get_index_ohlcv`) | 1990-01-03 기준시점, 실질 **20년 이상** | 파이썬 3줄. 단일 지수 20년치 약 1분 |
| KRX 정보데이터시스템 | 장기 | CSV/EXCEL 수동 다운로드 |
| 키움 `opt20006` 업종일봉조회 | 미확인 | 기존 인프라 재사용 가능 |

**현 venv32에 pykrx 미설치.** `pandas`, `requests`만 존재. 설치 필요.

### 3.2 KOSPI200 **선물** 일봉 — 공개 소스는 제한적

- **KRX 정보데이터시스템**: 파생상품 개별 월물 일별 시세 제공. 다만 **월물별 조회**라 연속월물은 직접 구성해야 하고, 웹 조회 기간 제한 여부 **확인 필요**
- **pykrx**: 주식·지수·ETF 중심. **파생상품 지원 여부 확인 필요** (지원한다는 근거를 찾지 못함)
- **Investing.com**: 코스피200 선물 과거 데이터 페이지 존재. 다만 연속월물 구성 방식이 공개되지 않아 **검증용으로는 부적합**. 교차 확인용으로만

### 3.3 지수로 신호 검증 → 선물로 체결 가정: **성립합니다. 조건부로.**

**성립하는 이유**

KOSPI200 선물과 현물 지수의 **일간 로그수익률 상관은 0.99 이상**입니다. 추세·모멘텀·변동성 돌파 계열 신호는 사실상 동일하게 발화합니다. 신호의 **존재 여부**를 판정하는 목적이라면 지수로 충분합니다.

**과소평가하면 안 되는 한계 4가지**

1. **베이시스 변동** — 선물가 = 지수 × e^((r-d)T). 금리·배당 기대가 흔들리면 선물이 지수와 다르게 움직입니다. 특히 **시장 급락 시 백워데이션 심화**로 선물이 지수보다 더 크게 빠집니다. 하락 신호의 성과가 **지수 기준으로는 과소평가**됩니다. 하락 레짐 검증이 이번 작업의 핵심 목적이므로 이건 정면으로 걸리는 문제입니다.
2. **롤오버 비용** — 지수엔 없습니다. 스윙 보유가 만기를 관통하면 분기당 1회 롤 스프레드가 발생하나 지수 백테스트엔 반영되지 않습니다.
3. **배당 효과** — 지수는 가격지수(배당 미반영), 선물은 배당을 선반영해 할인 거래됩니다. **4월 배당락 전후 구조적 괴리**가 반복 발생하며, 이 시기 신호는 지수-선물 간 성과가 갈립니다.
4. **야간세션** — 현재 선물은 야간거래가 있으나 지수 일봉엔 없습니다. 갭 구조가 다릅니다.

**권고 운용 방식**

> 지수 일봉으로 **신호 후보를 스크리닝**하고, 살아남은 소수만 선물 일봉으로 **재검증**합니다. 지수는 "무엇을 볼지 좁히는 도구"이지 "결론을 내리는 도구"가 아닙니다. 특히 **숏 신호는 반드시 선물로 재검증**해야 합니다.

---

## 4. 연속 월물 구성 문제

### 4.1 현재 DB 실태 (읽기 전용 쿼리 결과)

`futures_data.db` / `futures_ohlcv` (5분봉, 108,465행):

| code | 거래일 | 기간 | 성격 |
|---|---|---|---|
| `10500000` | **387** | 2025-01-10 ~ 2026-08-13 | 미니선물 **대표코드** (라이브 사용) |
| `10100000` | **386** | 2025-01-13 ~ 2026-08-13 | 표준선물 **대표코드** |
| `A0169000` | 150 | 2025-09-15 ~ 2026-07-10 | 개별 월물 |
| `A0566000` | 115 | 2025-12-15 ~ 2026-06-11 | 개별 월물 |
| `A0567000` | 98 | 2026-01-22 ~ 2026-07-09 | 개별 월물 |
| `A0568000` | 95 | 2026-02-13 ~ 2026-08-12 | 개별 월물 |
| `A0569000` | 87 | 2026-03-30 ~ 2026-08-13 | 개별 월물 |
| `A01609`, `A05608` | 27 | 2026-07-13 ~ | KIS 1분봉 (별도 코드체계) |

### 4.2 5분봉으로 연속월물을 만들 수 있는가 — **아니오**

**개별 월물 7종 union = 195 거래일 (2025-09-15 ~ 2026-08-13).** 대표코드 387일보다 **더 짧습니다**.

개별 월물을 이어붙여봐야 대표코드가 이미 제공하는 시계열의 절반을 재현할 뿐입니다. 얻는 게 없습니다.

**대표코드 자체가 이미 연속 시계열입니다.** 키움 대표코드는 최근월물을 자동 승계합니다. 다만 **백조정이 되어 있지 않습니다** — 롤 시점에 월물 간 가격차만큼 인위적 점프가 남아 있습니다.

### 4.3 이어붙이기 3방식 — 신호 검증에 미치는 영향

| 방식 | 정의 | 가격 정합성 | 수익률 정합성 | 지표 영향 |
|---|---|---|---|---|
| **무조정** (현재 상태) | 롤 시점에 그냥 다음 월물로 교체 | 실제 거래가와 일치 ✅ | 롤 시점에 **가짜 수익률 점프** ❌ | ATR·변동성 **과대평가**. 돌파 신호 **오발화** |
| **백조정 (차감식)** | 과거 구간에서 롤 스프레드를 뺄셈 | 과거 가격 왜곡 ❌ | 수익률 정확 ✅ | 장기 소급 시 **음수 가격** 가능. 비율지표(%) 계산 붕괴 |
| **비율조정** | 롤 스프레드를 곱셈 비율로 소급 | 과거 가격 스케일 왜곡 ❌ | 로그수익률 정확 ✅ | 음수 불가. **10년 백테스트에 가장 안전** |

**권고: 비율조정.** 10년 구간에서 분기 롤 40회를 차감식으로 소급하면 초기 가격이 음수로 갈 위험이 실재합니다. 비율조정은 이 문제가 없고, 로그수익률 기반 지표(모멘텀·변동성·ATR%)와 정합합니다.

**단, 원본 무조정 시계열을 반드시 함께 보관**해야 합니다. 손절폭·틱가치·증거금 같은 **절대가격 기준 계산은 무조정 가격으로** 해야 하기 때문입니다.

**롤 규칙도 사전 고정 필요**: 만기 며칠 전 롤할지(예: 만기 3영업일 전 or 거래량 역전일). 이걸 정하지 않으면 결과가 재현되지 않습니다.

### 4.4 전 세션 판정과의 관계

전 세션은 대표코드 롤 불연속을 검사해 "2025-03~2025-12 구간은 롤 불연속 없음, 2026-03(0.070)·2026-06(0.097)은 큼"이라 판정했습니다. **10년으로 늘리면 롤 40회를 검사하게 되므로 이 판정은 처음부터 다시 해야 합니다.**

---

## 5. 확보 계획 제안

### 5.1 권고 경로 — 키움 `opt50030` 단독

이유:
- 대표코드가 **연속 시계열을 이미 제공** → 월물 40개 이어붙이기 불필요
- 시세 전용·주문 미포함 수집기가 **이미 존재하고 실전 검증됨** (`night_futures_data_collector.py`)
- 900건/페이지 → 10년치가 **3페이지**. 총 호출 3~10회, 실행 10초 미만

### 5.2 작업 순서

| # | 작업 | 비고 |
|---|---|---|
| 0 | **탐침 1회** — `opt50030` / `10100000` / 1페이지만 호출, DB 쓰기 없이 표준출력으로 최고/최저 날짜만 확인 | **여기서 소급 깊이가 판명됩니다.** 이후 계획 전부가 이 결과에 달려 있음 |
| 1 | 결과 분기 — 10년 이상 나오면 진행. 3년 미만이면 **키움 경로 폐기**, 3.3절 지수 대체안으로 전환 | |
| 2 | 신규 테이블 `futures_ohlcv_1d` 생성 (**기존 테이블 절대 미접촉**) | `code, date(YYYYMMDD), open, high, low, close, volume, oi` |
| 3 | `10100000`, `10500000` 양쪽 수집 | 대조군 확보 |
| 4 | 롤 불연속 스캔 → 비율조정 시계열을 **별도 테이블**로 생성 | 원본 보존 |
| 5 | 레짐 구성 검증 — 하락장·횡보장이 실제로 포함됐는지 정량 확인 | 6절 |
| 6 | 지수(pykrx) 20년치를 **교차검증용**으로 병행 확보 | 선물 데이터 무결성 검증에 필수 |

### 5.3 수집 시 주의점

**API 호출 제한**
- 키움: 초당 5회 이하. 페이지 간 `QTest.qWait(500)` 유지 (`ai_trader/kiwoom_api.py:151` 기존 패턴)
- 총 호출 10회 미만이므로 제한에 걸릴 여지 자체가 거의 없음

**장중 수집 금지**
- 장 시간(08:45~15:45) 및 야간세션 중 수집 금지
- **키움 OpenAPI+는 프로세스별 로그인 세션이 별개가 아닙니다.** 동일 계정 동시 로그인 시 기존 세션이 끊길 수 있고, 그러면 **ERA가 시세를 잃습니다.** 이게 이번 작업의 **최대 실질 리스크**입니다

**기존 수집기와의 충돌**
- `night_futures_data_collector.py`는 포트 9992, `era_order_manager.py`는 9991로 중복 실행을 막습니다 → 신규 수집기는 **제3의 포트**(예: 9993) 사용
- KIS 야간수집기와 키움은 별개 인증이라 충돌 없음

### 5.4 실계좌 시스템 보호 — 필수 조건

현재 **ERA 가동 중 · 실계좌 LONG 1계약 보유**. 다음을 전부 만족해야 실행 가능합니다.

1. **ERA 완전 종료 후, 또는 포지션 청산 후에만 수집기 기동.** 동시 실행 금지 — 키움 세션 경합 때문
2. 대안: **KIS 경로 사용.** KIS는 REST + 별도 appkey라 키움 세션과 무관합니다. ERA 가동 중에도 안전합니다. 단 4.2절대로 월물 이어붙이기 부담을 감수해야 합니다
3. 수집 코드에 `SendOrder`/`SendOrderFO` **문자열이 단 한 줄도 없을 것** — 기존 야간수집기와 동일 원칙
4. `futures_ohlcv`, `futures_ohlcv_1m` **쓰기 금지**. 신규 테이블만
5. `config/active_strategy.json`, `era/futures_exit_state.json`, `tca/system_status_futures.json` 미접촉
6. 주말 또는 야간세션 종료 후(익일 06:00 이후) 실행

> **가장 안전한 순서**: 주말에 ERA 정지 상태에서 키움 탐침 1회 → 소급 깊이 확인 → 충분하면 그 자리에서 3페이지 수집 완료. 총 소요 5분.

---

## 6. 확보 후 무엇이 달라지는가

### 6.1 다시 판정해야 하는 결론 목록

| # | 현재 결론 | 재판정 필요 사유 |
|---|---|---|
| 1 | **하락 신호 8종 발화 0회** (`MA_align_dn`, `NEWLOW`, `MOM120_neg`, `MAX_50_200_dn`, `MAX_20_60_dn`, `MA60_stretch_dn`, `MA60_revert_dn`, `MA20_slope_dn`) | 하락장이 포함되면 **처음으로 측정 가능해집니다.** 가장 큰 변화 |
| 2 | **후보 61종 중 31종 발화 20회 미만 → 측정 불가** | 표본 6.5배 증가로 대부분 측정 가능 구간 진입 |
| 3 | **생존 신호 0개** | 위 2건이 바뀌므로 전면 재실행 |
| 4 | **상위 2개가 달력 더미(월말·금요일)** | 강세장 1.5년에서만 성립한 우연일 가능성 높음. 10년에서 재검정하면 대부분 소멸할 것으로 예상 |
| 5 | **IS 중앙 ATR 7.16 → OOS 52.51 (7.3배)** | 변동성 레짐 정의 자체를 다시 세워야 함 |
| 6 | **롤 불연속 없음 (2025-03~2025-12)** | 롤 6회 → 40회. 4.4절 |
| 7 | **연율 변동성 46.8% · 상승일 61.7%** | 1.5년 강세장 특성치. 10년 기준으로 재산출 |
| 8 | **"스윙 전환은 통계적 입증 가능성을 h배 악화"** (8.3절 결론) | 이 논증은 표본 385일 전제입니다. 2,450일이면 **h=20에서도 122개 독립 관측**이 나옵니다. **결론이 뒤집힐 수 있습니다** |
| 9 | 5분봉 기반 기존 전략 검증 전반 | 5분봉 자체는 여전히 1.5년. 일봉 10년으로 **레짐 라벨을 만들어** 5분봉 결과를 레짐별로 재해석하는 길이 열립니다 |

**단, 재판정되지 않는 것**: 7.6절의 **계좌 규모 제약(414만원)**. 이건 데이터 문제가 아니라 자본 문제입니다. 10년치를 확보해도 그대로 남습니다.

### 6.2 "단일 강세 레짐" 문제는 얼마나 해소되는가

2016~2026 구간에 포함되는 주요 레짐 (일반 지식 기준, **확보 후 데이터로 검증 필요**):

| 시기 | 성격 |
|---|---|
| 2016~2017 | 완만한 상승 |
| 2018 | **하락** (미중 무역분쟁) |
| 2019 | 횡보 |
| 2020-02~03 | **급락** (코로나) — 사상급 변동성 |
| 2020-04~2021 | 급등 |
| 2022 | **본격 하락장** (금리인상) |
| 2023~2024 | 횡보~완만 |
| 2025~2026 | **강세** (현 보유 표본) |

**하락장 2회 이상, 횡보장 2구간 이상, 극단 변동성 1회가 포함됩니다.** 전 세션에서 제기된 "숏 신호를 측정할 표본 자체가 없다"는 문제는 **해소됩니다.**

**정량**: 385 거래일 → 약 **2,450 거래일 (6.4배)**. 스윙 h=20 기준 독립 관측이 19개 → 122개.

### 6.3 정직한 유보

- 122개 독립 관측도 **다중검정 61종에는 여전히 빠듯합니다.** Bonferroni 보정 시 유의수준 0.0008 — 이 문턱을 넘는 신호가 나올 확률은 여전히 낮습니다. 10년치가 "판정 가능"을 보장하지는 않고, **"판정 불가"에서 "판정 가능성 있음"으로 옮길 뿐**입니다
- 코스피200 선물은 2010년대 중반 **거래단위 변경·야간거래 도입** 등 시장구조 변화가 있었습니다. 10년 전 데이터와 현재 데이터의 미시구조는 동일하지 않습니다
- 오래된 데이터일수록 **현재 시장에 대한 예측력은 감소**합니다. 2018년 하락장에서 발화한 신호가 2026년에도 유효하다는 보장은 없습니다

---

## 7. 확인 필요 항목 (추측하지 않은 것들)

| # | 항목 | 확인 방법 |
|---|---|---|
| 1 | `opt50030` 1페이지 수신 건수 | 실호출 1회 |
| 2 | 키움 대표코드 `10100000` 일봉 **최대 소급 연도** | 실호출 (연속조회 끝까지) |
| 3 | `opt50030` 입력 파라미터 규격 (기준일자·수정주가구분 등) | KOA Studio |
| 4 | KIS 일봉 엔드포인트 정확한 경로/tr_id | KIS 개발자센터 |
| 5 | KIS가 **상장폐지 과거 월물** 시세를 제공하는지 | KIS 개발자센터 또는 실호출 |
| 6 | KIS 모의(vps) 서버의 선물 시세 API 지원 범위 | 실호출 |
| 7 | pykrx의 **파생상품** 지원 여부 | 설치 후 확인 |
| 8 | 키움 `opt20006` 업종일봉으로 KOSPI200 지수 소급 가능 범위 | 실호출 |
| 9 | KRX 정보데이터시스템 파생 일별시세 **조회 기간 상한** | 사이트 직접 확인 |

---

## 부록 — 이번 세션에서 실행한 것 / 하지 않은 것

**실행**: 로컬 파일 읽기, `futures_data.db`/`unified_data.db` **읽기 전용(`mode=ro`) 쿼리**, 웹 검색

**미실행**: 주문 API · 계좌 API · 시세 API 실호출 · DB 쓰기 · 프로세스 기동/종료 · 금지 파일 수정 (`era/era_order_manager.py`, `indicators.py`, `bqa/kalman_backtester.py`, `config/active_strategy.json`, `era/futures_exit_state.json`, `tca/system_status_futures.json`) · TCA/KIS 야간수집기 접촉

---

## 출처

- [키움증권 open api 호출 TR 리스트 공유 — Hippochart](http://blog.hippochart.com/2016/12/open-api-tr.html) (OPT50028/50029/50030 매핑)
- [키움 REST API 자동매매 완전 가이드 2026 — 알고랩](https://algolab.co.kr/blog/kiwoom-rest-api-algotrading-guide-2026) (TR 호출 제한)
- [쓰로틀링을 통한 한국투자증권 API 호출 유량 제한 정책 대응](https://hky035.github.io/web/kis-api-throttling/) (KIS 실전 초당 20건 / 모의 초당 2건)
- [koreainvestment/open-trading-api — GitHub](https://github.com/koreainvestment/open-trading-api) (KIS 국내선물옵션 예제 위치)
- [sharebook-kr/pykrx — GitHub](https://github.com/sharebook-kr/pykrx) · [국내 지수 데이터 가져오기 (PyKrx)](https://wikidocs.net/226894)
- [KRX 정보데이터시스템](https://data.krx.co.kr/)
- 로컬 코드: `era/night_futures_data_collector.py`, `kis/kis_night_futures_collector.py`, `kis/inquire_time_fuopchartprice.py`, `kis/kis_auth.py`, `ai_trader/kiwoom_api.py`, `ai_trader/data_collector.py`
- 로컬 로그: `era/night_data_collector.log` (900건/페이지 실측)
- 로컬 DB: `futures_data.db` (읽기 전용)
