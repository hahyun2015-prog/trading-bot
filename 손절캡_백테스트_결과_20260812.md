# 손절 캡(SL Hard Cap) 백테스트 결과 — 라이브 SAR 설정 기준

작성: 2026-08-12 · ERA 미기동 · 라이브 코드 무수정 (백테스트만 실행)
스윕 스크립트: `scratch/slcap_sweep_20260812.py` (조합당 JSON 1개, `scratch/_slcap/`)

> **결론 1: 손절 캡을 씌우면 성과가 나빠진다. 좁게 씌울수록 더 나빠진다. 권고 = 캡 도입하지 말 것.**
> **결론 2: 그보다 큰 문제를 발견했다 — 현재 라이브 SAR 설정은 백테스트상 PF 0.99로 돈을 벌지 못한다.**
> **결론 3: 단, 이 백테스트는 봉 단위 SAR이고 라이브는 틱 단위다. 둘은 다른 전략이다(§5).**

---

## 1. 무엇을 물어봤나

라이브 SAR 경로의 손절폭은 `era_order_manager.py:4431` 한 줄로 정해진다.

```python
elif is_sar:
    sl_limit = max(getattr(self, "futures_atr_14", 5.0) * 1.0, 2.0)     # 상한 없음
```

바로 위 kalman 분기에는 상한이 있다.

```python
if is_kalman:
    sl_limit = ...
    sl_limit = min(sl_limit, self._effective_sl_hard_cap(c_std))         # 상한 있음
```

즉 **SAR 경로에만 상한이 빠져 있고**, 그 결과 ATR14가 73.93pt면 손절폭도 73.93pt가 된다
(미니선물 계약당 약 370만원). 질문은 "SAR에도 같은 상한을 달아야 하는가"였다.

백테스터 `scratch/backtest_sar_bb_20260809.py` 의 `sl_hard_cap_pt` 가 정확히 이 변경을
모사한다 — `max(atr14*mult, 2.0)` 계산 후 `min(v, cap)`. 라이브에 캡을 다는 것과 동일하다.

---

## 2. 기준선 — 라이브 설정 그대로

`config/config.json` + `config/config_local.json` (2026-08-12) 에서 매핑:

| 인자 | 값 | 출처 |
|---|---|---|
| `strategy` | `sar` | `futures_strategy_type` (local 오버라이드) |
| `Q` / `R` / `mult` | 0.00005 / 1.0 / 0.6 | `kf_q` / `kf_r` / `kf_mult` |
| `atr_cutoff` | **15.0** | config_local |
| `min_std_error_entry` | 1.5 | config |
| `trim_std_outliers` | 1 | config |
| `sar_af_init/step/max` | 0.02 / 0.02 / **0.10** | era 기본 + config |
| `ma_filter_period` | 200 | config |
| `consecutive_loss_limit` | 5 | config |
| `reentry_k` | 0.25 | config |
| `sar_sl_mult` | 1.0 | era 식 `ATR×1.0` |
| `point_value` | 50,000 | 미니선물 |

데이터: `10500000` 5분봉 31,793개 (2025-01-10 ~ 2026-08-12).

---

## 3. ⚠️ 7분기 교차검증이 불가능하다 — `atr_cutoff=15.0` 때문

먼저 확인해야 했던 것: **ATR이 분기마다 완전히 다른 세계다.**

| 분기 | ATR14 중앙값 | 종가 중앙값 | ATR/가격 | ATR<15인 날 |
|---|---:|---:|---:|---:|
| 2025-Q1 | 5.1pt | 339 | 1.5% | **51/51** |
| 2025-Q2 | 6.1pt | 347 | 1.8% | **60/60** |
| 2025-Q3 | 6.9pt | 432 | 1.6% | **65/65** |
| 2025-Q4 | 13.0pt | 566 | 2.3% | 38/59 |
| 2026-Q1 | 26.3pt | 781 | 3.4% | 11/59 |
| 2026-Q2 | 49.6pt | 1,179 | 4.2% | 0/61 |
| 2026-Q3 | **92.5pt** | 1,075 | **8.6%** | 0/30 |

가격이 3배 오르는 동안 ATR은 **18배** 올랐다. 비율로 봐도 1.5% → 8.6%로 약 6배다.
데이터를 5분봉까지 열어 확인한 결과 월물 교체나 데이터 오류가 아니라 **실제 시장 움직임**이다.

그 결과 `atr_cutoff=15.0`(ATR 15pt 미만이면 진입 차단)이 2025년 전체를 막는다.

```
2025-Q1  거래 0건      ← 진입 차단
2025-Q2  거래 0건      ← 진입 차단
2025-Q3  거래 0건      ← 진입 차단
2025-Q4  거래 31건
2026-Q1  거래 101건
2026-Q2  거래 107건
2026-Q3  거래 46건
```

**현재 설정으로는 7분기 교차검증을 할 수 없다.** 실질 검증 구간은 2025-Q4 이후 4분기뿐이며,
그나마 2025-Q4는 31건으로 표본이 얇다. 이건 캡과 무관한, 설정 자체의 한계다.

---

## 4. 스윕 결과 — 캡은 전 구간에서 해롭다

### 4-1. 전체기간

| 캡 | 거래 | PF | 수익률 | MDD | 최악 단일손실 |
|---|---:|---:|---:|---:|---:|
| **없음 (현행)** | 271 | **0.989** | **-1.87%** | **39.16%** | -75.05pt |
| 60pt | 273 | 0.950 | -8.74% | 39.16% | -75.05pt |
| 40pt | 277 | 0.898 | -19.30% | 45.58% | -75.05pt |
| 30pt | 282 | 0.917 | -15.40% | 44.54% | -75.05pt |
| 20pt | 298 | 0.797 | -43.45% | 66.59% | -75.05pt |
| 15pt | 316 | 0.823 | -37.74% | 57.08% | **-41.15pt** |
| 10pt | 347 | 0.799 | -44.52% | 66.68% | **-41.15pt** |
| 5pt | 393 | 0.785 | -47.50% | 68.00% | **-41.15pt** |

**캡을 좁힐수록 거래는 늘고(271→393) 성과는 나빠진다.** 예외 없이 단조롭다.

캡이 실제로 사주는 것은 **최악 단일손실뿐**이다 (-75pt → -41pt, 캡 15pt 이하에서).
그 대가로 PF가 0.989 → 0.82로, 수익률이 -1.9% → -37.7%로, MDD가 39% → 57%로 나빠진다.
**MDD가 같이 나빠진다는 게 핵심이다** — "한 방을 줄이려다 여러 번 깎여 총낙폭이 커진다".

### 4-2. 분기별 PF (굵게 = 그 분기 최고)

| 캡 | 2025-Q4 | 2026-Q1 | 2026-Q2 | 2026-Q3 |
|---|---:|---:|---:|---:|
| **없음** | 0.764 | **0.785** | **0.836** | **1.470** |
| 60pt | 0.764 | 0.785 | 0.836 | 1.282 |
| 40pt | 0.764 | 0.785 | 0.748 | 1.238 |
| 30pt | 0.764 | 0.785 | 0.751 | 1.351 |
| 20pt | 0.764 | 0.705 | 0.659 | 1.301 |
| 15pt | 0.764 | 0.661 | 0.698 | 1.274 |
| 10pt | 0.656 | 0.647 | 0.703 | 1.229 |
| 5pt | **0.768** | 0.671 | 0.646 | 1.138 |

- 2025-Q4는 ATR이 13pt대라 캡 15pt 이상은 **아예 걸리지 않는다**(값 동일). cap5의 0.768은
  0.764 대비 차이가 미미하고 거래 38건짜리라 잡음 수준이다.
- 2026-Q1/Q2/Q3 **전부 "캡 없음"이 최고**다.

### 4-3. 왜 이런가

메커니즘은 단순하다. 손절을 좁히면 **되돌아왔을 거래를 미리 끊는다.** 끊긴 자리에서 재진입하니
거래 수가 늘고(수수료·슬리피지도 늘고), 이길 거래를 못 이긴다. SAR 트레일링이 이미
청산을 담당하는 구조라 별도 손절이 좁게 끼어들면 트레일링이 일할 기회를 뺏는 셈이다.

---

## 5. ⚠️ 이 결과를 라이브에 그대로 대입하면 안 되는 이유

**백테스터는 봉 단위 SAR, 라이브는 틱 단위 SAR이다.**

```python
# 백테스터 (backtest_sar_bb_20260809.py:258)
sar.on_bar(c_close, c_close, clamp=peak_price)   # 5분봉마다 1회

# 라이브 (era_order_manager.py:4461 → _sar_tick)
self._sar_tick(current_price, self.futures_day_peak)   # 틱마다
```

회전율로 확인된다 — 백테스트 271건 / 약 200거래일 = **1.4건/일**, 라이브 실측 **22건/일**.
**약 16배 차이**로, 기존에 보고된 18배와 일치한다.

즉 §4의 결론은 **"봉 단위 SAR에서는 캡이 해롭다"** 까지만 말해준다. 라이브의 틱 단위 SAR은
손절이 걸리기 전에 SAR이 훨씬 빠르게 따라붙으므로 캡의 영향이 다를 수 있다.

**그래서 순서가 뒤바뀌어 있다.** 캡을 정하기 전에 `on_tick` / `on_bar` 를 먼저 맞춰야 한다.
그 전에는 **어떤 백테스트도 라이브 손익을 예측하지 못한다.**

---

## 6. 그 외 확인한 것

`atr_cutoff` 를 낮춰 2025년까지 거래를 열어보면:

| atr_cutoff | 거래 | PF | 수익률 | MDD |
|---|---:|---:|---:|---:|
| 15.0 (현행) | 271 | 0.989 | -1.87% | 39.16% |
| 5.0 | 382 | 0.959 | -7.67% | 42.24% |
| 0.5 | 391 | 0.964 | -6.76% | 41.48% |

현행 15.0이 셋 중 가장 낫지만 **어느 값도 PF 1.0을 넘지 못한다.**
진입 차단선을 조정해서 해결될 문제가 아니라는 뜻이다.

---

## 7. 권고

| # | 권고 | 근거 |
|---|---|---|
| 1 | **손절 캡을 도입하지 말 것** | 전체기간·2026 전 분기에서 캡 없음이 최고. 캡은 최악손실만 줄이고 PF·수익률·MDD를 모두 악화시킴 |
| 2 | **`on_tick` → `on_bar` 정합을 먼저 처리할 것** | 회전율 16배 차이. 이게 남아 있는 한 백테스트로 라이브를 판단할 수 없다 |
| 3 | **재기동 전 이 설정의 수익성을 재검토할 것** | PF 0.989 / 4분기 중 3분기 PF<1 / MDD 39%. 지표 단일화와 무관한 별개 문제지만, 켜기 전에 알고 켜야 한다 |
| 4 | 캡이 꼭 필요하다면 **고정 pt가 아니라 변동성 비례로** | ATR이 5pt→92pt로 18배 움직인 구간이라 고정 pt 캡은 분기마다 전혀 다른 강도로 작동한다. kalman 경로가 이미 쓰는 `clamp(2.0×std_error, 8, 30)` 방식이 형태상 맞다 |

### 이 검증의 한계 (정직하게)

- 봉 단위 SAR 기준이다 (§5). 라이브와 다르다.
- `sl_hard_cap_pt` 는 `sl_limit` 만 제한하고 **SAR 트레일링 시작 거리(`ATR×1.0`)는 제한하지 않는다.**
  라이브에 캡을 달아도 동일하다. "SAR 시작 거리까지 함께 캡" 하는 변형은 시험하지 않았다.
- 변동성 비례 캡(권고 4)은 백테스터에 해당 인자가 없어 시험하지 못했다. 고정 pt 캡만 시험했다.
- 실질 표본은 4분기이며 2025-Q4는 31건으로 얇다 (§3).
- 슬리피지 0.25pt 가정. 실계좌에서 더 나쁘면 전 조합이 함께 나빠진다.

---

## 부록. 산출물

| 경로 | 내용 |
|---|---|
| `scratch/slcap_sweep_20260812.py` | 스윕 러너 (조합당 JSON 1개, 재개 가능) |
| `scratch/_slcap/cap{none,5,10,15,20,30,40,60}_{분기}.json` | 조합별 원자료 45개 |

재현:

```bash
python3 scratch/slcap_sweep_20260812.py none,5,10,15,20,30,40,60 2025-Q4,2026-Q1,2026-Q2,2026-Q3,ALL
```
