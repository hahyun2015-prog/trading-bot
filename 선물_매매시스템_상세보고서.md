# AMATS 선물 매매 시스템 상세 운영 보고서 (Kalman 전략 기준)

본 보고서는 AMATS 통합 매매 엔진(era/era_order_manager.py)에 반영되어 실전 및 모의투자에 적용 중인 **칼만 하이브리드 전략(Kalman Hybrid Strategy)**의 진입타점, 손익절, 청산 로직 및 휩소 방지 필터에 대한 상세 구조를 분석하고 기록한 문서입니다.

---

## 1. 실시간 진입타점 (Entry Targets) 및 추세 필터

선물 진입 타점은 실시간 분봉 종가 데이터를 활용한 **단기 칼만 필터 예측값**과 **장기 추세 필터(60분봉 리샘플링)**를 결합하여 산출합니다.

### 1) 단기 칼만 필터 및 예측 타점 (5분봉 기준)
*   **분석 데이터**: 최근 300개의 5분봉 종가 데이터를 조회하여 사용합니다.
*   **예측 수식**: 아래의 1차원 칼만 필터 예측 모델을 따릅니다.
    *   *상태 전이 잡음 공분산 (Q)*: `0.0001`
    *   *측정 잡음 공분산 (R)*: `0.5`
*   **잔차 표준편차 (Std Error)**: 최근 20개 분봉의 실제 종가와 칼만 예측값(`kf_price`) 간 오차의 표준편차를 실시간 연산하여 시장 변동성을 측정합니다.
*   **최종 진입 타점 산출**:
    *   **LONG (매수) 진입 타점** = `kf_price` + `std_error` * `kf_mult`
    *   **SHORT (매도) 진입 타점** = `kf_price` - `std_error` * `kf_mult`
    *   *(시스템에 적용된 기본값: `kf_mult = 1.0`)*

### 2) 장기 추세 필터 (60분봉 리샘플링 칼만 필터)
단기 진입 방향이 장기 추세와 역행하여 발생할 수 있는 손실을 방지하는 추세 필터입니다.
*   **구조**: 5분봉 데이터를 60분봉으로 리샘플링한 뒤, 장기 칼만 필터($Q_{long} = 0.001$, $R_{long} = 1.0$)를 적용합니다.
*   **추세 판정**: 직전 봉 대비 장기 칼만값의 기울기(Slope)를 분석합니다.
    *   `Slope` > 0.01 ➡️ **`UP` (상승세)**
    *   `Slope` < -0.01 ➡️ **`DOWN` (하락세)**
    *   그 외 ➡️ **`NEUTRAL` (중립)**
*   **진입 제한 필터링**:
    *   장기 추세가 **`DOWN`**일 경우: **LONG(매수) 진입 신호를 완전히 차단**합니다.
    *   장기 추세가 **`UP`**일 경우: **SHORT(매도) 진입 신호를 완전히 차단**합니다.

### 3) 초저변동성 진입 차단 (ATR Cutoff)
*   시장의 변동성이 극도로 수축한 구간에서의 휩소를 방지하기 위한 필터입니다.
*   14일 ATR($\text{ATR}_{14}$) 값이 **`0.50pt` 미만**으로 하락할 경우 신규 진입을 일절 차단합니다.

---

## 2. 손익절 (Stop Loss & Take Profit) 및 청산 로직

포지션 진입 후 자산 보호 및 이익 극대화를 위해 삼중 청산 시스템(손절, 익절, 트레일링 스탑)이 실시간 작동합니다.

| 청산 유형 | 핵심 작동 조건 | 오늘 미니선물 SHORT 적용 사례 (평단: 1332.44) |
| :--- | :--- | :--- |
| **동적 손절 (Stop Loss)** | 하이브리드 동적 손절 제한 폭 적용<br>$\text{SL} = \max(\min(5.0 \times \text{std\_error}, 1.2 \times \text{ATR}_{14}), \text{Floor})$<br>*(단, $\text{Floor} = \max(0.5 \times \text{ATR}_{14}, 2.0)$)* | 당일 $\text{ATR}_{14}$가 크게 잡혀 넓은 변동성 허용폭 형성 |
| **동적 익절 (Take Profit)** | 동적 3-Sigma 상하단 밴드 도달 시 즉시 청산<br>• LONG 익절: $\text{kf\_price} + 3.0 \times \text{std\_error}$<br>• SHORT 익절: $\text{kf\_price} - 3.0 \times \text{std\_error}$ | SHORT TP 익절선이 실시간 하강 추적됨 |
| **이익보전 (Trailing Stop)** | 1. **활성화**: 진입 후 수익이 $1.5 \times \text{std\_error}$ 이상 벌어질 때 활성화<br>2. **트리거**: 최고 수익(피크 가격) 대비 $0.5 \times \text{std\_error}$ 만큼 반등/반락 시 청산 | 피크 가격(1325.40) 대비 반등 발생하여 **1328.60pt**에 청산 신호 발동 (체결가 1328.14pt, **+4.30pt 익절**) |
| **당일 강제청산 (Force Close)** | 주간 세션 종료 직전(15:35 이후) 미청산 잔고 존재 시 시장가 일괄 청산 | 오늘 장중 이익보전 청산이 완료되어 해당 사항 없음 |

---

## 3. 휩소 방지 및 위험 관리 필터 (Protection Shields)

### 1) 휩소 방지 재진입 차단 필터 (Whip-saw Re-entry Block)
청산 직후 가격이 좁은 박스권에 갇혀 잦은 진입과 청산이 반복되는 현상(수수료/슬리피지 갉아먹기)을 원천 차단합니다.
*   **차단 기준폭**: $\text{breakout\_unit} = \text{ATR}_{14} \times 0.35$ (K값 `best_k` 반영)
*   **차단 범위 설정**:
    *   **LONG 진입 차단**: $[\text{last\_exit} - 0.5 \times \text{breakout\_unit}, \; \text{last\_exit} + 0.2 \times \text{breakout\_unit}]$
    *   **SHORT 진입 차단**: $[\text{last\_exit} - 0.2 \times \text{breakout\_unit}, \; \text{last\_exit} + 0.5 \times \text{breakout\_unit}]$
*   **동작 방식**: 직전 청산이 발생하면 즉시 위 범위가 설정되며, 가격이 이 범위 내에 머무르는 동안에는 진입 조건이 충족되어도 **신규 진입이 차단**됩니다. 가격이 범위를 확실히 벗어나야만 차단이 해제됩니다.
*   *오늘 적용 사례: 1328.14pt 청산 직후 `[1318.95 ~ 1351.10]` 차단 범위가 설정되어 이후의 모든 SHORT 재진입 신호를 차단, 휩소 피해를 막아냈습니다.*

### 2) 일일 연속 손실 차단 (Daily Circuit Breaker)
*   칼만 전략으로 거래 시 당일 **연속 손실 횟수(Consecutive Losses)가 3회**에 도달하면, 당일의 모든 신규 선물 진입을 즉시 전면 차단합니다.
*   이 장치는 추세 추종 전략이 극단적인 횡보/휩소 장세를 만났을 때 계좌 자산이 손실되는 것을 제한하는 최종 방어선입니다.

### 3) 일일 거래 횟수 제한 (Daily Trade Limit)
*   `config.json` 및 `active_strategy.json`에서 파싱된 `max_trades_day` 및 `max_trades_night`에 따라 진입 횟수를 제어합니다 (현재는 999회 무제한 설정).
