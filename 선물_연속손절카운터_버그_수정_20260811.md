# 선물 — 연속손절 카운터 버그 수정 (실거래 영향)

작성 2026-08-11 | 대상 `era/era_order_manager.py` | 계정 모의투자

---

## 0. 한 줄 요약

SAR 청산 경로 두 곳에서 **이익이 나도 연속손절 카운터가 리셋되지 않았다.**
'연속 손절'이 아니라 '당일 누적 손실 횟수'로 동작했다.
같은 날 진입게이트를 전 전략 공통으로 바꿔 이 카운터가 실제로 진입을 막게 됐으므로,
**2026-08-12 개장부터 처음 충돌할 상황이었다.**
오늘 실거래로 재현하면 하루치 영향이 **375만원**이다.

---

## 1. 발견 경위

계좌 점검 중 `era/futures_exit_state.json`에서 불일치를 봤다.

```json
"day_consecutive_losses": 6,
"day_trade_count": 22
```

`day_consecutive_losses = 6`인데 로그의 마지막 거래들은 대부분 이익이었다.
연속 손절이라면 마지막이 이익인 순간 0이어야 한다.

오늘 실거래 22건을 집계하니 **손실이 정확히 6건**이었다. 카운터가 '연속'이 아니라 '누적'이었다.

---

## 2. 원인

[era_order_manager.py:4436, 4632](era/era_order_manager.py#L4436) — SAR LONG/SHORT 청산 경로:

```python
self.futures_day_consecutive_losses += (1 if realized_pnl < 0 else 0)
```

이익일 때 **0을 더한다.** 리셋이 아니다. 따라서 카운터는 하루 종일 단조 증가한다.

다른 청산 경로는 모두 올바른 관용구를 쓰고 있었다([4505행](era/era_order_manager.py#L4505) 샹들리에):

```python
if realized_pnl < 0:
    self.futures_day_consecutive_losses += 1
    if ... >= limit: 정지
else:
    self.futures_day_consecutive_losses = 0
```

### 전수 감사

| 경로 | 상태 |
|---|---|
| **SAR LONG 청산** (4436) | **깨짐** |
| **SAR SHORT 청산** (4632) | **깨짐** |
| SAR ATR손절 (4446·4642) | 정상 |
| 샹들리에 (4505·4701 등) | 정상 |
| 칼만 3-시그마 익절 (4543) | 정상 |
| BB 손절/익절 (4458·4469) | 정상 |
| 고정 손절·트레일링 | 정상 |
| 야간 전 경로 (5126~5318) | 정상 |

**깨진 곳은 2곳뿐이고, 둘 다 배포 중인 SAR 전략의 주 청산 경로였다.**

---

## 3. 왜 지금 문제가 되었나

두 변경이 오늘 처음 맞물릴 참이었다.

**하나** — 오늘 [진입 게이트(4816행)](era/era_order_manager.py#L4816)를 전 전략 공통으로 바꿨다.
종전에는 `is_kalman or is_chandelier` 조건이 붙어 **SAR은 이 카운터의 영향을 받지 않았다.**

```python
if getattr(self, "futures_day_consecutive_losses", 0) >= self.futures_consecutive_loss_limit:
    self._note_entry_block("연속손절", ...)
    return
```

**둘** — 카운터는 원래부터 깨져 있었으나, SAR이 면제였으므로 무해했다.

오늘 22거래가 정상 실행된 것은 **ERA가 20:42(장 마감 후)에 재기동돼 장중에는 옛 코드가 돌았기** 때문이다.
2026-08-12 09:00이 첫 충돌 시점이었다.

의도는 "5회 **연속** 손실 시 정지"인데 실제 동작은 "당일 손실 **누적** 5회"였다.

---

## 4. 영향 정량화 — 2026-08-11 실거래 재현

오늘 22거래 손익(pt, 시간순):

```
-0.14 +1.08 -1.82 +0.20 +0.44 +0.10 -0.66 -2.10 +0.66 +0.44 +0.62 -1.68
+0.24 +0.80 +0.48 +1.14 +0.82 +0.46 -0.60 +1.02 +0.60 +0.04
```

손실 발생 순번: **1, 3, 7, 8, 12, 19** → 5번째 손실이 12번째 거래.

| 상태 | 체결 | 손익 | 원화(15계약) |
|---|---:|---:|---:|
| 버그 (이익 시 리셋 안 함) | 12/22거래 | −2.86pt | −2,144,999원 |
| **수정 후** | **22/22거래** | **+2.14pt** | **+1,605,000원** |
| **차이** | | **+5.00pt** | **3,750,000원** |

12번째 거래 이후 10거래(+5.00pt)가 전부 차단됐을 것이다.
**이익을 아무리 내도 손실 5건이면 당일 매매가 멈추는 구조였다.**

---

## 5. 수정

4436·4632 두 곳을 다른 경로와 같은 관용구로 맞췄다.

```python
if realized_pnl < 0:
    self.futures_day_consecutive_losses += 1
else:
    self.futures_day_consecutive_losses = 0
```

한도 검사·정지 알림은 추가하지 않았다 — 진입 게이트(4816행)가 이미 차단을 담당하고,
다른 경로의 `futures_day_trade_count = max_trades_day` 방식은 별개 메커니즘이라
버그 수정 범위를 넘는다.

검증: `py_compile` 통과, 깨진 패턴 0건, 전 경로가 동일 관용구로 정렬됨.

---

## 6. 반영 시점

**즉시 재기동은 불필요.** 야간 세션이 실행 중이고 야간은 별도 카운터
(`futures_night_consecutive_losses`)를 쓰며 정상이다. 이 버그는 주간 세션에만 영향을 준다.
**07:00 Daily Reconnect가 09:00 개장 전에 자동 반영**한다.

---

## 7. 함께 확인한 계좌 상태 (2026-08-11 23:27)

```
선물 예수금        1,055,915,750원
30% 캡 가용         316,774,725원
현재 포지션        없음 (플랫)
계약당 증거금       9,870,800원 (987.08pt × 50,000 × 20%)
15계약 소요        148,062,000원 → 예수금 대비 위험노출 14.0%
```

오늘 성적: 22거래 승 16 / 패 6, 승률 72.7%, PF 1.31, **+1,605,000원** (예수금 대비 +0.152%).

거래 22건은 백테스트 평균(하루 0.53건)의 40배다.
원인은 변동성 — 칼만 ATR 77.70pt(평소 11.67pt의 6.6배), 전일Range 31.56pt(중앙값의 3.2배).
손절 77.7pt / 익절 155.41pt로 자동 확대돼 SAR이 잦게 역전됐다.

### 부수 확인 사항

- **ERA 중복 실행 없음.** 프로세스가 4개로 보여 확인했으나 계좌 동기화가 정확히 300초
  단일 주기라 인스턴스 1개가 확실하고, 나머지 쌍은 TCA다.
- **`era/era.lock`에 죽은 PID(17248)가 남아 있다.** 중복 실행 방지가 목적이라면 현재
  작동하지 않는 상태다. 오늘은 중복이 없었으므로 실해는 없었다. 별건으로 정리 필요.
