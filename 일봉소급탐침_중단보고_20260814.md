# 키움 일봉 소급 탐침 — 게이트 통과 후 실행 불가 중단 보고

작성: 2026-08-14 16:5x KST
결론: **게이트 1·2·4 통과, 게이트 3 판정 불가. 탐침 실행 자체가 불가능하여 ERA를 정지하지 않고 중단.**

---

## 1. 결론 (개조식)

- **소급 가능 기간: 미판정.** 탐침을 실행하지 못했다.
- **ERA 정지·복원: 수행하지 않음.** `emergency_kill.flag`를 생성하지 않았다. ERA 상태는 작업 시작 시점과 동일하다.
- **중단 사유:** 본 세션의 셸은 리눅스 샌드박스다. 키움 OpenAPI+는 Windows 32비트 OCX이며, 샌드박스에서 호출할 방법이 없다. 탐침을 실행할 수 없는 상태에서 ERA를 정지하면 **리스크만 100% 지고 이득은 0**이다.
- **판단:** 게이트 순서상 게이트 3(ERA 상태)이 판정 불가이므로 지침("하나라도 실패하면 중단")에 따라 중단하는 것이 맞다. 실행 불가 사유까지 겹쳤다.

---

## 2. 안전 게이트 결과

| 게이트 | 결과 | 근거 |
|---|---|---|
| 1. 시각 16:00 이후 | **통과** | 확인 시점 16:48 KST |
| 2. 포지션 없음 | **통과** | `tca/system_status_futures.json`: `futures_positions {}`, `isf_positions {}` (last_updated 15:58:42) |
| 3. ERA 상태 | **판정 불가** | 아래 상세 |
| 4. 예수금 기록 | **통과** | 23,967,550원 — 복수 출처 교차 확인 |

### 게이트 3 상세 — 왜 판정 불가인가

- `era/era.pid` = `16716` (파일 mtime 08-14 09:03)
- `era/era_order_manager.log` 최종 기록 **15:58:40**, 현재 16:48 → 약 50분 무기록
- 로그상 계좌 동기화 TR은 **5분 주기**(15:33:40 / 15:38:40 / … / 15:58:40)로 규칙적이었고, 16:03 이후 기록이 없다
- 해석이 둘로 갈린다:
  - (a) 장 마감 후 정상적으로 폴링을 멈춘 유휴 상태 — 프로세스는 살아 있음
  - (b) 프로세스가 종료됨
- **리눅스 샌드박스에서는 Windows PID 16716의 생존 여부를 확인할 수 없다.** `tasklist` 실행 불가, `/mnt/c` 미존재. 파일 mtime만으로는 (a)와 (b)를 구분할 수 없다.

### 게이트 4 상세 — 예수금 교차 확인

지침대로 단일 출처를 믿지 않고 두 곳을 대조했다.

| 출처 | 값 |
|---|---|
| `tca/system_status_futures.json` → `futures_balance` | 23,967,550 |
| `era/era_order_manager.log` 15:58:40 TR 동기화 → "선물 예수금" | 23,967,550원 |

- 두 값 일치.
- **주문가능현금 오인 여부 점검:** 로그는 "선물 예수금: 23,967,550원"과 "50% 캡 적용 가용금액: 11,983,775원"을 **별도 항목으로** 출력한다. 후자는 전자의 정확히 1/2이므로 파생값이며, 예수금과 혼동된 정황은 없다. 과거 오인 사례와 같은 패턴은 발견되지 않았다.

---

## 3. 실행 불가의 근거 (확인된 사실)

1. **셸 환경:** `uname -a` → `Linux claude 6.8.0-124-generic ... x86_64`. `python3 -c "import sys; print(sys.platform)"` → `linux`. `/mnt/c` 없음.
2. **키움 API 호출 방식:** `ai_trader/kiwoom_api.py`는 `QAxWidget("KHOPENAPI.KHOpenAPICtrl.1")` 기반 COM/OCX. `ai_trader/data_collector.py`는 PyQt5를 임포트하고 `venv32`(32비트 가상환경)를 전제한다. 리눅스에서 재현 불가.
3. **로그인 필요:** 어떤 TR이든 `CommConnect()` 선행이 필요하다. 이는 Windows 데스크톱 세션에서만 성립한다.
4. **컴퓨터 제어 우회도 막힘:** 터미널·IDE는 컴퓨터 제어에서 "click" 등급으로만 허용되어 **타이핑이 차단**된다. 명령을 입력해 스크립트를 띄울 수 없다.

### 부수 확인 — 사전 조사 문서의 버그 지적은 사실

- `ai_trader/data_collector.py:56` → `api.req_historical_data(future_code, "opt50028", "선물일봉차트조회")`
- 이름은 "선물일봉차트조회"인데 TR은 `opt50028`(틱/분차트 계열)이다. 지적대로 **오호출**이다.
- 저장소 전체 `.py`에서 `opt50030` 참조는 **0건**. 즉 일봉 TR을 호출하는 코드 경로가 아직 존재하지 않는다. 탐침은 신규 코드가 필요하다.

### 부수 확인 — 워치독은 kill flag를 존중한다 (지침이 옳았다)

`watchdog_check.bat` 상단:

```
if exist "system_stopped.flag" exit /b 0
if exist "emergency_kill.flag" exit /b 0
if exist "reconnect_in_progress.flag" exit /b 0
```

- `emergency_kill.flag`가 있으면 워치독은 즉시 빠져나가며 ERA를 되살리지 않는다. 주석에도 2026-08-13에 이 분기를 추가한 이력이 남아 있다.
- 즉 **kill flag 방식은 워치독과 충돌하지 않는다.** 향후 실제 실행 시 이 경로는 안전하다.
- 참고: 워치독은 `tasklist /fi "PID eq %_PID%" | findstr /i "python"`으로 생존을 판정한다. 사람이 ERA 생존을 확인하려면 동일 명령을 쓰면 된다.

---

## 4. 준비해 둔 것

`probe_opt50030_daily.py` (신규 파일, 저장소 루트).

- `opt50030`으로 `10100000` 일봉을 조회하고 **소급 한계까지만** 연속조회한 뒤 CSV 1개와 로그 1개를 남긴다.
- 설계상 지켜지는 금지사항: 주문 API 미사용, 계좌 조회 TR 미사용, DB 쓰기 없음, 기존 모듈 미수정(독립 실행), 화면번호 `9901`로 ERA와 분리, 호출 간격 1.2초.
- `emergency_kill.flag`가 없으면 실행을 거부한다(`--force`로만 강제). 세션 경합 사고 방지용 안전장치다.
- `opt50030`의 출력 필드명이 문서로 확정되지 않았으므로, 날짜 필드는 `일자/체결시간/거래일자` 후보를 순회해 **실제로 값이 있는 필드를 판별**하도록 했다. 이것이 이번 탐침의 핵심 미지수다.

**⚠️ 이 스크립트는 미검증이다.** 작성자는 리눅스 샌드박스에 있어 문법 이상의 검증(실행·응답 확인)을 하지 못했다. 첫 실행은 반드시 사람이 지켜보는 상태에서 해야 한다.

---

## 5. 다음 선택지

### 옵션 A — 사용자가 직접 실행 (권장, 가장 빠름)

ERA 정지 → 탐침 → 복원. 순서:

```bat
:: 1) ERA 정지 (워치독은 이 플래그를 존중한다)
type nul > C:\Antigravity\AI_T_Agent\emergency_kill.flag

:: 2) 종료 확인 — 2분 관찰. python이 안 나오면 종료된 것
tasklist /fi "PID eq 16716" | findstr /i python

:: 3) 탐침 (32비트 venv)
cd /d C:\Antigravity\AI_T_Agent
venv32\Scripts\python.exe probe_opt50030_daily.py

:: 4) 복원
del C:\Antigravity\AI_T_Agent\emergency_kill.flag
run_era.bat
```

생성되는 `probe_10100000_daily_*.log` / `.csv`를 주면 판정·품질 점검·대량 수집 계획을 이어서 작성한다.

### 옵션 B — 키움을 아예 건드리지 않는 우회 (ERA 정지 불필요)

세션 경합이 최대 리스크인데, 일봉 10년치는 키움 말고도 출처가 있다. TradingView MCP가 이 세션에 연결되어 있고 KOSPI200 선물/지수 일봉을 읽을 수 있다(현재 TradingView 데스크톱이 실행 중이 아니라 `tv_launch` 필요). **ERA를 한 번도 멈추지 않고** 소급 깊이를 먼저 가늠할 수 있다.

- 장점: 다운타임 0, 실계좌 리스크 0
- 단점: 키움 데이터와 정합성(가격 기준·롤오버 규칙)이 다를 수 있어, 실매매 백테스트의 최종 소스로 쓰려면 키움과 대조가 필요하다
- 용도: **소급 가능성 사전 확인** 및 키움 데이터 검증용 대조군으로 적합

옵션 B를 먼저 하고, 결과가 유망하면 옵션 A로 확정하는 순서를 권한다.

---

## 6. 미해결 — 확보 시 재판정 대상 (탐침 성공 전제)

일봉 10년치가 확보되면 아래는 모두 재판정 대상이다. 이번엔 판정 불가이므로 목록만 남긴다.

- 900셀 이상 백테스트의 다중비교 보정 결과 (생존 0개) — 표본이 134거래일에서 2,400+영업일로 늘면 검정력 자체가 달라진다
- 하락 레짐에서 한 번도 발화하지 않은 신호들 — 현재 "무발화"는 신호가 나쁜 게 아니라 표본에 하락장이 없었을 가능성이 크다
- BB 필터 임계(0.97%)·MA200 상승 판정 등 레짐 필터 파라미터
- Chandelier / SAR 계열 진입·청산 게이트 검토 결론
- 건당 엣지 허들 판정
- EOD 강제청산 정책 (만기일 아티팩트 포함)

단, 일봉은 5분봉 전략을 그대로 검증하지 못한다. **일봉으로 검증 가능한 것은 레짐 필터·보유기간 수일 이상 로직에 한정**되며, 5분봉 진입 타점 자체는 여전히 표본 부족이 남는다. 이 한계를 전제로 재판정 범위를 정해야 한다.

---

## 7. 준수 확인

- 포지션 보유 중 진행: 해당 없음 (포지션 없음, 애초에 진행 안 함)
- 주문 API·계좌 조회 API 호출: **없음**
- `taskkill` 사용: **없음**
- TCA·KIS 야간수집기 접촉: **없음**
- `era/era_order_manager.py`, `indicators.py`, `config/*` 수정: **없음**
- DB 쓰기: **없음**
- ERA 상태 변경: **없음** (`emergency_kill.flag` 미생성)
- 신규 생성 파일: `probe_opt50030_daily.py`, 본 보고서 — 둘 다 실행 전까지 시스템에 영향 없음
