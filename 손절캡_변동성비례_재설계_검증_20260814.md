# 손절 캡 4안(변동성 비례 재설계) 검증 — SAR 경로

작성: 2026-08-14 · **ERA 미기동·미종료 · 라이브 코드 무수정 · 주문/계좌 API 미호출**
스윕: `scratch/dyncap_sweep_20260814.py` · 원자료 `scratch/_dyncap/`

> ## 결론: **적용 권고하지 않음. 라이브에 아무것도 적용하지 않았습니다.**
>
> 1. **변동성 비례 캡도 성과를 악화시킨다.** 검증한 13개 조합 전부 현행(캡 없음)보다 PF가 낮다.
>    캡이 실제로 조여지는 순간부터 PF가 단조 하락하고, 캡을 풀수록 현행으로 수렴한다.
>    "중간 어딘가의 최적점"은 존재하지 않았다.
> 2. **더 중요한 발견 — 현행 SAR 경로에서 `sl_limit` 손절은 단 한 건도 발동하지 않는다.**
>    259건 전부 SAR역전(208) 또는 장마감 강제청산(51)으로 끝났다. 손절폭 81pt는
>    "위험한 손절선"이 아니라 **애초에 도달하지 않는 죽은 코드**다.
> 3. **최악손실 -74.90pt는 손절이 아니라 장마감 강제청산이다.** 손절폭을 어떻게 바꿔도
>    이 꼬리는 잘리지 않는다. 실제로 13개 조합 전부 최악손실이 **-74.90pt로 동일**했다.
>
> C-1(손절선이 잔고의 98%)이라는 진단은 숫자로는 맞지만, **그 손절선은 발동하지 않으므로
> 위험의 실제 경로가 아니다.** 진짜 꼬리위험은 ① 장마감 강제청산 ② 계좌 대비 과대 포지션이다.

---

## 1. 무엇을 만들었나

`scratch/backtest_sar_bb_20260809.py`에 인자 3개를 추가했다. **라이브 코드는 건드리지 않았다.**

```python
dyn_cap_mult=None, dyn_cap_min=None, dyn_cap_max=None,
```

`_sl_limit()` 안에서만 쓰인다. 라이브 kalman 경로의 `_effective_sl_hard_cap()`과 같은 형태다.

```python
if dyn_cap_mult is not None:
    _c = dyn_cap_mult * entry_std_error      # 진입 시점 스냅샷
    if dyn_cap_min is not None: _c = max(_c, dyn_cap_min)
    if dyn_cap_max is not None: _c = min(_c, dyn_cap_max)
    v = min(v, _c)
```

`std_error`는 **진입 시점 스냅샷**을 쓴다. 라이브가 보유 중 재추정으로 손절선이 흔들리는 것을
막으려고 진입 스냅샷을 쓰는 것과 같은 이유다(무한루프 버그 수정 이력).

### 회귀검증 — 기존 동작 무변경 확인

인자를 안 쓰면 기존과 100% 같아야 한다. 집계뿐 아니라 **거래 원장을 1건씩 대조**했다.

```
m1.0_cnone: n=259/259 pf=1.0200/1.02 worst=-75.05/-75.05 집계일치=True 원장일치=True
m1.0_c30:   n=269/269 pf=0.9421/0.9421 worst=-75.05/-75.05 집계일치=True 원장일치=True
REGRESSION PASS
```

---

## 2. 기준선 — 라이브 설정 그대로

`config/config.json` + `config/config_local.json` (2026-08-14) 매핑. `sar_update_mode='bar'`이므로
봉 단위이며, **이는 현재 라이브와 같은 cadence다**(8/13 전환 이후).

```
strategy=sar, Q=0.00005, R=1.0, mult=0.6, atr_cutoff=15.0, min_std_error_entry=1.5,
trim_std_outliers=1, sar_af_init/step/max=0.02/0.02/0.10, ma_filter_period=200,
consecutive_loss_limit=5, reentry_k=0.25, entry_end_hour=15, point_value=50,000
sar_sl_mult=1.0  ← 라이브 값. 이 스윕에서 고정(배수 변경은 별개 사안)
```

데이터: `10500000` 5분봉 (2025-01-10 ~ 2026-08-13). 실질 거래구간 2025-10-29 이후.

### 진입 시점 변동성 분포 (259건)

| 지표 | 중앙값 | 5%~95% |
|---|---:|---:|
| std_error | 5.00 | 1.61 ~ 17.51 |
| ATR14 | 40.78 | 15.93 ~ 95.94 |
| 현행 손절폭 (=ATR×1.0) | 40.78 | 15.93 ~ 95.94 |
| **ATR / std_error** | **9.11** | 2.90 ~ 23.03 |

분기별 중앙값:

| 분기 | std_error | ATR14 | 현행 손절폭 | 거래 |
|---|---:|---:|---:|---:|
| 2025Q4 | 1.84 | 16.87 | 16.87 | 24 |
| 2026Q1 | 3.30 | 27.49 | 27.49 | 81 |
| 2026Q2 | 6.19 | 49.64 | 49.64 | 107 |
| 2026Q3 | 7.69 | 92.42 | 92.42 | 47 |

**ATR/std 중앙값이 9.11**이라는 게 캡 배수 설계의 핵심이다. 현행 손절폭은 std_error의
약 9.1배다. 따라서 kalman이 쓰는 `2.0×std`를 그대로 가져오면 **현행보다 4.5배 좁은** 손절이
되고, 배수가 9~10에 이르러야 비로소 캡이 안 걸린다. 이 구조가 아래 결과를 그대로 설명한다.

---

## 3. 스윕 결과 — 전 조합이 현행보다 나쁘다

배수는 라이브 값 1.0으로 고정하고 캡만 바꿨다. `net합계`는 보수 비용(편도 0.25pt + 실측 수수료율) 차감 후.

| 조합 | 거래 | PF | 건당 gross | 최악손실 | MDDpt | net합계 |
|---|---:|---:|---:|---:|---:|---:|
| **none (현행)** | 259 | **1.041** | **+0.754** | -74.90 | **374.6** | **+50.5** |
| fix30 (1안 대조군) | 269 | 0.961 | +0.238 | -74.90 | 426.2 | -86.5 |
| k2.0_8_30 ← kalman과 동일 | 304 | 0.885 | -0.251 | -74.90 | 574.8 | -246.2 |
| k2.0_8_60 | 300 | 0.905 | -0.112 | -74.90 | 538.3 | -201.3 |
| k3.0_8_60 | 290 | 0.914 | -0.062 | -74.90 | 481.0 | -180.0 |
| k4.0_8_80 | 282 | 0.882 | -0.306 | -74.90 | 500.3 | -243.8 |
| k5.0_8_80 | 273 | 0.976 | +0.345 | -74.90 | 359.2 | -58.3 |
| k6.0_8_100 | 266 | 0.973 | +0.326 | -74.90 | 377.8 | -62.0 |
| k8.0_8_100 | 259 | 1.033 | +0.708 | -74.90 | 374.6 | +38.6 |
| k3.0_8_none | 290 | 0.914 | -0.062 | -74.90 | 481.0 | -180.0 |
| k4.0_8_none | 282 | 0.882 | -0.306 | -74.90 | 500.3 | -243.8 |
| k5.0_8_none | 273 | 0.976 | +0.345 | -74.90 | 359.2 | -58.3 |
| k6.0_8_none | 266 | 0.973 | +0.326 | -74.90 | 377.8 | -62.0 |
| k8.0_8_none | 259 | 1.033 | +0.708 | -74.90 | 374.6 | +38.6 |
| k10.0_8_none | 259 | **1.041** | +0.754 | -74.90 | 374.6 | +50.5 |

읽는 법:

- **k10은 현행과 완전히 동일하다.** 배수 10×std가 손절폭 9.1×std를 넘어 캡이 한 번도 안 걸린다.
- 거기서 배수를 낮출수록(=캡을 조일수록) 거래가 늘고(259→304) PF가 내려간다.
  k8(1.033) → k6(0.973) → k5(0.976) → k4(0.882) → k3(0.914) → k2(0.885).
- 순수 단조는 아니다(k4가 k3보다 나쁨). 이 들쭉날쭉함 자체가 **표본이 얇고 결과가 불안정하다**는
  신호다. 어떤 조합도 "최적점"으로 신뢰할 만큼 안정적이지 않다.
- **상한(dyn_cap_max)은 사실상 무의미하다.** `k3.0_8_60`과 `k3.0_8_none`이 소수점까지 같다.
  std 중앙 5.0에 배수 3이면 15pt라 상한 60에 애초에 닿지 않는다.

### 구간별 PF

| 조합 | IS(前半) | OOS(後半) | 최근60일 | IS n | OOS n | 60일 n |
|---|---:|---:|---:|---:|---:|---:|
| **none (현행)** | **1.014** | **1.048** | **1.524** | 93 | 166 | 69 |
| fix30 | 1.014 | 0.948 | 1.242 | 93 | 176 | 77 |
| k2.0_8_30 | 0.807 | 0.906 | 1.234 | 106 | 198 | 86 |
| k5.0_8_80 | 0.915 | 0.993 | 1.301 | 100 | 173 | 75 |
| k8.0_8_100 | 1.014 | 1.038 | 1.490 | 93 | 166 | 69 |

IS·OOS·최근60일 **세 구간 모두 현행이 최고**다. 특정 구간에만 유리한 조합조차 없다.

---

## 4. 왜 이런가 — 손절이 애초에 발동하지 않는다

현행 설정 259건의 청산 사유를 전수 집계했다.

| 사유 | 건수 | 평균 손익 | 최악 |
|---|---:|---:|---:|
| SAR역전 | 208 | -0.48pt | -57.11pt |
| 장마감 강제청산 | 51 | +3.26pt | **-74.90pt** |
| **손절(`sl_limit`)** | **0** | — | — |

**`sl_limit` 손절은 한 건도 발동하지 않았다.** ATR×1.0(중앙 40.8pt, 최근 92pt)이 너무 넓어서
SAR 트레일링이 항상 먼저 잡는다. 즉 손절폭 81pt는 "계좌를 비울 손절선"이 아니라
**한 번도 도달하지 않는 죽은 파라미터**다.

캡을 씌운다는 건 이 죽은 코드를 **살려내서 SAR 트레일링보다 먼저 끼어들게 만드는 것**이다.
실제로 그렇게 된다:

| 조합 | SAR역전 | 강제청산 | **손절** |
|---|---:|---:|---:|
| none | 208 | 51 | **0** |
| fix30 | 206 | 49 | **14** |
| k2.0_8_30 | 180 | 44 | **80** |

손절 발동이 0 → 14 → 80으로 늘고, PF는 1.041 → 0.961 → 0.885로 내려간다. 메커니즘은
20260812 보고서가 이미 지적한 것과 같다 — **되돌아왔을 거래를 미리 끊고, 끊긴 자리에서
재진입해 비용만 더 낸다.** SAR 트레일링이 이미 청산을 담당하는 구조에서 별도 손절은 방해물이다.

### 최악손실이 전 조합 동일한 이유

30pt 초과 손실 13건의 사유별 내역:

| 진입 | 사유 | 손익 | 진입시 ATR |
|---|---|---:|---:|
| 2026-07-09 13:30 | **강제청산** | **-74.90** | 98.56 |
| 2026-06-22 09:00 | SAR역전 | -57.11 | 75.45 |
| 2026-06-02 09:00 | SAR역전 | -55.30 | 61.08 |
| 2026-06-11 09:00 | SAR역전 | -50.90 | 76.67 |
| (이하 9건 -36.72 ~ -30.04) | SAR역전 | | |

**최대 손실은 유일한 강제청산 건이다.** 장마감에 시장가로 털리는 것이라 `sl_limit`와 무관하며,
어떤 캡도 이걸 못 막는다. 그래서 13개 조합 전부 최악손실이 -74.90pt로 같았다.

---

## 5. ⚠️ 이 결과의 한계 — 정직하게

**결론을 뒤집을 수 있는 한계가 하나 있다.** 판단에 반영하셔야 한다.

**백테스터는 5분봉 종가 체결, 라이브는 틱 판정이다.** 백테스트에서는 손절 캡이 봉 안에서
관통된다(봉 하나가 40pt 역행하면 30pt 캡을 그냥 넘김). 라이브는 틱마다 판정하므로 **캡이
훨씬 정확하게 걸린다.** 즉 이 백테스트는 캡의 **보호 효과를 과소평가**하고 있을 수 있다.

캡이 틱 단위로 완벽하게 걸렸다고 가정한 이론 상한(재진입 효과 미반영):

| 가정 캡 | 구제되는 손실 | 이론 PF |
|---|---:|---:|
| 30pt | 146.2pt | 1.144 |
| 40pt | 78.2pt | 1.093 |
| 50pt | 38.2pt | 1.066 |

이론 상한 1.144와 실측 0.961의 **차이 0.18이 곧 "잘린 거래를 재진입으로 되사는 비용"** 이다.
이론값은 손실만 깎고 그 거래가 살아났을 가능성과 재진입 비용을 무시한 수치라 상한일 뿐이다.
다만 라이브가 이 둘 사이 어디에 놓일지는 **이 백테스트로 결정되지 않는다.**

그 밖의 한계:

- 실질 표본 250여 건, 2025-10-29 이후 4분기뿐(`atr_cutoff=15`가 2025년 대부분을 차단).
- 슬리피지 편도 0.25pt 가정. 0.5pt 스트레스는 이번에 돌리지 않았다.
- 배수(`sar_sl_mult`)는 1.0 고정. 배수·캡 동시 최적화는 하지 않았다(별개 사안).
- SAR 트레일링 시작 거리(`ATR×1.0`)에는 캡을 적용하지 않았다. 여기까지 캡하는 변형은
  손절이 아니라 전략 교체이며 여전히 미검증이다.

---

## 6. 그래서 C-1은 어떻게 하나

진단(손절선 81pt = 잔고의 98%)은 산술적으로 맞다. 그러나 §4가 보여주듯 **그 손절선은
발동 경로가 아니다.** 실제로 계좌를 위협하는 경로는 둘이다.

| 실제 위험 경로 | 최악 실측 | 손절 캡으로 해결되나 |
|---|---:|---|
| 장마감 강제청산 | -74.90pt (374만원) | **아니오** — 시각 기준 청산이라 무관 |
| SAR역전 지연 | -57.11pt (286만원) | 부분적. 단 PF를 그 이상 깎는다 |
| 계좌 대비 과대 포지션 | 구조적 | **아니오** — 사이징 문제 |

**잔고 414만원 / 1계약 기준 환산**

- 실측 최악손실 -74.90pt = **374만원 = 잔고의 90.4%**
- 30pt = 150만원 = 잔고의 **36.2%**
- 즉 캡을 30pt로 걸어도 **강제청산 꼬리(90%)는 그대로 남는다.**

계약당 위험이 계좌를 삼키는 구조는 손절폭이 아니라 **계약수/계좌 규모의 문제**다.
1계약이 최소 단위인 이상, 이 상품에서 414만원 계좌로는 어떤 손절 설정으로도
1회 손실이 계좌의 상당 비율이 되는 것을 피할 수 없다.

---

## 7. 권고

| # | 권고 | 근거 |
|---|---|---|
| 1 | **동적 캡을 도입하지 말 것 (4안 기각)** | 13개 조합 전부 현행보다 PF 낮음. IS/OOS/최근60일 전 구간 동일 |
| 2 | **고정 캡도 도입하지 말 것 (1안 재확인)** | fix30 PF 0.961 vs 현행 1.041, MDDpt 374→426 악화, 최악손실 불변 |
| 3 | **장마감 강제청산을 먼저 볼 것** | 최악손실 1위가 여기서 나온다. `EOD강제청산_정책검토_20260813.md` 참조 |
| 4 | **계좌 대비 포지션 규모를 먼저 볼 것** | 캡 30pt여도 1회 손실이 잔고의 36%, 실측 최악은 90% |
| 5 | 손절폭을 정말 손보려면 **배수(`sar_sl_mult`)와 함께** 볼 것 | 캡 단독은 전 구간 실패. 배수 축은 이번 스윕에 없음 |

### 아직 미적용인 리스크 장치

- **일일 손실 서킷브레이커**: `daily_loss_limit_pct=0.10`(41.4만원 ≈ 8.3pt)로 완화돼 있다.
  손절 1회는커녕 SAR역전 1회에도 당일 신규 진입이 막히는 수준이다. 신규 진입만 차단하며
  보유 포지션의 청산·트레일링에는 영향 없다.
- **SAR 서킷브레이커**: 미구현.
- **연속손절 카운터**: `consecutive_loss_limit=5` 활성.

---

## 8. 라이브 변경 여부

**없습니다.** 다음 파일들은 이번 작업에서 **읽기만** 했습니다.

| 파일 | 상태 |
|---|---|
| `era/era_order_manager.py` | **무수정** |
| `config/config.json`, `config/config_local.json` | **무수정** |
| `config/active_strategy.json` | **무수정** |
| `indicators.py`, `bqa/kalman_backtester.py` | **무수정** |
| `era/futures_exit_state.json`, `tca/system_status_futures.json` | **무수정** |

수정한 파일은 백테스트 도구 2개뿐입니다.

| 파일 | 변경 | 백업 |
|---|---|---|
| `scratch/backtest_sar_bb_20260809.py` | `dyn_cap_*` 인자 3개 + 진단용 진입 스냅샷 2개 추가. 미지정 시 기존 동작과 원장 단위로 동일(회귀검증 PASS) | `scratch/backtest_sar_bb_20260809.py.bak_before_dyncap_20260814_030341` |
| `scratch/dyncap_sweep_20260814.py` | 신규 | — |

ERA는 기동·종료하지 않았고, 주문/계좌 API는 호출하지 않았습니다. 재기동은 불필요합니다
(라이브 로직이 그대로이므로 현재 보유 중인 LONG 1계약에도 영향 없습니다).

**되돌리기**: 백테스터를 원복하려면
`copy scratch\backtest_sar_bb_20260809.py.bak_before_dyncap_20260814_030341 scratch\backtest_sar_bb_20260809.py`
후 `scratch\dyncap_sweep_20260814.py` 삭제. 라이브에는 되돌릴 것이 없습니다.

---

## 9. 산출물

| 경로 | 내용 |
|---|---|
| `scratch/dyncap_sweep_20260814.py` | 스윕·회귀검증·진단·집계 (`regress`/`diag`/`run`/`report`) |
| `scratch/_dyncap/{조합}.json` | 조합별 거래 원장 15개 (진입시 std_error·ATR14 포함) |
| `scratch/_dyncap/summary.csv` | 구간별 집계표 |

재현:

```bash
python3 scratch/dyncap_sweep_20260814.py regress   # 무변경 회귀검증
python3 scratch/dyncap_sweep_20260814.py run       # 스윕
python3 scratch/dyncap_sweep_20260814.py diag      # 변동성 분포
python3 scratch/dyncap_sweep_20260814.py report    # 집계표
```

---

저는 투자 자문을 드릴 수 없습니다. 위 수치는 과거 자료의 재현이며 미래 성과를 보장하지 않습니다.
