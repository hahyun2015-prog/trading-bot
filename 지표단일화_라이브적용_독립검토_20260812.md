# 지표 단일화 라이브 적용 — 독립 검토 보고

작성: 2026-08-12 23:0x KST · 검토자: 별도 세션(선행 작업 무관여)
대상: `era/era_order_manager.py` (해시 `48f3bc69…8b7`) + `indicators.py` (해시 `a9d5d0f0…a8d9`)
방법: 선행 보고서 6건 정독 + **백업본 대비 전체 diff 직접 재검증** (보고서 주장을 신뢰하지 않고 원본 대조)

> **결론: 코드 교체 자체는 안전하다고 판단한다. 그러나 현 상태로 재기동하면 안 된다.**
> 막는 것은 코드 정확성이 아니라 **배포 구조 1건(치명)** 과 **검증 폭 1건(중)** 이다.
> 아래 §5 선결조건 3개를 처리하면 재기동 가능하다는 의견이다.

이 문서는 코드를 수정하지 않았고 ERA를 기동하지 않았다. 읽기 전용 검토다.

---

## 1. 무엇을 직접 확인했나 (보고서 인용이 아님)

| # | 검증 항목 | 방법 | 결과 |
|---|---|---|---|
| 1 | 라이브 파일 = 검증받은 파일 | `sha256sum` 3개 파일 직접 산출 | `era/era_order_manager.py` = `LIVE_applied.py` = `48f3bc69…8b7` **일치** |
| 2 | 백업본 무결성 | 동일 | `0ee30d3d…8e3c` 보고서 값과 일치 |
| 3 | 변경 범위 | `diff -u` 전문 육안 검토 (154행) | **9개 훅 전부 지표 계산부. 전략·비용·진입/청산 조건 변경 0건** — 보고서 주장 사실 확인 |
| 4 | 교체 등가성 | 훅별로 `indicators.py` 구현을 직접 읽고 원본과 대조 | **9/9 등가** (§2) |
| 5 | `indicators.py` 사후 변경 | 검증본 사본(`scratch/replay/indicators.py`=`0bb8136f`)과 현재 루트 파일 diff | **삭제·변경 0행 / 추가 95행 — 순수 append** (§3) |
| 6 | 신규 최상위 의존성 | `grep "^import"` | 없음. pandas는 함수 내부 지연 import, `ta` 패키지 미사용 → **기동 실패 유발 불가** |
| 7 | `sys.path` 오염 | 루트/`era` .py 파일명 교집합 + 표준라이브러리 충돌 | 충돌 0건 (§4-3) |
| 8 | git 추적 상태 | `git status --porcelain` | `?? indicators.py` **untracked 확인** |
| 9 | 실제 가동 전략 | `config_local.json` 오버라이드 추적 | **`parabolic_sar`** — 주 검증(8/11) 경로와 일치 |

---

## 2. 훅별 등가성 판정 — 9/9 통과

가장 위험한 두 곳을 직접 재계산해 확인했다.

### 2-1. 훅 7 `_load_prev_range` — **인덱스 산술 전수 검증** (최대 위험 지점)

유일하게 "값 계산"이 아니라 **"어느 행을 고를지"** 가 재작성된 곳이다.
원본은 `target_row`(rows[0] 또는 rows[1])의 고저폭을 그대로 썼고,
교체본은 `ind.prev_session_range(_asc_h, _asc_l, len(rows) - _j)` 로 바뀌었다.

`prev_session_range`는 내부에서 `j = upto_exclusive - 1` 을 쓴다. 전 경우를 전개하면:

| rows 길이 | 오늘 행 존재 | 원본이 고르는 행 | 교체본 `_asc[len(rows)-_j-1]` | 판정 |
|---:|---|---|---|---|
| 3 | 예 | `rows[1]` | `_asc[1]` = `rows[1]` | ✅ |
| 3 | 아니오 | `rows[0]` | `_asc[2]` = `rows[0]` | ✅ |
| 2 | 예 | `rows[1]` | `_asc[0]` = `rows[1]` | ✅ |
| 2 | 아니오 | `rows[0]` | `_asc[1]` = `rows[0]` | ✅ |
| 1 | 예 | **None → 블록 스킵** | `if target_row:` 가드 그대로 → **스킵** | ✅ |
| 1 | 아니오 | `rows[0]` | `_asc[0]` = `rows[0]` | ✅ |
| 0 | — | 스킵 | 스킵 | ✅ |

**전 경우 동일.** 특히 "오늘 행 1개뿐" 케이스에서 원본의 `target_row=None` 가드가
그대로 남아 있어 `futures_prev_range`가 오염되지 않는다. 재작성이 정확하다.

### 2-2. 훅 8·9 SAR — 연산 순서 보존 확인

원본은 ①SAR 전진 → ②클램프 → ③EP/AF 갱신 순서였고, `SarState._advance`가
`bull`이면 `min(clamp)`, `bear`면 `max(clamp)` 적용 **후** EP/AF를 올린다 —
원본과 동일한 순서다. `on_tick(current_price)`가 `extreme=current_price`로 들어가
원본의 `if current_price > self.sar_ep` 조건을 그대로 재현한다.
`sar_bull` 전환과 청산 판정은 호출부에 남아 있어 건드려지지 않았다.

> 원본 SHORT 분기가 클램프에 `futures_day_peak`(고점)를 쓰는 것은 방향상 어색하지만,
> **교체본이 그대로 보존**했다. 기존 거동 보존이 목표이므로 정상이다(별건으로 다룰 항목).

### 2-3. 나머지 훅

| 훅 | 등가성 근거 |
|---|---|
| 2 TR/ATR | `ind.true_range`의 `np.where(isnan)` = 원본 `.fillna(H−L)`. `kalman_atr` 루프 수식 동일. `[-1]` 인덱싱은 상위 `if len(daily) < 15: return` 가드로 보호됨 |
| 3 BB·밴드폭·스퀴즈·MA | `bollinger_series`/`rolling_quantile`/`moving_average_series` 모두 **내부에서 pandas rolling을 그대로 호출**. `ddof=1`, NaN 워밍업 의미론 완전 동일. `pd.Series(ndarray)`의 RangeIndex + `.iloc[-2]`는 위치 기반이라 인덱스 차이 무영향 |
| 4 칼만·std_error | 수식·trim 로직 동일. 차이는 폴백 조건 `pd.isna` → `not np.isfinite` 1건뿐(§4-4) |
| 5 15분봉 장기 칼만 | 수식 동일. `len(df_15m) >= 5` 가드 유지 |

---

## 3. 검증 이후 `indicators.py`가 바뀐 문제 — 해소됨

선행 보고서(20:45)가 검증한 `indicators.py`는 `0bb8136f…ae90`인데
**현재 디스크 파일은 `a9d5d0f0…a8d9`** 다(21:53 변경). 즉 "검증받은 파일이 그대로 돌고 있다"는
전제가 라이브러리 쪽에서 깨져 있었다.

직접 diff한 결과:

```
삭제·변경된 줄: 0
추가된 줄:     95   (line 49 __all__ 항목 추가 / line 580~673 ta_* 블록 append)
```

**기존 정의가 단 한 줄도 바뀌지 않았다.** ERA가 실제로 호출하는 12개 심볼
(`true_range`, `kalman_atr`, `bollinger_series`, `bandwidth_series`, `rolling_quantile`,
`moving_average_series`, `kalman_residual_std`, `kalman_path`, `Window`, `Series`,
`SarState`, `prev_session_range`)은 **검증본과 바이트 동일**하다.

추가된 `ta_*` 블록도 최상위 import를 늘리지 않는다(pandas는 함수 내부 지연 import,
`ta` 패키지 자체는 미사용·재구현). 따라서 **기동 실패나 거동 변화를 유발할 수 없다.**

→ 이 항목은 **위험 해소**로 판정한다. 다만 §5-3의 재발 방지가 필요하다.

---

## 4. 검토자 소견 — 선행 보고서와 다른 시각

### 4-1. 검증 폭이 좁다 (중대)

| 경로 | 검증된 날짜 | 검증 세션 |
|---|---|---|
| `parabolic_sar` (현 운영 전략) | **2026-08-11 하루** (cold + warm 2조건) | 선행 |
| `kalman` | 07-30(O→H→L→C, O→L→H→C), 08-04 | 후행 교차검증 |
| `chandelier` | 07-28, 07-30 | 후행 교차검증 |

선행 세션의 음성대조 보고서가 **스스로 이렇게 적었다**:

> "이번 검증은 8/11 하루에서만 수행했다. **N3이 이날 무증상이었던 것처럼 날짜에 따라
> 무증상인 결함이 있다.** B 대조는 최소 5거래일 이상으로 넓혀야 하며, 특히 MA200 근접일과
> SAR 전환이 잦은 날을 포함해야 한다."

이 조건이 **충족되지 않은 채 라이브 적용이 진행됐다.** 실제로 N6(prev_range)은 콜드스타트에서
전혀 검출되지 않았고 웜스타트에서만 잡혔다 — 조건 하나만 빠져도 결함이 숨는다는 실증이다.

다만 이를 "부실"로 보지는 않는다. 음성대조 7종이 전부 검출됐고(N4는 82/82봉,
N7은 SAR 82.08pt 차이), 하네스가 실제로 감시하고 있음은 증명됐다. 그리고 §2에서
훅 9개를 정적으로 전수 검토한 결과 등가성에 의심 가는 지점이 없다.
**"검출력은 입증됐으나 표본이 좁다"** 가 정확한 평가다.

→ 권고: 재기동 전 `parabolic_sar` 경로를 **MA200 근접일 1일 + SAR 전환 빈발일 1일**
추가로 돌릴 것. 리플레이 1회는 수십 초이므로 비용이 거의 없다.

### 4-2. `indicators.py` untracked — 유일한 치명 항목 (선행 보고서와 동일 결론)

```
 M era/era_order_manager.py
?? indicators.py          ← git이 모르는 파일
```

`run_era.bat`은 기동마다 `git pull origin main`을 돌린다. 그리고 기동 전 자가검사는
`py_compile`인데 **`py_compile`은 import를 실행하지 않으므로 `indicators.py` 유실을 못 잡는다.**
결과적으로 배치 파일은 `[OK] Syntax check passed`를 출력한 뒤 파이썬이 `ModuleNotFoundError`로
죽는다. 스케줄러 경로(`auto_reconnect_era.bat`)는 콘솔이 숨겨져 있어
**2026-07-31과 동일한 "조용히 안 뜬 채 장을 날리는" 실패 모드**가 재현된다.

선행 보고서도 §5.1에서 같은 지적을 했다. 검토자로서 이를 **권고가 아니라 기동 차단 조건**으로
격상할 것을 제안한다.

### 4-3. `sys.path.insert(0, root)` — 위험 낮음

루트가 sys.path **맨 앞**에 오게 됐다(기존 96행의 `append`보다 우선). 확인 결과:

- 루트 `*.py`와 `era/*.py`의 파일명 교집합 **없음**
- 루트 파일명(`notifier`, `test_era`, `tda_controller`, `check_db`, `generate_report` 등)이
  표준라이브러리·PyQt5·requests·bs4와 충돌하지 않음
- 루트는 96행에서 **이미 sys.path에 있었다**(`import notifier`가 그것에 의존) → 신규 노출 아님

→ 실질 위험 없음. 다만 앞으로 루트에 흔한 이름(`json.py`, `types.py`, `logging.py` 등)의
파일을 만들면 **실계좌 엔진이 그것을 표준라이브러리 대신 집어간다.** 규칙으로 남길 것.

### 4-4. 사소한 거동 차이 1건 (도달 불가)

`std_error` 폴백이 `pd.isna(s) or s<=0` → `not np.isfinite(s) or s<=0`으로 바뀌어
`inf`도 0.5로 폴백된다. 유한 실수의 표준편차는 `inf`가 될 수 없으므로 도달 불가.
선행 보고서가 이미 자진 신고한 항목이며 동의한다.

### 4-5. 이번 변경과 무관하나 켜기 전에 알아야 할 것

| 항목 | 내용 | 성격 |
|---|---|---|
| Kalman ATR 73.93pt | 지수 1,010선에서 7.3%. **SL 73.93pt / TP 147.86pt로 돌게 된다** | 교체 전부터 동일(A/B 차이 0). 사이징 직결 |
| SAR `on_tick` | 틱마다 AF 상승 → 회전율 백테스트의 **18배**(22건/일 vs 1.2건/일) | 교체 전부터 동일. `on_bar` 전환은 별건 |
| `MARGIN_RATE=0.10` | 실제 위탁증거금률 19.8%의 절반 | 백테스터 상수. 현재 `dynamic_sizing=False`라 즉시 영향 없음 |

**이번 리팩터링이 만든 문제가 아니다.** 다만 "지표 단일화를 켠다"는 결정과
"이 수치로 실매매를 돌린다"는 결정은 별개이며, 후자는 아직 내려지지 않았다.

### 4-6. 신규 지적 — `indicators.py`가 이제 라이브 부품이다

가장 중요한 구조적 변화이면서 어느 보고서에도 명시되지 않은 점이다.

이 파일은 지금까지 백테스트용 라이브러리였고, 그래서 **오늘 하루에만 최소 2번 수정됐다**
(13:51 최초 작성 → 21:53 `ta_*` 추가). 백테스터 마이그레이션이 진행 중이므로
**앞으로도 계속 수정될 예정**이다.

그런데 이제 이 파일은 실계좌 엔진이 import한다. 즉 **오늘부터 `indicators.py` 수정은
곧 실계좌 엔진 수정이다.** `era/era_order_manager.py`에 걸어둔 "수정 금지" 보호가
`indicators.py`에는 걸려 있지 않다. 이번에 `ta_*` 추가가 append-only여서 무사했지만
그건 운이 좋았던 것이고, 구조적 보장이 아니다.

→ **`indicators.py`를 `era_order_manager.py`와 동일한 변경 통제 대상으로 승격**하고,
백테스터용 확장은 별도 모듈(`indicators_bt.py` 등)로 분리하는 것을 권고한다.

---

## 5. 재기동 선결조건 (검토자 판정)

### 🔴 필수 — 이거 없이는 켜면 안 된다

1. **`indicators.py`를 git에 등록**
   ```bat
   git add indicators.py
   git commit -m "feat: single-source indicator module (live engine dependency)"
   ```
   `era/era_order_manager.py` 커밋 여부는 별개 판단.

2. **기동 전 import 자가검사 추가** — `run_era.bat`의 `py_compile` 다음 줄에
   실제 import를 시켜 유실을 잡는다(1줄 추가, 로직 무영향).
   ```bat
   "venv32\Scripts\python.exe" -c "import sys; sys.path.insert(0,'.'); import indicators"
   if %errorlevel% neq 0 ( echo [FATAL] indicators.py missing & exit /b 1 )
   ```
   ※ 이건 제가 넣지 않았습니다. 승인해 주시면 적용하겠습니다.

3. **`indicators.py` 해시 동결** — 현재 `a9d5d0f0…a8d9`.
   재기동 시점까지 이 해시가 유지되는지 확인하고, 다른 세션의 백테스터 작업이
   이 파일을 다시 건드리지 않도록 합의할 것. (지금도 다른 세션이 마이그레이션 4~5번을
   진행하며 이 파일을 열고 있었습니다.)

### 🟡 강력 권고

4. **리플레이 2일 추가** — `parabolic_sar` 경로로 MA200 근접일·SAR 전환 빈발일.
   실행당 수십 초. 선행 세션이 스스로 제시한 "최소 5거래일" 기준에 맞추는 작업.

5. **`scratch/replay/indicators.py` (구 버전 `0bb8136f`) 삭제** —
   하네스가 루트 대신 이 사본을 읽을 수 있어, 후행 세션이 `sys.modules` 선주입으로
   강제 우회해야 했다. 남겨두면 **다음 검증이 조용히 구 버전을 검증하는 함정**이 된다.

### 🟢 켜기 전 결정 (코드 아님)

6. 손절 캡을 먼저 넣을 것인가 (SL 73.93pt로 돌게 됨)
7. SAR `on_bar` 전환은 **이번에 같이 하지 말 것** — 거동이 바뀌어 문제 시 원인 분리 불가.
   선행 보고서 권고에 동의한다.

---

## 6. 최종 판정

| 축 | 판정 | 근거 |
|---|---|---|
| **코드 정확성** | ✅ **안전** | 9개 훅 전수 정적 검토 등가. 가장 위험한 `_load_prev_range` 인덱스 재작성도 7개 경우 전개 결과 원본과 완전 일치 |
| **검증 신뢰성** | ✅ 하네스는 신뢰 가능 | 음성대조 7/7 검출, 위양성 0. 값 대조는 허용오차 없는 완전 일치 |
| **검증 충분성** | ⚠️ **표본 부족** | SAR 1일 / kalman·chandelier 3일. 자체 기준(5거래일) 미달 |
| **배포 안전성** | 🔴 **미충족** | `indicators.py` untracked + `py_compile`이 못 잡는 실패 모드 |
| **변경 통제** | ⚠️ **구멍** | 라이브 부품이 된 `indicators.py`가 보호 대상에 없음 |

**한 줄 결론**: 리팩터링은 잘 됐다. 지금 막아야 할 것은 코드가 아니라 **파일 하나가
git에 없다는 사실**이다. §5의 🔴 3건을 처리하면 재기동에 동의한다.

---

## 부록. 검토 중 확인한 시스템 안전 상태

| 항목 | 상태 |
|---|---|
| `era/era.pid` | 부재 — ERA 정지, **기동하지 않았음** |
| `system_stopped.flag` / `emergency_kill.flag` | 둘 다 존재 |
| `run_era.bat` / `era/auto_reconnect_era.bat` STOP-GUARD | 각 4개 마커 잔존 |
| `era/era_order_manager.py` | **읽기만** 함 (해시 `48f3bc69…8b7` 불변) |
| 이 검토 세션의 파일 쓰기 | 본 문서 1개뿐 |
