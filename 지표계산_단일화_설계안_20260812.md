# `indicators.py` 설계안 — 지표 계산 단일 소스화

- 작성일: 2026-08-12
- 단계: 개선안 11단계 (b) 인터페이스 설계. **설계 문서만. 구현은 (c)에서 승인 후.**
- 근거: `선물_전략전수분석_및_사이징검증_종합_20260811.md` §(a) 감사, 갭 처리 3방식 측정(2026-08-12)
- 상태: ERA 정지 중, 라이브·기존 백테스터 무수정

---

## 0. 이 설계가 푸는 문제

같은 이름의 지표가 파일마다 다르게 계산되고 있습니다. (a) 감사에서 확정된 불일치가 **11건**(K-1~K-8, N-1~N-3)이고, 그중 일부는 이미 잘못된 결론을 만들었습니다. 이 설계의 목표는 **계산을 한 곳으로 모으고, 미래참조와 데이터 공급 차이를 구조적으로 불가능하게** 만드는 것입니다.

**설계 원칙 넷**

1. **지표 함수는 인덱스를 받지 않는다.** 이미 잘린 창(window)만 받는다 → 미래참조가 타입 수준에서 불가능
2. **데이터 공급 방식은 파라미터다.** 갭 처리 A·B·C1을 코드에 고정하지 않고 `FeedSpec`으로 표현
3. **pt 공간과 비율 공간을 타입으로 구분한다.** back-adjust된 가격이 밴드폭 분모에 들어가면 예외
4. **상태 있는 지표(SAR)는 명시적 상태 객체.** 직렬화가 8단계 상태 영속화와 그대로 맞물린다

---

## 1. 함수 인벤토리

### 1.1 데이터 공급 계층

```python
@dataclass(frozen=True)
class FeedSpec:
    """지표에 어떤 봉을 먹일지에 대한 계약. 갭 처리 방식 A/B/C1을 전부 표현한다."""
    session: tuple[str, str] | None = ("09:00", "15:35")
    merge_night: bool = False
    night_code: str | None = None
    night_cutoff_hours: int = 36
    night_resample: str = "5min"
    back_adjust: Literal[None, "subtract", "ratio"] = None
    warmup_bars: int = 200

FEED_A  = FeedSpec(session=None, merge_night=True, night_code="A05608")      # 현행 라이브
FEED_B  = FeedSpec(session=("09:00","15:35"), merge_night=False)             # 야간 제외
FEED_C1 = FeedSpec(session=("09:00","15:35"), back_adjust="subtract")        # 갭 조정

def build_feed(day_df, night_df, spec: FeedSpec) -> "PriceFrame"
```

`PriceFrame`은 `opens/highs/lows/closes/session_ids/index`와 `space`(actual|adjusted), `to_actual(x)`를 갖습니다.

### 1.2 공간 태그가 붙은 시계열

```python
@dataclass(frozen=True)
class Series:
    values: np.ndarray
    space: Literal["actual", "adjusted"]      # back-adjust 여부
    unit:  Literal["pt", "ratio", "index"]    # 단위
```

`back_adjust()`가 반환하는 `Series`는 `space="adjusted"`가 됩니다. 이것이 §6 강제 장치의 기반입니다.

### 1.3 지표 함수 (전부 순수 함수, 창만 받음)

| 함수 | 시그니처 | 반환 | 전제조건 |
|---|---|---|---|
| `back_adjust` | `(closes: Series, session_ids, mode) -> (adj: Series, offset: np.ndarray)` | 조정 시계열 + 복원용 오프셋 | `closes.space == "actual"` |
| `kalman_path` | `(w: Window, q, r) -> np.ndarray` | 평활 경로 | `len(w) >= 2` |
| `kalman_residual_std` | `(w: Window, q, r, std_window, trim) -> (std_error: float, kf_last: float)` | 잔차 표준편차 + 마지막 평활값 | `len(w) >= std_window` |
| `true_range` | `(high, low, prev_close) -> np.ndarray` | TR (갭 포함) | 일봉 |
| `intraday_range` | `(high, low) -> np.ndarray` | H−L (갭 제외) | 일봉 |
| `kalman_atr` | `(tr: np.ndarray, q=0.002, r=0.2) -> np.ndarray` | 평활 ATR | — |
| `bollinger` | `(w: Window, window, sigma, ddof=1) -> (mid, upper, lower)` | 밴드 3종 | `len(w) >= window` |
| `bandwidth` | `(upper, lower, mid_actual: Series) -> np.ndarray` | 밴드폭 | **`mid_actual.space == "actual"`** |
| `squeeze_threshold` | `(bw, window, quantile) -> np.ndarray` | 스퀴즈 임계 | — |
| `percent_b` | `(price, upper, lower) -> float` | %B | — |
| `moving_average` | `(w: Window, period) -> float` | 이동평균 | `len(w) >= period` |
| `wilder_adx` | `(high, low, close, window=14) -> np.ndarray` | ADX | `len >= 3*window` |
| `wilder_rsi` | `(w: Window, window=14) -> np.ndarray` | RSI | `len > window` |
| `SarState` | 클래스 (§3) | SAR 값 | — |
| `breakout_targets` | `(day_open, prev_range, k) -> (long, short)` | 돌파 타점 | — |
| `kalman_band_targets` | `(kf_price, std_error, mult) -> (long, short)` | 칼만밴드 타점 | — |
| `prev_session_range` | `(daily_high, daily_low, session_ids) -> float` | 전 세션 레인지 | — |
| `filter_session` | `(df, start, end) -> df` | 세션 필터 | — |

### 1.3.1 시리즈(벡터화) 변형 — 백테스터 전용

백테스터는 봉마다 스칼라 함수를 부르는 대신 전 구간 롤링 배열을 미리 만들어 씁니다.
31,793봉 × 48조합을 스칼라 호출로 돌리면 감당이 안 되기 때문입니다. 그래서 **같은 지표에
대해 스칼라판과 배열판을 둘 다** 둡니다.

| 시리즈 함수 | 대응 스칼라 | 시그니처 | 반환 |
|---|---|---|---|
| `bollinger_series` | `bollinger` | `(closes, window, sigma=2.0, ddof=1) -> (mid, upper, lower)` | 배열 3종 |
| `bandwidth_series` | `bandwidth` | `(upper, lower, mid_actual: Series) -> np.ndarray` | 밴드폭 배열 |
| `moving_average_series` | `moving_average` | `(closes, period) -> np.ndarray` | 이동평균 배열 |
| `rolling_quantile` | `squeeze_threshold` | `(arr, window, quantile) -> np.ndarray` | 롤링 분위수 배열 |
| `percent_b_series` | `percent_b` | `(closes, upper, lower) -> np.ndarray` | %B 배열 |

**이 변형이 지켜야 할 조건 셋** — 이걸 어기면 "단일 소스"가 두 갈래로 갈라져 원래 문제가
그대로 재현됩니다.

1. **수치 동일성.** 배열판의 `k`번째 원소는 같은 구간에 스칼라판을 부른 값과 같아야 합니다.
   `bollinger_series`가 pandas `rolling`을 쓰므로 `ddof` 의미론이 스칼라판(`np.std(ddof=1)`)과
   일치함을 `scratch/test_indicators_equivalence_20260812.py`가 검증합니다.
2. **공간 태그 강제는 동일하게 적용.** `bandwidth_series`도 `mid_actual: Series`를 요구하고
   `space != "actual"`이면 `ValueError`를 던집니다. 배열판이라고 §6 강제 장치를 우회할 수
   없습니다. back-adjust 백테스터를 만들 때 여기가 방어선입니다.
3. **미래참조 차단은 호출부 책임.** 시리즈 함수는 `Window`를 받지 않으므로 §2의 구조적
   차단(인덱스를 못 받게 하기)이 걸리지 않습니다. 배열 `x`의 `i`번째 원소는 당봉 종가를
   포함해 만들어진 값이므로, **호출부가 `x[i-1]`로 참조하는 관례를 지켜야** 합니다.
   `backtest_sar_bb_20260809.py`가 2026-08-11에 고친 미래참조 5건이 정확히 이 지점이었고,
   그래서 그 파일의 `bb_upper[i-1]` / `squeeze_limit[i-1]` / `bandwidth[i-1]` / `bb_mid[i-1]` /
   `ma_line[i-1]` 관례는 마이그레이션에서 **그대로 보존**했습니다.

> 조건 3은 이 설계의 약한 고리입니다. 시리즈 함수를 쓰는 한 미래참조는 규약으로만 막힙니다.
> 구조적으로 막으려면 "당봉 제외 시프트"를 함수 안에 넣어야 하는데, 그러면 기존 호출부의
> `[i-1]`과 이중 시프트가 나 동작이 바뀝니다. 마이그레이션이 전부 끝난 뒤 별도 항목으로
> 검토합니다(§9).

### 1.4 (a) 감사 항목 → 흡수 함수 매핑

| ID | 불일치 | 흡수 함수 |
|---|---|---|
| K-1 | SAR AF 틱 vs 봉 | `SarState.on_bar()` / `.on_tick()` (§4) |
| K-2 | `bb_sigma` 하드코딩 2 | `bollinger(sigma=...)` 단일 경로 |
| K-3 | BB 틱 20개 vs 봉 20개 | `bollinger()` + `FeedSpec` (계산은 통일, **선택은 전략 계층**) |
| K-4 | ddof 0/1 | `bollinger(ddof=1)` 명시, `ta` 의존 제거 |
| K-5 | 세션 필터 유무 | `FeedSpec.session` |
| K-6 | 슬리피지 상수 | **미흡수 — 비용 모델은 범위 밖(§8)** |
| K-7 | 재진입 쿨다운 | **미흡수 — 전략 계층(12단계)** |
| K-8 | 수수료율 | **미흡수 — 비용 모델은 범위 밖(§8)** |
| N-1 | 칼만 40봉의 야간 구성 | `FeedSpec.merge_night` + `night_cutoff_hours` |
| N-2 | `prev_range` 출처 | `prev_session_range()` + `FeedSpec` |
| N-3 | ADX/RSI 4벌 | `wilder_adx()` / `wilder_rsi()` 단일 |
| (추가) | std_error 라이브 재현 10% 오차 | `FeedSpec`이 병합 파라미터를 명시화 → 재현 가능 |

**8/11 해결, 3건 미해결**(K-6·K-7·K-8). 미해결 3건은 전부 **비용·전략 계층**이라 이 설계의 범위 밖입니다. §8에 명시했습니다.

---

## 2. 미래참조 차단 규약

### 문제

지금까지 발생한 미래참조 5건(`bb_upper[i]`, `bandwidth[i]` 등)은 전부 **함수가 배열과 인덱스를 함께 받아 내부에서 자르는** 패턴에서 나왔습니다.

### 선택안: 함수는 인덱스를 받지 않는다

```python
@dataclass(frozen=True)
class Window:
    """확정된 봉만 담는 불변 뷰. 생성 시점에 '현재 봉'은 이미 제외되어 있다."""
    values: np.ndarray
    space: Literal["actual", "adjusted"]

    @staticmethod
    def closed_upto(arr: np.ndarray, upto_exclusive: int, size: int, space="actual") -> "Window":
        """arr[upto_exclusive-size : upto_exclusive]. 이름이 배타적임을 드러낸다."""
```

지표 함수는 `Window`만 받습니다. **인덱스를 넘길 방법이 없으므로 `[i]`를 쓸 수가 없습니다.**

### 대안 비교

| 방식 | 강제력 | 비용 | 판정 |
|---|---|---|---|
| 명명 규칙만 (`closes_upto_i`) | 약함 — 지키면 되지만 안 지켜도 통과 | 0 | ❌ 지금까지 실패한 방식 |
| 어서션 (`assert i not in window`) | 중간 — 런타임에만 | 낮음 | 보조로 사용 |
| **`Window` 타입 + 인덱스 미전달** | **강함 — 구조적으로 불가능** | 슬라이스 1회 | ✅ **채택** |
| 정적 타입 검사(mypy) | 강하나 도입 비용 큼 | 높음 | 후순위 |

### 라이브 vs 백테스트 비대칭

라이브에는 **진행 중인 봉**이 있고 백테스트에는 없습니다. 이 차이를 감추면 다시 미래참조가 생깁니다. 그래서 **분리해서 명시적으로 전달**합니다.

```python
# 확정 봉으로 만든 지표
std_error, kf = kalman_residual_std(window, q, r, 20, trim=1)

# 진행 중 가격이 필요한 판정은 인자로 따로 받는다 (이름에 live_가 붙는다)
fired = sar_state.on_tick(live_price=current_price)
crossed = (live_price >= target_long)
```

**규약**: 지표 함수는 `live_price`를 받지 않습니다. 전략 계층만 받습니다. 이렇게 하면 "지표는 확정 봉만, 판정은 현재가"라는 경계가 코드에 그대로 드러납니다.

---

## 3. 상태 있는 지표

### 비교

| | 매 호출 전체 재계산 | 상태 객체 유지 |
|---|---|---|
| 구현 난이도 | 낮음 | 중간 |
| 상태 드리프트 | 없음 | 있음(직렬화 필요) |
| 비용 | O(W)/호출 | O(1)/호출 |
| 경로 의존 표현 | **불가** (SAR의 AF는 진입 이후 경로에 의존) | 가능 |
| 재기동 복원 | 자동 | **직렬화 필요** |

### 선택: 지표별로 다르게

**칼만(가격·잔차·ATR) → 전체 재계산.** 창이 40봉(ATR은 일봉)이라 비용이 무시 가능하고, 지금 라이브·백테스트 **양쪽 다 이미 전체 재계산**입니다. 상태를 들고 다닐 이유가 없고, 재시딩 특성이 그대로 보존됩니다.

**SAR → 상태 객체.** AF는 진입 이후 신고가 횟수에 누적 의존하므로 전체 재계산하려면 진입 이후 모든 틱을 다시 재생해야 합니다. 불가능합니다.

```python
@dataclass
class SarState:
    sar: float
    ep: float
    af: float
    bull: bool
    af_init: float = 0.02
    af_step: float = 0.02
    af_max: float = 0.10

    def on_bar(self, high: float, low: float) -> float: ...   # 기본
    def on_tick(self, live_price: float) -> float: ...        # 명시적 옵션
    def to_dict(self) -> dict: ...
    @staticmethod
    def from_dict(d: dict) -> "SarState": ...
```

### 8단계 상태 영속화와의 맞물림

오늘 아침(07:51) 누군가 `futures_exit_state.json`에 `sar_value`/`sar_ep`/`sar_af`/`sar_bull`을 추가했습니다. **이 설계는 그것을 대체하지 않고 흡수합니다.**

```python
state["sar"] = sar_state.to_dict()          # 저장
sar_state = SarState.from_dict(state["sar"]) # 복원
```

기존 평면 키(`sar_value` 등)와의 호환은 `from_dict`가 두 형태를 모두 받도록 처리합니다. **저장 시각(`saved_at`)이 오래되면 복원하지 않고 재초기화**하는 유효기간 가드를 함께 둡니다(8단계에서 지적한 항목).

---

## 4. 틱 갱신 vs 봉 갱신

### 핵심 결정: **봉이 기본, 틱은 명시적 옵션**

K-1(AF 틱 가속)이 백테스트-라이브 괴리의 최대 원인이고, 회전율 18배 차이를 만듭니다. 그래서 설계 수준에서 봉을 기본으로 못박습니다.

```python
class SarState:
    def on_bar(self, high, low) -> float      # 표준 Parabolic SAR 정의
    def on_tick(self, live_price) -> float    # 비표준. 호출부에 그대로 드러남
```

**불리언 플래그(`accel="tick"`) 대신 메서드를 둘로 나눈 이유**: 호출부가 문자 그대로 `sar.on_tick(...)`이라고 적히므로, 코드 리뷰에서 즉시 보입니다. 플래그는 설정 파일에 숨어 눈에 안 띕니다.

전략 계층에서는 설정으로 고릅니다.

```json
"sar_af_accel": "bar"     // 기본. "tick"으로 바꾸면 현행 라이브 동작
```

**기본값을 `"bar"`로 두는 것은 동작 변경입니다.** 라이브 이식 시 별도 승인 항목으로 분리합니다(§7).

### 다른 지표의 틱/봉

칼만·BB·MA·ADX·RSI는 **전부 봉 기준만 제공**합니다. 라이브가 틱 버퍼로 BB를 만드는 현행 동작(K-3)은 `bollinger()`로 표현되지 않습니다 — 그건 지표가 아니라 **전략 계층의 데이터 선택**이므로, 전략이 어떤 창을 만들어 넘기느냐의 문제로 옮깁니다.

---

## 5. 데이터 공급 계약

`FeedSpec`(§1.1) 하나로 A·B·C1을 전부 표현합니다. **어느 것도 코드에 고정하지 않습니다.**

| | session | merge_night | back_adjust |
|---|---|---|---|
| **A** (현행 라이브) | `None` (전 시간대) | `True` | `None` |
| **B** (야간 제외) | `("09:00","15:35")` | `False` | `None` |
| **C1** (갭 조정) | `("09:00","15:35")` | `False` | `"subtract"` |

`build_feed()`는 `PriceFrame`을 돌려주고, 전략은 거기서 `Window`를 잘라 지표에 넘깁니다. **갭 처리 방식을 바꾸는 것이 `FeedSpec` 한 줄 교체가 됩니다** — 지금은 라이브 함수 본문을 고쳐야 합니다.

`warmup_bars`는 지표가 안정되기 전 구간을 표시합니다. 갭 측정에서 개장 후 8봉까지 칼만 지연이 남는 것이 확인됐으므로, 전략이 이 값을 보고 진입 시작 시점을 정할 수 있게 합니다(도입 여부는 별도 결정).

---

## 6. back-adjust 제약 강제

### 문제 (갭 측정에서 실측)

차감식 back-adjust는 가격 수준 자체를 낮춥니다. `밴드폭 = 4σ / 중심선`에서 분모에 조정 가격을 쓰면 **8.419%** 가 나옵니다. 실제 가격 mid를 쓰면 **2.188%** 입니다. **3.8배 왜곡**입니다.

### 강제 장치

```python
def bandwidth(upper, lower, mid_actual: Series) -> np.ndarray:
    if mid_actual.space != "actual":
        raise ValueError(
            "밴드폭 분모에는 실제 가격 공간의 중심선이 필요합니다. "
            "back-adjust된 시계열을 넘겼습니다 — frame.to_actual(mid)로 환원하세요."
        )
```

`Series.space` 태그가 `back_adjust()`에서 `"adjusted"`로 붙고, `to_actual()`을 거쳐야 `"actual"`로 돌아옵니다. **실수로 조정 가격을 넣으면 즉시 예외**입니다.

### 공간별 지표 분류 (설계 문서에 고정)

| 공간 | 지표 | 이유 |
|---|---|---|
| **adjusted 사용 가능** | 칼만 평활·잔차 std_error, SAR, 손절폭, TR/ATR, 이동평균 | 전부 **차이(pt)** 기반. 평행이동에 불변 |
| **actual 필수** | 밴드폭 분모, %B의 가격, 체결가, 증거금, 손익 환산 | **비율 또는 절대 가격**. 평행이동에 민감 |

---

## 7. 마이그레이션 계획

**중간 상태에서도 시스템이 동작해야 하므로, 파일 단위로 하나씩 갈아끼웁니다.**

| 순서 | 대상 | 검증 방법 | 위험 |
|---|---|---|---|
| 0 | `indicators.py` 신규 + 유닛테스트 | 실 DB 데이터로 **골든 벡터** 생성 → 기존 구현과 부동소수 오차 내 일치 입증 | 없음(신규) |
| 1 | `scratch/backtest_bb_meanrev_20260811.py` | 전략 탈락 확정본이라 결과가 바뀌어도 무해. 추출 검증용 | **최저** |
| 2 | `scratch/backtest_sar_bb_20260809.py` | 전 구간 스윕 재실행 → 기존 결과와 **완전 일치** 확인 | 낮음 |

**진행 상황 (2026-08-12 16:15 기준)**

| 순서 | 상태 | 결과 |
|---|---|---|
| 0 | ✅ 완료 | 동일성 26항목 통과(최대오차 0.000) |
| 1 | ✅ 완료 | 14조합 5,570거래 전체 일치. 백업 `*.bak_before_indicators_20260812_135200` |
| 2 | ✅ 완료 | **48조합 51,974거래 전체 일치**(불일치 0). 백업 `*.bak_before_indicators_20260812_154500` |
| 3~8 | ⬜ 대기 | 3번은 샹들리에 경로를 골든 테스트에 필수 포함 |

2번에서 확인된 것 — 인라인 SAR을 `SarState`로 바꿀 때 **갱신 순서(전진 → 클램프 → EP/AF)**
와 **EP 갱신 기준값**을 둘 다 맞춰야 합니다. 이 백테스터는 EP를 고가/저가가 아니라 **종가**로
갱신하는 비표준 구현이라, `on_bar(high=c_close, low=c_close, clamp=peak_price)`로 넘겨
동작을 보존했습니다. 표준 정의(`on_bar(c_high, c_low)`)로 바꾸는 것은 **동작 변경**이므로
이 마이그레이션 범위 밖이며, 8단계와 함께 별도로 다룹니다.
| 3 | `bqa/kalman_backtester.py` | 샹들리에 리플리카 794건 재실행 → 일치 확인 | 중간(검증 이력 최다) |
| 4 | `bqa/enhanced_backtester.py`, `bqa/backtester.py`, `futures_trader/backtester.py` | 사실상 미사용. 일치만 확인 | 낮음 |
| 5 | **라이브** `update_bb_psar_filters()` | 읽기 전용 필터. 골든 테스트 | 중간 |
| 6 | **라이브** `update_futures_dynamic_sl_tp()` (ATR) | 오늘 실측 73.94 재현 확인 | 중간 |
| 7 | **라이브** `update_kalman_targets()` | 진입 타점·게이트에 직결. **가장 위험** | **높음** |
| 8 | **라이브** SAR → `SarState` + `on_bar` 전환 | **동작 변경**(K-1). 별도 승인 + 페이퍼 검증 | **최고** |

**5~8은 각각 별도 승인**을 받습니다. 특히 8은 회전율을 22건/일 → 1.2건/일로 바꾸는 실질적 전략 변경입니다.

### 검증 방법 둘

**① 추출 동일성 유닛테스트**: 실 DB에서 뽑은 고정 입력에 대해 기존 구현과 새 함수의 출력이 `np.allclose(rtol=1e-9)`로 일치. 기준 구현 위치는 아래에 확정해 뒀습니다.

| 대상 | 기준 구현 |
|---|---|
| 칼만(가격·잔차) | `era_order_manager.py:1124-1160` |
| 칼만 ATR | `era_order_manager.py:847-878` |
| BB·스퀴즈·MA | `era_order_manager.py:985-999` |
| 틱 BB | `era_order_manager.py:4406-4413` |
| SAR | `era_order_manager.py:4457-4461`, `4665-4668` |
| 칼만/ATR/BB (백테) | `scratch/backtest_sar_bb_20260809.py:57-127` |
| ADX/RSI/%B | `scratch/backtest_bb_meanrev_20260811.py:36-99` |
| 칼만 리플리카 | `bqa/kalman_backtester.py:682-712`, `1152-1182` |

**② 골든 테스트**: 백테스터는 전 구간 재실행 후 거래 로그 전체(진입시각·가격·청산사유·손익)가 바이트 단위로 일치해야 합니다. **불일치가 나오면 그건 버그 수정이므로 별도 항목으로 명시**하고, 어느 결론이 바뀌는지 함께 보고합니다.

---

## 8. 범위 밖

| 항목 | 이유 | 어디서 |
|---|---|---|
| **비용 모델**(슬리피지 K-6, 수수료 K-8) | 지표가 아니라 체결 모델. 측정 원장이 실측을 쌓는 중 | 별도 단계 |
| **재진입 쿨다운**(K-7) | 전략 규칙 | 12단계 |
| **전략 인터페이스 분리** | 진입/청산 로직의 재구성 | 12단계 |
| **리스크 계층 분리**(RiskManager) | 손실한도·사이징 | 12단계 |
| **백테스트-라이브 공유 실행 엔진**(틱 시뮬레이션) | 실행 모델. 지표보다 상위 | 별도 |
| **갭 방식 A/B/C1 최종 선택** | 이 설계는 셋 다 표현만 함. 선택은 전 기간 백테스트 후 | (d) 이후 |
| **성능 지표 표준화**(PF/MDD, 자본 음수 왜곡) | 평가 계층 | 별도 |
| **워밍업 진입 시작 시점** | 전략 파라미터 | C1 채택 후 검증 |

---

## 9. 결정이 필요한 항목

| # | 항목 | 선택지 | 제안 |
|---|---|---|---|
| D-1 | `SarState` 기본 가속 단위 | `on_bar` / `on_tick` | **`on_bar`** — K-1 해소. 단 라이브 적용은 8순위로 분리 |
| D-2 | `Window` 타입 도입 | 도입 / 명명 규칙만 | **도입** — 미래참조 5건이 전부 명명 규칙 실패에서 나옴 |
| D-3 | `Series.space` 태그 | 도입 / 주석으로만 | **도입** — 밴드폭 3.8배 왜곡을 런타임에 잡음 |
| D-4 | 칼만 상태 처리 | 전체 재계산 / 상태 객체 | **전체 재계산** — 현행과 동일, 비용 무시 가능 |
| D-5 | `ta` 라이브러리 의존 | 유지 / 제거 | **제거** — ddof=0 불일치(K-4)의 원인. `market_regime_monitor.py`도 자체 ADX로 |
| D-6 | 마이그레이션 1번 대상 | `bb_meanrev` / `sar_bb` | **`bb_meanrev`** — 탈락 확정본이라 위험 0 |
| D-7 | 라이브 이식 범위 | 5~7까지 / 8까지 | **일단 5~7만.** 8(SAR 봉 전환)은 페이퍼 검증 후 별도 |

---

## 10. 예상 작업량

| 단계 | 작업 |
|---|---|
| (c) `indicators.py` + 유닛테스트 | 3~4일 |
| (d) 백테스터 4개 이식 + 골든 테스트 | 3~4일 |
| 라이브 이식 5~7 | 2~3일 + 페이퍼 검증 |
| 라이브 이식 8 (SAR 봉 전환) | 1일 + **페이퍼 10거래일** |

**(c)~(d)까지는 라이브를 건드리지 않으므로 ERA 정지 여부와 무관하게 진행 가능합니다.**
