# 리플레이 하네스 — 1단계 shim 구축 및 격리 검증 보고

작성: 2026-08-12 / 범위: **shim 구축까지만**. 계측·스트림 공급·A/B 대조·음성 대조는 다음 세션.
근거 설계: `리플레이_하네스_설계안_20260812.md`

---

## 0. 결론

`era/era_order_manager.py`의 `ERAOrderManager`를 **`__init__`을 그대로 태워** 인스턴스화하는 데 성공했고,
주문은 어떤 경로로도 실 API에 닿지 않으면서 **기록만 남는 상태**임을 실행으로 입증했다.

| 검증 항목 | 결과 |
|---|---|
| 인스턴스화 (`__new__` 우회 없음) | **성공** — 속성 235개, 타이머 17개 등록 |
| 주문 시도 시 실 API 도달 | **0건** (기록 6건) |
| 인스턴스화 중 주문 시도 | **0건** |
| 라이브 상태 파일 쓰기 | **0건** |
| 네트워크 접속 시도 | **0건** |
| 서브프로세스 실행 | **0건** |
| DB 연결 | **0건** |
| 복사본 바이트 동일성 | **일치** (sha256 `0ee30d3d…8e3c`) |
| 라이브 원본 3개 파일 수정 | **없음** |

---

## 1. 산출물

```
scratch/replay/
  qt_shim.py            # PyQt5 / QAxContainer / QtCore / notifier 가짜 모듈
  guard.py              # 파일쓰기·네트워크·서브프로세스 감사 후크
  run_shim_check.py     # 1단계 검증 러너 (유일한 실행 진입점)
  config/               # 라이브 config 5종의 읽기전용 복사본
  variants/era_A.py     # 라이브 원본의 바이트 동일 복사본
  out/shim_check_report.json
```

실행:

```
cd scratch/replay
PYTHONDONTWRITEBYTECODE=1 python run_shim_check.py
```

`PYTHONDONTWRITEBYTECODE=1`을 붙이면 `__pycache__` 생성까지 사라져 **파일 쓰기 0건**이 된다.
붙이지 않으면 `site-packages/*/__pycache__` 4건(bs4·soupsieve·lxml)이 잡히는데, 전부 파이썬 바이트코드
캐시이고 라이브 상태와 무관하다.

---

## 2. 인스턴스화 방식 — `__init__`을 그대로 탄다

설계안 2-3의 방침대로 `__new__` 우회를 쓰지 않았다. `sys.modules`에 가짜 모듈을 먼저 심고,
그냥 `ERAOrderManager()`를 호출했다.

```python
qt_shim.install()                 # sys.modules 에 PyQt5 계열 주입
spec.loader.exec_module(era_mod)  # variants/era_A.py import
mgr = era_mod.ERAOrderManager()   # ← __init__ 전체 실행
```

`__init__`(L404–757)이 끝까지 돌았다는 근거:

- 인스턴스 속성 **235개** 생성
- Kiwoom 이벤트 5종 전부 connect 등록 (`OnEventConnect`, `OnReceiveTrData`, `OnReceiveChejanData`, `OnReceiveMsg`, `OnReceiveRealData`)
- QTimer 17개 생성 / 그중 10개 `start()` 호출 — `trading_mode='futures'` 분기까지 정확히 반영
- `load_config()` → `environment='live'`, `trading_mode='futures'`, `futures_strategy_type='parabolic_sar'` 로드
- `__init__` 말미의 재호출(`load_config()` / `load_futures_exit_state()`)까지 도달

### 경로 리디렉션이 자동으로 걸리는 구조

라이브 코드는 `workspace_root = os.path.abspath(os.path.join(current_dir, ".."))`로 계산한다.
복사본을 `scratch/replay/variants/`에 두면 `workspace_root`가 **`scratch/replay`로 자동 이동**하고,
그 아래 모든 경로가 따라 이동한다. 경로를 손으로 몽키패치할 필요가 없다.

| 속성 | 실제 값 |
|---|---|
| `workspace_root` | `…/scratch/replay` |
| `config_path` | `…/scratch/replay/config/config.json` (복사본) |
| `futures_db_path` | `…/scratch/replay/futures_data.db` (미존재) |
| `unified_db_path` | `…/scratch/replay/unified_data.db` (미존재) |
| `positions_persist_path` | `…/scratch/replay/era/era_positions.json` |
| 모듈 로그 `log_file` | `…/scratch/replay/variants/era_order_manager.log` |

즉 **라이브 `config/`, `era/`, `*.db`는 물리적으로 참조 대상이 아니다.**
설정값의 충실도를 위해 라이브 config 5종을 `scratch/replay/config/`로 복사해 두었다(읽기만).

---

## 3. 주문 차단 — 3중 장벽

### 장벽 0 (물리) — 실 PyQt5 부재
검증 환경에 PyQt5가 설치되어 있지 않다. `import PyQt5` → `ModuleNotFoundError`.
가짜 모듈을 심지 않으면 애초에 import조차 실패한다. 실 `KHOPENAPI.KHOpenAPICtrl.1` OCX는 생성 시도조차 없다.

### 장벽 1 — `self.kiwoom`이 가짜 객체
```
type(mgr.kiwoom) == qt_shim.FakeQAxWidget
```
라이브 코드의 유일한 외부 통로가 이 객체이며, 통로가 여기서 끝난다.

### 장벽 2 — `dynamicCall` 문자열 인터셉트
`FakeQAxWidget.dynamicCall`이 시그니처에 `SENDORDER` 토큰이 있으면
**기록 후 `0` 반환**한다. 예외를 던지지 않는다 — 라이브 로직이 "주문 성공(res==0)" 이후의
상태 전이(락 해제, `is_exiting` 마킹, 청산가 기록)를 계속 타야 그 흐름까지 대조 대상이 되기 때문이다.

### 정적 경로 추적 (AST)

소스 전체를 AST로 파싱해 `SendOrder` 문자열을 인자로 갖는 호출을 전수 조사했다.

| 항목 | 값 |
|---|---|
| `dynamicCall` 호출 지점 | 155개 |
| `dynamicCall`의 수신자 | **`self.kiwoom` 단 하나** |
| `self.kiwoom.dynamicCall` 밖의 주문 호출 | **0건** |
| 위험 import (`win32com`/`comtypes`/`pythoncom`) | **0건** |

`ctypes`·`socket` import는 `if __name__ == "__main__":` 블록에만 있고(L7157/7166),
하네스는 모듈을 import만 하므로 그 블록은 실행되지 않는다. `era.pid` 기록과 포트 9991 락도 같은 이유로 미실행이다.

> L6587의 `print("… SendOrderFO 전송 …")`는 로그 문자열이라 주문 호출이 아니다. 초기 스캔에서
> 오탐으로 잡혔고, 분류기를 고쳐 배제했다.

### 실행 검증 — 실제로 불러봤다

| 테스트 | 도달 | 전송 시그니처 | 결과 |
|---|---|---|---|
| `dynamicCall("SendOrderFO…")` 직접 | — | SendOrderFO | 기록, `0` 반환 |
| `dynamicCall("SendOrder…")` 직접 | — | SendOrder | 기록, `0` 반환 |
| `_execute_futures_direct("LONG_ENTER", 350.25, "101W9000", "KOSPI200_DAY")` | ✔ | SendOrderFO `[…,1,'2','3',1,'0','']` | 기록만 |
| `_execute_futures_direct("SHORT_ENTER", …)` | ✔ | SendOrderFO `[…,1,'1','3',1,'0','']` | 기록만 |
| `_execute_futures_direct("LONG_EXIT", …, 보유 2계약)` | ✔ | SendOrderFO `[…,1,'1','3',2,'0','']` | 기록만 |
| `_execute_futures_direct("SHORT_ENTER", …, "KOSPI200_NIGHT")` | ✔ | SendOrderFO (야간 분기) | 기록만 |

**주문 의도 6건 기록 / 실 API 호출 0건.** 수량 계산(고정수량 1, 청산 시 보유수량 2)과
주야간 분기, 매수/매도 구분(`slby_tp`)까지 라이브 로직 그대로 통과한 뒤 마지막 통로에서 잡혔다.

> 주의 — `signal_type`의 유효값은 `LONG_ENTER` / `SHORT_ENTER` / `LONG_EXIT` / `SHORT_EXIT`이다
> (L5549 `direction_map`). `LONG_ENTRY`처럼 어긋난 키를 주면 **조용히 `return`**하고 아무 로그도
> 남지 않는다. 다음 세션 계측에서 이 무증상 조기반환 지점을 반드시 기록 대상에 넣어야 한다.

---

## 4. 파일 쓰기 — 요청하신 목록

`sys.addaudithook`으로 `open`(쓰기모드)·`os.remove`·`os.rename`·`os.mkdir`·`sqlite3.connect`·
`socket.*`·`subprocess.*`를 전수 감시했다.

**인스턴스화 및 주문 테스트 전 과정에서 발생한 파일 쓰기: 0건.**
(`PYTHONDONTWRITEBYTECODE=1` 미설정 시에만 `__pycache__` 디렉터리 4개 생성 — 라이브 무관)

**라이브 상태 파일 접근: 전무.** 아래는 접근 시 즉시 `RuntimeError`로 프로세스를 세우도록
`guard.PROTECTED_BASENAMES`에 등재해 둔 목록이며, 한 건도 발동하지 않았다.

```
active_strategy.json, futures_exit_state.json, system_status_futures.json,
era.pid, era_positions.json, system_stopped.flag, emergency_kill.flag,
run_era.bat, auto_reconnect_era.bat,
era_order_manager.py, indicators.py, kalman_backtester.py,
futures_data.db, unified_data.db, config.json, config_local.json
```

리디렉션은 **이미 되어 있다.** 3절에서 설명한 대로 `workspace_root`가 복사본 위치를 따라
`scratch/replay`로 자동 이동하므로 별도 몽키패치가 필요 없다. 다음 세션에서 실 DB를 읽어야 할 때만
설계안대로 `mode=ro` URI로 열고, 가드가 `mode=ro`가 아닌 라이브 DB 연결을 차단하도록 이미 심어 두었다.

### 왜 쓰기가 0건인가 (코드 근거)

`__init__`이 부르는 로더 3종은 전부 읽기 전용이다.

- `load_config()` (L1253) — `open(..., "r")` 3회(`config.json`, `config_local.json`, `active_strategy.json`)
- `load_persisted_positions()` (L1506) — `open(..., "r")` 1회
- `load_futures_exit_state()` (L1535) — 읽기 + 순수 대입

쓰기를 하는 `persist_positions()`(L1516) / `save_futures_exit_state()`(L1685)는 별도 메서드이며
`__init__`에서 호출되지 않는다. 실제 실행에서도 sqlite 연결 0건, 파일 쓰기 0건으로 확인됐다.

---

## 5. 네트워크

**외부 접속 시도 0건.** 가드는 `socket.connect`·`socket.getaddrinfo`·`urllib.Request`·
`http.client.connect`를 전부 차단(즉시 중단)하도록 설정돼 있고 한 번도 발동하지 않았다.

기록된 유일한 소켓 이벤트는 **루프백 프로브 1건**이다:

```
urllib3/util/connection.py:137   HAS_IPV6 = _has_ipv6("::1")   →  sock.bind(("::1", 0))
```

`import requests`(라이브 L7)의 부수효과로 urllib3가 IPv6 지원 여부를 확인하려고 `::1`에
bind하는 것이다. 외부로 나가지 않는 로컬 능력 확인이므로 기록만 하고 통과시키되,
루프백이 아닌 모든 bind/connect는 계속 차단한다.

네트워크를 실제로 타는 라이브 코드 3곳은 전부 미실행이다:

| 위치 | 내용 | 미실행 사유 |
|---|---|---|
| L159 | 네이버 KODEX200 일봉 크롤링 | `AMATSDynamicAllocator.detect_regime` — `__init__` 미호출 |
| L356, L366 | 네이버 테마 크롤링 | `MorningPrepWorker(QThread)` — shim의 `start()`가 no-op |
| dynamicCall `CommConnect` | 키움 로그인 | 가짜 객체가 흡수, 실 OCX 없음 |

---

## 6. 라이브 시스템 안전 조건 — 준수 확인

| 조건 | 상태 |
|---|---|
| ERA 미기동 | `era/era.pid` 파일 자체가 없음. 기동 시도 없음 |
| 실 주문 API 호출 | 0건 |
| `era_order_manager.py` / `indicators.py` / `bqa/kalman_backtester.py` 수정 | 없음 (mtime·sha256 불변) |
| `config/active_strategy.json` 쓰기 | 없음 (mtime 08-08 그대로) |
| `era/futures_exit_state.json`, `tca/system_status_futures.json` 쓰기 | 없음 |
| `system_stopped.flag`, `emergency_kill.flag` | 존재 유지, 무접촉 |
| `run_era.bat` / `era/auto_reconnect_era.bat` STOP-GUARD | 각 4개 매치 유지, 무접촉 |
| TCA·KIS 야간수집기 | 무접촉 |

> `futures_data.db`의 mtime이 갱신되고 있으나 이는 실행 중인 TCA/KIS 야간수집기의 정상 동작이다.
> 하네스의 sqlite 연결은 **0건**으로 계측됐다.

---

## 7. 남은 충실도 격차 (다음 세션 입력)

이번 실행에서 관측된, 계측 전에 정리해야 할 항목들:

1. **`trade_ledger` 모듈 미해결** — `_ledger()`가 `No module named 'trade_ledger'`로 실패하고
   "기록 없이 매매 계속"으로 흘렀다. 라이브에서는 `workspace_root`에 있어 로드된다.
   측정원장 훅이 주문 경로에 붙어 있으므로(L5620 부근), 대조 대상에 넣을지 스텁으로 무력화할지
   정해야 한다. 지금은 라이브와 동작이 다르다.
2. **`datetime.now()` 미고정** — 설계안 2-2의 `FrozenDatetime` 미적용. 현재는 실제 시각으로 돈다.
   세션 판정·일일 리셋·`today_str` 비교가 전부 실제 날짜를 따르므로 스트림 공급 전에 반드시 필요하다.
3. **`sys.stdout` 처리** — 라이브 모듈이 import 시점에 `sys.stdout`을 `SafeStreamWrapper`로
   교체한다. 하네스가 즉시 되돌려 잡고 있으나, 계측 단계에서 로그 캡처 방식을 확정해야 한다.
4. **무증상 조기반환 지점** — 3절 말미의 `direction_map` 미스처럼, 로그 한 줄 없이 `return`하는
   게이트가 여러 곳 있다. 설계안 2-4(2)의 게이트 판정 계측이 이 지점들을 모두 덮어야 한다.
5. **타이머 발화 정책** — 17개 등록 중 10개가 `start()`된 상태로 남아 있다. shim은 자발 발화를
   하지 않으므로 안전하지만, 설계안 2-2(5)대로 15분 ATR 타이머만 리플레이 클럭으로 발화시키는
   선택 로직을 다음 세션에 넣어야 한다. (`FakeQTimer.fire()` / `drain_deferred()` 준비 완료)

---

## 8. 승인 요청 (설계안 4부에 대한 답을 아직 못 받은 상태)

설계안 4부의 3개 질문은 그대로 열려 있으며, 2번·3번은 다음 세션 시작 전에 정해져야 합니다.

1. 구현 순서 — 제안대로 ② 계측 → ③ P0 양성대조 → ④ N1~N6 음성대조 로 진행할지
2. 틱 근사 — 4틱/분(O,H,L,C) 두 순서로 확정할지
3. SAR 의미 차이 — `SarState.on_tick` 보존 / 봉 기반 전환 중 어느 쪽인지
   (봉 기반으로 바꾸면 하네스는 반드시 FAIL을 냅니다)

여기에 더해 이번 세션에서 생긴 질문 하나:

4. **`trade_ledger` 처리** — 대조 대상에 포함할지, 스텁으로 무력화할지 (7-1 항목)
