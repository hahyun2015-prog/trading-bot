# 선물 자동매매 전략 전수 종합분석 및 개선안

- 작성일: 2026-08-11
- 대상: `c:\Antigravity\AI_T_Agent` (ERA 주문엔진 + 국면 모니터 + 백테스트 스크립트)
- 범위: 주간/야간 지수선물 4개 전략(kalman, chandelier, parabolic_sar, bollinger_band) + 개별주식선물(ISF) + 공통 인프라
- 성격: **분석 및 제안 전용. 코드는 일절 수정하지 않았음.**
- 출발점: 직전 세션의 볼린저밴드 단독 분석 결과(본 문서 §3에서 전부 재확인 및 확장)

---

## 0. 한눈에 보기 — 가장 중요한 5가지

| # | 항목 | 심각도 | 한 줄 요약 |
|---|---|---|---|
| C-1 | ATR14가 일봉 TR(오버나잇 갭 포함)로 계산되어 **77pt**까지 부풀고, 그 값이 SAR의 **당일 손절폭**으로 그대로 쓰임 | 치명 | 하루 레인지(≈32pt)의 2.4배가 손절폭 → 15계약 기준 1회 손실 한도가 **5,800만원**. 손절이 사실상 무력 |
| C-2 | 선물에는 **금액 기준 일일/월간 손실 한도가 전혀 없음** (주식에만 존재) | 치명 | 서킷브레이커가 "연속 손실 횟수"뿐이고 그마저 SAR/BB에는 미적용 |
| C-3 | 국면 스위칭이 **포지션 보유 중에도 엔진을 강제 kill → 60초+ 대기 → 재기동** | 치명 | 그 사이 손절 감시 공백. 재기동 후엔 SAR 내부상태(sar_value/ep/af/bull)가 전부 유실되어 트레일링이 죽은 채로 재개 |
| C-4 | `bollinger_band` 전략은 **구조적으로 거래 0건** (std_error 미갱신 + BB 필터 미갱신) | 치명 | 국면 모니터가 박스권 판정 시 이 전략으로 전환하는데, 전환하는 순간 시스템이 조용히 멈춤 |
| H-1 | 백테스트에 미래참조 + 라이브와 계산 기준 불일치(틱 vs 봉, 서킷브레이커, 쿨다운) | 높음 | 현재 SAR/BB 백테스트 수치는 **의사결정 근거로 쓸 수 없는 수준** |

---

## 1. 전략 인벤토리

### 1.1 전략 목록과 활성 상태

| 전략 | 선택 키 | 진입 판단 위치 | 청산 판단 위치 | 현재 활성 | 동작 가능 여부 |
|---|---|---|---|---|---|
| `kalman` | `futures_settings.futures_strategy_type` | `_process_day_tick` L4714/4783 | L4374~4458 (`else` 분기) | ✗ | 가능 |
| `chandelier` | 〃 | 〃 | L4345~4373 / L4529~4560 | ✗ | 가능 (가장 검증량 많음) |
| `parabolic_sar` | 〃 | 〃 | L4276~4307 / L4470~4500 | **✓ (config_local.json)** | 가능하나 §3 결함 다수 |
| `bollinger_band` | 〃 | 〃 | L4308~4344 / L4501~4528 | ✗ | **불가 — 거래 0건 (§3 C-4)** |
| `volatility_breakout` (레거시 기본값) | 미지정 시 fallback | 〃 | L4374~4458 (`else`) | ✗ | 가능 |
| **ISF (개별주식선물)** | `individual_stock_futures` 배열 | `_process_isf_tick` L3905~3910 | L3823~3900 | ✗ (배열 빈값) | 가능 |
| 야간선물 | `trade_futures_night` | `_process_night_tick` L5177/5199 | L5215 이전 | ✗ (`false`) | kalman/chandelier/고정SL만 지원 |

> 파일은 전부 `era/era_order_manager.py` (6,854행). 라인 번호는 2026-08-11 시점 기준.

### 1.2 전략별 상세

#### (1) kalman
- **진입 타점**: `update_kalman_targets()` L1191-1194. 최근 40개 5분봉에 1차원 칼만필터(q=5e-5, r=1.0) → `target = kf_price ± std_error × kf_mult(0.6)`
- **진입 게이트**: 15분봉 장기 칼만 기울기(`trend_direction`) 역방향 차단 + `tp_price` 초과 시 진입금지(무한루프 방지, L4722)
- **손절**: `sl_limit = clamp(kf_sl_mult(5.0)×std_err, floor=max(1.5×std_err, 2.0), 1.2×ATR)` 후 하드캡 `_effective_sl_hard_cap()` (동적: 2.0×std_err, 8~30pt)
- **익절**: 진입시 스냅샷한 3-Sigma 타깃(`kf_tp_sigma_mult`=4.0, 추세일치 시 `trend_tp_sigma_mult`=10.0) + 3단 티어 트레일링(L4409-4443)
- **특이**: 유일하게 `_effective_sl_hard_cap`을 쓰는 전략

#### (2) chandelier
- **진입 타점**: kalman과 **동일** (`update_kalman_targets`, 코드 분기가 `("kalman","chandelier")`로 묶임)
- **진입 게이트**: kalman 게이트 + `regime_filter_enabled`(NEUTRAL도 차단, L4719/4788)
- **손절/익절 통합**: `sl_limit = inf`로 두고 샹들리에 트레일 단일공식 — `dist = min(mult(0.3)×ATR14, hard_cap 60pt)` → `_apply_session_range_cap` → `_apply_profit_lock` → `_day_time_stop_fire`
- **특이**: `session_range_cap` / `profit_lock` / `hard_stop` / `time_stop` **4개 리스크 장치를 독점**. 다른 전략에는 코드 경로 자체가 없음

#### (3) parabolic_sar (현재 운영 중)
- **진입 타점**: 칼만 아님. **변동성 돌파** — `day_open ± prev_range × best_k(0.2)` (L4153-4154). 하루 한 번 고정, 장중 불변
- **진입 게이트**: ATR컷오프(15.0) → std_error(≥1.5) → **MA200 방향필터** → BB Squeeze 회피 → BB 중심선 방향확인
- **손절**: `sl_limit = max(ATR14 × 1.0, 2.0)` (L4254) — **§3 C-1의 진원지**
- **청산**: SAR 값 역전 (`sar_af` 0.02→0.10, EP는 틱 단위 갱신)
- **누락**: 익절 개념 없음(SAR 역전만), time_stop/profit_lock/hard_stop/session_range_cap 전부 미적용, 서킷브레이커 미적용

#### (4) bollinger_band
- **진입 타점**: SAR과 동일한 **변동성 돌파** (BB를 진입에 안 씀 — 이름과 실제가 불일치)
- **진입 게이트**: ATR컷오프 → std_error(**여기서 전부 막힘**) → MA200. Squeeze 필터는 SAR 전용이라 미적용
- **손절**: `max(ATR14 × 1.2, 2.0)`
- **익절**: `bb_upper`/`bb_lower` 터치 — 이 밴드는 **틱 20개 롤링**(L4233-4241), 봉이 아님
- **폴백 트레일링**: `MFE ≥ 1.5×std_err` 이후 `0.5×std_err` 되돌림
- **서킷브레이커**: 코드 주석에 "생략합니다"라고 명시 (L4313)

#### (5) ISF (개별주식선물)
- **진입**: RSA 방향(LONG/SHORT/NEUTRAL) × `day_open ± prev_range × best_k(0.35)` 돌파
- **청산**: 고정 %손절(1.5%) / %트레일링 / 고정 %익절(4.0%)
- **미적용**: 서킷브레이커, 재진입 필터, 쿨다운, 장마감 강제청산, ATR/std_error 게이트 — **공통 인프라와 완전히 분리된 별개 시스템**
- 현재 `individual_stock_futures: []`로 비활성이나, 값을 채우면 즉시 무방비로 돌아감

---

## 2. 기법별 연관성 분석 (핵심)

### 2.1 공통 인프라 적용 매트릭스

`○` 적용 · `△` 코드는 있으나 실질 무효 · `✗` 미적용

| 공통 인프라 | 근거 위치 | kalman | chandelier | **parabolic_sar** | bollinger_band | ISF | 야간 |
|---|---|:--:|:--:|:--:|:--:|:--:|:--:|
| 칼만 타점 갱신 (`update_kalman_targets`) | L3184-3191 | ○ | ○ | △ (std_error만) | **✗** | ✗ | ○ |
| `futures_std_error` 채워짐 | L1161-1176 | ○ | ○ | ○ (b19294d 수정) | **✗ → 0.5 고정** | ✗ | ○ |
| std_error 진입게이트 (1.5) | L4701 | ○ | ○ | ○ | **○ (전면차단)** | ✗ | ○ |
| ATR14 (`update_futures_dynamic_sl_tp`) | L809 | ○ | ○ | ○ | ○ | ✗ | ○ |
| ATR 컷오프 게이트 (15.0) | L4695 | ○ | ○ | ○ | ○ | ✗ | ○ |
| BB/Squeeze 필터 갱신 | L883, L3192 | **✗** | **✗** | ○ | **✗** | ✗ | ✗ |
| BB Squeeze 진입 회피 | L4747/4816 | ✗ | ✗ | ○ | ✗ | ✗ | ✗ |
| **MA200 방향필터** | L4732/4801 | △ | △ | ○ | △ | ✗ | ✗ |
| 15분 칼만 추세필터 | L4712 | ○ | ○ | **✗** | **✗** | ✗ | ○ |
| 레짐필터 (NEUTRAL 차단) | L4719 | ✗ | ○ | ✗ | ✗ | ✗ | ✗ |
| 재진입 휩소필터 | L1647 | ○ | ○ | ○ | ○ | ✗ | ○ |
| 방향무관 쿨다운(90s) | L1614 | ○ | ○ | ○ | ○ | ✗ | ○ |
| 같은방향 쿨다운(301s) | L1633 | ○ | ○ | ○ | ○ | ✗ | ○ |
| SL 하드캡 (`_effective_sl_hard_cap`) | L1682 | ○ | ✗ | **✗** | **✗** | ✗ | ○(kalman만) |
| session_range_cap | L1695 | ✗ | ○ | ✗ | ✗ | ✗ | ✗ |
| profit_lock (2단계 이익보전) | L1720 | ✗ | ○ | ✗ | ✗ | ✗ | ✗ |
| hard_stop (초기 하드손절) | L1738 | ✗ | ○ | ✗ | ✗ | ✗ | ✗ |
| time_stop (10분/MFE 4pt) | L1759 | ✗ | ○ | ✗ | ✗ | ✗ | ✗ |
| **연속손실 서킷브레이커** | L4665 | ○ | ○ | **✗** | **✗** | ✗ | ○/✗ |
| 일일 거래 하드캡(50) | L4656 | ○ | ○ | ○ | ○ | ✗ | ○ |
| 일일 거래횟수 제한(10) | L4659-4660 | ✗(면제) | ✗(면제) | ✗(면제) | ✗(면제) | ✗ | ✗ |
| 15:00 진입종료 게이트 | L4686-4690 | ○ | ○ | ○ | ○ | ✗ | ✗ |
| 15:35 장마감 강제청산 | L4194 | ○ | ○ | ○ | ○ | **✗** | ✗ |
| 08:45 장전 안전청산 | L4099 | ○ | ○ | ○ | ○ | ✗ | ○ |
| **금액 기준 일일 손실한도** | — | **✗** | **✗** | **✗** | **✗** | ✗ | ✗ |
| **금액 기준 월간 킬스위치** | L6547 | ✗(주식만) | ✗ | ✗ | ✗ | ✗ | ✗ |

### 2.2 공통 게이트가 특정 전략에만 맞춰져 다른 전략을 무력화하는 지점

**(A) `min_std_error_entry = 1.5` — 대표 사례**
- 이 값은 kalman/chandelier의 `std_error` 분포(2026-07-30 bqa 백테스트)를 기준으로 정해졌다.
- 그런데 게이트는 L4701에서 **전략 무관**으로 걸린다. 반면 `std_error`를 채우는 책임은 L3184-3191에서 **전략별**로 나뉜다.
- 결과: `bollinger_band`는 값이 `0.5`(L621 초기값)에 고정 → `0.5 < 1.5` → **100% 차단**. SAR도 2026-08-09~10 이틀간 동일 증상(커밋 `b19294d`).
- **구조적 결함**: "게이트는 전역, 데이터 공급은 전략별"이라는 비대칭. 전략을 하나 추가할 때마다 같은 버그가 재발한다.

**(B) `futures_ma_filter_value` / `current_bb_mid` — 잠복형 동일 결함**
- MA200 게이트(L4732)와 BB중심선 게이트(L4746)는 값이 `None`/`0.0`이면 **조용히 통과**한다.
- 값을 채우는 `update_bb_psar_filters()`는 `parabolic_sar`일 때만 호출(L883, L3192).
- 따라서 지금 MA200 필터는 **사실상 SAR 전용**인데 코드는 전역처럼 생겼다. 누군가 kalman에서도 이 함수를 부르는 순간, 검증되지 않은 방향필터가 갑자기 켜진다.
- 더 나쁜 경우: `update_bb_psar_filters`는 `len(df) < 120`이면 **L930에서 조기 return**하는데, `futures_ma_filter_value = None` 초기화는 그 **뒤인 L947**에 있다. **월물 롤오버로 신규 코드의 봉이 120개 미만이 되면 직전 월물의 MA값이 그대로 남아** 한 방향 진입을 무기한 차단한다.

**(C) `atr_cutoff = 15.0`**
- SAR용으로 2026-08-09에 수동 지정한 값인데(config_local 주석), 전략 무관 게이트다. kalman/chandelier로 되돌리면 그쪽 백테스트가 쓰던 `atr_cutoff=0.5`와 30배 차이 나는 값이 그대로 적용된다.

**(D) `consecutive_loss_limit` 적용 비대칭**
- L4665는 `is_kalman or is_chandelier`일 때만 검사. SAR/BB는 `futures_day_consecutive_losses`를 **증가시키기만 하고 아무도 읽지 않는다**.
- chandelier 청산부(L4362)는 `futures_day_trade_count = futures_max_trades_day`로 정지시키려 하지만, chandelier는 L4659의 `is_unlimited` 목록에 들어 있어 **그 대입이 아무 효과가 없다**(죽은 코드).
- 수동 명령 경로 `_poll_futures_signals`(L6188)는 `losses >= 3` **하드코딩** — config의 5와 불일치.

### 2.3 지표 간 중복·상충

**(1) SAR(추세추종) × BB Squeeze 회피 × BB 중심선 × MA200 — 4중 방향 판단**
- SAR 진입은 이미 "변동성 돌파"라 방향성 자체를 내포한다.
- 여기에 ① Squeeze 밴드폭 < 25%분위 차단(저변동성 회피), ② 현재가 vs BB중심선(단기 방향), ③ 직전봉 종가 vs MA200(장기 방향)이 겹친다.
- ②와 ③은 **주기가 다른 같은 판단**이다. 돌파 타점은 시초가 기준 고정이므로, 상승추세일(MA200 위)에 하락 되돌림이 오면 ②는 통과·③은 통과·돌파는 SHORT 방향 — 이때 ③이 SHORT를 막아 **매매 기회가 한쪽으로만 남는다**. 실제 로그에서도 오늘 하루 `LONG만` 상태가 계속 유지되고 있다.
- ①의 Squeeze 임계는 `bandwidth < squeeze_limit`인데 `bandwidth`는 **σ=2 하드코딩**(L938-939)으로 계산된 반면, 익절 밴드는 `bb_sigma` 설정값을 쓴다. `bb_sigma`를 바꾸면 익절만 바뀌고 진입 필터는 안 바뀐다.
- ①과 ATR컷오프(15.0)도 "저변동성 회피"라는 **같은 목적의 이중 필터**다.

**(2) 국면 스위칭 계층 vs 인트라데이 필터 — 동일 판단의 이중화**
- `market_regime_monitor.py`: 일봉 ADX(14) 25/20 → 추세장이면 SAR, 박스권이면 BB
- 엔진 내부: 15분봉 칼만 기울기 `trend_direction`(kalman/chandelier), BB Squeeze(SAR), MA200(전역)
- **책임 분리가 되어 있지 않다.** "지금이 추세장인가"를 서로 다른 지표·주기·데이터로 **최소 3곳에서 독립적으로 판단**하고, 서로의 결론을 참조하지 않는다.
- 더 큰 문제: 상위 계층(ADX)이 "박스권"이라 판정해 BB로 바꿔도, BB 전략 내부에는 박스권 전용 진입 로직이 없다(진입은 여전히 추세형 변동성 돌파). **상위 판단이 하위 동작에 반영되는 경로가 청산 방식 하나뿐이다.**

**(3) session_range_cap × ATR14 — 서로를 보정하려는 구조**
- chandelier의 `session_range_cap`은 "ATR14가 일봉 지연지표라 오늘 흐름과 안 맞는다"를 보정하려고 도입됐다(L1696-1706 주석).
- 즉 **ATR 계산 자체의 horizon 불일치를 알고 있으면서 근본 수정 대신 chandelier에만 패치**를 붙였다. SAR은 그 패치를 못 받아 §3 C-1이 그대로 남았다.

### 2.4 국면 스위칭과 개별 전략의 책임 분리

| 질문 | 현재 상태 |
|---|---|
| 국면 판단의 단일 소유자가 있는가 | ✗ — ADX(모니터), 칼만기울기(엔진), MA200(엔진), Squeeze(엔진)가 각자 판단 |
| 상위 판단이 하위에 전달되는가 | ✗ — `active_strategy.json`의 `futures_strategy_type` 문자열 하나뿐. ADX 값 자체는 엔진에 전달되지 않음 |
| 전략은 자기 국면 필터를 꺼야 하는가 | 명세 없음 — SAR은 Squeeze로 스스로 국면을 또 판단 |
| 판정 데이터가 일치하는가 | ✗ — 모니터는 `10100000`(정규선물) 하드코딩, 실거래는 `10500000`/`A0568000`(미니선물) |
| 스위칭이 검증됐는가 | ✗ — BB 전략은 라이브에서 한 번도 체결된 적 없음 |

### 2.5 전략 전환 시 상태 인계

전환 경로: `perform_morning_analysis()`/`perform_intraday_check()` → `active_strategy.json` 저장 → `run_reconnect_script()` → `auto_reconnect_era.bat` (taskkill /f → 60초 대기 → `run_era.bat`)

| 상태 | 저장 여부 | 인계 결과 |
|---|---|---|
| 실계좌 포지션 | 브로커 보유 | ○ 재기동 후 `sync_futures_positions_and_balance()`로 복원 |
| `futures_day_peak` | `futures_exit_state.json` | ○ |
| 진입 스냅샷(std_err/ATR/tp) | 〃 | ○ |
| 연속손실·거래횟수 카운터 | 〃 | ○ |
| 일일리셋 완료 플래그 | 〃 | ○ |
| **`futures_day_entry_price`** | **✗** | 0으로 리셋 → L4223 `pos['price']` 폴백으로 겨우 복구 |
| **SAR 상태 (`sar_value`/`sar_ep`/`sar_af`/`sar_bull`)** | **✗** | **전부 초기값**(0.0/0.0/0.02/**True**) |
| **`bb_close_buf` / `bb_upper` / `bb_lower`** | **✗** | 빈 버퍼 → 20틱 재축적 전까지 BB 익절 불가 |
| **`futures_day_entry_time`** | **✗** | 0 → time_stop 영구 미발동 |
| `futures_day_open` / session_high/low | ✗ | DB 재조회로 복원(L4116/4132) |
| `futures_std_error` | ✗ | 0.5 → 다음 5분봉 경계까지 진입 전면 차단 |

**SHORT 보유 중 재시작이 특히 위험하다**: `sar_bull`이 `True`로 초기화되므로 SHORT 청산 로직의 `if not self.sar_bull:` (L4470)이 거짓 → `else` 분기로 빠져 **SAR 트레일링이 완전히 죽고 ATR 하드손절(77pt)만 남는다**.
**LONG 보유 중 재시작**도 `sar_value=0.0`에서 출발해 `sar = sar + af×(ep − sar)`로 0에서부터 서서히 올라오므로, 수십~수백 틱 동안 트레일링이 사실상 무방비다.

---

## 3. 문제점 — 심각도별

### 🔴 치명 (Critical)

---

#### C-1. ATR14가 일봉 오버나잇 갭을 포함해 부풀고, 그 값이 당일 손절폭으로 직결

- **위치**: `update_futures_dynamic_sl_tp()` L842-879 (일봉 resample + TR + 칼만 ATR) → 사용처 `_process_day_tick` L4254 (`sl_limit = max(ATR14 × 1.0, 2.0)`)
- **실측 (2026-08-11 11:19 라이브 로그)**:
  ```
  [AMATS 파생 최적화] 선물(A0568000) 동적 Kalman ATR 적용 완료:
      Kalman ATR=77.49pt  손절=77.49pt | 익절=154.97pt
  ```
  같은 날 실제 주간 세션 레인지는 `992.86 − 960.80 = 32.06pt`.
- **원인**: TR = `max(H−L, |H−prevClose|, |L−prevClose|)`인데, 이 시스템은 **매일 15:35에 전량 청산**하므로 오버나잇 갭은 절대 감수하지 않는다. 그런데 ATR에는 그 갭이 그대로 들어간다. 최근 종가 흐름(990→998→1039→979→976)이 보여주듯 갭이 일중 레인지보다 크다.
- **손실 경로**: 15계약 × 50,000원/pt = **750,000원/pt** → 77pt 손절 = **1회 최대손실 5,775만원**. 하루 레인지의 2.4배라 이 손절은 정상 시장에서 절대 발동하지 않는다 → **SAR 역전만이 유일한 청산 수단**이 되어 손절 장치가 이름만 남는다.
- **파생 영향**: `atr_cutoff=15.0` 게이트가 ATR≈77에서 무의미해짐. chandelier `dist = 0.3×77 ≈ 23pt`도 과대(그래서 session_range_cap이 필요했던 것).
- **재현/검증**: `python -c` 로 `futures_ohlcv` 일봉 resample 후 `H−L`(일중)과 TR(갭 포함)의 분포를 비교. 두 계열의 중앙값 비율이 2배 이상이면 확정. 또는 `era_order_manager.log`에서 `Kalman ATR=` 라인과 당일 세션 레인지를 대조.

---

#### C-2. 선물에 금액 기준 손실 한도가 존재하지 않음

- **위치**: 주식은 `stock_daily_loss_limit_pct`(L6536) + 월간 MDD 25% 킬스위치(L6547)가 있으나, `futures_*`에는 **대응 코드가 전혀 없음** (`grep futures_daily_loss` → 0건)
- **선물이 가진 안전장치 전부**:
  1. 연속손실 5회 → kalman/chandelier만
  2. 일일 거래 하드캡 50회 → 전 전략
  3. 진입 쿨다운 90s / 301s
- **손실 경로**: SAR 운영 중 15계약으로 하루 50회까지 거래 가능하고, 회당 손절폭 상한도 사실상 없다(C-1). **한 방향으로 계속 틀려도 아무것도 멈추지 않는다.**
- **재현**: 백테스트에서 `consecutive_loss_limit`을 끄고 최악 연속손실일의 일중 누적 손익을 산출. 또는 `futures_exit_state.json`의 `day_trade_count`와 실현손익을 대조.

---

#### C-3. 국면 스위칭이 포지션 보유 중 엔진을 강제 종료 → 감시 공백 + 상태 유실

- **위치**: `market_regime_monitor.py` L216-228 (`perform_intraday_check`) → `run_reconnect_script()` L119 → `era/auto_reconnect_era.bat`
- **auto_reconnect_era.bat 동작**: `taskkill /f /pid` → 키움 헬퍼 프로세스 kill → `ping 127.0.0.1 -n 61`(60초) → `run_era.bat`. **포지션 유무를 확인하는 코드가 한 줄도 없다.**
- **손실 경로 3단**:
  1. **감시 공백**: kill 시점부터 재기동+키움 로그인 완료까지(60초 + 로그인 수십 초~수 분) **손절/트레일링이 아예 돌지 않는다**. 이 구간의 급변은 무방비.
  2. **상태 유실**: 재기동 후 SAR/BB 내부상태가 초기화(§2.5). SHORT 보유 중이면 SAR 트레일링이 죽는다.
  3. **전략 불일치 청산**: SAR 규칙으로 진입한 포지션이 BB 규칙(`bb_upper` 터치 = 20틱 밴드)으로 청산된다. `bb_close_buf`가 비어 있다가 20틱 차는 순간 **초단위 밴드**가 즉시 터치되어 오청산될 개연성이 높다.
- **가중 요인**: `perform_intraday_check`는 09:00~15:45 사이 5분마다 돌고, `intraday_switched` 플래그로 **하루 1회는 반드시 허용**된다.
- **재현**: 모의계좌에서 포지션 보유 중 `auto_reconnect_era.bat`을 수동 실행하고, kill~첫 틱 처리까지의 시간과 `sar_bull` 로그를 관찰.

---

#### C-4. `bollinger_band` 전략은 구조적으로 거래 0건

- **위치**:
  - `_refresh_futures_targets_deferred` L3184-3192 — `("kalman","chandelier")`와 `"parabolic_sar"`만 분기. **`bollinger_band` 분기 없음**
  - `_process_day_tick` L4158-4159 — 세션 시작 시 std_error 보충도 `parabolic_sar`만
  - 결과: `futures_std_error`가 L621 초기값 `0.5`에 고정
  - `_process_day_tick` L4700-4703 — `0.5 < min_std_error_entry(1.5)` → `_note_entry_block` 후 `return`
- **손실 경로**: 오류도 예외도 없이 **하루 종일 거래 0건**. 국면 모니터가 박스권(ADX<20)을 판정해 BB로 전환하면 그 시점부터 시스템이 조용히 정지한다. 정확히 커밋 `b19294d`가 SAR에서 고친 것과 **같은 버그가 BB에 그대로 남아 있다**.
- **부수 결함(동시 존재)**:
  - `update_bb_psar_filters()`가 BB 전략에서 호출되지 않아 `current_bb_mid`/`current_bb_squeeze_limit`/`futures_ma_filter_value`가 전부 미갱신 → MA200 필터도 조용히 무효
  - 완화 장치는 있음: `_note_entry_block`(L971)이 60회 연속 차단 시 텔레그램 경보. 다만 **경보일 뿐 거래는 여전히 0건**
- **재현**: `config_local.json`의 `futures_strategy_type`을 `bollinger_band`로 두고 하루 운영 → 로그에서 `std_error 0.50 < 1.50` 차단 카운트가 78봉 내내 누적되는지 확인.

---

### 🟠 높음 (High)

---

#### H-1. 백테스트 미래참조 + 라이브와 계산 기준 불일치 → 백테스트 신뢰도 낮음

`scratch/backtest_sar_bb_20260809.py` 기준.

| 항목 | 백테스트 | 라이브 | 영향 |
|---|---|---|---|
| **BB 익절 밴드** | `bb_upper[i]` = **당봉 종가 포함** 20개 5분봉 → `c_high >= bb_tp` 판정 (L~236) | 틱 20개 롤링(L4233-4241) | **미래참조** + 시간축 자체가 다름(100분 vs 수십 초). 두 결과는 비교 불가 |
| **Squeeze 진입필터** | `bandwidth[i]`, `bb_mid[i]`, `squeeze_limit[i]` = 당봉 종가 포함 (L336-339) | 직전 완결봉 기준(DB 조회) | **미래참조** |
| **MA200 필터** | `closes[i-1]`, `ma_line[i-1]` (L325) | 직전 봉(L951-952) | ○ 일치 |
| **std_error** | `closes[i-kf_window:i]` (i 제외) | 직전 완결봉까지 | ○ 일치 |
| **표준편차 ddof** | `std(ddof=1)` (L~78) | 틱 BB는 `ddof=1`(L4239), `update_bb_psar_filters`는 pandas 기본 `ddof=1`, `market_regime_monitor`의 `ta.BollingerBands`는 **`ddof=0`** | 국면판정 BB폭만 계통오차(단, 스위칭은 ADX만 쓰므로 영향 제한적) |
| **연속손실 서킷브레이커** | `consec_losses >= 5` 적용(L~305) | SAR/BB는 **미적용** | **백테스트가 라이브보다 안전하게 나옴 → 실전 리스크 과소평가** |
| **재진입 쿨다운** | 없음 | 90s + 301s | 백테스트 거래수 과대 |
| **세션 필터** | `load_futures_data()`(bqa/kalman_backtester.py L81-103)에 **시간대 필터 없음** | 08:45~08:55 장전 단일가 봉도 DB에 적재됨(실측: `10500000` 08시대 87봉) | 개장 전 단일가 체결이 봉으로 섞여 밴드·ATR·타점을 오염 |
| **ATR** | `daily.groupby('date_day')` TR (갭 포함) | 동일 | 둘 다 C-1의 결함 공유 |
| **일일 리셋** | `last_*_exit=0`, `consec=0` 매일 | 08:40 리셋 | ○ 대체로 일치 |

- **결론**: 미래참조 2건 + 서킷브레이커 방향이 **낙관 편향**으로 걸려 있어, 현재 SAR/BB 백테스트 수치(PF, MDD, 최악손실)는 **의사결정 근거로 사용 불가**. chandelier는 `bqa/kalman_backtester.py`의 `run_chandelier_live_replica`로 상대적으로 잘 검증되어 있으나, 그것도 세션 필터 부재와 C-1은 공유한다.
- **재현**: 위 두 미래참조를 `[i-1]` 인덱스로 바꿔 동일 기간을 재실행 → PF/승률 하락폭이 곧 편향 크기.

---

#### H-2. `bb_close_buf`가 진입 시 초기화되지 않음 → 진입 직후 즉시 익절 위험

- **위치**: `bb_close_buf`는 L662 초기화, L4233-4235에서만 append/trim. **진입부(L4754-4782 / L4823-4845)에 리셋 없음.**
- **손실 경로**: 이전 포지션의 잔여 20틱으로 만든 밴드가 새 포지션 첫 틱부터 유효 → `current_price >= bb_upper` 즉시 성립 가능 → 진입 → 즉시 익절 → 90초 쿨다운 → 재진입 반복. 수수료·슬리피지만 누적되는 **체결 폭주(churn)** 패턴.
- **정황 증거 (라이브 로그, 오늘)**:
  ```
  [선물 재진입 차단] 방향무관 쿨다운: 최근 청산 후 1초 경과 (최소 90초 필요)
  [선물 재진입 차단] 방향무관 쿨다운: 최근 청산 후 2초 경과 (최소 90초 필요)
  ```
  청산 직후 1~2초 만에 재진입 조건이 성립하고 있다. 지금은 90초 쿨다운이 막고 있으나, **쿨다운이 유일한 방어선**이다.
- **동일 유형 선례**: 2026-07-01 칼만 3-Sigma 즉시 재청산 무한루프(4,243건 체결, 텔레그램 429). 그때는 진입 시점 스냅샷으로 해결했으나 BB에는 같은 처방이 안 들어갔다.

---

#### H-3. SAR/BB에 서킷브레이커가 없고, chandelier의 정지 코드는 죽어 있음

- **위치**: L4665 (`is_kalman or is_chandelier`만 검사), L4362/L4380 (`futures_day_trade_count = futures_max_trades_day` 대입) vs L4659 (`is_unlimited`에 chandelier 포함 → 대입 무효)
- **손실 경로**: SAR/BB는 연속손실 카운터를 증가만 시키고 아무도 안 읽음 → 하드캡 50회까지 무제한. C-1·C-2와 결합하면 **하루 최악 손실에 상한이 없다**.
- **추가 불일치**: `_poll_futures_signals` L6188은 `losses >= 3` 하드코딩 → 텔레그램 수동 진입은 3회, 자동 진입은 5회.

---

#### H-4. `active_strategy.json`이 최우선 오버라이드라 수동 설정을 영구히 덮어씀

- **위치**: `load_config()` L1204-1423. 우선순위 = `config.json` → `config_local.json`(1레벨 dict update) → **`active_strategy.json`(최상위)**
- **현재 상태**: `active_strategy.json`에 `futures_strategy_type` 키가 **없어서** `config_local.json`의 `parabolic_sar`가 살아 있다.
- **위험**: `market_regime_monitor.py` L172/L217이 **그 키를 새로 써 넣는다**. 한 번 쓰이면 그 뒤로는 `config_local.json`을 아무리 고쳐도 **엔진에는 반영되지 않는다**. 사용자는 `config_local.json`을 보고 SAR이라 믿는데 실제로는 BB로 돌 수 있다.
- **추가**: `atr_cutoff`도 동일 구조(L1392-1393). `config_local`의 15.0이 `active_strategy.json`의 값에 조용히 덮인다.
- **가드는 있음**: `approved_at` 키 없으면 스킵(L1377). 그러나 모니터는 기존 파일을 load→수정→save하므로 **`approved_at`이 보존된 채 전략만 바뀐다** → 가드가 작동하지 않는다.

---

#### H-5. `market_regime_monitor`가 실거래와 다른 종목으로 국면을 판정

- **위치**: `market_regime_monitor.py` L61/L71 — `WHERE code = '10100000'` **하드코딩**
- **실거래**: `config_local.json` `target_code_day = "10500000"` → 실제 체결 코드 `A0568000`
- **영향**: 정규선물과 미니선물은 같은 지수를 추종하나 호가단위·유동성·데이터 적재 품질이 다르다. 그리고 **전략을 바꿀지 말지를 결정하는 유일한 입력**이 다른 계약이다.
- 다만 실측상 두 코드의 일봉 레인지는 유사(§검증)하므로 즉시 손실보다는 **정합성/신뢰 문제**.

---

#### H-6. 월물 롤오버 시 MA200/BB 필터가 스테일 값으로 굳음

- **위치**: `update_bb_psar_filters()` L930 (`if df.empty or len(df) < 120: return`) vs L947-948 (`futures_ma_filter_value = None` 초기화)
- **손실 경로**: 신규 월물 코드로 롤오버되면 봉이 120개 미만인 기간이 발생 → 조기 return → 직전 월물의 MA200/BB중심선이 **그대로 살아남아** 한 방향 진입을 무기한 차단하거나, 엉뚱한 가격대의 중심선으로 방향을 판정한다.
- 로그는 갱신 실패를 **아무것도 남기지 않는다**(early return에 print 없음).

---

### 🟡 중간 (Medium)

**M-1. SAR 클램프 로직이 사실상 무효**
`self.sar_value = min(self.sar_value, self.futures_day_peak)` (L4281). 표준 PSAR은 SAR을 **직전 2봉의 저가 아래로** 제한하는데, 여기서는 LONG의 `day_peak`(최고가)로 제한한다. `sar_value < peak`는 거의 항상 참이라 no-op. SHORT 쪽 `max(sar_value, day_peak)`(L4474)도 동일. → SAR이 가격을 관통하는 것을 막는 안전장치가 없다.

**M-2. 라이브 SAR은 틱 단위, 백테스트는 봉 단위로 AF가 가속됨**
라이브는 `_process_day_tick`이 틱마다 호출되어 `sar_af`가 틱마다 `+0.02`씩(최대 0.10) 오른다. 백테스트는 봉마다 오른다. 5분봉 하나에 수백 틱이면 라이브 AF는 **즉시 최대치**에 도달해 SAR이 훨씬 공격적으로 붙는다. → 백테스트-라이브 거래 성격이 근본적으로 다르다.

**M-3. 세션 필터 부재로 장전 단일가 봉이 지표에 혼입**
`update_bb_psar_filters` L925-927, `update_kalman_targets` L1033-1037, `bqa/kalman_backtester.load_futures_data` 모두 시간대 필터가 없다. 실측: `10500000` 08시대 87봉(≈3봉/일 = 08:45/08:50/08:55). 단일가 구간 가격은 연속체결가와 성격이 다르다.

**M-4. `bb_sigma` 설정이 진입 필터에 반영되지 않음**
L938-939 `2 * df['std20']` 하드코딩. 익절(L4240-4241)만 `bb_sigma`를 쓴다.

**M-5. `_process_day_tick`의 들여쓰기 이상 (L4375-4443)**
```python
else:
# 1. 고정 손절 감시
 if pnl_pt <= -sl_limit:
```
`else:` 아래 1칸 들여쓰기 블록. 파이썬 문법상 유효하지만 **kalman/volatility_breakout 전용 청산 로직 전체**가 이 형태다. 편집 사고 위험이 매우 높다.

**M-6. 진입 락 해제 시점 비대칭**
`_execute_futures_direct` L5305 — EXIT는 즉시 `lock=False`, ENTER는 체결 콜백까지 유지. 모의서버가 체결 콜백을 누락하면 **진입 락이 영구히 잠긴 채 하루가 끝난다**(해제 타이머 없음).

**M-7. ISF가 공통 리스크 인프라 밖에 있음**
장마감 강제청산 없음(15:30 이후 틱 무시일 뿐 청산 아님) → **오버나잇 방치 가능**. 서킷브레이커·쿨다운·재진입 필터 전무. 현재 비활성이나 활성화 시 즉시 위험.

**M-8. `_check_periodic_atr_refresh`가 포지션 보유 중에도 ATR을 갱신**
L1784-1794, 15분 주기. chandelier의 `dist`(L4347)와 SAR의 `sl_limit`(L4254)은 **스냅샷이 아니라 라이브 ATR**을 읽는다. 보유 중 ATR이 튀면 손절선이 움직인다. (kalman만 진입 스냅샷 사용)

**M-9. 야간 세션은 SAR/BB를 지원하지 않음**
`_process_night_tick` L4917-4930 — `is_kalman`/`is_chandelier`만 분기, 나머지는 `sl_limit = futures_night_stop_loss_pt`(고정 5pt). SAR로 운영 중 야간을 켜면 **SAR 로직 없이 고정 손절만 도는 다른 전략**이 된다.

### 🟢 낮음 (Low)

- **L-1**: `notifier.send_message`가 청산 경로마다 중복 호출(print + telegram). 2026-07-01 429 사고 이력. 레이트리밋 가드 없음.
- **L-2**: `config.json`에 텔레그램 봇 토큰과 Gemini API 키 평문 저장. `config_local.json`에도 별도 키.
- **L-3**: `era_order_manager.log` 19MB + `.log.1` 20MB. 로테이션은 있으나 grep 기반 사후분석이 매우 느림. 구조화 로그(JSONL) 부재.
- **L-4**: `.py.bak*` 5개, `config.json.bak_*` 4개, `scratch/` 400+ 파일이 리포 루트에 혼재. 어떤 백테스트가 최신 기준인지 파일명만으로 판별 불가.
- **L-5**: `market_regime_monitor.py` L275 `start_time = datetime_time = now.time()` — 미사용 변수 + 오타성 이중 대입.
- **L-6**: `_get_today_futures_high_low` L898 — 조회 실패 시 `10500000`, `10100000`으로 폴백. **다른 계약의 고저가**를 가져올 수 있다.

---

### 부록 A. 하드코딩 매직넘버 목록

| 값 | 위치 | 의미 | 설정화 필요성 |
|---|---|---|---|
| `3` | L6188 `losses >= 3` | 수동명령 서킷브레이커 | **높음** (config는 5) |
| `2` | L938-939 | Squeeze BB 시그마 | **높음** (`bb_sigma` 무시) |
| `100`, `0.25` | L942 | Squeeze 윈도우/분위수 | 중간 |
| `1.0` | L4254 | SAR ATR 손절 배수 | **높음** (C-1 직결) |
| `1.2` | L4257 | BB ATR 손절 배수 | 높음 |
| `1.5`, `0.5` | L4334, L4553 | BB 폴백 트레일링 배수 | 중간 |
| `2.0` | L4249 `max(1.5*c_std, 2.0)` | 칼만 SL 플로어 | 중간 |
| `1.2` | L4250 `1.2 * c_atr` | 칼만 SL ATR 상한 | 중간 |
| `3.0`, `2.0` | L4445-4446 | 레거시 돌파 트레일링 | 낮음 |
| `2.0`/`5.0`, `4`/`2` | L4415-4428 | 칼만 티어/락인 | 중간 |
| `0.4`, `0.6` | L4419-4421 | 콜백 축소율 | 중간 |
| `40`, `20` | L1075, L1102 | 칼만 윈도우/std 슬라이스 | 중간 |
| `300` | L1034 | 조회 봉수 | 낮음 |
| `0.001`, `1.0`, `0.01` | L1124-1145 | 15분 추세 칼만 파라미터 | 중간 |
| `0.002`, `0.2` | L860-861 | ATR 칼만 Q/R | 중간 |
| `1.0`, `2.0` | L877-878 | ATR→SL/TP 배수 | **높음** |
| `3000`, `50`, `15` | L827-849 | ATR 조회/최소행/최소일 | 낮음 |
| `120`, `250` | L924-930 | BB 필터 최소 봉수 | 중간 (H-6) |
| `60` | L992 | 진입차단 경보 임계 | 낮음 |
| `0.03` | L638 | 틱 이상치 임계 | 중간 |
| `15`, `0.30` | L5283-5293 | 최대계약수/마진캡 기본 | 중간 |
| `50000`/`250000` | L1880 | 승수 매핑 | 낮음 |
| `0.25` | L6553 | 월간 MDD 킬스위치(주식) | 중간 |
| `25`/`20` | `market_regime_monitor.py` L127-139 | ADX 임계 | **높음** |
| `5.0` | 〃 L202 | ADX 급변 알림 임계 | 낮음 |
| `20000`, `500`, `30` | 〃 L63-99 | 조회/최소 데이터 | 낮음 |
| `60초` | `auto_reconnect_era.bat` | 재기동 대기 | 중간 |

---

## 4. 개선안

원칙: **① 돈이 새는 것부터 막고 ② 조용한 실패를 시끄럽게 만들고 ③ 그 다음에 구조를 고친다.** 모든 항목은 기존 동작을 보존하는 config 플래그로 도입 가능해야 한다(이 코드베이스의 기존 관행과 일치).

### 4.1 즉시 조치 (핫픽스) — 오늘~내일

| 우선 | 항목 | 대상 | 방향 | 작업량 | 기대효과 | 리스크 |
|:--:|---|---|---|:--:|---|---|
| **P0** | ATR을 일중 horizon으로 분리 | `update_futures_dynamic_sl_tp()` L854-879 | TR 대신 **일중 레인지(H−L)** 기반 `intraday_atr`를 별도 산출해 SAR/chandelier의 손절·트레일 폭에 사용. 기존 `futures_atr_14`는 컷오프용으로 유지 → 두 용도를 분리 | 0.5일 | 손절폭 77pt → ≈20pt대. 리스크 3~4배 축소 | 손절이 실제로 발동하기 시작 → 거래 성격 변화. **반드시 백테스트 선행** |
| **P0** | SAR 손절폭 절대 캡 | L4254 | `sl_limit = min(max(ATR×mult, 2.0), sar_sl_hard_cap_pt)` 신설. 초기값은 kalman이 쓰는 `dynamic_cap_max`(30pt) 수준 권장 | 0.2일 | 단일 거래 최악손실 상한 확보 | 낮음(상한만 추가, 기존보다 좁아지기만 함) |
| **P0** | 선물 일일 손실 한도 | `_handle_chejan_data` 선물 체결부 + `_process_day_tick` L4654 | 선물 실현손익 누적기 `futures_daily_loss` 추가 → `futures_daily_loss_limit_won`(또는 예수금 대비 %) 초과 시 신규진입만 차단(청산은 유지), 08:40 리셋에서 해제. **주식의 `stock_daily_halted` 패턴을 그대로 복제** | 1일 | C-2 해소. 최악의 날에 바닥 확보 | 낮음 |
| **P0** | 국면 스위칭 시 포지션 가드 | `market_regime_monitor.py` L216-228 | `run_reconnect_script()` 호출 **전에** 포지션 유무 확인(`era/futures_exit_state.json` 또는 신설 `era/positions_open.flag`). 포지션 보유 시 재기동을 **다음 08:45 이후로 연기**하고 텔레그램 알림만 | 0.5일 | C-3의 감시 공백 제거 | 낮음. 전환 지연은 오히려 안전 |
| **P0** | `bb_close_buf` 진입 시 초기화 | L4754-4782 / L4823-4845 | 진입 확정 직후 `self.bb_close_buf = []`, `self.bb_upper = self.bb_lower = 0.0` | 0.1일 | H-2 즉시 익절 루프 차단 | 낮음. BB 익절이 20틱 뒤부터 유효해질 뿐 |
| **P1** | SAR/BB에 서킷브레이커 적용 | L4665 | `is_kalman or is_chandelier` 조건 삭제 → 전 전략 공통 적용. chandelier의 죽은 `trade_count` 대입(L4362/4380)은 제거 | 0.2일 | H-3 해소 + 백테스트와 정합 | 거래수 감소. 백테스트는 이미 이 조건으로 돌고 있으므로 **오히려 정합성 개선** |
| **P1** | 수동명령 임계 하드코딩 제거 | L6188 | `losses >= 3` → `losses >= self.futures_consecutive_loss_limit` | 0.05일 | 자동/수동 경로 일치 | 없음 |
| **P1** | `bollinger_band` std_error/필터 공급 | L3184-3192, L4158-4159 | 두 곳의 분기에 `bollinger_band`를 추가 (`update_kalman_targets(code, std_error_only=True)` + `update_bb_psar_filters(code)`) — `b19294d`가 SAR에 한 것과 동일 | 0.2일 | C-4 해소 | **BB가 실제로 거래를 시작한다.** H-2·H-1 미해결 상태에서 켜면 위험 → 반드시 H-2 핫픽스 후, 그리고 §5 페이퍼 검증 후 |
| **P1** | 게이트 조기 return 로깅 강화 | L4700-4703 외 모든 `return` | `_note_entry_block` 호출을 **모든** 진입 게이트에 적용(MA200 필터는 하루 1회 로그 유지). 임계 60회 → 20회로 하향 검토 | 0.3일 | "조용한 실패" 탐지 시간 단축 | 로그 증가 |

### 4.2 단기 (1~2주)

| 우선 | 항목 | 대상 | 방향 | 작업량 | 기대효과 | 리스크 |
|:--:|---|---|---|:--:|---|---|
| **P0** | 백테스트 미래참조 제거 | `scratch/backtest_sar_bb_20260809.py` L~236, L336-339 | BB 밴드·bandwidth·bb_mid·squeeze_limit을 전부 `[i-1]`로 이동. 수정 전/후 수치를 병기한 보고서 작성 | 1일 | H-1 해소. **이후 모든 전략 의사결정의 전제** | 기존 채택 결론(SAR 선택 등)이 뒤집힐 수 있음 |
| **P0** | 백테스트-라이브 파라미터 정합 | 위 스크립트 + `bqa/kalman_backtester.py` | 재진입 쿨다운(90s/301s), SAR AF의 틱 가속(M-2), 세션 필터(09:00~15:35)를 백테스트에 반영 | 2일 | 거래수·PF의 계통편차 제거 | 없음(정확도만 개선) |
| **P0** | 세션 필터 단일화 | `load_futures_data()`, `update_bb_psar_filters()`, `update_kalman_targets()` | 공용 헬퍼 `filter_day_session(df)` 도입, 09:00~15:35만 사용(장전 단일가 제외). 야간은 별도 플래그 | 1일 | M-3 해소, 라이브/백테 동시 개선 | 지표 값이 미세 이동 → 재검증 필요 |
| **P1** | 재기동 시 SAR/BB 상태 영속화 | `save_futures_exit_state()` L1570-1594, `load_futures_exit_state()` L1483 | `sar_value/sar_ep/sar_af/sar_bull`, `bb_close_buf`(또는 최근 N개), `day_entry_time`, `day_entry_price`, `futures_std_error`를 상태 JSON에 추가 | 0.5일 | C-3의 상태 유실 해소 | 낮음. 다만 **오래된 상태 복원 방지**를 위해 타임스탬프 유효기간(예: 10분) 필요 |
| **P1** | 월물 롤오버 안전장치 | `update_bb_psar_filters()` L930 | 조기 return 전에 `futures_ma_filter_value/close`, `current_bb_*`를 **먼저 None/0으로 리셋** + 실패 사유 로그 + 연속 실패 시 텔레그램 | 0.2일 | H-6 해소 | 없음 |
| **P1** | `active_strategy.json` 역할 정리 | `load_config()` L1371-1423, `market_regime_monitor.py` | ① `futures_strategy_type`은 `active_strategy.json`이 아니라 **전용 파일**(`config/regime_state.json`)로 분리, ② 적용 시 반드시 로그+텔레그램으로 "어느 파일의 어느 값이 최종 채택됐는지" 출력 | 0.5일 | H-4 해소. 설정 추적 가능 | 낮음 |
| **P1** | 국면 판정 데이터 정합 | `market_regime_monitor.py` L61/L71 | `10100000` 하드코딩 → `config_local.json`의 `target_code_day`를 읽도록. 미니선물 데이터로 ADX 재산출 후 임계(25/20) 재검증 | 0.3일 | H-5 해소 | 임계값 재튜닝 필요 |
| **P2** | 보유 중 ATR 스냅샷화 | L4347(chandelier), L4254(SAR) | 진입 시 `futures_day_entry_atr` 스냅샷을 chandelier/SAR도 사용(kalman과 동일) | 0.3일 | M-8 해소, 보유 중 손절선 이동 방지 | 낮음 |
| **P2** | 진입 락 타임아웃 | `_execute_futures_direct` L5257 | ENTER 락에 `QTimer.singleShot(30초)` 해제 + 경고. 체결 콜백 누락 시 하루가 죽는 것 방지 | 0.3일 | M-6 해소 | 중복 주문 위험 → 반드시 계좌 재조회로 포지션 확인 후 해제 |
| **P2** | 들여쓰기 이상 정리 | L4375-4443 | `else:` 블록을 정상 4칸 들여쓰기로 (동작 불변 리팩터) | 0.2일 | M-5 해소 | 낮음(단, diff가 크므로 단독 커밋 권장) |

### 4.3 중기 (1~2개월) — 아키텍처

#### A-1. 전략 인터페이스 통일 (가장 큰 효과)

현재 `_process_day_tick`은 750행짜리 단일 함수에 4개 전략이 `if is_sar / elif is_bb / elif is_chandelier / else`로 인라인되어 있다. 새 전략을 넣을 때마다 §2.2의 "게이트는 전역, 데이터 공급은 전략별" 비대칭이 재발한다.

```
Strategy 프로토콜 (제안)
  required_indicators() -> {"std_error", "atr", "bb", "ma", ...}   # 엔진이 이 목록만 보고 공급
  on_bar_close(ctx)                                                # 5분봉 경계 갱신
  entry_signal(ctx) -> None | LONG | SHORT                          # 전략 고유 진입
  exit_signal(ctx, pos) -> None | (reason, price)                   # 전략 고유 청산
  on_enter(ctx) / on_exit(ctx)                                      # 상태 초기화 훅 ← bb_close_buf 리셋이 여기
  serialize() / restore()                                           # 재기동 상태 인계 ← SAR 상태가 여기
```

- **`required_indicators()`가 C-4/H-6/§2.2(B)를 구조적으로 없앤다.** 전략이 "나는 std_error가 필요하다"고 선언하면 엔진이 반드시 공급하고, 공급 실패 시 **진입을 조용히 막는 게 아니라 명시적으로 에러를 낸다.**
- **`on_enter()` 훅이 H-2를 구조적으로 없앤다.**
- **`serialize()/restore()`가 C-3의 상태 유실을 없앤다.**
- 작업량: 3~4주 (기존 로직 이식 + 회귀 테스트). 리스크: 이식 과정의 미묘한 동작 변화 → **전략별 골든 테스트**(동일 틱 시퀀스 입력 → 동일 주문 시퀀스 출력) 선행 필수.

#### A-2. 지표 계산 단일 소스화

`indicators.py`(신규)에 순수 함수로:
```
kalman_filter(closes, q, r) -> (path, std_error, trend)
kalman_atr(daily_tr, q, r)  |  intraday_atr(daily_hl)
bollinger(closes, window, sigma, ddof) -> (mid, upper, lower, bandwidth)
squeeze_limit(bandwidth, window, quantile)
moving_average(closes, period)
filter_day_session(df, start, end)
```
- **라이브와 백테스트가 같은 함수를 import**하면 ddof·윈도우·세션·미래참조 불일치가 원천 봉쇄된다(H-1, M-3, M-4).
- 각 함수에 **미래참조 금지 계약**을 명시: 입력은 `closes[:i]`(현재 봉 제외)만 받는 시그니처로 강제.
- 작업량: 1~2주. 리스크: 낮음(순수 함수 추출).

#### A-3. 백테스트-라이브 공유 실행 엔진

- 전략 로직(A-1)을 `Context` 추상화 위에 올리고, `LiveContext`(키움 틱)와 `BacktestContext`(봉 리플레이)를 갈아 끼운다.
- "라이브는 틱, 백테스트는 봉"이라는 M-2·H-2의 근원 차이는 완전히 없앨 수 없으므로, **`BacktestContext`에 틱 시뮬레이션 모드**(봉 내부를 O→H→L→C 또는 O→L→H→C로 4틱 전개)를 추가해 최소한 AF 가속·밴드 갱신 빈도를 근사시킨다.
- 작업량: 3주. 기대효과: 백테스트 신뢰도가 의사결정 근거 수준으로 회복.

#### A-4. 국면 판단 책임 분리

```
[국면 계층]  단일 소유자: RegimeService
  입력: 실거래 종목의 일봉 (ADX, BB width, 실현변동성)
  출력: regime_state.json { regime, adx, confidence, updated_at, source_code }
  권한: "어느 전략을 쓸지" + "리스크 예산 배수"만 결정. 진입/청산은 결정하지 않음

[전략 계층]  개별 전략
  입력: regime_state (읽기 전용) + 인트라데이 지표
  권한: 진입/청산 타이밍만 결정
  금지: 자체 국면 판단 중복 (SAR의 Squeeze 필터 → 국면 계층으로 이관 검토)
```
- 전환은 **엔진 재시작이 아니라 런타임 핫스왑**으로: 엔진이 `regime_state.json`을 폴링해 **포지션이 없을 때만** 전략 객체를 교체. → C-3이 근본적으로 사라진다.
- 작업량: 2주 (A-1 선행 필요).

#### A-5. 리스크 계층 분리

전략과 무관하게 항상 도는 `RiskManager`:
```
- 계약당/일일/월간 손실 한도 (금액 기준)     ← C-2
- 단일 거래 최대 손실폭 (계약수 × 손절폭)     ← C-1
- 연속손실 / 거래횟수 / 쿨다운               ← H-3
- 장마감 강제청산 / 오버나잇 금지            ← M-7(ISF 포함)
- 포지션 사이징 (마진캡 × 최대계약수)
```
현재 이 규칙들이 전략 분기 안에 흩어져 있어 **전략을 바꾸면 리스크 관리도 같이 바뀌는** 상태다. 리스크는 전략의 함수가 아니어야 한다.

#### A-6. 관측성

- 구조화 로그(JSONL): `{ts, event, strategy, gate, values, decision}`. 게이트 통과/차단을 전부 기록 → "왜 오늘 거래가 없었나"를 grep 대신 집계로 답할 수 있게.
- 일일 자동 리포트: 게이트별 차단 횟수, 전략별 체결/손익, ATR·std_error·MA 값의 하루 궤적.
- **거래 0건 알람**: 진입 게이트를 한 번도 통과하지 못한 채 12:00을 넘기면 텔레그램. (현재 `_note_entry_block`은 사유별 60회 기준이라 사유가 분산되면 안 울린다.)

---

## 5. 검증 계획

### 5.1 정적 검증 (코드 수정 전, 즉시 가능)

| 항목 | 방법 | 합격 기준 |
|---|---|---|
| C-1 확인 | `futures_ohlcv` 일봉 resample 후 `H−L`(일중)과 TR(갭 포함)의 중앙값 비교 | 비율 산출. 1.5배 초과면 horizon 불일치 확정 |
| C-4 확인 | `futures_strategy_type=bollinger_band`로 두고 코드 경로 추적 (`update_kalman_targets` 호출부 grep) | BB에서 호출 0건 확인 |
| 게이트 매트릭스 검증 | 각 전략별로 `_process_day_tick`을 유닛 하네스에서 호출, 어떤 게이트가 실제로 걸리는지 카운트 | §2.1 매트릭스와 일치 |
| 매직넘버 | 부록 A 목록을 config로 뺐을 때 기본값이 현재값과 동일한지 확인 | 동작 불변 |

### 5.2 백테스트 검증

**단계 0 — 기준선 재수립 (미래참조 수정 전/후 대조)**
1. `backtest_sar_bb_20260809.py`를 수정 없이 실행 → `BEFORE` 저장
2. 미래참조 2건(`bb_upper[i]`, `bandwidth/bb_mid/squeeze_limit[i]`)을 `[i-1]`로 수정 → `AFTER`
3. **PF·승률·최악손실의 하락폭이 곧 그동안의 낙관 편향.** 이 수치를 보고서에 명시하고, 편향이 20% 이상이면 **기존 SAR 채택 결정 자체를 재검토**

**단계 1 — 라이브 정합성 (backtest ≈ live)**
- 라이브 로그에서 실제 체결 시각·가격·사유를 추출(`scratch/sar_live_vs_backtested_20260810.py` 확장)
- 동일 기간 백테스트를 돌려 **거래 건수 / 진입 시각 / 청산 사유 분포**를 대조
- 합격 기준: 거래 건수 오차 ±15% 이내, 청산 사유 분포(SAR역전/손절/강제청산) 상위 3개 순서 일치
- 불일치가 남으면 그 항목(쿨다운·AF가속·틱BB)을 백테스트에 이식할 때까지 반복

**단계 2 — 개선안 효과 측정 (한 번에 하나씩)**
각 항목을 **단독으로** 켜고 전체기간 / 최근 60일 / 최근 30일 / 분기별 7구간에서 측정:

| 개선 | 측정 지표 | 합격 기준 |
|---|---|---|
| intraday ATR (P0) | 최악단일손실, PF, MDD | 최악손실 대폭 개선 + PF 악화가 10% 이내 |
| SAR 손절 캡 (P0) | 최악단일손실, 평균손 | 최악손실 개선, 7구간 중 악화 2구간 이하 |
| 일일 손실한도 (P0) | 일별 최악손실, 연간수익 | 최악일 손실 개선, 수익 감소 15% 이내 |
| SAR/BB 서킷브레이커 (P1) | 거래수, PF | 거래수 감소분 대비 PF 개선 |
| BB 활성화 (P1) | 거래수, PF, 즉시청산 비율 | **진입 후 60초 이내 청산 비율 5% 미만** (H-2 회귀 감지) |

**단계 3 — 워크포워드**
- 학습 6개월 / 검증 1개월 롤링 2회 이상. 두 검증 구간 모두에서 개선이 유지되어야 채택.
- 이 리포지토리의 기존 관행(`선물_이평선방향필터_및_손익폭_검증_20260811.md`의 워크포워드 2회)을 그대로 따를 것.

### 5.3 페이퍼 트레이딩 (모의계좌) — 최소 10 거래일

| 관찰 항목 | 로그 근거 | 합격 기준 |
|---|---|---|
| 손절 실제 발동 | `ATR손절` 로그 건수 | 0건이 아님 (C-1이 고쳐졌다는 증거) |
| ATR 값 궤적 | `Kalman ATR=` / 신규 `Intraday ATR=` | 당일 세션 레인지의 0.5~1.5배 범위 |
| 진입 후 즉시 청산 | 진입~청산 시각 차 | **60초 이내 청산 5% 미만** |
| 게이트 차단 분포 | `_note_entry_block` 집계 | 특정 사유가 하루 78봉 전체를 막지 않음 |
| MA200 필터 편향 | `상승(LONG만)` / `하락(SHORT만)` 로그 | 한 방향 고착이 5거래일 이상 지속되지 않음 |
| 일일 손실한도 발동 | 신규 `futures_daily_halted` 로그 | 발동 후 청산은 정상, 신규진입만 차단됨 |
| 재기동 상태 인계 | 재시작 직후 `sar_bull`/`sar_value` 로그 | 재시작 전후 값이 동일 |

### 5.4 전략 전환 리허설 (C-3 전용)

모의계좌에서 **포지션 보유 중** 다음을 실시:
1. `market_regime_monitor.py`의 스위칭을 수동 트리거
2. 측정: ① kill~첫 틱 처리까지 경과 시간, ② 그 사이 가격 변동폭, ③ 재기동 후 `sar_*`/`bb_*` 값
3. 합격 기준: **포지션 보유 중에는 재기동이 아예 일어나지 않을 것** (P0 개선 적용 후)

### 5.5 회귀 방지

- **골든 테스트**: 저장된 틱 시퀀스(하루치) → 주문 시퀀스 스냅샷 비교. 리팩터링(A-1) 시 필수.
- **게이트 커버리지 테스트**: 각 전략 × 각 게이트에 대해 "적용/미적용"이 §2.1 매트릭스와 일치하는지 자동 검증. 전략 추가 시 이 테스트가 깨지면서 C-4 유형의 누락을 잡아낸다.
- **미래참조 린트**: 백테스트 스크립트에서 `[i]` 인덱스를 진입/청산 판단에 쓰는 패턴을 CI에서 경고.

---

## 6. 권장 실행 순서

```
Week 0 (즉시)
  1. 백테스트 미래참조 수정 → BEFORE/AFTER 대조 보고서
     └ 이게 먼저다. 편향된 수치로 다른 판단을 내리면 안 된다.
  2. bb_close_buf 진입 리셋 (H-2)  ← 0.1일, 리스크 없음
  3. 국면 스위칭 포지션 가드 (C-3) ← 0.5일, 리스크 없음

Week 1
  4. intraday ATR 분리 + SAR 손절 캡 (C-1)  ← 백테스트 검증 후 적용
  5. 선물 일일 손실 한도 (C-2)
  6. SAR/BB 서킷브레이커 통일 (H-3) + 수동명령 임계 (H-3)

Week 2
  7. 세션 필터 단일화 (M-3) + 백테스트 파라미터 정합
  8. 재기동 상태 영속화 (C-3 잔여) + 월물 롤오버 가드 (H-6)
  9. active_strategy.json 역할 분리 (H-4) + 국면 판정 종목 정합 (H-5)
      ※ 여기까지 끝나기 전에는 bollinger_band 활성화 금지

Week 3+
 10. bollinger_band 공급 경로 수정 (C-4) → 페이퍼 10일 → 백테스트 재검증
 11. indicators.py 단일 소스화 (A-2)
 12. Strategy 인터페이스 (A-1) → RiskManager 분리 (A-5) → RegimeService (A-4)
```

---

## 7. 마지막으로 — 이 코드베이스의 강점

문제 목록이 길지만, 다음은 명백한 강점이며 개선안이 훼손하지 않아야 한다.

- **모든 위험 변경에 config 플래그 + 되돌리는 법이 주석으로 남아 있다.** (`_2026-07-30_note`, `되돌리려면 ...`)
- **사고마다 원인·날짜·실측치가 주석으로 기록되어 있다.** (2026-07-01 무한루프, 2026-07-20 트레일링 유실, 2026-08-03 EOD 청산 실패 등) 이 문서의 분석 상당 부분이 그 주석 덕분에 가능했다.
- **`_note_entry_block`(L971)처럼 "조용한 실패를 시끄럽게 만드는" 장치를 이미 도입하기 시작했다.** 이 방향을 전 게이트로 확장하는 것이 §4.1 P1의 핵심이다.
- **`b19294d`처럼 근본 원인을 정확히 짚은 수정 이력이 있다.** C-4는 그 수정을 BB에 마저 적용하는 일에 가깝다.
