# 실거래 SAR과 백테스트된 SAR의 불일치 규명 (2026-08-10)

- 작성일: 2026-08-10
- 대상: AMATS 주간선물 (전략=parabolic_sar, 미니 코스피200, 환경=mock)
- 재현: `scratch/sar_live_vs_backtested_20260810.py`
- 계기: 2026-08-09 SAR 전환 후 **첫 거래일(08-10) 실거래 0건**

---

## 1. 발단 — 신호는 나왔는데 거래가 없었다

08-10 주간 세션은 정상 거래일이었다(A0568000 83봉, 08:45~15:45 전부 수신).
타점도 정상 설정됐다.

| 항목 | 값 |
|---|---:|
| 시초가 | 989.74 |
| LONG 목표 | 998.52 |
| SHORT 목표 | 980.96 |
| 당일 고가 | **999.24** (LONG 목표 관통) |
| 당일 저가 | **967.68** (SHORT 목표 관통) |
| 실거래 | **0건** |

양방향 목표가를 모두 관통했는데 진입이 없었고, **차단 로그조차 남지 않았다.**
`futures_exit_state.json`의 `day_trade_count`도 0이다. 로그 전체를 뒤져도
`[주간선물(SAR)]` 문자열이 단 한 번도 나오지 않는다 — SAR 진입 평가 자체에
도달하지 못했다는 뜻이다.

---

## 2. 직접 원인 — std_error 게이트

`era_order_manager.py:4604`:

```python
if getattr(self, 'futures_std_error', 0.5) < self.futures_min_std_error_entry:
    return          # 로그 없이 조용히 반환
```

`futures_std_error`를 갱신하는 곳은 `update_kalman_targets()` 안(1114행) **단 하나**다.
그런데 이 함수의 호출 지점 3곳이 **전부 `("kalman", "chandelier")`로 제한**돼 있다.

| 호출 지점 | 조건 |
|---|---|
| 3102 `_refresh_futures_targets_deferred` | `in ("kalman","chandelier")`, SAR은 `update_bb_psar_filters`만 |
| 4051 주간 세션 시초가 설정 | `in ("kalman","chandelier")` |
| 4731 야간 세션 중도 기동 | `in ("kalman","chandelier")` |

즉 SAR에서는 한 번도 호출되지 않아 `futures_std_error`가 **초기값 0.5에 고정**된다.
`min_std_error_entry`는 1.5(config.json)이므로 `0.5 < 1.5`는 **항상 참** —
모든 신규 진입이 조용히 반환된다.

로그의 마지막 거래 흔적이 전부 `[주간선물(샹들리에)]`인 것도 이와 일치한다.
**08-09 SAR 전환 이후 단 한 건도 체결되지 않았다.**

---

## 3. 더 깊은 원인 — 실거래가 백테스트한 전략이 아니었다

게이트만 고치려다 백테스터를 읽고 확인했다. 어긋난 곳이 하나가 아니다.

`scratch/backtest_sar_bb_20260809.py:156` (검증 코드):
```python
target_long, target_short = kf_price + band, kf_price - band     # 칼만 밴드
```

`era/era_order_manager.py:4049` (실거래):
```python
if strategy_type in ("kalman", "chandelier"):
    ...                                          # 칼만 밴드 타점
else:                                            # ← SAR이 여기로 빠진다
    self.futures_target_long  = day_open + self.futures_prev_range * self.futures_best_k
```

08-10 로그의 `998.52 / 980.96`이 정확히 `989.74 ± 0.2 × 43.90`이다. **돌파 타점이다.**

| | 백테스트 | 실거래 |
|---|---|---|
| 진입 타점 | 칼만 밴드 (`kf ± std_error×mult`, 장중 이동) | 돌파 (`시초가 ± 전일Range×K`, 하루 고정) |
| `std_error` | 매 봉 재계산 | 초기값 0.5 고정 |

게다가 검증 스크립트(`sar_param_resweep_20260809.py:34`)는
`min_std_error_entry=fs.get("min_std_error_entry", 1.5)`를 **명시적으로 넣고** 돌렸다.
주석도 "entry_cutoff+min_std_error_entry는 이미 검증된 값으로 고정"이다.
그 필터는 백테스트에서 **켜진 채** 통과한 조건이었다.

**따라서 게이트만 끄면 "돌파 타점 + 게이트 없음"이라는, 백테스트한 적 없는
세 번째 조합이 된다.** 그래서 고치기 전에 네 조합을 모두 측정했다.

---

## 4. 비교 — 네 조합

백테스터에 `entry_target_mode` 파라미터를 추가했다(기본값 `kalman_band`로 기존 동작 보존).
비용·기간·나머지 파라미터는 전부 동일하게 두고 **타점 방식과 게이트 두 축만** 바꿨다.

- 비용: 수수료 0.0030%(2026-08-04 실측), 슬리피지 진입1.5/SL3.0/익절0.5/강제2.0pt
- 설정: `atr_cutoff=15.0`, `K=0.2`, 진입종료 15:00, 초기자본 5,000만

### 4.1 `10500000` — 08-09 검증이 쓴 자료 (384거래일)

**전체기간 (2025-01-10 ~ 2026-08-10)**

| 조합 | 거래 | 승률 | PF | MDD | 최악손실 | 최종자본 |
|---|---:|---:|---:|---:|---:|---:|
| A  칼만밴드 + 게이트1.5 | 289 | 45.33% | 0.76 | 83.03% | −59.95pt | 2,336만 |
| A' 칼만밴드 + 게이트없음 | 319 | 43.57% | 0.74 | 90.92% | −59.95pt | 1,941만 |
| B1 돌파타점 + 게이트없음 | 251 | 46.61% | 0.80 | 63.79% | −71.79pt | 3,251만 |
| **B2 돌파타점 + 게이트1.5** | 231 | **49.35%** | **0.84** | **55.06%** | −71.79pt | **3,691만** |

A의 PF 0.76은 [[project_sar_alternative_strategy_20260809]]에 기록된 "전체 PF 0.75"와
일치한다 — 재현이 맞다는 확인이다.

**최근 구간**

| 구간 | A | A' | B1 | B2 |
|---|---|---|---|---|
| 최근60일 PF | 1.14 | 1.14 | **1.22** | **1.22** |
| 최근60일 MDD | 19.76% | 19.76% | **12.73%** | **12.73%** |
| 최근30일 PF | **1.32** | **1.32** | 1.05 | 1.05 |

### 4.2 `A0568000` — 실제 체결 종목 (93거래일)

| 구간 | 지표 | A | B1 = B2 |
|---|---|---:|---:|
| 전체 | PF | 1.29 | **1.88** |
| 전체 | MDD | 27.29% | **8.14%** |
| 전체 | 최악손실 | −126.95pt | **−71.79pt** |
| 최근60일 | PF | 1.14 | **1.49** |
| 최근30일 | PF | **1.63** | 1.18 |

---

## 5. 읽어야 할 것

### 5.1 전체기간에서는 어떤 조합도 돈을 잃는다

검증 자료(384거래일) 기준 **네 조합 모두 PF < 1.0**이고, 초기자본 5,000만이
1,941만~3,691만으로 줄어든다. **MDD 55~91%**다. 최근 구간(PF 1.05~1.63)만
보면 달라 보이지만, 이는 [[feedback_backtest_regime_overfit]]에서 경계한 바로 그
패턴이다. 08-09 전환도 "전체 0.75 / 최근 1.20~1.37"을 알고 최근 쪽에 건 결정이었다.

**이 보고서는 그 판단을 다시 열지 않는다.** 다만 아래 선택이 "손실 전략 중
무엇을 고를 것인가"임을 분명히 해둔다.

### 5.2 우연히 돌아가던 쪽(B)이 검증된 쪽(A)보다 낫다

전체기간에서 B2가 A보다 PF 0.84 vs 0.76, MDD 55% vs 83%, 자본 3,691만 vs 2,336만이다.
실제 체결 종목(A0568000)에서는 차이가 더 크다(PF 1.88 vs 1.29, MDD 8.14% vs 27.29%).
최근 30일만 A가 앞선다.

**의도치 않게 실거래에 들어가 있던 돌파 타점이, 검증을 통과한 칼만 밴드 타점보다
대부분의 구간에서 낫다.** 표본이 크지 않으니 단정할 것은 아니지만, 최소한
"A로 되돌리는 것이 당연히 옳다"는 전제는 성립하지 않는다.

### 5.3 게이트는 거의 아무것도 막지 않는다

`A0568000`(93일)에서는 B1과 B2가 **완전히 동일**하다 — std_error가 1.5 밑으로
내려가는 봉이 없기 때문이다. 긴 자료(384일)에서만 작동하고, 그때도 효과는
PF 0.80 → 0.84 수준이다.

즉 **1.5라는 문턱 자체는 큰 장치가 아니다.** 문제는 그 값이 아니라, 비교 대상인
`std_error`가 SAR에서 영원히 0.5로 굳어 있다는 사실이었다.

---

## 6. 수정 방향

측정 결과 **B2(돌파 타점 + 게이트 1.5)가 네 조합 중 가장 낫다.** 그런데 이 조합은
실거래에서 `std_error`가 갱신되어야만 성립한다 — 지금은 0.5로 굳어 게이트가
전부를 막는다.

따라서 필요한 수정은 **"게이트를 끄는 것"이 아니라 "std_error를 갱신하는 것"**이다.

- SAR에서도 칼만 잔차(`std_error`)를 계산한다
- 단, **진입 타점은 덮어쓰지 않는다** — 돌파 타점을 유지해야 B2가 된다
- `update_kalman_targets()`를 그대로 부르면 타점까지 칼만 밴드로 바뀌어 A가 되므로,
  std_error만 갱신하는 경로가 필요하다

**미적용 상태다.** 위 수치를 근거로 별도 결정이 필요하다.

---

## 7. 한계

- 실거래 표본이 없다. SAR 전환 후 체결이 0건이라 백테스트를 실거래로 대조할 수 없다.
- `A0568000`은 93거래일뿐이다. 5.2의 큰 격차(PF 1.88 vs 1.29)는 이 짧은 표본에서
  나온 값이라 그대로 믿을 수 없다. 384일 자료의 격차(0.84 vs 0.76)가 더 신뢰할 만하다.
- MDD는 [[project_sar_alternative_strategy_20260809]] 계열 보고서와 마찬가지로
  자료 선택에 민감하다. 같은 조합이 두 자료에서 55.06%와 8.14%로 갈린다.
- 전 구간 mock 데이터이며, 오버나잇 갭이 실시장보다 크다(평균 3.21%).
