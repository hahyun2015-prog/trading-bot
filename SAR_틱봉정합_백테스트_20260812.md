# SAR 틱/봉 정합 — 라이브 cadence를 백테스트로 재현한 결과

작성: 2026-08-12 · ERA 미기동 · `era/era_order_manager.py` 무수정 (백테스터에만 옵션 추가)

> **결론 1: 라이브의 틱 단위 SAR은 봉 단위보다 모든 분기에서 나쁘다. PF 0.989 → 0.674.**
> **결론 2: 원인은 둘이다 — ① 청산 자체가 나쁘고 ② 거래가 4배로 늘어 비용이 곱해진다. 둘 다 독립적으로 확인됐다.**
> **결론 3: 그러므로 `on_tick` → `on_bar` 전환은 "정합을 위한 정리"가 아니라 성과 개선 후보다.**
> **결론 4: 다만 봉 단위로 바꿔도 PF 0.989다. 비용 0일 때만 1.109 — 이 전략은 여전히 손익분기선 위가 아니다.**

---

## 1. 무엇을 했나

라이브와 백테스터가 SAR을 갱신하는 주기가 달랐다.

```python
# 라이브 (era_order_manager.py:4461 → _sar_tick)
self._sar_tick(current_price, self.futures_day_peak)      # 틱마다
# 백테스터 (backtest_sar_bb_20260809.py)
sar.on_bar(c_close, c_close, clamp=peak_price)            # 5분봉마다 1회
```

회전율이 **백테 1.3건/일 vs 라이브 22건/일**로 어긋나 있어, 백테스트로 라이브 손익을
예측할 수 없었다. 그래서 백테스터에 **라이브와 같은 cadence로 도는 옵션**을 추가해
같은 조건에서 A/B를 했다.

**라이브 코드는 건드리지 않았다.** 백테스터에만 옵션을 넣었다.

### 추가한 것 (가산적, 기본 꺼짐)

`scratch/backtest_sar_bb_20260809.py`

| 인자 | 기본값 | 의미 |
|---|---|---|
| `sar_tick_mode` | **False** | False = 기존 봉 단위 그대로. True = 라이브 cadence |
| `sar_ticks_per_bar` | 24 | 봉 하나를 O→H→L→C 경로의 합성 틱 N개로 편다 |

틱 경로 규약은 리플레이 하네스(`--tick-order OHLC --ticks-per-bar`)와 동일하다.
틱마다 `SarState.on_tick(price, clamp)` 을 호출해 SAR 전진·AF 상승·청산 판정을 하고,
청산가는 **그 틱 가격**을 쓴다 — 라이브와 같다.

백업: `scratch/backtest_sar_bb_20260809.py.bak_before_ticksar_20260812_233129`

### 기본 동작이 안 바뀌었음을 먼저 증명했다

교체 **전** 파일로 골든을 뜨고, 교체 **후** 다시 떠서 거래 단위로 대조했다.

```
조합 16개 / 누적거래 17,975건 / 불일치 0개
```

라이브 설정 조합(`L_live_exact`, 271건) 포함 전 조합이 진입·청산 시각·가격·손익까지
완전 일치했다. `sar_tick_mode=False`(기본)에서는 아무것도 바뀌지 않았다.

---

## 2. 결과 — 틱 cadence는 전면적으로 나쁘다

라이브 설정(`config.json` + `config_local.json`) 기준, 10500000 5분봉 31,793개
(2025-01-10 ~ 2026-08-12). 진입 가능 구간 209거래일.

| 모드 | 거래 | 건/일 | PF | 수익률 | MDD | 최악 단일손실 |
|---|---:|---:|---:|---:|---:|---:|
| **봉 단위 (현 백테스터)** | 271 | 1.3 | **0.989** | **-1.87%** | **39.16%** | -75.05pt |
| 틱 12/봉 | 885 | 4.2 | 0.638 | -106.73% | 117.94% | -45.91pt |
| **틱 24/봉** | 1,096 | 5.2 | 0.674 | **-96.65%** | 105.77% | -37.62pt |
| 틱 48/봉 | 1,294 | 6.2 | 0.556 | -148.68% | 148.71% | -34.47pt |

**틱을 잘게 쪼갤수록 거래가 늘고 성과가 무너진다.** 수익률 -96%는 자본이 사실상
전멸한다는 뜻이고, MDD가 100%를 넘는다.

### 분기별 — 예외 없이 전 분기 열세

| 분기 | 봉 단위 (거래/PF) | 틱 24 (거래/PF) |
|---|---:|---:|
| 2025-Q4 | 31 / 0.764 | 120 / **0.368** |
| 2026-Q1 | 101 / 0.785 | 375 / **0.418** |
| 2026-Q2 | 107 / 0.836 | 429 / **0.630** |
| 2026-Q3 | 46 / **1.470** | 206 / **1.053** |

유일하게 수익이 나던 2026-Q3조차 PF 1.470 → 1.053으로 깎인다.

---

## 3. 원인 분해 — 비용 탓인가, 청산이 나쁜가

거래가 4배로 늘면 비용도 4배다. 그게 전부인지 확인하려고 슬리피지·수수료를 0으로 두고 다시 돌렸다.

| 모드 | 거래 | PF | 수익률 | MDD |
|---|---:|---:|---:|---:|
| 봉 단위 · 비용 있음 | 271 | 0.989 | -1.87% | 39.16% |
| 봉 단위 · **비용 0** | 272 | **1.109** | **+16.99%** | 27.59% |
| 틱24 · 비용 있음 | 1,096 | 0.674 | -96.65% | 105.77% |
| 틱24 · **비용 0** | 1,117 | **0.888** | -29.41% | 51.46% |

**두 가지가 독립적으로 작동한다.**

1. **청산 품질 자체가 나쁘다.** 비용을 완전히 0으로 둬도 틱 0.888 < 봉 1.109다.
   틱마다 AF가 오르니 SAR이 가격에 너무 빨리 따라붙어, 정상적인 되돌림에도 튕겨나간다.
2. **비용이 곱해진다.** 비용이 갉아먹는 PF가 봉에서는 0.12(1.109→0.989),
   틱에서는 0.21(0.888→0.674)이다. 거래가 4배라 비용 부담도 약 2배다.

즉 "수수료만 아끼면 되는" 문제가 아니다. 틱 cadence는 청산 로직으로서도 열등하다.

---

## 4. ⚠️ 이 수치도 라이브를 과소평가하고 있다

모델의 회전율은 **5.2건/일**인데 라이브 실측은 **22건/일**이다. 아직 4배 남았다. 이유:

1. **합성 틱이 실제 틱보다 매끄럽다.** O→H→L→C를 직선 보간해서 걷는다. 실제 시세는
   봉 안에서 훨씬 많이 진동하므로 SAR이 더 자주 뒤집힌다.
2. **재진입이 봉 단위로 묶여 있다.** 이번 구현은 봉 안에서 청산까지만 하고, 재진입은
   다음 봉부터다. 라이브는 같은 5분 안에 나갔다 다시 들어올 수 있다.
3. 진입 판정은 봉 단위 그대로 뒀다 — 타점(`update_kalman_targets`)이 라이브에서도
   5분마다 갱신되기 때문이다. 청산 cadence 하나만 바꿔 변수를 분리했다.

따라서 **실제 라이브는 위 표의 틱24 행보다 더 나쁠 가능성이 높다.** 틱 48/봉이
PF 0.556으로 더 나빴던 것이 그 방향을 시사한다.

---

## 5. 권고

| # | 권고 | 근거 |
|---|---|---|
| 1 | **`on_tick` → `on_bar` 전환을 진지하게 검토할 것** | 전 분기에서 봉 단위가 우세. PF 0.674 → 0.989. `indicators.SarState.on_bar`가 이미 준비돼 있고 호출부 한 줄이다 |
| 2 | **단, 지표 단일화와 같이 켜지 말 것** | 거동이 크게 바뀌는 변경이다. 같이 켜면 문제 발생 시 원인 분리가 불가능하다. 지표 단일화 먼저 켜서 관찰 → 그다음 별도 A/B |
| 3 | **전환하더라도 이 전략은 아직 손익분기선 위가 아니다** | 봉 단위 PF 0.989 / 수익률 -1.87%. 비용 0일 때만 1.109. 슬리피지 가정 0.25pt가 정확히 손익분기점이라, 실계좌가 이보다 나쁘면 손실이다 |
| 4 | 전환 전 **리플레이 하네스로 실제 틱 재현 A/B**를 한 번 더 | 본 검증은 합성 틱이다. `scratch/replay` 는 실제 5분봉을 틱으로 펴서 라이브 코드를 그대로 태우므로 더 정확하다. 단 §6의 하네스 문제를 먼저 해소해야 한다 |

### 한계 (정직하게)

- 합성 틱이며 실제 틱이 아니다 (§4).
- 재진입이 봉 단위로 묶여 회전율을 과소 추정한다 (§4).
- 진입 로직은 봉 단위 그대로다 — 청산 cadence만 격리해 측정했다.
- 실질 표본 4분기, 2025-Q4는 31건(봉)/120건(틱)으로 얇다.
- `atr_cutoff=15.0` 때문에 2025년 1~3분기는 거래 0건이다 (`손절캡_백테스트_결과_20260812.md` §3).
- MDD > 100%는 시뮬레이터가 음수 자본을 허용한다는 뜻이다. 실계좌에서는 그 전에
  마진콜/강제청산이 난다 — 즉 **실제로는 더 일찍 끝난다.**

---

## 6. 산출물

| 파일 | 내용 |
|---|---|
| `scratch/backtest_sar_bb_20260809.py` | `sar_tick_mode` / `sar_ticks_per_bar` 추가 (기본 False = 무변경) |
| `…py.bak_before_ticksar_20260812_233129` | 교체 직전 백업 (`d7f89766…`) |
| `scratch/sar_tick_golden_20260812.py` | 기본동작 무변경 골든 대조기 (capture/compare) |
| `scratch/_golden_sartick/{base,new}/*.json` | 16조합 × 2 = 골든 원자료 |
| `SAR_틱봉정합_백테스트_20260812.md` | 본 문서 |

재현:

```bash
python3 scratch/sar_tick_golden_20260812.py capture base   # 교체 전 파일로
python3 scratch/sar_tick_golden_20260812.py capture new
python3 scratch/sar_tick_golden_20260812.py compare        # 0 불일치여야 정상
```
