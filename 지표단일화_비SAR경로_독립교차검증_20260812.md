# 지표 단일화 — 비(非)SAR 전략 경로 독립 교차검증

작성: 2026-08-12 / 검증자: 별도 세션에서 독립 구현한 리플레이 하네스
결과: **kalman·chandelier 경로 A/B 전부 0 차이 — 공백 해소**
ERA 미기동, 주문 API 호출 0건, 라이브 코드 무수정(읽기만).

---

## 0. 왜 이 검증이 따로 필요했나

선행 세션의 라이브 적용 검증(`지표단일화_라이브적용_보고_20260812.md`)은 실행 전부가
`parabolic_sar` 전략이었다. 그런데 SAR 경로는 `update_kalman_targets`를
`std_error_only=True`로 부르고, 그 분기는 **타점 설정 블록 전체를 조기 return으로 건너뛴다**
(L1210~1225).

이게 왜 사각지대인가 — 교체 전후 코드를 나란히 보면 분명하다.

| | 교체 전 (A) | 교체 후 (B) |
|---|---|---|
| kf_price 출처 | 인라인 칼만 루프의 `kf_prices[-1]` | `ind.kalman_residual_std(...)`의 두 번째 반환값 |

`std_error_only=True`(SAR)에서는 `kf_price`가 **계산되고 그대로 버려진다.** 즉 이 값이
틀려도 SAR 리플레이에서는 어떤 스냅샷에도 나타나지 않는다. `kf_price`가 실제로 쓰이는 곳은
kalman/chandelier 경로의 타점 4종뿐이다:

```
futures_target_long  = kf_price + band
futures_target_short = kf_price - band
futures_tp_price_long/short = kf_price ± kf_tp_sigma_mult * std_error
```

따라서 **`ind.kalman_residual_std`가 돌려주는 kf_price의 정확성은 SAR 실행으로는 검증
불가능**했다. 지금 운영 전략이 SAR이라 당장 무해하지만, 레짐 전환이나 전략 되돌림으로
kalman/chandelier가 되는 순간 미검증 코드가 실계좌에서 처음 돈다.

---

## 1. 검증 대상 (해시 고정)

| 대상 | SHA256 |
|---|---|
| A = 교체 전 (`era_order_manager.py.bak_before_indicators_live_20260812_203829`) | `0ee30d3d…8e3c` |
| B = **현재 디스크의 라이브 파일** (`era/era_order_manager.py`) | `48f3bc69…8b7` |
| `indicators.py` (루트 — 라이브가 실제 import하는 것) | `a9d5d0f0…a8d9` |

> ⚠️ `indicators.py`는 선행 세션이 검증한 시점(`0bb8136f…ae90`, 20:45)과 **다르다.**
> 21:53에 `ta_*` 호환 함수 6종이 **추가**됐다(순수 append, 기존 정의 변경 0 — diff 확인).
> 본 검증은 **현재 루트 파일(a9d5d0f0)** 을 강제로 물려 돌렸으므로, 지금 디스크 상태
> 그대로에 대한 검증이다.

하네스가 `variants/` 기준으로 `scratch/replay/indicators.py`(구 버전)를 읽지 않도록,
루트 `indicators.py`를 `sys.modules`에 선주입해 고정했다. 실행 메타에 경로·해시를 기록한다.

---

## 2. 하네스 신뢰성 — 먼저 증명한 것

이 경로를 실제로 감시하고 있음을 보이지 않으면 "0 차이"는 아무 의미가 없다.

| 대조 | 기대 | 결과 |
|---|---|---|
| **P0 양성** (A vs A 바이트 동일 복사본), kalman | 0 차이 | **PASS — bar/tick/주문/게이트 전부 0** |
| **N3 음성** (A에 칼만 윈도 off-by-one 주입: `df.tail(40)` → `df.iloc[:-1].tail(40)`), kalman | 반드시 검출 | **FAIL 검출 11건** |

N3 검출 내역 (하네스가 잡은 것):

```
futures_std_error       최대상대오차 0.24     best_shift=+1   ← off-by-one 지문
futures_target_long     최대상대오차 0.00217
futures_target_short    최대상대오차 0.00189
futures_tp_price_long   최대상대오차 0.0106
futures_tp_price_short  최대상대오차 0.0117
futures_day_entry_price / day_peak / trade_count 불일치
스냅샷 시퀀스 불일치 — index 353에서 A는 tick, B는 order (진입 시각이 어긋남)
```

`best_shift=+1`은 "B의 값이 A보다 한 스텝 늦다"는 뜻으로, 한 봉 밀림의 직접적인 증거다.
**타점 4종까지 전파되어 주문 시퀀스가 바뀌는 것**을 확인했으므로, 이 하네스는
kf_price 오류를 확실히 드러낸다.

---

## 3. 본 검증 결과 — A vs B

전부 동일 스트림, 동일 계측, 상대오차 문턱 `1e-12`(PASS) / `1e-9`(WARN) / 초과(FAIL).

| # | 날짜 | 전략 | 틱 경로 | 봉 | 틱 | 주문 의도 | 판정 |
|---|---|---|---|---|---|---|---|
| 1 | 2026-07-30 | kalman | O→H→L→C | 112 | 1,644 | 12건 | **PASS (0/0)** |
| 2 | 2026-07-30 | kalman | O→L→H→C | 112 | 1,644 | 18건 | **PASS (0/0)** |
| 3 | 2026-08-04 | kalman | O→H→L→C | 112 | 1,644 | 30건 | **PASS (0/0)** |
| 4 | 2026-07-30 | chandelier | O→H→L→C | 112 | 1,644 | 14건 | **PASS (0/0)** |
| 5 | 2026-07-28 | chandelier | O→H→L→C | 107 | 1,528 | 2건 | **PASS (0/0)** |

**WARN(부동소수 재배열 수준)도 0건.** 즉 미세 오차조차 없는 완전 일치다.

대조 항목: 봉 경계 지표 20종(`futures_std_error`, `futures_target_long/short`,
`futures_tp_price_long/short`, `futures_atr_14`, `futures_stop_loss_pt`,
`futures_take_profit_pt`, `futures_prev_range`, `trend_direction` 등) · 틱 상태 9종
(`sar_value/ep/af/bull`, `day_peak`, 세션 고저, 진입가, 거래횟수) · 포지션 상태(매 틱) ·
주문 의도 시퀀스(시각·방향·가격·코드·슬롯) · 진입 게이트 차단 기록 ·
**지표 입력 구간(`db_last_date`/`db_rows`)** — 미완성 봉 포함 여부가 바뀌면 여기서 먼저 걸린다.

주문 의도가 실제로 발생한 상태에서의 비교다(2~30건). 게이트가 전부 막아 양쪽 0건인
공허한 일치가 아니다.

---

## 4. 하네스 격리 (안전)

- `PyQt5`/`QAxWidget`을 가짜 모듈로 대체해 **라이브 코드를 무수정으로 `__init__`부터 그대로 태움.**
  `__new__` 우회를 쓰지 않았으므로 필드 누락으로 인한 왜곡이 없다.
- 주문 2중 차단: ① `self.kiwoom`이 가짜라 실 API에 물리적으로 닿을 수 없음
  ② `_execute_futures_direct`를 의도 기록 스텁으로 대체. **`orders_via_kiwoom = 0`**
  (2차 장벽에 도달한 호출이 0건 = 스텁이 앞에서 전부 잡았다는 뜻).
- `notifier` 전면 no-op — 텔레그램 발신 0건.
- 실 `futures_data.db`는 `mode=ro`로만 열고, 복제본에만 기록. 복제본은 로컬 임시경로.
- 워크스페이스 루트가 `scratch/replay`로 잡혀 실 `config/`·`era/`를 건드리지 않는다.
- 시각 고정(`datetime.now()` → 리플레이 클럭). 주기 타이머는 지표 관련(15분 ATR)만 발화.
- ERA 미기동, `system_stopped.flag`·`emergency_kill.flag`·STOP-GUARD 무접촉.

---

## 5. 여전히 검증되지 않은 것

| 항목 | 이유 |
|---|---|
| 실제 분 내 틱 경로 | 원시 틱 미저장. 1분봉 O/H/L/C 4틱 근사(두 순서 모두 시도) |
| 5분봉만 있는 구간(2026-08-05~) | `futures_ohlcv_1m`이 08-04까지만 존재 |
| 체결가·슬리피지·부분체결 | 주문 스텁. 틱 가격 즉시체결 가정 |
| Kiwoom TR/체잔 콜백, 잔고 동기화 | 리플레이 범위 밖 |
| 이벤트 루프 부하·세션 끊김·하드리셋 | 재현 불가 |
| 야간 세션 / KIS 병합의 실시간 타이밍 | 주간만 실행 |
| `volatility_breakout` 전략 | 미실행 (칼만 타점 블록을 안 씀 — 우선순위 낮음) |

**근사가 이 결론을 약화시키지 않는다.** A와 B에 완전히 동일한 합성 스트림을 주므로
A≠B는 코드 차이에서만 나온다. 근사가 제한하는 것은 "라이브가 실제로 그렇게 움직였는가"
(절대적 현실성)이지 "교체 전후가 같은가"(차분 검증)가 아니다.

---

## 6. 결론과 남은 조건

**kalman·chandelier 경로의 공백은 해소됐다.** 5개 실행 전부 0 차이이고, 같은 하네스가
같은 경로에 심은 off-by-one은 정확히 잡아냈다. 선행 세션의 SAR 검증과 합치면
현재 라이브 파일은 **4개 전략 중 3개 경로에서 교체 전과 동일**하다(SAR·kalman·chandelier).

재기동 전 남은 조건은 코드가 아니라 배포 쪽이다.

1. **`indicators.py`가 git untracked** — 엔진이 파일 2개가 짝을 이뤄야만 도는 구조가 됐다.
   `run_era.bat`이 기동마다 `git pull`을 돌리고, 2026-07-31에 동기화가
   `era_order_manager.py`를 덮어써 장중을 날린 전례가 있다. `git add indicators.py` 선행 필요.
2. **`indicators.py`가 검증 후 또 바뀌었다**(20:45 → 21:53). 이번 검증은 21:53 버전 기준이다.
   **재기동 시점에 해시가 `a9d5d0f0…a8d9`인지 다시 확인해야 한다.** 또 바뀌었다면 재검증.
3. **같은 저장소에 두 세션이 동시에 작업 중**이다. 실계좌 엔진에서 이건 검증 자체를
   무의미하게 만든다 — 검증한 파일과 기동할 파일이 다를 수 있다.

---

## 부록 · 재현 명령

```bash
cd scratch/replay
python3 run_variant.py --variant era_A --date 20260730 --scenario open --strategy kalman
python3 run_variant.py --variant era_B --date 20260730 --scenario open --strategy kalman
python3 compare.py era_A_20260730_open_OHLC_kalman era_B_20260730_open_OHLC_kalman

# 하네스 자체 검증
python3 inject.py                       # off-by-one 주입본 N1~N5 생성
python3 run_variant.py --variant era_N3 --date 20260730 --scenario open --strategy kalman
python3 compare.py era_A_..._kalman era_N3_..._kalman   # FAIL이 나와야 정상
```

구성 파일: `qt_shim.py`(격리) · `run_variant.py`(스트림·계측) · `compare.py`(대조·래그 탐지) ·
`inject.py`(음성 대조본 생성). 보고서는 `scratch/replay/out/report_*.md`.
