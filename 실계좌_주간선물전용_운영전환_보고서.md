# 실계좌 전환 — 국내 주간선물 전용 운영 조정 보고서

- 작성일: 2026-07-25
- 배경: 실계좌 전환 후에는 국내 주간선물만 거래할 계획. 지금의 주식+선물 통합 매매
  시스템(era/era_order_manager.py)을 어떻게 조정해야 하는지, 또는 별도 프로세스 분리가
  나은지 검토.

## 1. 조사 결과 — 코드 수정 없이 이미 가능

`era_order_manager.py`에는 최상위 `trading_mode`("stock" / "futures" / "both") 스위치가
이미 있고, 다음 지점을 전부 관통한다(직접 코드 확인함):

- 주식 계좌 자체를 비활성화(1855-1890행: `trading_mode`가 stock/both가 아니면 "선물 전용
  모드 — 주식 계좌 비활성화" 출력, 계좌 자체를 안 잡음)
- 주식 관련 타이머(테마 스캔, 장중 동적 종목편입 등)가 애초에 시작 안 됨(480, 541, 680,
  686, 692행)
- 실시간 주식 체결 틱을 아예 무시(5845-5847행)
- 주식 신호 폴링이 안 돎(5151-5152행) → 이 경로 안에서만 호출되는 **RSA 서브프로세스
  (rsa_coordinator.py)도 자동으로 함께 비활성화**됨(호출부가 `_poll_stock_signals` 안,
  5330행에만 있음을 확인)

야간선물은 이미 `trade_futures_night: false`로 꺼져 있어(신규 진입만 차단, 기존 포지션
안전감시는 계속 — 의도된 설계) 결과적으로 "국내 주간선물만" 운영 상태가 된다.

`config_local.json`이 `config.json`을 1레벨 덮어쓰는 로컬 오버라이드 구조임을 확인했다
(era_order_manager.py:1107-1123, `load_config`). 이 PC(NUCBOX_M8)의 실제 동작을 결정하는
건 `config_local.json` 쪽 값이라, 여기를 바꿔야 실제로 반영된다.

## 2. 별도 프로세스 분리는 비권장

`trading_mode`가 이미 ERA·TCA 양쪽에서 검증되어 동작 중인 스위치라, 프로세스를 쪼개면
배치파일 하드코딩·포트락(9991)·PID/워치독 로직을 전부 재작업해야 해서 오히려 안정성이
떨어진다. 참고로 2026-07-08에 "주식 리더종목 스크리닝의 블로킹 HTTP 요청이 Kiwoom 메시지펌프를
지연시켜 크래시를 유발"한 이력이 실제로 있었다(이미 수정됨) — 주식 처리 로직 자체가 과거에
실제 안정성 문제의 원인이었던 전례이기도 해서, 주간선물 전용 운영이 안정성 면에서도 합리적이다.

## 3. 적용한 변경

`config/config_local.json` (git 추적 대상 아님 — `.gitignore`에 등록돼 있고 API 키 등
민감정보를 담고 있어 이 파일은 커밋하지 않음, 이 보고서에만 변경 사실을 기록):

| 항목 | 이전 | 이후 |
|---|---|---|
| `trading_mode` | `"both"` | `"futures"` |
| `_comment` | "단일 PC에서 주식과 선물을 동시에 매매하는 통합 모드(both)" | "실계좌 전환 후 국내 주간선물만 운영하기로 함(2026-07-25, futures 전용 모드)" |

전환 전 `era/era_positions.json`을 확인해 **보유 주식 포지션이 없음(`{}`)을 확인**했다 —
방치되는 포지션 없이 안전하게 전환 가능한 시점이었다.

## 4. 아직 남은 작업 (사용자 확인 필요)

- **ERA 프로세스 재시작 필요.** 지금 이 PC에 python.exe 프로세스가 여러 개 떠 있어(확인
  시점 기준 6개) 기존 `era_order_manager.py`가 이미 구동 중인 것으로 보인다 — 이 프로세스는
  시작 시점에 `config_local.json`을 한 번 읽어들인 상태라, **재시작해야 이번 변경이 실제로
  반영된다**. `era/auto_reconnect_era.bat` 또는 "AMATS ERA Reconnect" 예약작업으로
  재기동하는 걸 권장한다(직접 `leader_order_manager.py`를 실행하면 리더종목 필터가
  우회된다는 기존 경고가 `watchdog_check.bat`에 있음 — 반드시 예약작업 경유).
- **"AMATS ERA Reconnect"·워치독 예약작업은 이 저장소 안에 등록 스크립트가 없다** — 운영
  서버의 Windows 작업 스케줄러에 수동 등록된 것으로 보인다. 재시작 전에 실제 서버에서 이
  예약작업들이 정상 등록돼 있는지 직접 확인할 것을 권한다(코드로는 확인 불가한 부분).
- 재시작 후 로그에서 `[ERA] trading_mode = futures`와 "선물 전용 모드 — 주식 계좌 비활성화"
  메시지가 찍히는지로 정상 반영 여부를 확인할 수 있다.
