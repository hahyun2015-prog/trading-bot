# 리플레이 하네스 설계안 — 지표 단일화 라이브 이식 검증

작성: 2026-08-12 / 상태: **설계 제출, 승인 대기** (구현 전)
전제: 이번 세션은 조사·설계만. `era/era_order_manager.py`, `indicators.py`, `bqa/kalman_backtester.py` 무수정.

---

## 1부 · 조사 결과 (수정 없음)

### 1-1. 라이브의 지표 호출 지점과 호출 주기

| # | 지점 | 위치 | 호출 주기 | 산출물 |
|---|------|------|-----------|--------|
| A | `update_kalman_targets(code)` | L1052 | 5분봉 경계 1회 | 칼만 평활가 `kf_price`, `std_error`, band, 15분봉 추세, 타점 4종 |
| A' | `update_kalman_targets(code, std_error_only=True)` | L1052 | 5분봉 경계 1회 (SAR 전략) | `std_error`, `trend_direction`만 (타점 미변경) |
| B | `update_bb_psar_filters(code)` | L959 | 5분봉 경계 1회 (SAR 전략) | `current_bb_mid`, `current_bb_bandwidth`, `current_bb_squeeze_limit`, `futures_ma_filter_value/close` |
| C | `update_futures_dynamic_sl_tp()` | L816 | 15분 타이머 + 세션 시작 + 09:00 폴백 | 일봉 TR → 칼만 ATR → `futures_atr_14`, `futures_stop_loss_pt`, `futures_take_profit_pt` |
| D | SAR AF/EP 갱신 | L4456-4461(LONG), L4663-4668(SHORT) | **틱마다** | `sar_value`, `sar_ep`, `sar_af`, `sar_bull` |
| E | `_load_prev_range()` | L3698 | 세션 시작 1회 | `futures_prev_range` (돌파 타점 재료) |

**호출 사슬**

```
Kiwoom OnReceiveRealData (틱)
 └ _handle_real_data (L6923)
    ├─(1)─ _process_futures_tick → _process_day_tick   ← 지점 D, 진입/청산 판정
    └─(2)─ _update_futures_ohlcv (L3272)
            └ 새 5분 구간의 첫 틱이면
               QTimer.singleShot(0, _refresh_futures_targets_deferred)  (L3311)
                 └ _flush_ohlcv_buffer()  ← 버퍼 → DB 기록
                    → 지점 A/A' + B
```

지점 C는 별도 QTimer(15분, L505-508) + `_init_futures_strategy` 경로(L3526-3527, L3599-3600)에서 돈다.
지점 C 안에서 SAR 전략이면 지점 B를 한 번 더 호출한다(L890-892) — **B는 5분봉 경계와 15분 ATR 타이머 두 경로에서 불린다.**

### 1-2. "진행 중인 봉"이 입력에 포함되는가 — 지점마다 다르다

핵심은 `_flush_ohlcv_buffer()`(L3362)가 **버퍼의 모든 구간을 무조건 DB에 REPLACE**한다는 것이다. 진행 중인 구간도 예외 없이 들어간다(L3375-3383). 그리고 `_refresh_futures_targets_deferred`는 flush를 먼저 하고 나서 DB를 읽는다(L3347).

새 봉의 첫 틱에서 경계 이벤트가 트리거되므로, 그 시점 버퍼의 마지막 구간은 **틱 1개짜리 봉(o=h=l=c=그 틱 가격, v=1)** 이다. 즉:

> **지표 계산 시점의 DB 마지막 행 = 방금 시작한, 틱 1개짜리 미완성 봉.**

이 전제 위에서 지점별 취급이 갈린다.

| 지점 | 미완성 봉 포함? | 근거 |
|------|----------------|------|
| A 칼만 평활/std_error | **포함** | `df.tail(40)` → `kf_prices[-1]`이 곧 미완성 봉의 종가를 흡수 (L1123-1145) |
| A 15분 추세 | **포함** | `resample('15Min').last()` 마지막 버킷에 미완성 봉이 들어감 (L1160-1165) |
| B 볼린저 mid/bandwidth/squeeze | **포함** | `last_row = df.iloc[-1]` (L1005) |
| B 이평선(MA200) 필터 | **제외** | `iloc[-2]` 명시적 사용 + 주석 "진행 중인 당봉 종가는 미래참조" (L992-1002) |
| C 일봉 ATR | **제외** | 오늘 날짜 일봉이면 `daily.iloc[:-1]`로 잘라냄 (L850-853) |
| D SAR | **해당 없음(틱 기반)** | 봉이 아니라 `current_price`로 직접 EP/AF 갱신 |
| E prev_range | **제외** | `rows[0][0] == today_str`면 `rows[1]` 사용 (L3719-3724) |

**즉 `update_bb_psar_filters` 한 함수 안에서 BB(포함)와 MA(제외)의 기준이 다르다.** 이게 이식 시 가장 먼저 깨질 자리다. `indicators.py`의 `Window.closed_upto(arr, i, size)`가 `arr[i-size:i]` 배타 구간이므로, BB를 그대로 `closed_upto`로 옮기면 미완성 봉이 빠지면서 **백테스트 골든은 통과하는데 라이브 값만 한 봉씩 밀린다.**

부수 발견 — 설계 판단이 필요한 항목:
- **지점 D의 SAR은 봉 기반이 아니라 틱 기반이다.** `sar_ep`가 봉 고저가 아니라 `current_price`로 갱신되고, AF도 틱마다 오를 수 있다. 백테스터의 봉 기반 SAR과 의미 자체가 다르다. `indicators.py`에 `SarState.on_tick`(L467)이 이미 있어 이식 자체는 가능하지만, **off-by-one 문제가 아니라 의미 차이**이므로 이식 시 반드시 `on_tick` 경로를 써야 한다.
- `update_kalman_targets`의 KIS 야간 병합(L1094-1112)은 `drop_duplicates(subset='date')` + `head(300)`이라 키움 5분봉과 KIS 리샘플 5분봉이 같은 타임스탬프면 **키움 행이 이긴다**(concat 순서상 키움이 앞). 이식 시 `merge_night`(indicators.py L511)의 우선순위 규칙이 이것과 같은지 확인해야 한다.

### 1-3. 라이브가 실제로 받는 데이터의 형태

- **틱 콜백**: `_handle_real_data(code, real_type, real_data)` → `GetCommRealData(code, 10)` 문자열 하나. 실제로 쓰는 값은 **가격 스칼라 `abs(float(raw))` 단 하나**다. 체결량·호가·시각은 쓰지 않는다(선물 경로 기준). 시각은 전부 `datetime.now()`.
- **DB 조회**: 전부 `futures_data.db`의 `futures_ohlcv` 단일 테이블. 컬럼 `(code, date TEXT '%Y%m%d%H%M%S', open, high, low, close, volume)`, `UNIQUE(code,date)`.
- 코드 폴백 체인이 곳곳에 있다(`real_day_code` → `futures_prefix+"00000"` → `10500000`/`10100000`).

### 1-4. 리플레이 입력 재현 가능성

`futures_data.db` 실측:

| 테이블 | 코드 | 행수 | 기간 |
|--------|------|------|------|
| `futures_ohlcv` (5분) | 10100000 | 31,797 | 2025-01-13 ~ 2026-08-12 |
| `futures_ohlcv` (5분) | 10500000 | 31,793 | 2025-01-10 ~ 2026-08-12 |
| `futures_ohlcv` (5분) | A01609 / A05608 (KIS 야간) | 11,596 / 11,570 | 2026-07-13 ~ 2026-08-12 |
| `futures_ohlcv_1m` | 10100000 | 157,818 | 2025-01-03 ~ **2026-08-04** |
| `futures_ohlcv_1m` | 10500000 | 157,583 | 2025-01-03 ~ **2026-08-04** |

**원시 틱은 저장되어 있지 않다.** 최선의 근사는 `futures_ohlcv_1m`(1분봉)이며, 1분봉은 2026-08-04까지만 있다. 그 이후 구간은 5분봉만으로 근사해야 한다.

판단: **재현 가능하다. 단 "근사 스트림"이며, 그 근사가 무엇을 못 잡는지는 3-1에 명시한다.**

---

## 2부 · 하네스 설계안

### 2-1. 구조

```
scratch/replay/
  replay_harness.py        # 스트림 공급 + 스냅샷 수집 + 대조 (신규, 유일한 실행 진입점)
  qt_shim.py               # PyQt5/QAxWidget/QTimer 가짜 모듈
  variants/
    era_A.py               # era_order_manager.py 무수정 복사본 (교체 전 = 기준)
    era_A2.py              # A의 두 번째 복사본 (양성 대조용)
    era_B.py               # 교체 후 (다음 세션에서 생성)
    era_N1..N5.py          # 음성 대조용, 의도적 off-by-one 주입본
  replay.db                # futures_data.db에서 잘라낸 읽기 전용 시드
  out/<variant>/snap.jsonl # 스냅샷
  out/report_<pair>.md     # 대조 보고서
```

라이브 원본은 읽기만 하고 `variants/`로 복사한다. 원본 파일은 절대 열지 않는다(쓰기 모드로도).

### 2-2. 스트림 재현 방식

**봉 DB를 통째로 주지 않고, 시간에 따라 증분 구축한다.** 이것이 off-by-one을 드러내는 유일한 방법이다.

1. 시드: `futures_data.db`를 **읽기 전용(`mode=ro`)** 으로 열어 대상 기간 + 워밍업(직전 400봉, MA200+여유)만 뽑아 `out/<variant>/live_replica.db`에 복사. 각 variant는 자기 DB 사본을 갖는다(교차 오염 방지).
2. 리플레이 클럭 `T`를 진행시키며 이벤트를 생성:
   - **틱 이벤트**: 1분봉 구간이면 한 분에 4틱 `(O, H, L, C)` 또는 `(O, L, H, C)`. 5분봉만 있는 구간이면 5분에 4틱. 각 틱에 가상 시각 부여(분 내 0/15/30/45초).
   - 각 틱마다 A와 B에 **동일한** `(code, price, T)`를 준다.
3. 하네스는 라이브 콜백 함수를 **직접 호출**한다: `obj._handle_real_data(code, "선물체결", "")` 대신, `GetCommRealData`를 가짜 kiwoom이 현재 틱 가격으로 되돌려주게 해서 **L6923부터의 실제 코드 경로를 그대로 탄다.** 즉 `_process_futures_tick` → `_process_day_tick`(D, 진입/청산) → `_update_futures_ohlcv`(버퍼) 순서, 5분 경계 감지, `QTimer.singleShot(0,...)` 지연 실행까지 라이브와 같은 순서로 재현된다.
4. `QTimer.singleShot(0, f)`는 shim이 **틱 처리 종료 직후 실행되는 큐**에 넣는다 — 라이브의 이벤트 루프 양보와 같은 순서.
5. `QTimer(주기)`는 shim이 리플레이 클럭 기준으로 발화시킨다(15분 ATR 타이머 = 지점 C 재현). 나머지 주식/연결감시 타이머는 등록만 하고 발화시키지 않는다(선물 경로와 무관, 결정성 확보).
6. 시각: `era_X.datetime`을 `FrozenDatetime`으로 교체해 `datetime.now()`가 리플레이 클럭을 돌려주게 한다. 이걸 안 하면 세션 판정·일일 리셋·`today_str` 비교가 전부 실제 오늘 날짜로 흘러 리플레이가 무의미해진다.

**근사의 한계(무엇을 검증 못 하는가)는 3-1에 별도로 정리.**

### 2-3. 라이브 코드 격리 방식

**`__init__`을 우회하지 않고 그대로 태운다.** `__new__`로 필드를 손으로 세우면 어떤 필드를 빠뜨렸는지 알 수 없고, 그 자체가 새로운 오차원이 된다.

import 전에 `sys.modules`에 가짜 모듈을 심는다:

| 대상 | 처리 |
|------|------|
| `PyQt5.QtWidgets` / `QAxContainer` / `QtCore` | shim 모듈. `QAxWidget`는 `dynamicCall`이 하네스 제어값을 돌려주는 가짜 객체 |
| `notifier` | 텔레그램 전송 전부 no-op + 호출 기록 |
| `requests`, `bs4` | 실제 import는 두되 네트워크 호출 경로는 타지 않음(선물 경로 미사용) |

인스턴스 생성 후 즉시 적용하는 격리:

1. **주문 차단 — 2중 장벽.**
   - 1차: 모든 주문이 `self.kiwoom.dynamicCall("SendOrder…"/"SendOrderFO…")`를 지난다. `self.kiwoom`이 가짜이므로 물리적으로 실 API에 닿지 않는다.
   - 2차: 가짜 `dynamicCall`이 `SendOrder`/`SendOrderFO` 문자열을 보면 **주문 의도를 기록하고 0을 반환**한다. 그 외 알 수 없는 호출은 기록 후 `""` 반환.
   - 추가: `_execute_futures_direct`(L5511)를 래핑해 진입/청산 의도를 구조화 기록하고, 체결은 그 틱 가격으로 즉시 체결된 것으로 시뮬레이션.
2. **파일 쓰기 차단.** `save_futures_exit_state`, `era_positions.json` 저장, `export_status`, `trade_ledger` 기록 경로를 전부 `out/<variant>/` 하위로 리다이렉트하거나 no-op. **`config/active_strategy.json`은 읽기만, 쓰기 경로는 예외로 즉시 중단.**
3. **DB 경로 치환.** `self.futures_db_path`/`self.unified_db_path`를 variant 전용 사본으로 교체. 실 DB는 `mode=ro`로만 열린다.
4. **상태 초기화(결정성).** 두 variant가 같은 지점에서 출발해야 한다:
   - `load_persisted_positions` / `load_futures_exit_state`가 읽는 json을 하네스가 준비한 고정 시드로 교체(기본: 포지션 없음, SAR 상태 없음).
   - `futures_strategy_type`, `futures_ma_filter_period`, SAR AF 파라미터 등은 하네스가 **명시 시나리오로 주입**(active_strategy.json에 의존하지 않음 — 그 파일은 언제든 바뀔 수 있고 재현성이 깨진다).
   - 난수 없음 확인됨. 유일한 비결정 요소는 시각(고정)과 dict 순서(파이썬 3.7+ 삽입순 보장).

### 2-4. 대조 항목과 허용 오차

스냅샷은 세 종류로 남긴다(JSONL, 한 줄 = 한 이벤트).

**(1) 봉 경계 스냅샷 — `kind="bar"`.** 경계 이벤트마다 `_refresh_futures_targets_deferred` **직전과 직후 두 번** 찍는다. 직전/직후 쌍이 있어야 "값이 한 봉 늦게 반영됐다"를 구분할 수 있다.

기록: `bar_ts`, `kf_price`, `std_error`, `band`, `trend_direction`, `futures_target_long/short`, `futures_tp_price_long/short`, `current_bb_mid`, `current_bb_bandwidth`, `current_bb_squeeze_limit`, `futures_ma_filter_value`, `futures_ma_filter_close`, `futures_atr_14`, `futures_stop_loss_pt`, `futures_take_profit_pt`, `futures_prev_range`, `df_len`, `df_last_date`(입력 마지막 봉의 타임스탬프 — 미완성 봉 포함 여부를 직접 드러냄).

**(2) 틱 스냅샷 — `kind="tick"`.** 매 틱, `_process_day_tick` 직후: `sar_value`, `sar_ep`, `sar_af`, `sar_bull`, `futures_day_peak`, `futures_day_session_high/low`, 포지션 유무·방향·진입가, 그리고 **게이트 판정 결과**(어느 게이트에서 return했는지 — `_note_entry_block` 래핑 + BB/MA 차단 지점 계측).

**(3) 주문 의도 — `kind="order"`.** `(리플레이 시각, signal_type, 가격, 수량, order_code, pos_key)`.

**허용 오차**

| 항목 | 판정 |
|------|------|
| 지표 스칼라 | `상대오차 <= 1e-12` → PASS(동일). `1e-12 < 상대오차 <= 1e-9` → WARN(부동소수 재배열). `> 1e-9` → **FAIL** |
| `df_last_date`, `df_len` | 완전 일치 필수. 불일치 = 입력 구간 자체가 달라짐 = **즉시 FAIL** |
| `trend_direction`, `sar_bull`, 게이트 판정, 포지션 상태 | 완전 일치 필수 |
| 주문 의도 시퀀스 | 시각·방향·가격·수량 전부 완전 일치 필수. 개수 불일치도 FAIL |

off-by-one은 통상 수 pt~수십 pt 차이를 만들므로 1e-9 문턱은 충분히 안전하고, 동시에 `np.std(ddof)` 같은 미세 구현차와 진짜 버그를 구분해준다.

**전용 검사기 — 래그 탐지기.** 단순 일치 비교만으로는 "어디가 틀렸는지"를 못 준다. 각 스칼라 시계열에 대해 shift ∈ {-2,-1,0,+1,+2}로 B를 밀어 A와의 오차를 재고, **shift≠0에서 오차가 급감하면 그 크기와 방향을 명시**한다. 이것이 off-by-one의 지문이다. 보고서는 항목별로 `best_shift`를 반드시 출력한다.

### 2-5. 음성 대조 — 하네스 자체를 먼저 검증

**이 절차를 통과하기 전에는 A/B 대조 결과를 신뢰하지 않는다.**

**양성 대조 P0**: `era_A` vs `era_A2`(바이트 동일 복사본, 모듈명만 다름). 전 항목 0 차이여야 한다. 하나라도 차이가 나면 하네스가 스스로 노이즈를 만들고 있는 것이므로 그것부터 고친다.

**음성 대조 N1~N5**: 각각 A의 복사본에 off-by-one 하나만 주입. 하네스는 전부 FAIL로 잡아야 하고, 래그 탐지기가 해당 항목에 `best_shift=±1`을 찍어야 한다.

| ID | 주입 | 노려야 할 탐지 |
|----|------|----------------|
| N1 | `update_bb_psar_filters` `df.iloc[-1]` → `df.iloc[-2]` (미완성 봉 제외) | `current_bb_mid/bandwidth/squeeze` FAIL, shift=+1 |
| N2 | 같은 함수 MA `iloc[-2]` → `iloc[-1]` (미완성 봉 포함) | `futures_ma_filter_value/close` FAIL, shift=-1, **LONG/SHORT 차단 판정 변화** |
| N3 | `update_kalman_targets` `df.tail(40)` → `df.iloc[:-1].tail(40)` | `kf_price`,`std_error`,타점 FAIL, shift=+1 |
| N4 | `update_futures_dynamic_sl_tp`의 오늘 미완성 일봉 제거 라인 삭제 | `futures_atr_14`,`stop_loss_pt`,`take_profit_pt` FAIL |
| N5 | `_load_prev_range`의 `today_str` 분기 삭제 | `futures_prev_range` FAIL → 돌파 타점 FAIL |

**추가 음성 대조 N6(무증상 시나리오 검증)**: 주문이 한 건도 나지 않는 조용한 구간에서 N1을 돌려, **주문 의도가 동일해도 지표 차이는 잡히는지** 확인. 잡히지 않으면 스냅샷 밀도가 부족하다는 뜻이므로 계측 지점을 늘린다.

합격 기준: **P0 = 0 차이, N1~N6 = 전부 FAIL 검출 + 원인 항목 정확히 지목.** 하나라도 놓치면 그 지점의 계측을 보강하고 전체를 다시 돌린다.

### 2-6. 시나리오 매트릭스

| 축 | 값 |
|----|-----|
| 전략 | `parabolic_sar`(현행), `kalman`, `chandelier`, `volatility_breakout` |
| 코드 | `10100000`, `10500000` |
| 데이터 밀도 | 1분봉 구간(~2026-08-04), 5분봉만 있는 구간(08-05~08-12) |
| 틱 경로 | 분내 `O,H,L,C` / `O,L,H,C` 두 순서 |
| 세션 | 주간 / 야간(KIS 병합 경로 포함) |
| 워밍업 | MA200 충족 / 미달(= `_invalidate_filter_state` 경로) |

최소 합격선: 각 전략 × 최근 20거래일 × 두 틱 경로.

---

## 3부 · 검증 가능한 것과 불가능한 것의 경계

### 3-1. 근사가 못 잡는 것

| 항목 | 이유 |
|------|------|
| 실제 분 내 틱 경로 | 원시 틱 미저장. H/L 순서를 모른다 → SAR EP/AF 궤적과 진입 시각이 실제와 다를 수 있다 |
| 틱 간격·빈도 | 4틱/분 고정. 실제는 초당 수 틱. `_is_sustained_tick_move`, 이상치 필터, 락아웃 로직은 **표면만 탄다** |
| 5분봉 구간(08-05~) | 봉 내부 해상도가 1/5. SAR 궤적 오차가 커진다 |
| 미완성 봉의 실제 내용 | 리플레이는 "그 봉의 첫 틱"을 재구성하지만 실제 첫 틱과 다를 수 있다 |
| 체결가·슬리피지·부분체결 | 주문은 스텁. 체결 시뮬레이션은 틱 가격 즉시체결 가정 |
| Kiwoom TR/체잔 콜백 | 잔고 동기화, 미체결 관리, 재전송 로직 미검증 |
| 이벤트 루프 부하·타이밍 | `singleShot` 지연, 세션 끊김, 하드리셋 경로 미검증 |
| 스레드 경합 | `MorningPrepWorker` 등 미기동 |

**중요 — 근사가 off-by-one 검출을 약화시키지는 않는다.** 하네스는 A와 B에 **완전히 동일한 합성 스트림**을 준다. 따라서 A≠B는 근사와 무관하게 **코드 차이에서만** 나온다. 근사가 제한하는 것은 "라이브가 실제로 그렇게 움직였는가"(절대적 현실성)이지, "교체 전후가 같은가"(차분 검증)가 아니다. 이 하네스는 후자를 위한 도구다.

### 3-2. 하네스가 답하지 못하는 질문

- "교체 후 값이 **옳은가**" — 하네스는 A와의 동일성만 본다. A 자체의 버그(예: BB는 미완성 봉 포함, MA는 제외)는 그대로 통과한다. 그 판단은 별도 결정 사항이며, **의도적으로 동작을 바꾸려면 하네스는 반드시 FAIL을 내야 하고 그 FAIL을 사람이 승인해야 한다.**
- "라이브에서 정말 안 터지는가" — 실 API·네트워크·장중 부하는 범위 밖. 최종 확인은 모의계좌 병행 관찰이 필요하다.
- 야간 KIS 병합의 실시간 타이밍(수집기가 언제 쓰느냐)은 재현 불가. DB 스냅샷 기준으로만 본다.

### 3-3. 안전 확인

- ERA 미기동. `era/era.pid` 건드리지 않음. 하네스는 별도 프로세스, Kiwoom OCX 미생성.
- 주문 2중 차단(가짜 QAxWidget + SendOrder 문자열 인터셉트).
- 실 DB는 `mode=ro`. 쓰기는 전부 `scratch/replay/out/` 하위.
- `config/active_strategy.json`, `system_stopped.flag`, `emergency_kill.flag`, STOP-GUARD 블록 무접촉.
- 라이브 원본 3개 파일(`era_order_manager.py`, `indicators.py`, `bqa/kalman_backtester.py`) 무수정.

---

## 4부 · 승인 요청 사항

다음 3개에 대한 판단을 주시면 구현에 들어갑니다.

1. **구현 순서** — 제안: ① shim + A 단독 기동 확인 → ② 스냅샷 계측 → ③ **P0 양성 대조** → ④ **N1~N6 음성 대조** → ⑤ 여기서 보고·중단. B(교체본) 제작과 A/B 대조는 다음 세션. 즉 **이번 세션은 "하네스가 off-by-one을 잡는다"를 증명하는 데까지**입니다.
2. **틱 근사 승인** — 4틱/분(O,H,L,C) 두 순서. 더 촘촘한 근사(예: 1분봉을 선형 보간해 12틱)가 필요한지.
3. **SAR 의미 차이 처리** — 지점 D가 틱 기반이라는 사실을 이식 시 `SarState.on_tick`으로 보존할지, 아니면 봉 기반으로 바꿀지. **바꾸면 하네스는 반드시 FAIL을 냅니다** — 그 FAIL을 "의도된 변경"으로 승인할 것인지 미리 정해야 합니다.
