# 매매시스템 점검 보고서 (2026-07-31)

- 점검 시각: 2026-07-31 19:30 (금)
- 대상: AMATS 주간선물 자동매매 (ERA, PID 2988, 09:03:38 기동, 모의투자)
- 결론 요약: **오늘(7/31) 하루 종일 시세 피드가 락아웃되어 매매가 전혀 이뤄지지 않음.**
  프로세스는 살아있었으나 내부 가격이 934pt에 고정된 채 실제 시세(972~1036pt)를 전부
  거부했다. 이번 주 작성된 복구 패치가 이 상황을 해소할 수 있으나 **아직 라이브에 배포되지
  않았다**(코드는 17:21 작성, 프로세스는 09:03 기동).

---

## 1. 심각 — 오늘 하루 시세 락아웃 (P0)

### 증상

| 항목 | 값 |
|---|---|
| 틱 거부 건수 | **93,972건** (오늘 로그 전체 라인의 98.6%) |
| 내부 봉 시가(고정값) | 932.66pt → 934.02pt (하루 종일 고정) |
| 실제 유입 틱 | 972.80 → 979 → **1036.28pt** |
| 당일 체결 | **0건** |
| DB 저장 봉 | 83개 중 **82개가 o=h=l=c 동일(평탄봉)**, 당일 레인지 1.36pt |

비교 — 직전 5거래일은 정상이었다(이번 장애는 오늘만 발생):

| 날짜 | 틱 거부 | DB 당일 레인지 | 평탄봉 |
|---|---|---|---|
| 07/24 | 0건 | 64.98pt | 2/84 |
| 07/27 | 0건 | 56.56pt | 1/83 |
| 07/28 | 0건 | 69.88pt | 2/79 |
| 07/29 | 3건 | 162.24pt | 1/78 |
| 07/30 | 0건 | 76.36pt | 1/83 |
| **07/31** | **93,972건** | **1.36pt** | **82/83** |

### 원인 (3단 연쇄)

**① 시초가를 DB에서 시딩 — 실제 시세와 4.3% 괴리**

> ⚠️ **정정 (2026-07-31 20:10)**: 최초 작성 시 이 항목을 "DB 시초가와 실시간 피드가
> 4.3% 어긋난 상태로 출발했다"고 적었으나, **틀린 진단이었다.** 봉별 시세를 정밀
> 대조한 결과 DB 시딩값(932.66)은 그 시점 실제 가격과 정확히 일치했고, 진짜 방아쇠는
> 그 뒤에 발생한 정규장 개장 갭이었다. 아래는 정정된 원인이다.

정규장 개장 갭이 3% 임계를 넘었다. 오늘 봉별 종가 변화를 보면 3%를 넘은 구간은
**단 한 번뿐**이다:

| 시각 | 종가 | 직전봉 대비 |
|---|---|---|
| 08:45~09:00 | 932.66 | 0.00% (개장 전 동일가) |
| **09:05** | **991.50** | **+6.31%** ← 유일한 3% 초과 |
| 09:10 이후 | 999 → 1036 | 전부 ±1.2% 이내 |

즉 데이터 불일치 문제가 아니라, **08:45 개장 전 가격에서 09:00 정규장이 열리며 발생한
정상적인 갭**이 임계를 넘은 것이다. 이는 시장에서 반복적으로 일어나는 정상 이벤트다.

> **어느 쪽이 진짜였나 — 실시간 피드가 옳았음이 확정됨.**
> 재시작(19:50) 시 키움 서버에서 오늘 5분봉을 새로 받아온 결과, A0568000의 오늘 실제
> 봉은 **932.66 → 1036.28pt(레인지 103.62pt)**로 확인됐다. 실시간 틱이 보고한
> 흐름이 실제 시장이었고, **이상치 필터가 현실을 글리치로 오판해 하루치 시세를 통째로
> 버린 것**이다.

**② 신규 봉 경로가 갭을 "글리치"로 보고 스테일 가격으로 대체**

`_update_futures_ohlcv()`는 봉 시가 대비 3%(`futures_tick_max_jump_pct`) 초과 틱을
글리치로 보고 버리고, 신규 봉의 첫 틱이 직전 종가 대비 3%를 넘으면 **직전 종가로
대체**한다. 09:05 갭(991.50)이 이 규칙에 걸려 시가가 932.66으로 되돌려졌고, 이 순간부터
내부 기준가가 현실과 어긋났다. 원래 이 필터는 2026-06-23 단발 글리치 사고 대응으로
들어간 것인데, **지속적인 갭과 단발 글리치를 구분하지 못한다**는 것이 이번에 드러났다.

**③ 신규 봉 생성 경로가 스테일 가격을 계속 전파 — 자기영속 락아웃**

`era_order_manager.py:2820-2825`:

```python
if is_new_candle:
    prev_close = buf[max(buf.keys())]['c']
    if prev_close > 0 and abs(price - prev_close) / prev_close > max_jump:
        print(f"[ERA 이상치 필터] ... 직전 종가로 대체")
        price = prev_close        # ← 스테일 가격이 다음 봉으로 그대로 승계
buf[period_str] = {'o': price, 'h': price, 'l': price, 'c': price, 'v': 1}
```

한 번 기준가가 어긋나면, 5분마다 봉이 새로 열려도 첫 틱이 3% 밖이라 **직전 종가(스테일)로
대체**되고, 그 스테일 값이 다음 봉으로 계속 승계된다. 오늘 이 대체가 78건 발생했다
(≈봉 개수와 일치). 이것이 하루 종일 풀리지 않은 결정적 이유다.

### 왜 복구 패치가 작동하지 않았나

2026-07-31자로 작성된 "지속이동 복구" 로직(`era_order_manager.py:2842-2864`)은 정확히
이 상황을 겨냥한 것이 맞다. 오늘 마지막 20틱은 전부 1036.28pt로 동일(범위 0.00%)이라
복구 조건(범위 ≤1%)을 충족했고, 장 초반 첫 20틱(972.80~979.08, 범위 0.64%)도 충족했다.
즉 **패치가 돌고 있었다면 개장 직후 수 초 내 복구됐을 상황**이다.

작동하지 않은 이유는 단순하다 — **배포되지 않았다.**

- `era/era_order_manager.py` 최종 수정: 2026-07-31 **17:21:57**
- 실행 중인 프로세스(PID 2988) 기동: 2026-07-31 **09:03:38**

프로세스는 09:03 시점의 코드(복구 로직 없음)를 메모리에 들고 하루를 났다.

### 남은 결함 — 복구 로직의 적용 범위

복구 로직은 **기존 봉 갱신 경로(`else` 분기, 오늘 93,894건)에만** 들어갔고,
**신규 봉 생성 경로(2820-2825행, 오늘 78건)에는 없다.** 재시작 후에도 봉 경계마다
스테일 시딩이 한 번씩 재발하고 20틱 뒤에야 복구되는 형태로 동작한다(첫 복구 이후엔
prev_close가 정상화되므로 자연 수렴하긴 하나, 구조적으로는 미완결).

---

## 2. 파급 영향

**오늘 손익**: 진입 자체가 없어 실현손익 0. 예수금 1,097,959,380원 변동 없음.
직접적 금전 손실은 없고 기회비용만 발생했다.

**내일(월, 8/3) 이월 영향 — 재시작으로 자동 해소됨**:

장애 당시 DB에는 평탄봉 82개(레인지 1.36pt)가 기록돼 있었고, 이대로면 월요일
`_load_prev_range()`가 7/31을 전일로 잡아 `futures_prev_range = 1.36pt`(정상 56~162pt)를
읽어 재진입 휩소 방지 밴드(`prev_range × reentry_k 0.25`)가 56배 축소될 상황이었다.

**재시작 시 5분봉 자동 재동기화가 이 오염 봉을 서버 실제 데이터로 덮어써서 해소됐다.**
현재 DB의 20260731은 레인지 103.62pt로 정상화됐고(평탄봉 17/83, 정상 범위), 월요일
prev_range는 103.62pt로 올바르게 로드된다. 별도 DB 정리 작업은 불필요하다.

ATR14도 영향이 제한적이다. 일봉 TR은 전일 종가 대비 갭까지 반영하는 데다 칼만
평활(K≈0.095)이라 하루치 왜곡이 크게 남지 않는다.

**검증한 정상 항목**: 오늘 세션에 예외/Traceback 0건. 프로세스 생존(19:28까지 로그 갱신).
포지션 flat 상태로 정상 종료. 7/28 도입한 청산 재시도 취소 로직은 7/30 장마감 청산에서
단일 체결로 정상 종료됨(중복 체결·유령 LONG 재발 없음).

---

## 3. 개선 권고

우선순위 순.

### P0 — ERA 재시작 ✅ 완료 (2026-07-31 19:50)

복구 패치를 배포했다. 신규 PID **412**로 정상 기동 확인, 전일 Range 76.36pt(7/30 기준)
정상 로드, 5분봉 재동기화 완료. 이제 오늘과 같은 락아웃은 20틱(수 초) 내 자동 해소된다.
부수 효과로 오염됐던 7/31 DB 봉도 서버 실데이터로 복구됐다(2절 참조).

### P1 — 신규 봉 경로에도 복구 로직 적용 ✅ 완료 (2026-07-31 20:05)

거부 스트릭 판정을 `_is_sustained_tick_move()` 헬퍼로 분리하고, **신규봉 경로와 기존봉
경로가 이 스트릭을 공유**하도록 했다. 신규봉 첫 틱이 3% 밖이어도 지속 이동으로 판정되면
직전 종가로 대체하지 않고 새 레벨을 그대로 채택한다. 알림도 `_notify_tick_recovery()`로
통합(코드별 1회).

**검증 — 오늘 실제 시세로 재현 테스트** (`scratch/test_tick_lockout_recovery_20260731.py`):
실제 `_update_futures_ohlcv()`를 그대로 호출해 09:05 갭(932.66→991.50) 이후 오늘 실측
종가 흐름을 투입했다.

| 구성 | 투입 틱 | 거부 | 복구 | 내부가격 최종 | 판정 |
|---|---|---|---|---|---|
| 복구 OFF (사고 당시와 동일) | 626 | **626 (전량)** | 0 | 932.66pt (고정) | 락아웃 재현 |
| 복구 ON (이번 수정) | 626 | 38 | 2 | **1007.50pt** (실제와 일치) | 정상 추종 |

사고가 그대로 재현되고, 수정 적용 시 락아웃이 해소됨을 확인했다.

### ~~P1 — 시초가 시딩을 첫 실시간 틱과 교차검증~~ — 불필요 (진단 정정)

최초에는 DB 시초가와 실시간 피드의 괴리를 근본 원인으로 봤으나, 정밀 대조 결과
**DB 시딩값은 정확했다**(1절 정정 참조). 시딩은 문제가 아니었으므로 이 변경은
불필요하다. 실제 방아쇠인 개장 갭은 위 P1 복구 로직이 처리한다.

### P2 — 피드 정지 감시 알람

오늘처럼 프로세스는 살아있는데 시세만 죽은 상태를 외부에서 알 방법이 없었다.
"N분간 채택된 틱 0건" 또는 "거부율 90% 초과"를 텔레그램으로 알리는 워치독이 필요하다.
복구 패치에 `tick_health_alert`가 들어있으나 이는 *복구에 성공했을 때* 알리는 것이라,
복구가 안 되는 상황에는 여전히 침묵한다.

### ~~P2 — 오늘(7/31) 오염 봉 처리~~ ✅ 재시작 시 자동 해소

재시작의 5분봉 재동기화가 오염 봉을 서버 실데이터(레인지 103.62pt)로 덮어써서 별도
조치가 불필요해졌다. 다만 **오염 → 재동기화로 덮어쓰기**가 우연히 맞아떨어진 것이지
설계된 안전장치는 아니라는 점은 기억해 둘 필요가 있다(재시작 없이 다음 거래일을
맞았다면 오염된 prev_range가 그대로 쓰였다).

### P3 — 기존 미결 사항 (이번 점검 이전부터 열려 있음)

- 장마감 청산 재시도가 15:35~15:45 창을 넘기면 그날은 더 이상 재시도하지 않는다
  (증상인 "감시 중단"만 7/27에 수정, 창 초과 자체는 미해결).
- `run_era.bat`에 기동 전 `py_compile` 자가검사 미적용 — 디스크 손상 시 조용한 실패 방지용.
- 2026-06-11 실거래정지 갭 꼬리위험은 구조적으로 미해결 상태 유지
  (`선물매매_최종설정_백테스트_20260731.md` 3절).

---

## 4. 재현/근거

- 로그: `era/era_order_manager.log` 115468행 이후(오늘 세션)
- 락아웃 시작: 115543행, 시초가 DB 복원(932.66) 직후 첫 틱(972.80)부터
- DB: `futures_data.db` → `futures_ohlcv` code='A0568000' date LIKE '20260731%'
- 코드: `era/era_order_manager.py:2806-2878`(틱 필터/복구), `:3223-3244`(시초가 DB 복원)
