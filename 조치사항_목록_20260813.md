# 2026-08-13 검토 결과 — 조치사항 목록

작성 2026-08-13 · 이 문서는 **측정·정리 전용**이며 조치를 실행하지 않았습니다.
근거: 오늘 작성된 보고서 14건 + 본 세션의 라이브 상태 실측

---

## ⚠️ 먼저 — 세션 전제와 실제 상태가 다릅니다

이 세션은 "ERA 정지 상태"를 전제로 시작했습니다. **실제로는 가동 중입니다.**

| 항목 | 전제 | **실측 (2026-08-13 20:44)** |
|---|---|---|
| ERA 프로세스 | 정지 | **가동 중** — `era/era.pid` 존재(09:04 생성), 로그가 5분마다 갱신 중 |
| `system_stopped.flag` | 존재 | **없음** |
| `emergency_kill.flag` | 존재 | **없음** |
| 포지션 | — | **없음**(flat). `[507287] 자료가 존재하지 않습니다` |
| 선물 예수금 | — | 13,967,549원 (30% 캡 가용 4,190,264원) |

오늘 다른 보고서 2건(`EOD강제청산_정책검토`, `BB_진입타점_미검증용법_검토`)도
"ERA 가동 중(실계좌 LONG 1계약 보유)"로 기록하고 있어, **가동 상태가 맞습니다.**

**본 세션에서 플래그를 지우거나 ERA를 기동한 적은 없습니다.**
`git diff` 상 보호 대상 파일 7종 전부 무변경을 확인했고, 새로 만든 것은
`scratch/` 아래 분석 스크립트 3개와 `scratch/_edge/` 산출물뿐입니다.

→ **A-1, A-2, A-3이 여기서 나옵니다.**

---

## A. 즉시 확인 — 안전·변경통제 (실계좌 가동 중이므로 최우선)

### A-1. 🔴 라이브 엔진 해시가 승인본과 다릅니다

`LIVE_INTEGRITY.json`은 "ERA 재기동 전에 반드시 실행할 것. 해시가 달라졌다면
누군가 라이브 부품을 바꿨다는 뜻이다. **검증 없이 켜지 말 것**"이라고 적혀 있습니다.

| 파일 | 승인 해시 | 현재 | 판정 |
|---|---|---|---|
| `indicators.py` | `a9d5d0f0…` | 동일 | ✅ 일치 |
| `era/era_order_manager.py` | `48f3bc69…` | `26b83cb8…` | 🔴 **불일치** |

**추적 결과 — 고의 변경이며 롤백 지점은 온전합니다.**

백업 `era/era_order_manager.py.bak_before_sarbar_20260813_011736`의 해시가
`48f3bc69…`로 **승인본과 정확히 일치**합니다. 즉 승인 시점 이후 변경분은 다음 202행이고,
**두 가지 기능이 함께 들어갔습니다.**

| 변경 | 추가된 식별자 | 문서화 |
|---|---|---|
| ① SAR 봉 단위 전환 | `_sar_step`, `sar_update_mode`, `_sar_last_bar_key` | ✅ `config_local.json` 주석 + `SAR_틱봉정합_백테스트_20260812.md` |
| ② **월물 자동 롤오버** | `pick_front_month`, `_futures_expiry_date`, `_parse_futures_code` | ⚠️ `config_local.json` 주석만. **독립 검증 보고서 없음** |

**조치**

1. ②(자동 롤오버)의 검증 근거를 확인할 것. 만기(둘째 목요일) 판정과 월물 전환은
   **잘못되면 청산 불가·미결제 이월로 직결**됩니다. A/B 리플레이 검증이 있었는지,
   없다면 다음 만기 전에 반드시 할 것.
2. 검증이 끝났다면 `LIVE_INTEGRITY.json`의 `sha256`·`verified_at`·`verified_by`를 갱신할 것.
   **지금처럼 항상 불일치가 뜨는 상태를 방치하면 검증 장치가 무력화됩니다** —
   "또 빨간불이네" 하고 넘기게 됩니다.

> 판단 주체: 사용자 · 라이브 코드 변경 아님(문서 갱신) · 위험도 낮음

### A-2. 🔴 STOP-GUARD 블록이 제거되어 있습니다

`LIVE_INTEGRITY.json`이 요구하는 값은 **4개 이상**입니다.

| 파일 | 요구 | 현재 |
|---|---:|---:|
| `run_era.bat` | 4 | **0** |
| `era/auto_reconnect_era.bat` | 4 | **0** |

백업 `run_era.bat.bak_before_guard_removal_20260813_022622`가 남아 있어
**02:26에 의도적으로 제거된 것**으로 보입니다.

**조치**: 제거가 의도된 것인지, 제거 사유가 해소됐는지 확인. 해소됐다면 복원하거나,
영구 제거라면 `LIVE_INTEGRITY.json`의 `guards` 항목을 함께 지울 것.
**요구사항과 실제가 어긋난 채로 두는 것이 가장 나쁩니다.**

> 판단 주체: 사용자 · 위험도 중간(자동 재기동이 정지 의도를 무시할 수 있음)

### A-3. 🟡 정지 플래그 부재 — 의도 확인

`system_stopped.flag`·`emergency_kill.flag`가 둘 다 없습니다.
어제 23:00 시점(`백테스터_문제점_조치완료_20260813.md` §6)에는 **둘 다 존재**했습니다.

**조치**: 재기동이 의도된 것이면 조치 불요. 아래 B-1과 함께 판단할 사안입니다.

---

## B. 전략 채택 판정 — 오늘 결론이 하나로 모입니다

오늘 독립적으로 진행된 5개 세션이 **서로 다른 경로로 같은 결론**에 도달했습니다.

| 보고서 | 대상 | 건당 gross 엣지 | 허들 0.8pt |
|---|---|---:|:---:|
| `건당엣지_허들판정` (본 세션) | SAR 봉단위 12조합 | 최선 +1.036pt | ❌ **미달**(P=0.55) |
| `chandelier_MA200_맞대결` | chandelier vs SAR | 최선 +1.036pt | ❌ **전 조합 미달** |
| `chandelier_진입게이트_재설계` | 게이트 8종 ablation | 앵커 +0.813pt | ❌ **미달** |
| `신호수준_전수스크리닝` | 5분봉 신호 31종 × 298셀 | — | ❌ **생존 0개** |
| `일봉스윙_신호스크리닝` | 일봉 신호 30종 × 150셀 | — | ❌ **생존 0개** |
| `BB_진입타점_미검증용법_검토` | BB 진입 10후보 × 200셀 | — | ❌ **생존 0개** |

### B-1. 🔴 SAR 실계좌 운용 지속 여부 — 결정 필요

**현재 ERA가 가동 중이고, 채택 기준을 통과한 전략은 없습니다.**

- 건당 gross 엣지 최선값 +1.036pt는 점추정으로 허들 1.29배이나
  95% CI **[-1.50, +3.72]**, P(진짜 평균 ≥ 0.8pt) = **55%**.
- 4분기 중 3분기가 허들 미달, 2026Q2는 **-0.69pt로 음수**.
- 실체결 측정(`체결모델_정합_2단계`): 27건 비용 반영 **PF 0.91, -0.73pt**.

**선택지 — 어느 쪽도 제가 대신 정할 수 없습니다.**

| 안 | 근거 | 반대 근거 |
|---|---|---|
| **B-1a. 계속 가동하며 표본 축적** | 판정에 필요한 표본이 부족하다는 것이 오늘의 결론. 멈추면 표본이 안 쌓임. 봉 단위 전환으로 회전율이 22 → 1.6건/일로 내려와 **손실 속도도 함께 낮아짐** | 기대 엣지가 0과 구분되지 않는 상태에서 실계좌 자금이 소모됨 |
| **B-1b. 정지하고 재설계** | 통과 기준을 못 넘긴 전략을 실계좌에 두지 않는다는 원칙 | 표본 축적이 멈춤. 재설계 방향에 대한 근거도 오늘 대부분 소진됨 |
| **B-1c. 축소 운용** | 1계약 고정 유지 + 일손실한도 원복(현재 0.10 → 0.03)으로 소모 속도 제한 | 절충안이라 어느 목적도 완전히 달성 못 함 |

**참고 사실 하나** — 이 회전율에서 건당 엣지 1pt를 통계적으로 입증하려면
**유의 기준 4년, 검정력 80% 기준 8.5년**의 표본이 필요합니다
(건당 SD 21pt, 1.6건/일). B-1a를 고르더라도 **"언젠가 입증된다"는 기대는 성립하지 않습니다.**
`chandelier_진입게이트_재설계`가 목적함수를 "유의에 필요한 연수"로 잡은 이유가 이것입니다.

> 판단 주체: **사용자** · 위험도 높음 · **이 목록에서 가장 먼저 결정해야 할 항목**

### B-2. 🟡 chandelier vs SAR — 표본 효율에서는 chandelier 우위

`chandelier_MA200_맞대결`: 우열은 통계적으로 판별 불가(P(SAR우위)=0.64, CI가 0을 포함)이나,
**건당 SD와 회전율에서 chandelier가 명확히 낫습니다** — 즉 같은 기간에 더 빨리 판별됩니다.
공통기간으로 자르면 순위가 뒤집힙니다(chandelier 추세필터off +1.214 vs SAR +1.036).

**조치**: B-1을 "계속 운용"으로 정할 경우, **판별 속도가 빠른 쪽을 고르는 것이 합리적**입니다.
단 chandelier 실체결 표본은 PF 0.45(27건)로 SAR(0.91)보다 나빴다는 점을 함께 볼 것.

> 판단 주체: 사용자 · config 변경만으로 전환 가능(`futures_strategy_type`) · 라이브 코드 수정 불요

---

## C. 백테스터 정합 — 근거 있고 실행 가능한 것들

### C-1. 🔴 수수료율 기본값이 실측의 2.17배입니다

| 위치 | 값 |
|---|---|
| `bqa/kalman_backtester.py` (4곳) | `commission_rate=0.000065` |
| `scratch/backtest_sar_bb_20260809.py` | `commission_rate=0.000065` |
| **실측(잔고 역산 확정)** | **0.000030** |

오늘 작성된 보고서 3건(`chandelier_MA200_맞대결`, `chandelier_gap_guard_검토`,
`chandelier_진입게이트_재설계`)은 이미 `0.000030`을 **명시 지정**해 돌렸으나,
**기본값은 그대로**라 지정을 잊으면 조용히 2배 비용으로 계산됩니다.

**조치**: `realistic_gap_fill`에 적용한 것과 같은 센티널 방식(P1 조치)을 수수료율에도 적용.
기본값을 바꾸면 과거 결과와 비교가 끊기므로, **미지정 시 경고**가 안전합니다.

> 실행 가능 · 백테스터만 수정 · 라이브 무관 · **골든 대조 필수**

### C-2. 🟡 `max_trades_day` 미스터리 — 해소됐습니다

`체결모델_정합_2단계_SAR` §4(2)가 "`max_trades_day=10`인데 08-11에 22건 체결.
확인 필요"로 남긴 항목입니다. **코드에서 원인을 찾았습니다.**

```python
# era/era_order_manager.py:4957
is_unlimited = getattr(self, "futures_strategy_type", "") in \
               ["parabolic_sar", "bollinger_band", "kalman", "chandelier"]
if not is_unlimited and self.futures_day_trade_count >= self.futures_max_trades_day:
    return
```

**현재 운용 중인 4개 전략 전부가 `is_unlimited`에 들어 있습니다.**
즉 `max_trades_day`는 **어떤 전략에서도 강제되지 않습니다.** 실제로 작동하는 상한은
`futures_day_max_trades_hard_cap = 50`(config `max_trades_hard_cap`) 하나뿐입니다.

**조치**: ① 설정값이 무의미하다는 사실을 문서화하거나, ② 회전율 억제가 필요하면
`is_unlimited` 목록을 조정할 것(**라이브 코드 수정 → 승인 사항**).
봉 단위 전환으로 회전율이 1.6건/일로 떨어졌으므로 **현재는 긴급하지 않습니다.**

> ①은 즉시 가능 · ②는 승인 필요

### C-3. 🟡 백테스터가 모델링하지 않는 라이브 제약

세 보고서가 반복 지적한 항목입니다(`체결모델_정합_1단계` §7-2, `2단계` §5, `샹들리에_복귀_점검` P1).

| 제약 | 값 | 백테스터 | 실제 영향 |
|---|---|---|---|
| `global_cooldown_sec` | 90 | 없음 | **봉 단위에서는 무의미**(5분봉 = 300초) |
| `reentry_cooldown_sec` | 301 | 없음 | **봉 단위에서는 무의미**(301초 ≈ 5.02분) |
| `max_trades_day` | 10 | 없음 | **강제되지 않음**(C-2) |
| `daily_loss_limit_pct` | 0.10 | 있음 | 반영됨 |

**정정**: 본 세션 분석 결과, **쿨다운 2종은 봉 단위 백테스터에 구현할 필요가 없습니다.**
5분봉 단위에서 이미 자동으로 만족되기 때문입니다. 이 제약들은 **틱 단위 시절의 장치**이며,
봉 단위 전환으로 사실상 비활성화됐습니다.

**조치**: C-3을 "구현 필요" 목록에서 내리고, **그 사실을 기록**할 것.
`샹들리에_복귀_점검` P1의 "35건 vs 27건 차이가 여기서 나온다"는 설명은
chandelier 기간(틱 단위 시절) 한정으로 범위를 좁혀야 합니다.

> 즉시 가능 · 문서 정정만

### C-4. 🟡 chandelier + MA200 백테스트 불가 문제 — 해소됐습니다

`샹들리에_복귀_점검` P3가 "가장 중요"로 표시한 항목이나,
같은 날 나중 세션에서 **이미 구현됐습니다**(`bqa/kalman_backtester.py`에
`ma_filter_period` 인자 추가, 기본 None = 기존 동작 불변, 골든 62조합 전수 일치).

**조치**: `샹들리에_복귀_점검_20260813.md`에 해소 표기를 남길 것.
그렇지 않으면 다음 세션이 같은 작업을 반복합니다.

> 즉시 가능 · 문서 정정만

---

## D. 기각 확정 — 다시 시도하지 말 것

오늘 소진된 가설들입니다. **재시도 방지를 위해 명시적으로 남깁니다.**
근거 없이 되살리면 오늘 쓴 시간을 그대로 반복하게 됩니다.

| # | 안 | 기각 근거 | 출처 |
|---|---|---|---|
| D-1 | **손절 캡 도입** | 전체기간·2026 전분기에서 "캡 없음"이 최고. 캡은 최악손실만 줄이고 PF·수익률·MDD를 모두 악화 | `손절캡_백테스트_결과_20260812` |
| D-2 | **틱 단위 SAR 유지** | 비용 0으로 둬도 봉(1.109) > 틱(0.888). 청산 로직으로서 열등 | `SAR_틱봉정합_백테스트_20260812` |
| D-3 | **진입 조건을 조여 회전 반감** | 무작위 절반은 건당 엣지를 안 바꿈(+1.036 → +1.125). 선택적으로 손실을 거를 신호가 없으면 무효 | `건당엣지_허들판정` §5-1 |
| D-4 | **비용 절감(F안)으로 허들 통과** | 허들이 0.6pt로 내려가도 신뢰구간 불변. 봉 단위에서 비용은 병목이 아님(거래 ±14pt vs 비용 0.27pt) | `건당엣지_허들판정` §5-3 |
| D-5 | **chandelier에 MA200 추가** | 건당 엣지 +0.507 → +0.490(악화). 칼만 장기추세 필터와 역할 중복 | `chandelier_MA200_맞대결` |
| D-6 | **`gap_guard_mult`** | 축이 잘못 겨냥됨 + 신호 무정보(FWER p=0.184) + 9시대에 무력 | `chandelier_gap_guard_검토` |
| D-7 | **EOD 강제청산 완화** | 강제청산은 이익을 자르고 있지 않음. 추가보유 증분이 전 구간 음수 | `EOD강제청산_정책검토` |
| D-8 | **BB 진입타점 교체** | 200셀 검정, FWER 보정 후 생존 0. IS/OOS 부호 반전 | `BB_진입타점_미검증용법_검토` |
| D-9 | **일봉 스윙 전환** | 표본 문제가 해소가 아니라 **악화**. 필요 자본 3,000만원(현 1,397만원) | `일봉스윙_신호스크리닝` |
| D-10 | **변동성 기반 게이트 강화 일반** | pt 엣지가 ATR에 비례(≈0.016×ATR). 평균과 SD를 같은 비율로 올려 판별 가능성 개선 없음 | `chandelier_진입게이트_재설계` §3 |

**D-10이 가장 중요합니다.** `atr_cutoff`, `min_std_error_entry`, ATR 분위수 등
변동성을 대리하는 모든 축이 여기 해당합니다. `min_std_error_entry`를 0 → 3.0으로 밀면
건당 엣지가 2배(+0.648 → +1.290pt)가 되지만 **유의 필요 연수는 1.34 → 2.15년으로 악화**됩니다.
**"건당 엣지가 올랐다"만 보고 채택하면 안 되는 이유**를 이 사례가 보여줍니다.

---

## E. 남은 탐색 — 근거가 있는 것만

### E-1. 🟢 `sar_af_max` 하향 스윕 (0.10 → 0.06 / 0.04 / 0.02)

**오늘 유일하게 남은 미탐색 축입니다.**

근거: 보유시간과 건당 엣지가 단조 동행합니다(`건당엣지_허들판정` §5-5).

| 보유시간 분위 | 중앙 | 건당 gross |
|---|---:|---:|
| Q1 (최단) | 75분 | **-12.83pt** |
| Q2 | 130분 | -0.31pt |
| Q3 | 185분 | +9.59pt |
| Q4 (최장) | 283분 | +8.31pt |

AF는 SAR이 조여오는 속도를 **직접 지배하는 유일한 파라미터**인데 4단계 스윕에 없었습니다.
`sl_mult`·`cap`을 푸는 방향(보유시간 ↑)이 실제로 엣지를 올렸으므로, AF도 같은 계열입니다.

**⚠️ D-10의 함정을 피할 것**: 건당 엣지만 보지 말고 **SD와 "유의 필요 연수"를 함께**
목적함수로 삼아야 합니다. 평균만 오르고 SD가 더 커지면 실패입니다.

> 백테스트만 · 라이브 무수정 · 즉시 실행 가능 · **본 세션에서 실행 가능**

### E-2. 🟢 선물 일봉 10년치 확보 (실호출 검증)

`일봉데이터확보_가능성조사`: 키움 `opt50030` TR 존재 확인, 호출 3~10회 · 10초 미만 ·
매매 영향 사실상 없음. **소급 깊이는 실호출 전까지 확인 불가.**

주의: D-9로 일봉 스윙 전환은 기각됐으나, **데이터 확보 자체는 별개 가치**가 있습니다
(레짐 다양성 검증, 장기 백테스트 기반). 다만 `일봉스윙_신호스크리닝`이
"리샘플해도 동일한 1.53년"이라 했으므로, **10년치가 정말 다른 레짐을 포함하는지**가
확보보다 어려운 과제입니다.

> ERA 가동 중이므로 **키움 API 동시 호출 영향 확인 후** 실행할 것 · 승인 권장

### E-3. 🟡 SAR SHORT 0건 — 방향 편향 확인

`체결모델_정합_2단계` §4(1): 27건 전부 LONG, SHORT 차단 로그도 0건.
표본 2일이라 단정 불가하나, MA200 방향필터가 상승장에서 LONG만 허용했을 가능성.

**조치**: 오늘 로그에 SAR 거래가 11,277행 쌓였습니다. 방향 분포를 다시 볼 표본이
확보됐으므로 재측정할 것.

> 로그 읽기만 · 즉시 실행 가능

---

## 실행 순서 제안

| 순서 | 항목 | 성격 | 누가 |
|---|---|---|---|
| 1 | **B-1** SAR 실계좌 운용 지속 여부 | 결정 | **사용자** |
| 2 | **A-1** 롤오버 검증 근거 확인 → `LIVE_INTEGRITY.json` 갱신 | 확인·문서 | 사용자 |
| 3 | **A-2** STOP-GUARD 제거 의도 확인 | 확인 | 사용자 |
| 4 | **C-3·C-4** 해소된 항목 문서 정정 | 문서 | 제가 가능 |
| 5 | **C-1** 수수료율 센티널 | 백테스터 수정 | 제가 가능(승인 후) |
| 6 | **E-1** `sar_af_max` 스윕 | 백테스트 | 제가 가능 |
| 7 | **E-3** SAR 방향 분포 재측정 | 로그 분석 | 제가 가능 |
| 8 | **E-2** 일봉 확보 | API 호출 | 사용자 승인 필요 |

**1~3은 제가 대신 결정할 수 없습니다.** 특히 B-1은 실계좌 자금이 걸린 사안이라
근거만 정리해 드렸습니다. 저는 투자 자문을 드릴 수 없으며,
위 수치는 과거 자료 재현이고 미래를 보장하지 않습니다.

**4·6·7은 라이브를 건드리지 않으므로 지시하시면 바로 진행하겠습니다.**

---

## 부록. 오늘 작성된 보고서 14건

| 보고서 | 판정 |
|---|---|
| `건당엣지_허들판정_20260813.md` | 허들 미달 (본 세션) |
| `chandelier_MA200_맞대결_20260813.md` | MA200 무익 · 전 조합 허들 미달 |
| `chandelier_진입게이트_재설계_20260813.md` | 개선 없음 · 허들 미달 |
| `chandelier_gap_guard_검토_20260813.md` | 기각 |
| `EOD강제청산_정책검토_20260813.md` | 개선안 기각 |
| `BB_진입타점_미검증용법_검토_20260813.md` | 생존 0 |
| `신호수준_전수스크리닝_20260813.md` | 생존 0 |
| `일봉스윙_신호스크리닝_20260813.md` | 생존 0 · 전환 근거 기각 |
| `일봉데이터확보_가능성조사_20260813.md` | 확보 가능성 높음(미검증) |
| `scratch/전략변경_검토_20260813.md` | 선택지 A/B/C 제시 |
| `scratch/샹들리에_복귀_점검_20260813.md` | 샹들리에 실측 PF 0.45 |
| `scratch/체결모델_정합_1단계_20260813.md` | 괴리 원인 = 설정 매핑 누락 |
| `scratch/체결모델_정합_2단계_SAR_20260813.md` | 체결 모델 정상 · SAR 실측 PF 0.91 |
| `scratch/백테스터_문제점_조치완료_20260813.md` | P1·P3·P5 완료 |
