# 리플레이 하네스 — 양성·음성 대조 검증 보고

작성 2026-08-12 · 대상일 2026-08-11 · 틱밀도 24틱/봉 · 순서 OHLC
범위: **하네스가 off-by-one 을 실제로 잡아내는가**의 증명까지. 교체본(B) 제작·최종 A/B 대조는 다음 세션.

---

## 0. 결론 요약

| 항목 | 결과 |
|---|---|
| **P0 양성 대조** | **PASS** — 바이트 동일 복사본 대비 **차이 0** (bar/tick/init 필드 0, 주문·체결 완전 동일) |
| **음성 대조 7건** | **7/7 탐지.** 단 N6 은 종일 리플레이에서 **결함이 실행되지 않아** 웜스타트를 추가해야 했다 |
| **N4 (BB↔MA 기준 통일)** | **탐지. 이번 하네스의 존재 이유가 입증됨** — 주문 20건 → 26건, 진입 시각 14분 이동 |
| **래그 탐지기** | shift −2..+2 구현. 값 기반 탐지기는 미완성 봉 지표에서 실패 → **값-역해석 탐지기(lag2)** 추가로 해결 |
| **라이브 원본 3파일** | 세션 전후 SHA256 **완전 동일** (무수정) |
| **주문 API** | 실 호출 0건. 3중 차단 유지, 가드 위반 0건, 네트워크 0건 |

**가장 중요한 결과 3가지**

1. **N1·N2·N3 은 주문 시퀀스를 전혀 바꾸지 않았다.** 지표값만 틀어졌다. 즉 **골든 거래목록 대조로는 이 3건을 100% 놓친다.** 지표 단위 스냅샷 대조가 있어야만 잡힌다.
2. **N3 은 게이트 판정조차 바꾸지 않았다** (tick 차이 필드 2개, 전부 지표값). 게이트 계측만으로도 부족하고 **지표값 자체를 대조해야** 한다.
3. **N6 은 종일 리플레이에서 코드가 실행조차 되지 않았다.** 하네스의 구조적 커버리지 구멍이며, 아래 §4 에 원인과 처방을 적었다.

---

## 1. P0 양성 대조

라이브와 바이트 동일한 복사본(`P0_identical`, sha `0ee30d3d2f2a` = 라이브와 일치)을 기존 기준선 `era_A` 와 대조.

```
[CMP P0] 탐지=아니오 | bar필드 0 / tick필드 0 / init 0 | 주문동일=True 체결동일=True
```

레코드 수도 완전 일치(tick 2075 / bar 82 / order 20 / fill 20 / atr_timer 28).
이 실행은 WSROOT 변경과 소스 DB 재복사를 거쳤는데도 차이가 0이었다 — **하네스는 스스로 노이즈를 만들지 않는다.** 이후 모든 음성 대조의 기준선으로 이 스냅샷을 썼다.

---

## 2. 음성 대조 결과

`inject.py` 가 라이브 원본을 **읽기만** 하고 `scratch/replay/variants/<case>.py` 를 생성한다.
앵커는 **정확히 1회** 등장해야 하며 아니면 즉시 중단한다 — 실제로 CRLF 줄바꿈 때문에 다중행 앵커가 0회 매치되자 주입이 멈췄다. 조용한 오적용이 막혔다는 뜻이다.

| # | 주입 결함 | 탐지 | 지목 지표 / 최초 시점 | 차이 크기 | 진입·청산 / 주문 시퀀스 변화 |
|---|---|---|---|---|---|
| N1 | 칼만 창 `tail(40)`→`tail(41)[:40]` | **예** | `kf_price`·`std_error`·`band`, **82/82봉**, 08:50 | std_error 2.598→2.102 (maxΔ **1.854**), kf_price Δ0.778 | **아니오** (20건 동일). 단 13:50 이후 게이트 50틱 변화 |
| N2 | std_error `errors[-20:]`→`[-21:-1]` | **예** | `std_error`·`band` 만, **82/82봉**, 08:50. **kf_price 는 불변** | maxΔ **1.847** | **아니오** (20건 동일) |
| N3 | MA `iloc[-2]`→`iloc[-1]` | **예** | `ma_filter_value`·`ma_filter_close`, **82/82봉**, 08:45 | MA Δ**0.154**, close Δ**2.2** | **아니오** (20건 동일). **게이트 변화도 0** |
| **N4** | **BB `iloc[-1]`→`iloc[-2]`** | **예 (대량)** | `bb_mid`·`bb_bandwidth`·`bb_squeeze_limit`, **82/82봉**, 08:50 | bb_mid maxΔ **1.432**, sar_value maxΔ **82.08** | **예. 20건 → 26건** |
| N5 | 일봉 ATR 에 당일 포함 | **예** | `atr_14`·`stop_loss_pt`·`take_profit_pt`, 77/82봉, 09:15 | ATR 77.49→**71.56** (Δ5.93), TP Δ11.87 | **예** (주문·체결 모두 불일치) |
| N6 | prev_range 를 당일로 | **아니오 → (웜스타트) 예** | 종일: **값 불변**. 웜스타트: `prev_range` 13:05 | 웜스타트 31.56→**36.04** (Δ4.48) | 아니오 |
| N7 | SAR AF 갱신 순서 뒤집기 | **예** | `sar_value`·`sar_af`·`sar_ep`·`sar_bull`, 10:20 | sar_value maxΔ **58.49** | **예. 20건 → 22건** |

### N4 상세 — 가장 일어나기 쉬운 실수

"BB 도 MA 처럼 완결봉 기준으로 통일하자"는 그럴듯한 명분의 1글자 변경(`-1`→`-2`)이다.

- 지표: 첫 봉경계 08:50 부터 즉시 갈라짐. `bb_mid` A=976.483 → B=977.162
- SAR 로 전파: `sar_value` 최대 82.08pt 차이, `sar_bull` 방향 자체가 뒤집힘(11:30)
- **주문 시퀀스**: 20건 → **26건**, 16건이 불일치

```
i=10  A: 11:41:00 LONG_ENTER @987.12   B: 11:27:48 LONG_ENTER @981.13
i=11  A: 11:52:12 LONG_EXIT  @989.27   B: 11:37:24 LONG_EXIT  @984.64
i=12  A: 12:01:12 LONG_ENTER @991.24   B: 11:44:00 LONG_ENTER @986.37
```

진입이 **약 14분 앞당겨지고 진입가가 6pt 낮아진다.** 백테스트 골든으로는 절대 안 잡히는 유형이며, 하네스는 지표·SAR·주문 3계층 모두에서 잡아냈다.

### N1·N2·N3 — 주문은 그대로인데 지표는 틀어진 경우

셋 다 주문 20건이 **완전히 동일**하다. 8/11 하루에서는 손익이 1원도 다르지 않다.
그러나 지표는 **82봉 전부에서** 어긋나 있다. 특히 N2 는 `std_error` 만 움직이고 `kf_price` 는 그대로여서, 두 값을 분리 관측하지 않으면 원인 지목이 불가능하다 — 계측이 이를 분리해 기록하고 있어 정확히 지목됐다.

**N3 은 게이트 판정조차 바꾸지 않았다.** 8/11 은 가격이 MA200 에서 멀어 방향필터가 구속력이 없었기 때문이다. 즉 **다른 날이면 진입이 뒤집힐 수 있는 결함이 이날은 완전히 무증상**이다. 골든 대조·게이트 대조 어느 쪽으로도 안 잡히고, 지표값 대조만이 잡는다.

---

## 3. 래그 탐지기

### 3-1. 설계안대로의 shift ∈ {−2..+2} 탐지기 (`compare.py`)

봉 단위 값 시계열 `B[i]` 를 `A[i+s]` 와 맞춰 일치율 최고인 `s` 를 고른다.

N4 결과:

```
post.bb_squeeze_limit   best_shift=-1 → B 가 A 대비 1봉 지연 (일치율 93.8%)
post.bb_mid             best_shift=+0 → 단순 시프트 아님 (최고 일치율 0.0%)
```

**`bb_squeeze_limit` 은 정확히 −1봉으로 지목했지만 `bb_mid` 는 실패했다.** N3 의 MA 도 마찬가지로 실패(1.2%).

**원인 (중요).** 미완성 봉을 참조하는 지표는 **같은 봉이라도 관측 시점마다 값이 다르다.** 봉경계 i 에서 A 가 읽은 진행중 봉의 종가는 그 봉의 **최종 종가가 아니다**(틱이 1개만 쌓인 상태). 그래서 B[i] 가 "A 가 다음 봉경계에서 읽을 값"과 일치하지 않는다. `squeeze_limit` 은 100봉 25% 분위수라 봉 하나에 둔감해서 우연히 맞은 것이다.

→ **값 기반 시프트 탐지기는 미완성 봉 지표에 원리적으로 부적합하다.**

### 3-2. 값-역해석 래그 탐지기 (`lag2.py`) — 추가 구현

계측이 남긴 "실제로 쓰인 행의 종가"를 완성봉 종가 테이블과 진행중 봉 버퍼에 역조회해 **어느 봉인지 타임스탬프로 지목**한다.

```
[LAG2 N4]
  BB 가 쓴 봉        A오프셋={0:82}        B오프셋={-1:75, 0:7}
                     → B 가 A 대비 1봉 과거로 이동 (지배율 91%, 미해석 0)
      예) 봉경계 20260811085000  A=20260811085000(+0)  B=20260811084500(-1)
  MA 방향필터가 쓴 봉  A오프셋={-1:75, 0:7}  B오프셋={-1:75, 0:7} → 동일 봉 참조 (100%)

[LAG2 N3]
  BB 가 쓴 봉        A오프셋={0:82}        B오프셋={0:82} → 동일 봉 참조 (100%)
  MA 방향필터가 쓴 봉  A오프셋={-1:75, 0:7}  B오프셋={0:82}
                     → B 가 A 대비 1봉 미래로 이동 (지배율 91%, 미해석 0)
      예) 봉경계 20260811085000  A=20260811084500(-1)  B=20260811085000(+0)
```

**방향과 봉 수가 명시적으로 나온다.** N4 는 과거로 1봉, N3 은 미래로 1봉. 미해석 0건.
지배율이 100%가 아닌 91%인 이유는, 진행중 봉의 부분 종가가 직전 완성봉 종가와 **우연히 같은** 봉경계가 82개 중 7개 있어 역해석이 양자택일 불가이기 때문이다. 이 모호성은 §4-2 의 계측 보강으로 완전히 제거된다.

---

## 4. 탐지 실패·약점과 처방

### 4-1. N6 — 결함이 실행되지 않았다 (구조적 커버리지 구멍)

종일 리플레이에서 N6 은 `prev_range` 값이 A 와 **완전히 동일**(31.56pt)했다.

**원인.** `_load_prev_range()` 는 세션 초기화 시 **1회만** 호출된다. 그런데 리플레이 리플리카는 세션 시작(08:45) **직전까지만** 시드된다. 따라서 그 시점 DB 에는 당일 행이 하나도 없고, 결함이 노린 분기(`rows[0][0] == today_str`)는 **한 번도 타지 않는다.** 양쪽 모두 8/10 행을 골랐다 — 8/10 고저폭 999.24−967.68 = **31.56pt**, 관측값과 정확히 일치.

즉 **계측이 놓친 게 아니라 코드가 실행되지 않았다.** 무증상 결함보다 위험한 상태다. 하네스가 PASS 를 줘도 그 경로는 검증된 바가 없기 때문이다.

**처방 — 웜스타트 리플레이(구현·검증 완료).** `--start` 로 장중부터 시작하면 리플리카에 당일 봉이 시드되어 분기가 살아난다.

```
--start 20260811130000
  A: prev_range=31.56pt      B: prev_range=36.04pt   (Δ4.48)
[CMP N6_warm] 탐지=예 | bar필드 3 / tick필드 6 / init 1
  post.prev_range    31.56 → 36.04     post.target_long/short  Δ0.896
```

→ **다음 세션의 A/B 대조는 콜드스타트(08:45)와 웜스타트(13:00) 두 조건 모두에서 돌려야 한다.** 세션 초기화 1회성 로직(prev_range, 일봉 ATR, 상태 복원)은 콜드스타트만으로는 검증되지 않는다.

### 4-2. 계측 보강 3건 (다음 세션 착수 전 필수)

**(1) `verdict.exit_line` 은 절대 라인번호다 — B 대조에서 전면 오탐을 낸다.**
N6 은 동작이 전혀 안 바뀌었는데도 `exit_line` 이 **2075틱 전부에서** A=4275 / B=4271 로 달랐다. 주입이 6줄을 2줄로 바꿔 이후 라인번호가 4씩 밀렸을 뿐이다.
교체본 B 는 `indicators.py` 로 로직을 옮기므로 **라인번호가 전면적으로 달라진다.** 지금 상태로 A/B 를 돌리면 모든 틱이 "차이 있음"으로 뜬다.
→ 게이트를 **라인번호가 아니라 게이트 식별자(이름)** 로 비교하도록 바꿀 것. 하네스는 이미 게이트 43곳을 지도화하고 `verdict.gate` 에 이름을 남기고 있으므로, 비교 대상에서 `exit_line` 을 제외하고 `gate` 이름만 쓰면 된다.

**(2) 칼만 창 이동이 provenance 에 안 보인다.**
N1 은 `tail(40)`→`tail(41)[:40]` 로 입력 창을 1봉 밀었는데, 계측이 남기는 `kalman_tail40_first_date` 와 `df_short_len` 은 **A 와 B 가 동일**했다. 계측이 변이본의 실제 `df_short` 를 읽지 않고 **자기가 `tail(40)` 을 다시 계산해서** 기록하기 때문이다. 값(kf_price/std_error)이 달라져서 탐지는 됐지만, **"몇 봉 밀렸는지"는 지목하지 못했다.**
→ `instrument.py` 에서 변이본의 실제 지역변수 `df_short` 의 `iloc[0]['date']` / `iloc[-1]['date']` 를 캡처할 것. 그러면 칼만도 lag2 와 같은 정수 오프셋 지목이 가능해진다.

**(3) "실제로 쓰인 행의 타임스탬프"를 직접 기록할 것.**
현재 계측은 쓰인 행의 **값**(`last_row_close`, `prev_close`)만 남기고 **날짜**는 남기지 않는다. lag2 는 값을 역조회해 우회했지만 82봉 중 7봉에서 모호성이 남았다.
→ BB·MA·ATR·prev_range 각각에 대해 사용 행의 `date` 를 그대로 캡처하면 역해석이 불필요해지고 지배율 100% 가 된다. 위 (2)와 같은 작업이다.

### 4-3. 부수 수정 (이번 세션에 반영함)

- `run_tag` 에 `--start/--end` 가 빠져 있어 **웜스타트 실행이 종일 실행 스냅샷을 덮어썼다.** 태그에 `_s<HHMMSS>` 를 추가하고 종일 P0 스냅샷을 재생성했다.
- `prepare_ws` 가 항상 라이브를 복사하도록 되어 있어 결함 주입본을 실행할 수 없었다. `variants/<name>.py` 가 있으면 그것을, 없으면 라이브 복사본을 쓰도록 바꿨다(라이브는 어느 경로에서도 읽기 전용). 계측 라인번호가 실제 실행 파일과 어긋나지 않도록 `SRC` 반환값도 변이본 소스로 교정했다.
- `/tmp/replay_ws` 가 이전 세션의 다른 uid 소유라 쓰기 불가 → `REPLAY_WSROOT` 환경변수로 분리.

---

## 5. 안전 확인

| 항목 | 결과 |
|---|---|
| `era/era_order_manager.py` | `0ee30d3d…8e3c` — 세션 시작 기준선과 **동일** |
| `indicators.py` | `0bb8136f…ae90` — **동일** |
| `bqa/kalman_backtester.py` | `d2ea61cc…d005` — **동일** |
| 결함 주입 위치 | 전부 `scratch/replay/variants/` 복사본. 원본 쓰기 0회 |
| 주문 API | 실 호출 **0건**. 3중 차단 무변경, `reached_send_order=True` 는 2차 장벽(FakeQAxWidget)에서 전량 차단 |
| 가드 위반 | **0건** (전 실행). 네트워크 **0건**, 서브프로세스 0건 |
| ERA 엔진 | 미기동. `system_stopped.flag`·`emergency_kill.flag` 무변경 |
| 보호 설정파일 | `config/active_strategy.json`(8/8), `era/futures_exit_state.json`(8/12 10:33) — 갱신시각 무변경 |
| DB | 실 `futures_data.db` 는 `mode=ro` 스냅샷 복사만. 모든 쓰기는 `/tmp/rws*/…/ws/` 아래 |

---

## 6. 산출물

| 파일 | 내용 |
|---|---|
| `scratch/replay/inject.py` | 결함 주입기. 케이스 8종, 앵커 1회 매치 강제, diff 줄수·SHA 기록 |
| `scratch/replay/compare.py` | A/B 스냅샷 대조기 + shift −2..+2 래그 탐지기 |
| `scratch/replay/lag2.py` | 값-역해석 래그 탐지기 (봉 타임스탬프로 지목) |
| `scratch/replay/variants/*.py`, `*.meta.json` | 주입본 8종 + 주입 메타(diff 전문 포함) |
| `scratch/replay/out/compare/cmp_*.json` | 케이스별 대조 결과 전문 |
| `scratch/replay/out/compare/lag2_*.json` | 래그 탐지 결과 |
| `scratch/replay/out/<case>/snap_*.jsonl` | 케이스별 스냅샷 |
| `scratch/replay/out/LIVE_HASHES_BASELINE.txt` | 라이브 3파일 기준 해시 |

---

## 7. 다음 세션 진입 조건

하네스는 **off-by-one 을 잡는다.** 특히 백테스트 골든이 못 잡는 N3·N4 유형을 지표 단위로 지목한다. B 제작으로 진행해도 된다.

다만 착수 **전에** 아래 3건을 끝내는 것을 권한다. 셋 다 B 대조의 신뢰성을 직접 좌우한다.

1. **`exit_line` 을 게이트 이름 비교로 교체** — 안 하면 B 대조에서 전 틱 오탐 (§4-2-1). 가장 시급하다.
2. **사용 행 타임스탬프 캡처** — 칼만 창 포함. 래그 지목을 100% 로 (§4-2-2·3)
3. **웜스타트 조건을 표준 실행 세트에 추가** — 세션 초기화 1회성 로직 커버 (§4-1)

또한 이번 검증은 **8/11 하루**에서만 수행했다. N3 이 이날 무증상이었던 것처럼 **날짜에 따라 무증상인 결함이 있다.** B 대조는 최소 5거래일 이상으로 넓혀야 하며, 특히 MA200 근접일과 SAR 전환이 잦은 날을 포함해야 한다.
