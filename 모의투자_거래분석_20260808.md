# 모의투자 실거래 분석 및 백테스트 대조 — 2026-08-08

## 0. 요약

샹들리에 전환(2026-07-15) 이후 실제 모의투자 거래(47건)를 분석하고, 같은 기간을
백테스트와 대조했다. 그 과정에서 **백테스트 엔진(`run_chandelier_live_replica`)의
치명적 버그를 발견·수정**했다: 이 버그 때문에 지금까지의 백테스트 결과(오늘 오전 작성한
`선물매매_현재설정_백테스트_20260808.md` 포함, 그리고 그 이전 chandelier_mult=0.3 채택
등 과거 검증들도 잠재적으로)가 실현 불가능한 손익을 반영하고 있었다.

**버그 수정 후 결론**: 실제 모의투자와 수정된 백테스트가 이제 같은 방향을 가리킨다 —
**현재 설정(chandelier_mult=0.3 + regime_filter + profit_lock)은 최근 3.5주간 실제로
손실을 내고 있다** (PF 0.6대, 승률 30%대). 이전 백테스트가 보여준 "PF 40~80" 같은 수치는
전부 허구였다.

## 1. 실제 모의투자 거래현황 (2026-07-16~08-07, 주간선물만)

`era/era_order_manager.log`에서 `[주간선물(샹들리에)] 청산!` 로그를 전수 추출했다
(재현: `scratch/analyze_mock_chandelier_trades_20260808.py`).

| 지표 | 값 |
|---|---|
| 거래수 | 47건 |
| 승 / 패 | 17 / 30 |
| 승률 | 36.17% |
| Profit Factor | **0.63** (손실 시스템) |
| 총손익 | **-130.27pt** |
| 평균승 / 평균패 | +12.91pt / -11.66pt |
| 최고 / 최악 | +70.46pt / -37.74pt |
| 타임스톱 발동 | 0건 (기능이 08-07 심야 도입이라 이 구간엔 미적용) |

날짜별로는 07-29(+37.68pt), 07-30(+6.64pt), 08-06(+6.90pt)만 플러스였고 나머지는 대부분
마이너스, 특히 08-04(-66.16pt, 9건)가 가장 나빴다.

## 2. 발견한 버그: 백테스트 갭관통 유령체결

같은 기간을 현재 설정으로 백테스트했더니 처음엔 **거래 125~147건, 승률 83~88%, PF
45~66, 총손익 +2,700~2,800pt**로 나왔다 — 실거래와 완전히 반대 방향. 원인을 추적했다.

### 2.1 원인
`run_chandelier_live_replica`의 진입 체결 로직은 `target_long`/`target_short`(칼만
필터 기반 진입 목표가)에 봉의 고가/저가가 닿으면 **목표가 그대로** 체결 처리한다. 문제는
**봉의 시가 자체가 이미 갭으로 목표가를 넘어선 경우**에도 예외 없이 목표가로 체결
처리한다는 것이다 — 실제로는 시장이 그 가격에 거래된 적이 없는데도.

실측: 07-15 종가 1165pt → 07-16 개장 1101pt (하룻밤 -64pt 갭다운). 이 갭을 만난 백테스트는
07-16 09:00 SHORT를 **1161.03pt**에 체결 처리했다 — 실제로는 시장이 이미 1101pt대에서
열려서 그 가격에 팔 수 없었다. 이런 식으로 전체 손익의 실현 여부를 검증했더니:

**전체 328건 중 275건(84%)이 그 봉의 실제 고가/저가 범위를 벗어난 체결가였고, 이
'유령체결' 거래들의 손익 합이 전체 이익의 99.9%(+9,664pt / +9,671pt)를 차지했다.**
(재현: `scratch/quantify_gap_fill_bug_20260808.py`)

이건 데이터 품질 문제가 아니라 엔진 자체의 구조적 버그다 — 데이터가 촘촘한 구간(2026-07-01
이후)만 써서 재현해도 동일하게 나타났다.

### 2.2 이미 있던 해결책이 방치되어 있었음
같은 파일의 더 오래된 함수 `run_kalman_breakout_fair`(파일 최상단)에는 이미
`realistic_gap_fill`/`gap_guard_mult` 옵션으로 이 문제가 해결돼 있었다 — 시가가 목표가를
넘었으면 시가 기준으로 체결하거나, 갭이 너무 크면 그 진입 자체를 건너뛰는 방식. 하지만
이 수정이 현재 라이브 전략인 `run_chandelier_live_replica`로는 전파되지 않았다.
**오늘 마진계산 버그(수동주문 경로만 옛 요율로 남아있던 것)와 정확히 같은 패턴** —
한쪽 함수만 고치고 나머지 중복 구현에 반영을 안 하는 게 이 코드베이스의 반복되는
취약점이다.

### 2.3 조치
동일한 `realistic_gap_fill`/`gap_guard_mult` 옵션을 `run_chandelier_live_replica`에
이식했다(기본값 False/None=기존 동작과 100% 동일, 하위호환 유지). 코드:
`bqa/kalman_backtester.py`.

## 3. 수정 후 백테스트 vs 실거래 대조

`realistic_gap_fill=True`로 같은 기간(2026-07-16~08-07)을 재검증:

| | 거래수 | 승률 | PF | 총손익 |
|---|---|---|---|---|
| **실제 모의투자** | 47 | 36.17% | 0.63 | -130.27pt |
| 백테스트 (수정 전, 무효) | 125~147 | 83~88% | 45~66 | +2,700~2,800pt |
| **백테스트 (수정 후)** | 130 | 30.00% | 0.36 | -343.43pt |

수정 후 백테스트가 **실거래와 같은 방향(손실, PF<1)** 으로 수렴한다. 거래수(130 vs
47)와 손실폭(-343 vs -130pt) 차이는 남아있는데, 유력한 이유:

- 백테스트는 `reentry_cooldown_sec=301초`(청산 후 5분 동방향 재진입 차단, 라이브에는
  있음)를 모델링하지 않는다 — 백테스트가 더 자주 재진입해서 거래수와 손실이 부풀 수 있다
  (2026-08-08 시스템점검에서 이미 발견한 별개의 기존 한계, [[project_system_audit_20260808_open_issues]]).
- config가 기간 중 계속 바뀌었다(07-27 entry_cutoff, 07-30 regime_filter/profit_lock,
  08-04 margin_rate, 08-07 time_stop) — 백테스트는 현재 설정을 전체 구간에 균일 적용하므로
  완전히 같을 수는 없다.

두 차이 모두 "왜 정확히 일치하지 않는가"는 설명하지만 "왜 시스템이 손실 중인가"라는
핵심 결론은 바꾸지 않는다.

## 4. 원인 분석 — 왜 지금 손실인가

- 실거래 손실 거래의 트레일링 폭(되돌림)은 평균 20.6pt, 66%가 20pt 이상이다
  (`chandelier_mult=0.3 × ATR14`가 정상 작동 중, 폭 자체는 설계대로).
- 평균승(+12.91pt)과 평균패(-11.66pt)는 손익비가 거의 1:1로 균형잡혀 있다 — 손익비
  문제가 아니라 **순수하게 승률이 너무 낮다(36%)**.
- 손익분기 승률(평균패/(평균승+평균패))은 약 47.5%인데 실제 승률은 36%로, 이 격차가
  그대로 손실로 이어졌다. 즉 최근 3.5주 장세가 추세추종형 트레일링 스탑에 불리한
  (횡보·휩소가 잦은) 구간이었을 가능성이 높다.
- [[project_futures_trailing_stop_misconfig]]에 이미 기록된 우려("고변동성 국면에서
  chandelier_mult=0.3의 dist가 과도하게 넓어짐")와 같은 계열의 증상으로 보이나, 그 메모의
  0.25 vs 0.3 비교 자체도 이 gap-fill 버그가 있던 엔진으로 검증된 것이라 **결론을
  재확인해야 한다.**

## 5. 개선안

1. **(완료)** 백테스터 gap-fill 버그 수정. 앞으로 모든 백테스트는 `realistic_gap_fill=True`를
   기본으로 쓸 것.
2. **(중요, 미실행) 과거에 이 버그 있는 엔진으로 검증됐던 결정들 재검증 필요**:
   - `chandelier_mult=0.3`(vs 0.25) 선택
   - `regime_filter_enabled`/`profit_lock_enabled` 채택 (2026-07-30)
   - 오늘 이식한 `time_stop` 결과([[project_system_audit_20260808_open_issues]]에 기록한
     "PF 3구간 모두 개선" 결론도 이 버그의 영향을 받았을 가능성 — 재확인 필요)
   - 이전 진입필터 4라운드 검토([[project_futures_entry_filter_diminishing_returns]])도
     같은 엔진 기반이라면 재확인 대상
3. **현재 설정이 실제로 손실 중이므로**, 재검증된(gap-fill 수정 후) 백테스트로
   `chandelier_mult`/`min_std_error_entry`/`regime_filter` 재스윕을 권장한다. 단
   [[project_futures_entry_filter_diminishing_returns]]의 경고대로 필터를 성급하게
   더 추가하지 말고, 이번엔 신뢰할 수 있는 엔진으로 baseline부터 다시 잡을 것.
4. 백테스트-실거래 잔여격차 축소를 원하면 `reentry_cooldown_sec`을 백테스터에 이식.
5. 구조적 권고: 이 파일의 5개 백테스트 함수(`run_kalman_breakout_fair`,
   `run_kalman_live_replica`, `run_chandelier_live_replica`,
   `run_kalman_night_replica`, `run_kalman_live_replica_oc`)에 진입/체결 로직이
   중복 구현돼 있어, 오늘처럼 한쪽만 고쳐지고 나머지가 방치되는 패턴이 반복되고 있다.
   공용 헬퍼로 통합하는 리팩터링을 장기 과제로 고려할 것.

재현 스크립트: `scratch/analyze_mock_chandelier_trades_20260808.py`,
`scratch/quantify_gap_fill_bug_20260808.py`, `scratch/backtest_vs_live_20260808.py`
