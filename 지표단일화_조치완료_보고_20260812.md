# 지표 단일화 라이브 적용 — 지적사항 조치 완료 보고

작성: 2026-08-12 23:1x KST
근거 문서: `지표단일화_라이브적용_독립검토_20260812.md` §5
ERA 미기동 · `era/era_order_manager.py` 무수정 · STOP-GUARD/정지플래그 전부 보존

---

## 0. 조치 결과 한눈에

| # | 항목 | 등급 | 결과 |
|---|---|---|---|
| 1 | `indicators.py` git 등록 | 🔴 필수 | ✅ **완료** — 커밋 `b9d3680` (§1) |
| 2 | 기동 전 import 자가검사 | 🔴 필수 | ✅ 완료 |
| 3 | `indicators.py` 해시 동결 | 🔴 필수 | ✅ 완료 |
| 4 | SAR 경로 리플레이 2일 추가 | 🟡 권고 | ✅ 완료 — **2일 모두 0 차이** |
| 5 | 구 버전 `indicators.py` 사본 무력화 | 🟡 권고 | ✅ 완료 |
| 6 | 손절 캡 / SAR `on_bar` 전환 | 🟢 결정 | 미조치 (사용자 판단 사항) |

**🔴 필수 3건 전부 완료. 재기동 차단 요인 없음** (검증 폭·손절 캡 등 판단 사항은 별개).

---

## 1. ✅ `indicators.py` git 등록 — 커밋 `b9d3680`

사용자 승인(2026-08-12 23:09) 후 처리했습니다.

### 처리 전 락 상태 (죽은 락 확인)

| 항목 | 값 |
|---|---|
| 경로 | `.git/index.lock` |
| 크기 | **0 바이트** (git 이 새 index 를 쓰기 시작하지도 못하고 끊긴 흔적) |
| mtime | 2026-08-12 **15:38** — 처리 시점 기준 7시간 31분 경과, 변화 없음 |
| 동반 상태파일 | `MERGE_HEAD` 없음, 다른 `*.lock` 없음 |

착수 전 `.git/index` 를 `/tmp/git_index_backup_20260812_230921` 로 백업했습니다.

### 결과

```
b9d3680 feat(indicators): register single-source indicator module as live engine dependency
e478c3c docs: the deployed K sits on a spike, so the baseline is too optimistic   ← 직전 HEAD
```

| 검증 | 결과 |
|---|---|
| 커밋에 포함된 파일 | **`indicators.py` 단 1개** (다른 수정 파일 휩쓸림 없음) |
| 커밋된 blob vs 실제 파일 | `b5cc925b…` = `b5cc925b…` — **검증받은 그 파일이 그대로 들어감** |
| `indicators.py` sha256 | `a9d5d0f0…a8d9` **불변** |
| `era/era_order_manager.py` | HEAD blob `bc05d584` ≠ 작업트리 `9afa1f28` → **수정 상태 유지, 커밋 안 됨** (의도대로) |
| 브랜치 | `main`, 원격 push **안 함** |

> ⚠️ **커밋은 로컬까지입니다.** 다른 머신 배포 위험까지 없애려면 `git push` 가 필요한데,
> 원격과 타 머신의 라이브 엔진에 영향을 주므로 별도 지시를 기다립니다.

### 처리 중 확인한 환경 제약 (알아두셔야 할 것)

이 샌드박스는 공유폴더(FUSE)에서 **파일 unlink 가 금지**돼 있습니다.
그래서 락을 삭제하지 못하고 **개명으로 우회**했습니다(되돌리기 가능하게 의도한 것이기도 합니다):

```
.git/index.lock          → .git/index.lock.STALE_removed_20260812_2309
.git/HEAD.lock           → .git/HEAD.lock.STALE_20260812_2310
.git/next-index-8.lock   → .git/next-index-8.lock.STALE_20260812_2310
.git/objects/maintenance.lock → …maintenance.lock.STALE_20260812_2310
```

**결과적으로 15:38 의 그 락도 제가 만든 게 아니라 같은 원인일 가능성이 큽니다** —
샌드박스에서 git 을 돌릴 때마다 락이 남습니다. 지금은 전부 정리해 **락 0개** 상태이고,
`.git/objects` 에 정리 못 한 임시 객체 5개가 남아 있지만 무해하며 `git gc` 가 치웁니다.

- **윈도우에서 직접 git 을 쓰실 때는 이 문제가 없습니다** (FUSE 제약이 아니므로).
- 앞으로 제가 이 레포에서 git 쓰기 작업을 하면 같은 잔여물이 생깁니다. 필요하면
  윈도우 쪽에서 직접 실행하시는 편이 깔끔합니다.
- 정리하고 싶으시면: `del .git\*.STALE_*` 와 `git gc --prune=now`

---

## 2. ✅ 기동 전 `indicators.py` import 자가검사

`run_era.bat` 의 `py_compile` 검사 **직후**에 실제 import 를 실행하는 블록을 넣었습니다.

- 백업: `run_era.bat.bak_before_import_check_20260812_225940` (원본 해시 `7a6b6aa8…`)
- 변경: 삭제 0행 / 추가 25행 + FATAL 안내문 1행 교체
- **STOP-GUARD 마커 4개 전부 보존** (변경 전후 동일 확인)

```bat
echo [CHECK] Verifying indicators.py import...
"venv32\Scripts\python.exe" -c "import sys, os; sys.path.insert(0, os.getcwd()); import indicators; print('[OK] indicators.py import OK -> ' + indicators.__file__)"
if %errorlevel% neq 0 ( ... exit /b 1 )
```

동작 시험 (샌드박스 실측):

| 경로 | 결과 |
|---|---|
| 정상 | `[OK] indicators.py import OK -> …/indicators.py`, **exit 0** |
| `indicators.py` 없음 | `ModuleNotFoundError`, **exit 1** → 기동 중단 |

`era/auto_reconnect_era.bat` 은 101행에서 `run_era.bat auto` 를 호출하므로
**스케줄러 경로도 이 검사를 자동으로 통과합니다.** 별도 수정 불필요.

### 함께 고친 것 — FATAL 안내문의 위험한 명령어

기존 문구는 `git checkout -- era\era_order_manager.py` 를 안내했습니다. 이 파일은
git HEAD 보다 앞서 있어(측정 원장 훅 + 지표 단일화) **그 명령이 두 작업을 통째로
날립니다.** 가장 다급한 순간에 출력되는 문구라 그대로 둘 수 없다고 판단해,
타임스탬프 `.bak` 복원 안내로 바꾸고 `git checkout` 금지를 명시했습니다.
로직 변경은 없고 `echo` 문자열만 바뀌었습니다.

---

## 3. ✅ 해시 동결 + 무결성 점검 도구

새 파일 2개 (둘 다 읽기 전용 도구, 매매 로직과 무관):

| 파일 | 역할 |
|---|---|
| `LIVE_INTEGRITY.json` | 라이브 부품의 승인 해시 고정 + 변경 정책 명시 |
| `verify_live_integrity.py` | 해시 대조 · **실제 import 실행** · STOP-GUARD 마커 · 정지 플래그 상태 |

```bat
python verify_live_integrity.py
```

고정된 해시:

| 파일 | sha256 |
|---|---|
| `indicators.py` | `a9d5d0f0…a8d9` |
| `era/era_order_manager.py` | `48f3bc69…8b7` |
| (롤백 대상) `…bak_before_indicators_live_20260812_203829` | `0ee30d3d…8e3c` |

현재 실행 결과: **전 항목 통과 (exit 0)**. STOP-GUARD 각 4개, 정지 플래그 2개 존재,
`era.pid` 없음 확인.

> 이 도구의 요점은 "지금 도는 게 검증받은 그 파일인가"를 **기계적으로** 확인하는 것입니다.
> `indicators.py` 는 백테스터 마이그레이션 때문에 오늘만 2번 바뀐 파일이고 앞으로도
> 바뀔 예정이라, 사람 기억에 의존하면 안 됩니다.

---

## 4. ✅ SAR 경로 리플레이 2일 추가 — 둘 다 0 차이

### 대상일 선정 근거 (데이터로 산출)

독립검토가 지적한 "MA200 근접일 · SAR 전환 빈발일" 기준으로 6월 이후를 스캔했습니다.

| 날짜 | 선정 사유 | 지표 |
|---|---|---|
| **2026-08-10** | MA200 **교차일**, 종가–MA200 최소격차 최상위 | min_gap **0.007pt**, cross=1 |
| **2026-08-06** | 방향 반전 최다 (8/11 과 동률 1위, 8/11 은 기검증) | flips **51회** |

둘 다 기존 검증(8/11, 8/4, 7/30, 7/28)에 없던 날입니다.

### 결과

| 날짜 | 스냅샷 | 봉경계 지표 | 틱 상태(SAR 포함) | 주문 의도 | 판정 |
|---|---:|---|---|---|---|
| 2026-08-10 | 564 | 20항목 × 224회 **전부 0** | 10항목 × 332틱 **전부 0** | A 8건 = B 8건, 시퀀스 일치 | **PASS** |
| 2026-08-06 | 566 | 동일 **전부 0** | 동일 **전부 0** | A 10건 = B 10건, 시퀀스 일치 | **PASS** |

`fails=0 warns=0`, 상대오차 허용치 `1e-12` 기준 **최대 상대오차 0**.
대조된 SAR 상태: `sar_value` · `sar_af` · `sar_ep` · `sar_bull` × 332틱
→ **훅 8·9(`_sar_tick`)가 실제로 실행된 상태에서 대조됐습니다.**

두 실행 모두 `indicators_sha256 = a9d5d0f0…a8d9` (루트 파일), `orders_via_kiwoom: 0`.

### 대조기 검출력 확인 (이번 실행 자체에 대해)

"0 차이"가 대조기 무능 탓이 아님을 같은 산출물로 확인했습니다.

| 대조 | 기대 | 결과 |
|---|---|---|
| A(8/10) vs B(8/10) | 0 | **PASS 0건** ✅ |
| A(8/06) vs B(8/06) | 0 | **PASS 0건** ✅ |
| A(8/10) vs **A(8/06)** — 교차일 | 반드시 검출 | **FAIL 5건** (`ma_filter_close` 0.0597, `prev_range` 0.463, `std_error` 0.708 등) ✅ |

### 검증 폭 갱신

| 경로 | 이전 | **현재** |
|---|---|---|
| `parabolic_sar` (현 운영 전략) | 8/11 1일 | **8/11, 8/10, 8/06 — 3일** |
| `kalman` | 7/30(×2), 8/04 | 동일 |
| `chandelier` | 7/28, 7/30 | 동일 |
| **합계 (중복 제거)** | 5거래일 | **7거래일** |

선행 세션이 제시한 "최소 5거래일" 기준을 충족하며, 그중 SAR 경로만 3일입니다.

---

## 5. ✅ 구 버전 `indicators.py` 사본 무력화

`scratch/replay/indicators.py` (구 해시 `0bb8136f`) 를 **삭제하지 않고 개명**했습니다.

```
scratch/replay/indicators.py
  → scratch/replay/_ARCHIVED_indicators_0bb8136f_20260812.py.txt
```

삭제 대신 개명한 이유: 이 파일이 "선행 세션이 검증한 시점의 `indicators.py`" 그 자체라
**순수 append 임을 증명한 diff 의 근거 증거**입니다. `.py` 가 아니게 되어 import 는
불가능하고, 감사 추적은 남습니다.

효과 확인 — 이번 리플레이 4회 모두 메타에 루트 경로가 찍혔습니다:
`"indicators_path": "…/AI_T_Agent/indicators.py"`.
후행 세션이 `sys.modules` 선주입으로 강제하던 우회가 이제 불필요합니다.

---

## 6. ⚠️ 조치 중 발견한 새 문제 2건

### 6-1. `replay_harness.py` 가 깨져 있다 (라이브 승인 근거의 재현 불가)

```
TypeError: install() got an unexpected keyword argument 'stub_notifier'
```

`qt_shim.py` 가 **21:50** 에 수정되면서 `install()` 에서 `stub_notifier` 인자가 사라졌는데,
`replay_harness.py`(20:15) 는 아직 옛 시그니처로 호출합니다.

문제의 성격: **8/11 라이브 적용 승인 근거(cold/warm 0 차이)를 만들어낸 바로 그 하네스가
지금은 실행되지 않습니다.** 다른 세션의 진행 중 작업으로 보여 제가 고치지 않았습니다.
재현이 필요해지면 먼저 이 불일치를 해소해야 합니다.

이번 검증은 후행 세션이 쓴 최신 진입점 `run_variant.py` 로 우회했습니다
(비SAR 교차검증에서 N3 결함 11건을 검출해 검출력이 입증된 경로).

### 6-2. `run_variant.py` 의 임시경로 하드코딩 + 소유권 충돌

`tmpdir = os.path.join("/tmp", "replay_db", tag)` 가 하드코딩인데, `/tmp/replay_db` 가
다른 세션 소유(`nobody:nogroup`, 755)라 쓰기가 막혔습니다.

원본을 수정하지 않고 **1줄만 환경변수화한 사본**을 만들어 실행했습니다:

```
scratch/replay/run_variant_indep_20260812.py   (원본과 diff 1줄)
REPLAY_TMP 환경변수로 임시경로 지정, 기본 /tmp/replay_db_indep
```

원본 `run_variant.py` 는 **무수정**입니다. 정리하실 때 이 사본은 지우셔도 되고,
원본에 환경변수화를 반영하면 사본은 불필요해집니다.

---

## 7. 이번 조치로 만들거나 바꾼 파일 전체

| 파일 | 성격 |
|---|---|
| `run_era.bat` | **수정** (백업 `…bak_before_import_check_20260812_225940`) |
| `LIVE_INTEGRITY.json` | 신규 |
| `verify_live_integrity.py` | 신규 |
| `지표단일화_조치완료_보고_20260812.md` | 신규 (본 문서) |
| `scratch/replay/_ARCHIVED_indicators_0bb8136f_20260812.py.txt` | 개명 |
| `scratch/replay/run_variant_indep_20260812.py` | 신규 (원본 사본, 1줄 차이) |
| `scratch/replay/out/{era_A,LIVE_applied}_{20260810,20260806}_…/` | 리플레이 산출물 |
| `scratch/replay/out/report_…__vs__….md` × 3 | 대조 보고서 |

**건드리지 않은 것**: `era/era_order_manager.py`, `indicators.py`,
`era/auto_reconnect_era.bat`, `config/*`, 정지 플래그 2개, TCA·KIS 수집기.

---

## 8. 최종 상태

| 항목 | 상태 |
|---|---|
| `era/era_order_manager.py` | `48f3bc69…8b7` **불변** |
| `indicators.py` | `a9d5d0f0…a8d9` **불변** |
| `era/era.pid` | 없음 — **ERA 정지, 기동하지 않았음** |
| `system_stopped.flag` / `emergency_kill.flag` | 둘 다 존재 |
| STOP-GUARD (`run_era.bat` / `auto_reconnect_era.bat`) | 각 4개 |
| 주문 API 호출 | 0건 (리플레이 4회 전부 `orders_via_kiwoom: 0`) |
| `verify_live_integrity.py` | **전 항목 통과** |

| git 락 | **0개** — 전부 정리 완료 |
| `indicators.py` | 커밋 `b9d3680` 으로 추적 시작 (로컬, push 안 함) |

**🔴 필수 조치 3건이 전부 끝나 무결성·배포 관점의 재기동 차단 요인은 없습니다.**
검증 폭도 SAR 3일 / 전체 7거래일로 기준을 충족했습니다.

남은 것은 코드가 아니라 **판단 사항**입니다.

1. `git push` 여부 — 타 머신 배포 위험을 없애려면 필요. 원격에 영향
2. **손절 캡** — 현재 Kalman ATR 73.93pt → SL 73.93pt / TP 147.86pt 로 돌게 됨
3. **SAR `on_bar` 전환** — 회전율 18배 차이. 다만 이번에 같이 켜면 문제 시 원인 분리 불가
4. 재기동 시점 — 첫 지표 갱신 로그(5분)를 눈으로 볼 수 있는 시간대 권장
