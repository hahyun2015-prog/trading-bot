# 지표 계산 단일화 — 교체본(B) 제작 및 A/B 대조 검증 보고

- 일자: 2026-08-12
- 대상 거래일: 2026-08-11 (틱 밀도 24틱/봉)
- 결론: **표준 실행 세트(종일 + 13:00 웜스타트) 양쪽에서 값 차이 0, 게이트 차이 0, 주문·체결 완전 동일.**
- 라이브 원본은 이번 세션에서 **한 바이트도 수정하지 않았다.**

---

## 0. 한 줄 요약

`era/era_order_manager.py`의 지표 계산 5개 지점을 `indicators.py` 호출로 교체한 사본(`era_B`)을
만들어 원본 사본(`era_A`)과 대조했다. 지표값·게이트 판정·주문 의도가 전 시점에서 동일하다.
**라이브 적용은 다음 세션에서 승인 후 진행한다.**

---

## 1. 산출물

| 파일 | 내용 |
|---|---|
| `scratch/replay/variants/era_B.py` | 교체본. 라이브 적용 후보 |
| `scratch/replay/variants/P0B_identical.py` | B의 바이트 동일 복사본(양성 대조) |
| `scratch/replay/variants/NB4_bb_closed_upto.py` | B에 주입한 음성 대조(BB를 MA와 같은 완결봉 기준으로 통일) |
| `scratch/replay/out/compare/cmp_era_{cold,warm}.json` | A/B 대조 원자료 |
| `scratch/replay/out/compare/cmp_{P0B,NB4}_{cold,warm}.json` | B 기준 양성·음성 대조 |

---

## 2. 교체 내역 — 함수별

교체는 **한 함수씩** 진행하고 매번 대조로 차이 0을 확인한 뒤 다음으로 넘어갔다.
전략 로직·비용 모델·진입 조건·청산 조건은 한 줄도 건드리지 않았다.

### 2.1 `update_bb_psar_filters` — BB / 밴드폭 / 스퀴즈 / MA200

```
pandas rolling 인라인  →  ind.bollinger_series(closes, 20, sigma=2.0, ddof=1)
                          ind.bandwidth_series(upper, lower, Series(mid, "actual"))
                          ind.rolling_quantile(bandwidth, 100, 0.25)
                          ind.moving_average_series(closes, ma_period)[-2]
```

- **BB·밴드폭·스퀴즈: 미완성 봉 포함**(`df.iloc[-1]`) — 그대로 유지.
- **MA200: 미완성 봉 제외**(`iloc[-2]`) — 그대로 유지.
- 스퀴즈 임계는 `squeeze_threshold`(numpy 분위수)가 아니라 `rolling_quantile`(pandas 롤링 분위수)을
  썼다. 라이브가 `rolling(100).quantile(0.25)`이고, 창 안에 NaN이 있을 때 두 함수의 결과가
  달라진다(pandas는 NaN 전파, numpy판은 NaN 제거 후 계산). **현행 동작 보존이 목적이므로
  pandas 경로를 골랐다.**
- 중간 열 `std20`은 상·하단 밴드를 만들기 위한 임시 열이라 제거했다(값 소비처 없음).

> **한 함수 안에서 BB와 MA의 기준이 다르다. 이 차이는 의도된 것이며 보존했다.**
> 통일하면 어떻게 되는지는 4절 음성 대조(NB4)에 실측으로 있다.

### 2.2 `update_futures_dynamic_sl_tp` — 일봉 ATR

```
np.maximum 3항 + fillna  →  ind.true_range(high, low, prev_close)
칼만 루프 인라인          →  ind.kalman_atr(tr, q=0.002, r=0.2)[-1]
```

- **당일 미완성 일봉 절단(`daily.iloc[:-1]`)은 교체 전과 동일 위치에 그대로 둔다** — 미완성 봉 **제외**.
- 오버나잇 갭을 포함하는 정통 TR 정의 유지(`intraday_range`가 아니다).

### 2.3 `update_kalman_targets` — 평활가 / std_error / 15분 추세

```
칼만 루프 + 잔차 + trim + np.std  →  ind.kalman_residual_std(w, q, r,
                                        std_window=20, trim=n, fallback=0.5)
15분 칼만 루프                    →  ind.kalman_path(Window(closes_15m), 0.001, 1.0)
```

- **미완성 봉 포함**(`df.tail(40)`). 따라서 `Window.closed_upto`가 아니라 창 전체를 넘긴다.
- trim(이상치 잔차 제외) 개수는 `futures_std_trim_outliers` 설정을 그대로 인자로 전달.
- `np.std`의 **ddof=0**(라이브 기본)이 `kalman_residual_std` 안에 그대로 있다 —
  같은 함수 안 BB의 `ddof=1`(pandas 기본)과 다르다. 둘 다 현행 그대로 보존.
- **KIS 야간 데이터 병합부(L1094-1112)는 한 줄도 손대지 않았다.** `ind.merge_night`는
  중복 제거 후 300행 절단 방식이 라이브와 달라 쓰지 않았다. 갭 처리 A/B/C1은 이번 범위 밖.

### 2.4 `_load_prev_range` — 전일 레인지

```
prev_h - prev_l  →  ind.prev_session_range(highs_asc, lows_asc, upto_exclusive)
```

- 날짜 내림차순 행을 오름차순으로 뒤집고 배타적 인덱스를 넘긴다. 오늘 행이 있으면
  `upto_exclusive`를 하나 물린다 — **미완성 봉 제외**가 인덱스로 코드에 드러난다.
- 이 경로는 세션 초기화 때 1회만 돌기 때문에 **웜스타트 실행에서만 검증된다**(N6 근거).

### 2.5 SAR 틱 갱신 — `_sar_tick` 신설

```
인라인 SAR 갱신 2곳(LONG/SHORT)  →  self._sar_tick(current_price, self.futures_day_peak)
                                     내부에서 ind.SarState(...).on_tick(...)
```

- **라이브 SAR은 틱 기반이다.** EP가 봉 고저가가 아닌 `current_price`로 갱신되고
  AF도 틱마다 오른다. 그래서 표준 정의인 `on_bar`가 아니라 **`on_tick`**을 쓴다.
  봉 기반 전환은 이번 범위가 아니다.
- 클램프(`futures_day_peak`) 적용 → EP/AF 갱신 순서까지 `SarState._advance`가 동일하게 재현.
- 방향 전환(`sar_bull`)과 청산 판정은 호출부에 그대로 남겼다 — 값 갱신만 분리했다.

---

## 3. A/B 대조 결과

명령: `python3 run_set.py --variants era_B --baseline era_A` (종일 + 13:00 웜스타트, 24틱/봉)

| 실행 | 봉 레코드 | 틱 레코드 | 값 차이 | 게이트 차이 | init 차이 | 주문 | 체결 |
|---|---|---|---|---|---|---|---|
| 종일(cold) | 82 | 2,075 | **0** | **0** | **0** | 20건 동일 | 20건 동일 |
| 웜스타트(warm, 13:00) | 31 | 800 | **0** | **0** | **0** | 2건 동일 | 2건 동일 |

대조 항목: 칼만 평활가·std_error·밴드·타점, ATR·SL/TP, BB 중심선·밴드폭·스퀴즈임계,
MA200 값과 직전 종가, SAR 값/EP/AF/방향, prev_range, 진입 타점, 게이트 판정(gate_id 기준),
주문 의도(시각·방향·가격·수량), 봉 경계 스냅샷, 지표 입력 행의 실제 타임스탬프.

SAR 경로가 실제로 실행됐음을 확인했다 — 종일 실행에서 SAR 갱신 틱 1,624회,
AF가 0.02에서 0.10(상한)까지 진행, A·B 완전 일치.

### 3.1 값 차이가 아닌 것으로 분류한 항목

계측은 지표 함수 반환 시점의 지역 표(DataFrame/배열)를 **이름을 미리 정하지 않고** 통째로
관측한다. 리팩터링이 중간변수를 없애거나 새로 만들면 그 관측 키가 한쪽에만 존재하게 된다.
이는 코드 구조 차이지 동작 차이가 아니므로 값 차이와 분리해 보고하도록 `compare.py`를 보강했다.

| 변수 | 쪽 | 사유 |
|---|---|---|
| `update_kalman_targets.kf_prices`, `errors`, `std_slice`, `order` | A만 | `kalman_residual_std` 내부로 이동 |
| `update_kalman_targets.kf_long` | B만 | 리스트 → ndarray(관측 대상이 됨) |
| `update_bb_psar_filters._mid`, `_up`, `_lo`, `_closes` | B만 | 새 중간 배열 |
| `update_bb_psar_filters.df.cols`, `last_row.len` | 열목록 | `std20` 열 제거(열 이름·열 개수) |

**완화 범위는 `used.*` 관측 네임스페이스로 한정했다.** 창 길이(`used.*.<배열>.len`),
입력 행 타임스탬프, 지표 결과값(`loc.*`, `post.*`, `state.*`), 게이트, 주문은 전부 엄격 대조 그대로다.
`last_row`의 값들은 `loc.update_bb_psar_filters.last_row_{sma20,bandwidth,squeeze,close}`로
따로 엄격 대조되므로 이 완화로 가려지는 동작은 없다.

---

## 4. 하네스 보강과 재검증

이번 세션에 하네스를 3곳 손봤다. **라이브 코드가 아니라 `scratch/replay/` 도구다.**

1. `replay_harness.py` — 샌드박스 워크스페이스 루트에 `indicators.py`를 배치(라이브의 디렉터리
   배치와 동일하게 맞춰 import 경로 해석을 같게 만듦). 리포트에 `indicators_sha256` 기록.
2. `compare.py` — 3.1의 구조 차이 분리.
3. `compare.py` — `used.*.cols` / `used.*.last_row.len`을 값 차이에서 제외.

보강이 탐지력을 깎지 않았음을 기존 스냅샷으로 재확인했다(재실행 없이 대조만).

| 대조 | 종일 | 웜스타트 | 기대 | 결과 |
|---|---|---|---|---|
| P0_identical (원본 양성) | 차이 0 | 차이 0 | 무탐지 | ✅ |
| N8_comment_shift (주석만 추가) | 차이 0 | 차이 0 | 무탐지 | ✅ |
| N1 칼만 창 | 탐지 | 탐지 | 탐지 | ✅ |
| N2 std_error 창 | 탐지 | 탐지 | 탐지 | ✅ |
| N3 MA 당봉 사용 | 탐지 | 탐지 | 탐지 | ✅ |
| N4 BB 직전봉 사용 | 탐지 | 탐지 | 탐지 | ✅ |
| N5 ATR 당일 포함 | 탐지 | 탐지 | 탐지 | ✅ |
| N6 prev_range 당일 오용 | 무탐지 | **탐지** | 웜스타트 전용 | ✅ |
| N7 SAR AF 순서 | 탐지 | 탐지 | 탐지 | ✅ |

**N6는 종일 실행에서 여전히 안 잡힌다. 웜스타트는 선택이 아니라 필수다.**

### 4.1 B에 대한 양성·음성 대조 (3단계)

- **양성(P0B)**: `era_B`의 바이트 동일 복사본 → 종일·웜스타트 모두 차이 0.
  **교체본 위에서도 하네스가 노이즈를 만들지 않는다.**
- **음성(NB4)**: `era_B`의 BB를 MA와 같은 완결봉 기준으로 통일 → **즉시 FAIL**.

  | 항목 | era_B | NB4(통일) |
  |---|---|---|
  | 주문 수(종일) | 20건 | **26건** |
  | 첫 진입 시각 | 11:41:00 | **11:27:48** (13분 12초 앞당겨짐) |
  | BB 중심선 첫 차이 | 976.483 | 977.162 |
  | 밴드폭 | 1.482% | 1.211% |

  기존 N4(라이브 원본 기준)와 같은 성격·같은 방향의 결과다.
  **`closed_upto`로 통일하고 싶은 유혹은 실측으로 기각된다.**

---

## 5. 안전 조건 준수

- ERA 기동 없음. `system_stopped.flag`·`emergency_kill.flag` 그대로.
- 주문 API 호출 0. shim 3중 차단(가짜 OCX / SendOrder 차단 / guard) 그대로.
- 실 DB는 `mode=ro`. 모든 쓰기는 샌드박스 하위.
- 키움·증권사 네트워크 접속 없음.
- **라이브 원본 3개 파일 해시 불변**(매 실행 후 확인):

```
era/era_order_manager.py   0ee30d3d2f2ab5206873ba52860d15742162ba7e9a7412e6fe6fdffc77db8e3c
indicators.py              0bb8136f6ef526deb45c6125774fee431a3c64fa91032bc9dcaba7190c80ae90
bqa/kalman_backtester.py   d2ea61ccb1ff3de2dad73078cd7ad086a9f3c3f1c9a610a6dcca580f3d9db005
```

---

## 6. 라이브 적용 시 주의점과 절차 (다음 세션)

### 6.1 반드시 확인할 것

1. **import 경로.** 교체본은 파일 위치 기준으로 루트를 `sys.path`에 올린다
   (`dirname(dirname(__file__))`). 라이브에서는 `era/` → 루트가 되어 `indicators.py`를 찾는다.
   **ERA를 다른 작업 디렉터리에서 기동하거나 `era/`를 옮기면 깨진다.** 기동 배치파일
   (`run_era.bat`)의 실행 위치를 함께 확인할 것.
2. **`indicators.py`가 배포 대상에 포함되는지.** 동기화 스크립트(`sync_dump`, `update.bat`)가
   루트의 `indicators.py`를 실제로 옮기는지 확인. 빠지면 ERA가 기동 즉시 ImportError로 죽는다.
3. **numpy 버전.** 교체본은 `np.quantile`을 새로 쓰지 않지만 `indicators.py` 전반이 numpy에
   의존한다. venv32 환경에서 import 스모크 테스트를 먼저 돌릴 것.
4. **예외 처리 위치.** 지표 함수 3개는 전부 `try/except`로 감싸져 있어 `indicators` 호출이
   실패해도 **조용히 이전 값을 유지한 채 반환**한다. 즉 적용 직후 로그에
   `[ERA 칼만] ...에러`, `[ERA BB 필터 오류]`가 뜨는지 반드시 눈으로 확인해야 한다.
   값이 멈춘 것과 정상 동작이 로그 없이는 구별되지 않는다(2026-08-10과 같은 실패 양상).
5. **SAR 상태 파일 호환.** `_sar_tick`은 `self.sar_*` 속성을 그대로 읽고 쓴다.
   `era/futures_exit_state.json` 스키마는 바뀌지 않는다. 재기동 복원 경로도 무변경.

### 6.2 적용 절차 (제안)

1. 장 마감 후 ERA 정지 상태에서 진행. `system_stopped.flag` 유지.
2. `era/era_order_manager.py` 백업(`.bak_before_indicators_YYYYMMDD_HHMMSS`).
3. `scratch/replay/variants/era_B.py` → `era/era_order_manager.py` 복사.
4. 적용 직후 **하네스 재실행**: 새 라이브 원본을 A로, 백업본을 별도 variant로 두고
   표준 세트 재대조 → 차이 0 확인(복사 과정 사고 검출).
5. `python -c "import indicators"` 스모크 + ERA 드라이 기동 로그에서 아래 4줄이 정상 출력되는지 확인:
   - `[AMATS 파생 최적화] ... Kalman ATR 적용 완료`
   - `[ERA BB 필터] 갱신 완료 | 중심선=... MA200=...`
   - `[ERA 칼만 주간] 타점 설정 완료 ...`
   - `[ERA 선물] 전일 Range 로드 완료`
6. 첫 거래일은 관찰 운영. 진입 건수와 첫 진입 시각을 전일 대비로 확인
   (NB4가 보여주듯 기준이 어긋나면 **건수와 진입 시각에 먼저 나타난다**).

### 6.3 이번 범위가 아닌 것 (다음 과제)

- SAR 봉 기반 전환(현행 틱 기반이 백테스트 대비 회전율 18배의 원인)
- KIS 야간 데이터 갭 처리 방식 A/B/C1 선택
- `bqa/kalman_backtester.py`의 `indicators.py` 이식
- BB의 `ddof=1`과 std_error의 `ddof=0` 불일치 정리 여부 판단
