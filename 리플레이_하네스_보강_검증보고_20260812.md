# 리플레이 하네스 보강 3건 + 재검증 보고

작성일 2026-08-12 · 대상 `scratch/replay/` · 대상 거래일 20260811 · 틱 밀도 24틱/봉

---

## 0. 요약

| 항목 | 결과 |
|---|---|
| 보강 1 — `exit_line` → 게이트 이름 대조 | **완료.** N8(주석시프트) 오탐 2075틱 → **0** |
| 보강 2 — 사용 행 타임스탬프 직접 캡처 | **완료.** N1 창 이동이 provenance에 노출 |
| 보강 3 — 웜스타트 표준 세트 편입 | **완료.** 표준 세트 = 종일 + 13:00 시작 |
| P0 양성 대조 재실행 | **PASS.** 종일·웜 모두 차이 0 |
| 음성 대조 N1~N7 재실행 | **7/7 탐지.** 단 N6은 **웜스타트에서만** |
| 게이트 매핑 실패 | 0건 (전 실행 `unmapped_tick_total=0`, 표식 소실 0) |
| 라이브 원본 3개 파일 해시 | **불변** (기준선과 일치) |

**가장 중요한 발견**: 직전 세션의 "N6 탐지"는 **오탐이었다**. 라인번호가 4줄 밀린 것을 차이로 읽은 것이지, 동작 차이를 잡은 게 아니었다. 보강 1로 오탐을 제거하자 N6 종일 실행은 **미탐지**로 바뀌었고, 웜스타트에서만 진짜 차이가 드러났다. 즉 **웜스타트는 선택이 아니라 필수**다.

---

## 1. 보강 1 — 조기반환 지점을 게이트 이름으로 식별

### 문제

`verdict.exit_line`(라인번호)을 대조 필드로 썼다. N6은 라이브 대비 4줄이 줄어드는 변이라 동작이 전혀 안 바뀐 틱에서도 라인번호가 밀렸고, 2075틱 전부가 "차이 있음"으로 떴다. 교체본(B)은 `indicators.py`로 코드를 옮기면서 라인번호가 전면 달라지므로 이 상태로는 A/B 대조가 성립하지 않는다.

### 조치 (`instrument.py`, `compare.py`)

**게이트 ID 부여** — 라인번호를 전혀 쓰지 않는다.

```
gate_id = "RET:"  + 조건식 체인(안쪽→바깥쪽, " ← " 결합)
        | "FALL:" + 통과 못한 if 조건식
        | "EXEC:" + 주문 집행 지점 + 신호명 리터럴(LONG_ENTER 등)
        + 동명이인일 때 "#n"  ← n 은 라인번호의 **상대 순서**
```

- 조건식은 `_GATE_ALIASES`로 먼저 사람이 읽을 이름(하드캡, ATR컷오프, BB스퀴즈 …)으로 환원하고, 별칭이 안 잡히면 정규화한 조건식 원문을 쓴다. **함수명·조건식이라는 안정적 단서만 사용**한다.
- 중복 ID의 `#n`은 절대 라인번호가 아니라 **상대 순서**로 매기므로 코드가 통째로 밀려도 불변이다.
- 라이브 코드는 한 줄도 고치지 않는다. 지도는 변이본 소스에서 AST로 매번 자동 생성한다.

**대조 기준 교체** — `verdict.gate_id`와 `verdict.gate_mapped`를 대조한다. `exit_line`·`lines_executed`는 `~` 접두사를 붙여 **참고 정보**로 강등했다. 스냅샷에는 그대로 남아 포렌식에 쓸 수 있지만, `DETECTED` 판정과 래그 탐지기에서는 제외된다.

**매핑 실패의 명시적 노출** — 조용히 넘기지 않는다.

- 지도에 없는 종료지점 → `gate_id = "UNMAPPED"`. 대조 필드이므로 A/B 중 한쪽만 UNMAPPED면 **차이로 잡힌다**.
- 리포트에 `gate_mapping.unmapped_exit_lines`(라인별 발생 틱 수), `unaliased_hits`(별칭 미매칭 게이트), `probes_missing`(리팩터로 사라진 표식) 기록.
- 발생 시 콘솔에 `★★` 경고 출력.

### 검증 — 라인번호만 밀고 동작은 같은 케이스 (직접 증명)

`N8_comment_shift` 변이본을 만들었다. `era_A.py`의 `_process_day_tick` 정의 직전에 **주석 7줄**을 끼운 것이 전부다.

```
AST(구문트리) 동일 = True    ← 실행 문장 차이 0, 라인번호만 +7
```

| 대조 | 보강 전이었다면 | 보강 후 실측 |
|---|---|---|
| N8 종일 | exit_line 2075틱 전부 차이 → **오탐** | **탐지=아니오** (bar 0 / tick 0 / init 0, 주문·체결 동일) |
| N8 웜스타트 | exit_line 800틱 전부 차이 → **오탐** | **탐지=아니오** |

`~verdict.exit_line`은 여전히 2075틱 / 800틱 전부 다르다고 보고되지만 참고 정보로 분리되어 판정에 관여하지 않는다. **오탐이 사라졌다.**

지도 수준에서도 확인했다. N6(라인 4줄 감소) 대비:

```
RET 게이트 ID 집합 동일 = True     FALL/EXEC ID 집합 동일 = True
라인번호 집합 동일 = False
ID 충돌 = 0건 (RET 43곳, FALL/EXEC 177곳)
```

---

## 2. 보강 2 — 사용 행 타임스탬프 직접 캡처

### 문제

계측이 칼만 창을 **스스로 재계산**해 기록했다.

```python
out["kalman_tail40_first_date"] = str(df["date"].iloc[-40])   # ← 하네스의 재계산
```

N1은 `df.tail(40)` → `df.tail(41).iloc[:40]`으로 창을 1봉 옮기는 변이인데, 하네스는 여전히 전체 `df`에서 `-40`을 세고 있었으므로 창 이동이 provenance에 전혀 안 보였다.

### 조치 (`instrument.py`)

재계산을 **삭제**하고 관측으로 바꿨다. `_observe_used_rows()`가 지표 함수의 return 시점 지역변수를 훑어 DataFrame·Series·1차원 배열을 **있는 그대로** 기록한다.

- 변수 이름을 미리 정해두지 않는다. 지표가 창을 어떻게 만들든 **결과 객체 자체**를 읽는다.
- `date` 컬럼 또는 `DatetimeIndex`가 있으면 `used_first` / `used_last` / `used_2nd_last` 타임스탬프.
- 타임스탬프가 없는 파생 배열(잔차 등)은 `val_first` / `val_last` / `val_2nd_last` 값 지문으로 창 이동을 잡는다.
- `_load_prev_range`는 선택된 행 원본(`used_row_raw`)을 그대로 남긴다.
- `compare.py`가 이를 `used.<함수>.<변수>.<필드>`로 평탄화해 필드 단위 대조·래그 탐지에 태운다.

### 검증 — N1이 provenance에 드러나는가

**드러난다.**

| 필드 | A(era_A) | B(N1) | 래그 탐지기 |
|---|---|---|---|
| `used.update_kalman_targets.df_short.used_first` | `20260811020500` | `20260811020000` | `best_shift=-1`, 일치율 **100%** |
| `used.…df_short.used_last` | — | 1봉 앞 | `best_shift=-1`, 일치율 **100%** |
| `used.…closes_short.val_first` | 975.16 | 973.7 | `best_shift=-1`, 일치율 **100%** |

래그 탐지기가 "B가 A 대비 **1봉 지연**"이라고 정확히 진단한다. 보강 전에는 `post.kf_std_error` 등 하류 결과값만 달랐을 뿐, **어느 입력 행이 바뀌어서 그렇게 됐는지**가 보이지 않았다.

부수 효과로 N2(std_error 창 이동)도 `used.…std_slice.val_first/last`, `used.…errors.*`에서 창 이동이 직접 보인다. 이전에는 `std_error` 결과값 차이만 있었다.

---

## 3. 보강 3 — 웜스타트를 표준 실행 세트에 추가

### 조치 (`run_set.py` 신규)

표준 세트를 두 실행으로 고정했다.

| 모드 | 인자 | 세션 초기화 시점의 리플리카 상태 |
|---|---|---|
| `cold` (종일) | 없음 | 당일 봉 0개 (08:45 직전까지 시드) |
| `warm` (장중) | `--start 20260811130000` | 당일 봉 존재 (13:00까지 시드) |

`_load_prev_range()`는 **세션 초기화 시 1회만** 돈다. 종일 실행은 당일 행이 아직 없으므로 "오늘 행을 만났을 때"의 분기를 아예 안 탄다. 세션 초기화 이후에만 도는 코드 경로를 덮으려면 장중 시작이 필요하다.

`run_set.py`가 실행 + 대조를 한 번에 돌린다. 스냅샷 파일명에 `_s130000` 태그가 붙어 종일 실행 결과를 덮어쓰지 않는다.

### 검증 — N6은 웜스타트 없이 잡히는가

**안 잡힌다. 웜스타트가 필수다.**

`_load_prev_range`가 실제로 선택한 행(`used_row_raw`, 보강 2로 캡처):

| 실행 | 후보 첫 항목 | era_A 선택 | N6 선택 | 결과 |
|---|---|---|---|---|
| 종일 | `20260810` (오늘 아님) | `20260810 999.24 967.68` | `20260810 999.24 967.68` | **동일** — 분기 미진입 |
| 웜스타트 | `20260811` (오늘) | `20260810 999.24 967.68` | `20260811 996.84 960.80` | **차이** |

종일 실행에서는 N6의 변이 코드가 실행되긴 하지만 후보 목록 첫 항목이 당일이 아니라 `rows[0]`을 쓰는 결과가 원본과 우연히 일치한다. 즉 **결함이 발현할 조건 자체가 없다.**

웜스타트에서의 탐지:

```
session_init: prev_range  A=31.56  →  B=36.04
bar 3필드 / tick 4필드 / init 1필드
  post.prev_range, post.target_long(Δ0.896), post.target_short(Δ0.896)
  verdict.gate_id 26틱 변화 (13:32:48 첫 발생 — BB스퀴즈 게이트로 경로 이동)
```

> **결론 — 표준 세트에서 웜스타트를 빼면 N6 계열(세션 초기화 1회성 경로) 결함을 놓친다.** 교체본(B) A/B 대조 시 종일·웜스타트 두 실행을 모두 돌려야 한다.

---

## 4. 재검증 전량 결과

기준선 A = `era_A`(라이브 바이트 동일 복사본), 24틱/봉, 20260811.

| 라벨 | 탐지 | bar필드 | tick필드 | init필드 | 주문동일 | 체결동일 | 라인의존(판정제외) |
|---|---|---|---|---|---|---|---|
| P0_cold | 아니오 | 0 | 0 | 0 | True | True | 0 |
| P0_warm | 아니오 | 0 | 0 | 0 | True | True | 0 |
| N8_cold | 아니오 | 0 | 0 | 0 | True | True | 2075 |
| N8_warm | 아니오 | 0 | 0 | 0 | True | True | 800 |
| N1_cold | 예 | 23 | 4 | 0 | True | True | 101 |
| N1_warm | 예 | 23 | 4 | 0 | True | True | 101 |
| N2_cold | 예 | 9 | 4 | 0 | True | True | 101 |
| N2_warm | 예 | 9 | 4 | 0 | True | True | 101 |
| N3_cold | 예 | 4 | 2 | 2 | True | True | 0 |
| N3_warm | 예 | 4 | 2 | 2 | True | True | 0 |
| N4_cold | 예 | 17 | 17 | 3 | **False** | **False** | 870 |
| N4_warm | 예 | 9 | 4 | 2 | True | True | 32 |
| N5_cold | 예 | 7 | 17 | 0 | **False** | **False** | 28 |
| N5_warm | 예 | 4 | 4 | 3 | True | True | 0 |
| N6_cold | **아니오** | 0 | 0 | 0 | True | True | 2075 |
| N6_warm | 예 | 3 | 4 | 1 | True | True | 826 |
| N7_cold | 예 | 8 | 14 | 0 | **False** | **False** | 327 |
| N7_warm | 예 | 1 | 1 | 0 | True | True | 0 |

- **P0 양성 대조**: 종일·웜스타트 모두 **차이 0**. 보강이 양성 대조를 깨지 않았다.
- **음성 대조 N1~N7**: 표준 세트(종일+웜스타트) 기준 **7/7 탐지**.
- **게이트 이름 대조로 탐지가 약해졌는가**: 아니다. N1·N4·N5·N6·N7에서 `verdict.gate_id`가 여전히 판정 경로 변화를 잡는다(N1 50틱, N5 13틱, N6 26틱 등). 약해진 것은 **오탐뿐**이다.
- **게이트 매핑 실패**: 전 실행 0건. `unmapped_tick_total=0`, `probes_missing=[]`. 별칭 미매칭 게이트는 41곳으로 원문 라벨을 쓰지만 라인번호 무관하므로 대조에는 안전하며, 리포트 `unaliased_hits`로 노출된다.

---

## 5. 안전 조건 준수

| 조건 | 상태 |
|---|---|
| ERA 매매 엔진 | 기동하지 않음. `system_stopped.flag`·`emergency_kill.flag` 그대로 존재 |
| 주문 API 호출 | 0건. shim 3중 차단 그대로, 약화 없음 |
| `era/era_order_manager.py` / `indicators.py` / `bqa/kalman_backtester.py` | **해시 불변** (기준선 일치, 아래) |
| 보호 설정·상태 파일 쓰기 | 0건 (`file_writes_OUTSIDE_sandbox`는 전부 파이썬 라이브러리 `__pycache__`) |
| 네트워크 접속 | 0건 (`network_attempts=[]`) |
| 가드 위반 | 0건 |
| 작업 범위 | `scratch/replay/` 이하 + 보고서 1건 |

```
0ee30d3d2f2ab5206873ba52860d15742162ba7e9a7412e6fe6fdffc77db8e3c  era/era_order_manager.py
0bb8136f6ef526deb45c6125774fee431a3c64fa91032bc9dcaba7190c80ae90  indicators.py
d2ea61ccb1ff3de2dad73078cd7ad086a9f3c3f1c9a610a6dcca580f3d9db005  bqa/kalman_backtester.py
```

### 부수 수정 1건

`prepare_ws()`의 샌드박스 루트가 이전 세션이 남긴 `/tmp/replay_ws`(소유자 `nobody`)를 지우지도 덮지도 못해 실행이 죽었다. 쓰기 가능한 루트를 자동 선택하도록 고쳤다. **옛 디렉터리를 재사용하지 않는다** — 재사용하면 지난 세션의 DB·상태파일이 섞여 리플레이가 오염되므로, 지울 수 없으면 새로 판다.

---

## 6. 다음 세션(교체본 B 제작 · A/B 대조)을 위한 메모

1. **표준 세트는 반드시 2회**: `python3 run_set.py --variants <B> --baseline era_A` (종일 + 웜스타트 자동).
2. **`DETECTED` 판정만 보면 된다.** `~` 접두 필드(`exit_line`, `lines_executed`)는 B가 `indicators.py`로 옮겨가면 전부 달라지는 게 정상이다. 차이로 세지 않는다.
3. **`gate_mapping`을 매 실행 확인할 것.** B의 리팩터로 `_process_day_tick`의 조건식 표기가 바뀌면 `probes_missing` 또는 `unaliased_hits` 급증으로 나타난다. 그때는 `_GATE_ALIASES`에 별칭을 추가해 게이트 정체성을 다시 고정한다. `UNMAPPED`가 뜨면 지도가 B를 못 따라간 것이므로 **대조 결과를 신뢰하지 말고 지도부터 고친다.**
4. **N6 계열 주의**: 세션 초기화 1회성 경로는 종일 실행에서 조용히 통과한다. 웜스타트 결과를 별도로 읽을 것.
5. 남은 미완성 봉 5지점 중 BB(`iloc[-1]`) vs MA(`iloc[-2]`) 불일치는 여전히 미해결 과제로, 교체본 설계 시 통일 여부를 명시적으로 결정해야 한다(N4가 그 통일을 모사한 음성 대조).

---

## 부록 — 변경/추가 파일

| 파일 | 변경 |
|---|---|
| `scratch/replay/instrument.py` | 게이트 ID 생성(`_assign_ids`), `_label_for` 별칭 플래그, `verdict()` gate_id/gate_mapped/UNMAPPED, 매핑실패 카운터, `_observe_used_rows`/`_row_stamps` 신규, 칼만 창 재계산 제거 |
| `scratch/replay/compare.py` | `verdict.gate_id` 대조, `~` 참고필드 분리(`info_fields_line_dependent`), `used.*` 평탄화, 래그 탐지기 참고필드 제외 |
| `scratch/replay/replay_harness.py` | `gate_mapping` 리포트 + `★★` 경고, 별칭/표식 커버리지 기록, 샌드박스 루트 자동 선택 |
| `scratch/replay/run_set.py` | **신규** — 표준 실행 세트(종일 + 웜스타트) 러너 |
| `scratch/replay/variants/N8_comment_shift.py` | **신규** — 주석 7줄만 삽입(AST 동일). 보강 1의 직접 증명용 |
| `scratch/replay/out/compare/cmp_*_{cold,warm}.json` | 재검증 대조 결과 18건 |
