# 새로운 매매기법 탐색 — 2026-08-09

## 0. 배경 및 방법론

칼만 진입 + 샹들리에 청산 외의 대안 기법을 두 경로로 찾았다: (1) `scratch/`에 이미
존재하는 과거 실험 재검토, (2) `era_order_manager.py`에 이미 구현돼 있지만 한 번도
백테스트된 적 없는 대안 청산 방식(Parabolic SAR, Bollinger Band) 신규 백테스트.
TradingView MCP로 커뮤니티 지표를 직접 리서치하려 했으나 CDP 연결이 계속 불안정해
포기했다 — 다만 실제 열려 있던 저장 레이아웃(`MNQ100_5min_bb_ka_ma_smc`)이 이미
scratch/의 볼린저/KAMA/SMC 실험들의 출처였음을 확인해, 대표적 커뮤니티 기법은 이미
과거에 반영·검증됐던 것으로 판단했다.

모든 백테스트는 [[feedback_backtester_gapfill_bug]] 교훈에 따라 `realistic_gap_fill`을
처음부터 적용했고, 신규 작성한 SAR/BB 재현체는 완료 후 유령체결 0건을 직접 검증했다.

## 1. 기존 scratch/ 실험 재검토 — 전부 기각

| 기법 | 최고 PF | 판정 |
|---|---|---|
| 로렌치안 KNN (15분봉) | 0.46 | 기각 |
| IB(30분)+VWAP Z-score | 0.08 | 기각 |
| SMC Fair Value Gap | 0.18 | 기각 |
| Bollinger 6종 (Squeeze/RSI역추세/MTF) | 0.11~0.33 | 기각 |
| KAMA 5종 | 0.15~0.19 | 기각 |
| Heikin-Ashi+EMA+RSI (30/60분) | 0.13~0.25 | 기각 |
| TV진입(HA_EMA/Supertrend/Donchian)+AMATS청산 4종 | 0.02~0.04 | 기각 (승률 3~7%, 코드 결함 의심) |
| 로렌치안 KNN (60분 스윙) | 1.54 (34건) | **무효** — 라벨 생성에 미래데이터(`close.shift(-2)`) 참조하는 lookahead bias 발견 |

9개 전부 지금 조합보다 못하거나(대부분), 유일하게 좋아 보였던 것(60분 로렌치안)도
방법론 결함으로 신뢰할 수 없다.

## 2. 신규 발견: 이미 구현됐지만 미검증인 대안 청산 (SAR / BB)

`era_order_manager.py`의 `futures_strategy_type`은 `chandelier`/`kalman` 외에
`parabolic_sar`, `bollinger_band`도 지원하지만, `bqa/kalman_backtester.py`엔 이
둘의 재현 함수가 없어 한 번도 백테스트되지 않았다. 진입 로직(칼만 40봉 롤링 재적합 +
15분 장기추세필터 + 재진입휩소필터)은 `run_chandelier_live_replica`와 완전히 동일하게
복사해 정확히 일치시키고, 포지션 보유 중 청산 로직만 실제 라이브 코드
(`era_order_manager.py` :4118-4460, :4600-4694)를 그대로 이식했다.
재현: `scratch/backtest_sar_bb_20260809.py`.

- **Parabolic SAR**: ATR 기반 초기 손절 + 가속인수(AF 0.02→0.20, step 0.02) 트레일링.
  SHORT/LONG 진입 시 볼린저 스퀴즈 구간(밴드폭 <25분위수) 및 BB중심선 역방향이면
  진입 차단(라이브와 동일).
- **Bollinger Band**: ATR×1.2 손절 + 반대편 밴드 터치 익절 + 폴백 트레일링.

### 결과 (필터 없음, 연속시리즈 10500000)

| | 전체기간 PF | 최근60일 PF | 최근30일 PF |
|---|---|---|---|
| Parabolic SAR | 0.46 | **1.18** | **1.32** |
| Bollinger Band | 0.20 | 0.49 | 0.66 |
| (참고) 샹들리에 베이스라인 | 0.48 | 1.00 | 1.08 |

Bollinger Band는 전 구간 부진 — 기각. **Parabolic SAR은 필터 없이도 최근 구간에서
베이스라인 샹들리에보다 우수하다.**

### SAR + 검증된 진입필터(entry_cutoff, min_std_error_entry) 조합

| | 전체기간 PF | 최근60일 PF | 최근30일 PF |
|---|---|---|---|
| 샹들리에 제안조합 (기존 최선) | 0.66 | 1.02 | 1.11 |
| **SAR + entry_cutoff + min_std_error_entry** | **0.63** | **1.22** | **1.37** |

전체기간은 거의 동일(0.63 vs 0.66), **최근60일·최근30일 모두 SAR 쪽이 명확히 우수**하다.
유령체결 검증: 803건 중 0건.

## 3. SAR 파라미터 재스윕

`sar_af_max`(0.10~0.40), `sar_af_step`(0.01~0.05), 스퀴즈필터 on/off를 재스윕했다
(재현: `scratch/sar_param_resweep_20260809.py`).

- **`sar_af_max`**: 현재값(0.20)이 이미 최적에 가까움 — 0.10~0.40 전 구간에서 큰 차이
  없고, 최근30일만 0.20이 뚜렷이 최고(PF 1.37). **유지.**
- **`sar_af_step`**: 레짐에 따라 갈림 — 0.01은 전체기간(PF 0.67)·최근60일(PF 1.43)에서
  현재값 0.02(0.63/1.22)보다 우수하지만, 최근30일은 0.02가 더 좋음(1.37 vs 1.18).
  [[feedback_backtest_regime_overfit]]과 같은 유형의 트레이드오프라 단정하지 않음 —
  사용자 판단 필요.
- **스퀴즈필터**: 켜는 쪽이 세 구간 모두 명확히 우수(0.63/1.22/1.37 vs 0.51/0.75/0.94).
  **유지 확정.**

## 4. SAR 조합의 실제 환산 수익률

초기자본 5천만원, 1계약 고정(비복리) 기준.

| 구간 | af_step=0.02(현재) | af_step=0.01 |
|---|---|---|
| 전체기간(약1.5년) | -53,033,672원 (-106.07%) | -47,590,332원 (-95.18%) |
| 최근60일 | +9,531,836원 (+19.06%) | +16,718,696원 (+33.44%) |
| 최근30일 | +7,743,771원 (+15.49%) | +3,984,771원 (+7.97%) |

**주의**: 전체기간의 "-106%"는 실현 가능한 손실이 아니라, 손실이 나도 계속 같은
크기로 거래를 이어가는 단순 비복리 모델의 산물이다. 실제로는
`consecutive_loss_limit`, 월간 MDD 25% 자동정지 등 서킷브레이커가 그 전에 발동한다.
핵심은 "PF<1(전체기간)"이라는 방향성이지 이 숫자 자체가 아니다. 반대로 최근 구간의
+15~33%는 실제로 의미 있는 수익 규모다.

## 5. UT Bot Alerts + 200EMA + Volume/RSI 검증 — 기각

표준 UT Bot Alerts(ATR 트레일링 스탑 크로스오버, key_value=1/atr_period=10 기본값) +
200EMA 추세필터 + Volume(20SMA 대비) + RSI(14) 모멘텀 필터 조합을 신규 구현
(재현: `scratch/backtest_utbot_20260809.py`). 진입은 챈들리에/SAR과 무관한 별도
신호체계(칼만 타점 아님)이며 일일 EOD 무조건청산(15:35~15:45)을 기본 적용, 실제
402건 중 125건(31.1%)이 이 경로로 청산됨을 확인.

| 구성 | 전체기간 PF | 최근60일 PF | 최근30일 PF |
|---|---|---|---|
| UT Bot 단독(기본값) | 0.18 | 0.94 | 0.80 |
| +EMA/Volume/RSI 전체조합 | 0.18 | 0.60 | 0.49 |
| UT Bot 단독(key_value=3, 최적화) | 0.69 | 2.71 (53건) | 0.83 |
| 사용자 지정안(KeyVal2.5/ATR7/EMA150/RSI48-52, Volume없음) | **0.57** (402건) | 2.15 (42건) | 1.35 (19건) |
| 위 + Volume 추가 | 0.60 (263건) | 2.77 (19건) | 2.66 (**7건**) |
| 위 + 일일청산 없음(오버나잇 허용) | 0.72 (412건) | 1.68 (43건) | 1.37 (18건), **최악손실 -106.58pt**(청산판 -33.78pt 대비 3배+) |

**필터(EMA/Volume/RSI)는 개별로도 함께여도 도움이 안 되고 대체로 해롭다.** 최근 구간의
높은 PF(1.35~2.77)는 표본이 7~53건으로 작아 신뢰하기 어렵다 — 가장 표본이 큰
전체기간(263~412건) 기준 최고 PF는 0.72(일일청산 없앤 버전)인데, 이건 최악손실이
-106.58pt로 3배 뛰는 오버나잇 갭 리스크를 새로 떠안는 대가다. **모든 변형이 SAR
조합(0.63/1.22/1.37, 표본 41~85건)보다 못하거나 리스크 프로파일이 나쁘다 — 기각.**

## 6. Donchian 채널 단독 브레이크아웃 + SAR 청산 — 기각

칼만 타점 없이 순수 N봉 고저점 돌파만으로 진입(N=10~55 스윕), 청산은 검증된 SAR
로직 재사용(재현: `scratch/backtest_donchian_sar_20260809.py`).

| N | 전체기간 PF | 최근60일 PF | 최근30일 PF |
|---|---|---|---|
| 10~55 전체 | **0.09~0.11** | 0.49~0.65 | 0.45~0.83 |

전체기간 승률이 4.4~5.1%(2,000~5,300건)로 극히 낮다. N을 아무리 바꿔도 전체기간
결과가 거의 그대로라 소표본 노이즈가 아니라 신뢰할 수 있는(나쁜) 결과다. 필터 없는
순수 브레이크아웃은 이 시장·타임프레임에 안 맞음 — 기각.

## 7. SAR 고도화 시도 (챈들리에에서 검증된 요소 이식) — 둘 다 무효

챈들리에에서 효과가 있었던 `regime_filter`, `time_stop`을 SAR+진입필터 조합에
추가로 이식해 테스트.

| 구성 | 전체기간 PF | 최근60일 PF | 최근30일 PF |
|---|---|---|---|
| 기존 SAR+진입필터 | 0.63 | 1.22 | 1.37 |
| +regime_filter | 0.63 | 1.19 | 1.37 |
| +time_stop | **0.31** | **0.94** | **1.10** |
| +둘 다 | 0.30 | 0.89 | 1.10 |

`regime_filter`는 챈들리에처럼 여기서도 중립. **`time_stop`은 명확히 해롭다**
(전 구간 대폭 하락, 거래수는 428→1101건으로 급증) — SAR은 이미 가속인수(AF)로 스스로
빠르게 반응하는 구조라 time_stop이 SAR의 판단보다 먼저 개입해 트레이드를 조기에
자르는 것으로 보인다. **한 전략(챈들리에)에서 통한 개선이 다른 전략(SAR)엔 그대로
안 통한다는 것을 확인** — SAR은 진입필터만 추가한 원래 상태가 최선.

## 8. 최종 결론 및 개선안

1. **Parabolic SAR 청산 + entry_cutoff + min_std_error_entry**(regime_filter/time_stop
   없이)가 오늘 검증한 모든 후보 중 유일하게 유효한 개선안이다.
2. UT Bot Alerts, Donchian 단독, SAR+regime/time_stop 추가— 전부 기각. 새 매매기법
   탐색은 수확체감 구간에 들어섰다고 판단, 여기서 마무리.
3. **주의**: SAR 조합도 아직 실전 전환 여부는 별도 논의 필요(전략 자체를 바꾸는 큰
   결정). `sar_af_step` 0.01 vs 0.02는 레짐 트레이드오프로 사용자 판단이 남아있다.
4. Bollinger Band 전략은 채택 근거 없음.
5. TradingView 커뮤니티 리서치는 연결 문제로 완료하지 못함 — 필요시 재시도 가능.

## 9. 전체기간·최근구간 승률 격차의 근본 원인 — 시장 자체의 구조적 변화

SAR 거래를 월별로 쪼개보니 승률이 2025년 15~30%대에서 2026년 들어 꾸준히 40~62%까지
계단식으로 개선됐다. 추세효율성(Kaufman ER)은 이 흐름과 상관관계가 없었으나, **일평균
가격 레인지가 2025-01 3.8pt(가격수준 334pt, 1.13%)에서 2026-07 83.0pt(가격수준
1,117pt, 7.43%)로 약 15~20배 폭증**했음을 확인했다 — 가격 수준 자체도 3~4배 올랐고
퍼센트 변동성도 별도로 4~5배 커졌다. 이 시스템의 핵심 파라미터(`min_std_error_entry`,
`chandelier_hard_cap_pt`, ATR 기반 SAR 손절폭 등)는 전부 절대 포인트 단위라, 지금
설정값들은 사실상 현재의 고변동성 장세에 맞춰진 것이고 2025년의 훨씬 조용한 시장에는
애초에 안 맞는 잣대였다.

**추가 확인**: 저변동성(2025년) 구간에서 역추세(평균회귀) 기법도 시험했으나 더 나쁘게
나왔다(BB+RSI PF 0.01, IB+VWAP Z-score PF 0.05 — 고변동성 구간의 0.09~0.18보다도 못함).
즉 저변동성 구간의 부진은 진입 방향(추세추종 vs 역추세) 문제가 아니라 **비용 구조** 문제다
— 저변동성 구간의 일평균 레인지(~4.5pt) 대비 왕복거래비용(슬리피지+수수료 약 3.8pt)
비율이 약 78%에 달해, 방향을 완벽히 맞혀도 남는 이익이 거의 없다.

## 10. 개선안: `atr_cutoff` 상향 — 순수 개선, 트레이드오프 없음

목표 비용비율(왕복비용/포착폭)별로 필요한 ATR14 최소값을 역산(SAR 초기손절폭=1.0×ATR14
기준): 비용비율 25% 목표 시 ATR14≥15.2pt, 35% 목표 시 10.9pt. 실제 SAR+진입필터
조합에 적용해 검증:

| `atr_cutoff` | 전체기간 PF | 최근60일 PF | 최근30일 PF |
|---|---|---|---|
| 현재값(0.5pt) | 0.63 | 1.22 | 1.37 |
| 10.9pt | 0.70 | 1.20 | 1.35 |
| **15.2pt** | **0.75** | 1.20 | 1.35 |

15.2pt로 올리면 전체기간 PF가 0.63→0.75로 뚜렷이 개선(거래 428→296건, 저품질
저변동성 거래 제거)되고, 최근60일·최근30일은 거의 그대로다(84→80건, 42→38건 — 최근은
이미 고변동성이라 이 필터에 거의 안 걸림). **미래에 저변동성 국면이 재현돼도 방어되면서
현재 성과는 그대로 유지되는, 트레이드오프 없는 순수 개선.** 현재 라이브 `atr_cutoff`
기본값(0.5pt)은 사실상 아무것도 걸러내지 못하는 수준임을 확인.

## 11. 최종 SAR 권장 조합 (갱신)

- `chandelier_mult` 대신 `sar_af_max=0.20`, 스퀴즈필터 켬 (기존값 유지, 재스윕으로 확인)
- `entry_end_hour=15`, `min_std_error_entry=1.5` (기존 검증 유지)
- **`atr_cutoff`를 0.5pt → 약 15pt로 상향 (신규 발견, 순수 개선)**
- `regime_filter_enabled`, `time_stop_enabled`는 추가하지 않음 (효과 없거나 해로움)
- `sar_af_step` 0.01 vs 0.02는 전체기간/최근구간 트레이드오프로 미결정 — 사용자 판단 필요

재현: `scratch/backtest_sar_bb_20260809.py`, `scratch/sar_param_resweep_20260809.py`,
`scratch/backtest_utbot_20260809.py`, `scratch/backtest_donchian_sar_20260809.py`

## 12. TradingView 지표 라이브러리 탐색 후 추가 검증 (ADX 필터, ORB) — 둘 다 기각

TradingView Desktop 연결(크롬 CDP 우회, 별도 기록)이 복구돼 지표 라이브러리를 직접
검색했다. 이미 차트에 얹혀있던 LuxAlgo SMC·Kalman ATR[BackQuant]·`(RSI+CCI)×(MACD/ATR)^2`는
전부 과거 세션 탐색 흔적이거나 오늘 이미 자체구현 후 기각한 계열(SMC FVG PF 0.18)과
겹쳐 새로 볼 필요 없음. Supertrend·Keltner Channel·TTM Squeeze는 챈들리에/SAR/볼린저
(전부 오늘 이미 검증·기각 또는 채택 완료)와 같은 ATR밴드-트레일링 계열이라 별도
검증 실익이 낮다고 판단해 제외. 진짜 새로운 후보 2개만 실제 백테스트했다.

### 12-1. ADX/DMI 추세강도 진입필터 (SAR+entry_cutoff+atr_cutoff=15.2 기준선에 추가)

9절에서 확인한 "전체·최근구간 격차의 근본 원인=저변동성이 아니라 방향성 없는 횡보"에
직접 대응하는 가설: `atr_cutoff`는 변동성 크기만 보므로, 크기는 크지만 방향성 없는
움직임은 걸러내지 못한다. ADX는 시스템의 기존 추세필터와 동일하게 15분봉 위에서
계산했고(lookahead 방지: 직전에 완결된 15분봉의 ADX만 사용), DI 방향 일치 여부도
함께 테스트했다.

| 조건 | 전체기간 PF | 최근60일 PF | 최근30일 PF |
|---|---|---|---|
| 기준선(ADX 필터 없음) | **0.75** | **1.20** | **1.35** |
| ADX≥15 + DI방향 일치 | 0.68 | 1.24 | 1.54 |
| ADX≥20 + DI방향 일치 | 0.60 | 1.09 | 1.18 |
| ADX≥25 + DI방향 일치 | 0.67 | 1.13 | 1.59 |
| ADX≥30 + DI방향 일치 | 0.58 | 1.08 | 1.14 |
| ADX≥20, DI방향 요구 없음 | 0.60 | 0.98 | 1.10 |

**기각.** 전체기간은 모든 threshold에서 기준선보다 나쁘고(0.75→0.58~0.68), 최근60일도
전부 기준선(1.20) 이하다. 최근30일만 threshold 15·25에서 소폭 개선되는데(1.35→1.54,
1.59) 표본이 19~35건뿐이라 잡음일 가능성이 높고 threshold 20·30에서는 오히려
악화돼 일관성이 없다. regime_filter/time_stop과 같은 패턴 — "추세 방향에 대한
추가 필터"는 이 시스템에서 거래 수만 줄일 뿐 품질을 높이지 못한다.

### 12-2. 개장범위 브레이크아웃(ORB) — 신규 진입 방식 자체 검증

오늘 테스트한 모든 기법은 결국 가격추종 트레일링 아니면 지표크로스 계열이라
서로 상관관계가 높다. ORB는 진입 신호 생성 방식 자체가 다르다(장 시작 N분 레인지
이탈). 공정 비교를 위해 청산은 검증된 SAR 트레일링을 그대로 재사용하고 진입 로직만
교체, 비용구조·강제청산 시각·atr_cutoff=15.2도 동일 적용했다.

| 개장범위 | 전체기간 PF | 최근60일 PF | 최근30일 PF |
|---|---|---|---|
| 5분 | 0.66 | 0.95 | 0.79 |
| 15분 | 0.60 | 1.01 | 0.66 |
| 30분 | 0.56 | 0.64 | 0.62 |
| 60분 | 0.59 | 0.73 | 0.54 |
| (참고) 15분, atr_cutoff 무필터 | 0.41 | 1.01 | 0.66 |

**기각.** 모든 개장범위 길이·모든 구간에서 현재 SAR 조합(전체 0.75 / 최근60일 1.20 /
최근30일 1.35)보다 확연히 열세다. 최선인 5분 설정조차 최근구간 PF가 0.79~0.95로
기준선에 크게 못 미친다. 국내 주간선물 5분봉에서는 장 초반 레인지 이탈이 이후
추세 지속을 예측하는 신호로 작동하지 않는 것으로 보인다.

### 12-3. 결론

TradingView 라이브러리 탐색에서 나온 후보까지 포함해 두 가지를 추가 검증했으나
둘 다 기각. 8절의 최종 결론(SAR+entry_cutoff+min_std_error_entry, atr_cutoff 상향)에서
바뀌는 것 없음 — 이 조합이 여전히 유일하게 유효한 개선안이다.

재현: `scratch/backtest_sar_adx_filter_20260809.py`, `scratch/backtest_orb_20260809.py`
