# 지표 계산 단일화 — 마이그레이션 4번 완료 보고

**대상**: `bqa/backtester.py` · `bqa/enhanced_backtester.py` · `futures_trader/backtester.py`
**일자**: 2026-08-12
**결과**: 3파일 전부 교체 완료, 골든 테스트 전수 일치, 음성 대조 전건 검출

---

## 1. 요약

| 항목 | 결과 |
|---|---|
| 교체 파일 | 3/3 |
| 골든 대조 | **전수 일치** (조합 34개 · 체결 15,703건 · 지표 스칼라 647,903개) |
| 음성 대조 | **17/17 검출** (ta호환 5 + bqa 4 + futures_trader 4 + enhanced 4) |
| 기존 회귀 | `test_indicators_equivalence` **26/26 유지** |
| 라이브 영향 | 없음 — `era/` 경로 파일 무변경, `config/active_strategy.json` md5 불변 |

---

## 2. 선행 작업 — `indicators.py`에 ta 호환 변형 추가

교체 전 실측에서 **단순 교체 시 동작이 바뀐다**는 사실이 드러나 (§5 N1/N2), ta의 시맨틱을
그대로 재현하는 함수 4종을 `indicators.py` §9에 추가했다.

| 추가 함수 | 재현 대상 | ta 대비 오차 |
|---|---|---|
| `ta_rsi_series` | `ta.momentum.RSIIndicator` | 0.000e+00 |
| `ta_ema_series` | `ta.trend.EMAIndicator` | 0.000e+00 |
| `ta_macd_series` | `ta.trend.MACD` (macd/signal/diff) | 0.000e+00 |
| `ta_atr_series` | `ta.volatility.AverageTrueRange` | 0.000e+00 |

볼린저는 기존 `bollinger_series`에 **`ddof=0`을 명시**하면 완전 일치하여 새 함수를 만들지 않았다.

검증: `scratch/test_ta_compat_20260812.py` — 실데이터 31,797봉 기준 **양성 10/10 · 음성 5/5**.
`indicators.py`는 578행 → 673행 (+95행, 대부분 "왜 ta 시맨틱을 보존하는가" 주석).

`ta_` 접두를 붙여 **표준판(`wilder_rsi` 등)과 물리적으로 구분**했다. 새 코드가 실수로
비표준판을 집어가지 않도록 하기 위함이며, docstring에 차이를 명시했다.

---

## 3. 파일별 교체 결과

### ① `bqa/backtester.py`

| 항목 | 내용 |
|---|---|
| 교체 지표 | BB(20,2) → `bollinger_series(..., ddof=0)`, RSI(14) → `ta_rsi_series` |
| diff | -7 / +14행 (증가분은 전량 주석) |
| 골든 대조 | 체결 **2,496건** · 지표 스칼라 **127,112개** · stdout 원문 — **전수 일치** |
| 음성 대조 | **4/4 검출** |
| 백업 | `bqa/backtester.py.bak_20260812_215222` |

### ② `futures_trader/backtester.py`

| 항목 | 내용 |
|---|---|
| 교체 지표 | ①과 동일 |
| diff | -7 / +20행 |
| 골든 대조 | 체결 **2,038건** · 지표 스칼라 **107,924개** · stdout 원문 — **전수 일치** |
| 음성 대조 | **4/4 검출** |
| 백업 | `futures_trader/backtester.py.bak_20260812_215222` |

DB 경로가 cwd 상대(`"futures_data.db"`)인 기존 동작은 **고치지 않았다**(발견 N5).
골든 테스트는 base·new 모두 cwd를 `futures_trader/`로 고정해 동일 조건으로 대조했다.

### ③ `bqa/enhanced_backtester.py`

| 항목 | 내용 |
|---|---|
| 교체 지표 | BB, RSI, ATR, MACD(3계열), EMA(10/34) — **ta 의존 전량 제거** |
| diff | -13 / +24행 |
| 골든 대조 | 조합 **28개** · 체결 **11,169건** · 지표 **13종 412,867 스칼라** — **전수 일치** |
| 음성 대조 | **4/4 검출** |
| 백업 | `bqa/enhanced_backtester.py.bak_20260812_215222` |

**알려진 미래참조는 지시대로 그대로 보존**했다. 교체 후 위치:
- L69 `bb_width`를 당봉 포함으로 계산
- L88-89 `bb_width_min_100` · `is_squeeze` 동일
- L117/202/326 `row = df.iloc[i]`로 당봉 진입 판정

⚠️ **라이브 설정 쓰기 회피**: 이 파일의 `main()`은 `config/active_strategy.json`을 덮어쓴다
(§5 N4). 골든 하네스는 `__name__`을 `'__main__'`이 아닌 값으로 두어 `main()`을 실행하지 않고
전략 함수만 직접 호출했으며, `RESULTS_FILE`을 임시 경로로 무력화하는 이중 안전장치를 뒀다.
캡처 전후 `config/active_strategy.json`의 md5가 `016c0510…19f1`로 **동일함을 확인**했다.

---

## 4. 골든 테스트 방법과 한계

### 방법

세 파일 모두 거래 내역을 외부로 노출하지 않아(중첩 함수 / 요약 dict만 반환),
**소스에 계측 훅을 주입한 사본을 exec**하는 방식을 썼다.

- `scratch/golden_bt_harness_20260812.py` — ①②용. 훅 3종(모드 마커, 체결 기록, 지표 덤프)
- `scratch/golden_enh_harness_20260812.py` — ③용. 훅 1종(체결 기록) + 전략 함수 직접 호출
- `scratch/golden_bt_compare_20260812.py`, `scratch/golden_enh_compare_20260812.py` — 대조기

공정성 장치:
- base·new에 **동일한 패치 문자열**을 적용하고, 패치 건수가 기대와 다르면 즉시 중단
- base·new가 **같은 `serialize()` 함수**를 통과한 뒤에만 비교
- 대조기는 어느 쪽도 정답으로 가정하지 않고 양방향 diff만 보고

### 거래 단위 대조 — 가능했음

요약지표로 후퇴하지 않았다. 체결마다 `(자본, 진입가, 방향)` 또는 `(자본, 누적거래수, 누적승수)`를
기록해 **시퀀스 전수 비교**했다. 총 15,703건.

### 남은 한계 (명시)

1. **체결 시각·청산사유는 대조 대상이 아니다.** 훅이 기록하는 값에 포함되지 않는다.
   다만 자본 시퀀스가 원소 단위로 같으면 체결 시점·손익이 같다는 강한 방증이다.
2. **`enhanced_backtester`의 K 스윕 범위를 하네스가 재현했다.** `main()`을 실행할 수 없어
   `np.arange(0.3, 0.85, 0.05)`와 모드 3종을 하네스에 옮겨 적었다. `main()`의 나머지 흐름
   (best_k 선정, 출력 포맷, JSON 저장)은 대조되지 않았다. 단 그 부분은 지표를 쓰지 않는다.
3. **`enhanced_backtester`의 `B_best_*` 조합은 K=0.5로 고정**했다. 실제 `main()`은 스윕 결과에서
   best_k를 뽑아 쓰는데, 그 값이 base/new에서 같음은 `runs`의 전 필드 일치로 간접 확인된다.
4. **단일 DB 스냅샷 기준**이다. `futures_data.db`(31,797봉) 및 `futures_trader/futures_data.db`
   (26,981봉) 한 시점의 데이터로만 대조했다.

---

## 5. 발견한 비표준·의심 지점 (기존 7건에 추가 — N1~N8)

| # | 파일 | 내용 | 조치 |
|---|---|---|---|
| **N1** | 3파일 전부 | `ta` 볼린저는 **모표준편차(ddof=0)**. 라이브 `era_order_manager.py` L984-988은 pandas 기본 **ddof=1**. 실측 상단밴드 **최대 3.195pt 차이** — 백테스터와 라이브가 서로 다른 밴드를 써왔다 | 보존(`ddof=0` 명시) |
| **N2** | 3파일 전부 | `ta` RSI는 **Wilder RSI가 아니다.** 초기 SMA 시드 없이 0에서 출발하는 EWM(`adjust=False`), 첫 유효 인덱스도 13 vs 14. `wilder_rsi`와 **최대 26.15 차이** | 보존(`ta_rsi_series`) |
| **N3** | `bqa/backtester.py` L53-56<br>`futures_trader/backtester.py` L55-58 | `rsi_min_5`/`rsi_max_5`/`low_min_5`/`high_max_5`를 계산만 하고 **어디서도 쓰지 않음** (죽은 코드) | 보존 |
| **N4** | `bqa/enhanced_backtester.py` L524-527 | 백테스터가 **라이브 설정 `config/active_strategy.json`을 덮어쓴다.** 이 파일은 `era_order_manager.py` L643-646이 런타임에 읽어 주간선물 파라미터를 오버라이드한다 | 보존 · 하네스에서 회피 |
| **N5** | `futures_trader/backtester.py` L19 | DB 경로가 cwd 상대 → 실행 위치에 따라 **다른 DB**를 읽는다 (7.2MB vs 53MB, 26,981봉 vs 31,797봉) | 보존 |
| **N6** | `bqa/backtester.py` ↔ `futures_trader/backtester.py` | 216행 중 **약 200행이 완전 중복** (전략·비용·청산 로직 전부) | 보존 |
| **N7** | `bqa/backtester.py` L74/80 | `for i in range(10, ...)` 안의 `if i >= 10` — 항상 참인 죽은 분기 | 보존 |
| **N8** | `bqa/enhanced_backtester.py` | `ta` ATR은 워밍업 구간을 **NaN이 아니라 0.0**으로 채운다(`np.zeros`). 따라서 `df.dropna()`에 걸러지지 않고, 첫 13봉이 `atr=0`으로 남는다. 다행히 진입부 `if pd.isna(atr_val) or atr_val <= 0: continue`가 막아주지만 **우연한 방어**다 | 보존(`ta_atr_series`가 동일 재현) |

기존 7건에 더해 **누적 15건**.

---

## 6. 제거된 중복 코드 규모

### 이번 마이그레이션

| 파일 | 지표 계산부 제거 | 추가(주석 포함) |
|---|---|---|
| `bqa/backtester.py` | -7행 | +14행 |
| `bqa/enhanced_backtester.py` | -13행 | +24행 |
| `futures_trader/backtester.py` | -7행 | +20행 |
| **계** | **-27행** | +58행 |

라인 수만 보면 늘었다. 추가분은 대부분 "왜 ta 시맨틱을 보존하는가"를 설명하는 주석이고,
실질 효과는 라인 수가 아니라 **지표 정의가 파일마다 흩어져 있던 상태의 해소**다.
구체적으로 이번에 없앤 것은 **독립적인 지표 정의 지점 19곳**(BB 3파일×3밴드=9, RSI 3,
ATR 1, MACD 3, EMA 2, 그리고 ddof·시드 규약 각 1)이다.

### 마이그레이션 누적 (1~4번)

| 회차 | 대상 | 대조 규모 |
|---|---|---|
| 1 | `scratch/backtest_bb_meanrev_20260811.py` | 14조합 5,570거래 |
| 2 | `scratch/backtest_sar_bb_20260809.py` | 48조합 51,974거래 |
| 3 | `bqa/kalman_backtester.py` | 62조합 52,360거래 · 577,983 스칼라 |
| **4** | **백테스터 3종** | **34조합 15,703거래 · 647,903 스칼라** |
| **누계** | **6파일** | **158조합 · 125,607거래 · 1,225,886 스칼라** |

`ta` 패키지 의존은 저장소 전체에 아직 **28개 파일**에 남아 있다(scratch 2개 포함).
다만 **라이브 경로(`era/`)에는 `ta` 의존이 0건**이다.

---

## 7. 이 3개 파일은 정말 미사용인가 — 최종 판단

### 결론: **"import 미사용"은 맞지만 "완전 미사용"은 아니다.**

| 파일 | Python import | .bat 진입점 | 판단 |
|---|---|---|---|
| `bqa/backtester.py` | 0건 | `run_bqa.bat:54` (메뉴 "2") | **수동 실행 가능** |
| `futures_trader/backtester.py` | 0건 | `futures_trader/run_backtester.bat:10` | **수동 실행 가능** |
| `bqa/enhanced_backtester.py` | 0건 | 없음 | **완전 미사용** |

자동화·스케줄·라이브 경로 어디에서도 호출되지 않는다. 사람이 .bat을 더블클릭할 때만 돈다.

### 삭제 권고 여부

**지금 당장의 삭제는 권하지 않는다.** 이유:

1. `bqa/backtester.py`는 `run_bqa.bat` 메뉴에 물려 있다. 파일만 지우면 메뉴 항목이 깨진다.
   지우려면 `run_bqa.bat`의 분기까지 같이 정리해야 하고, 그건 이 마이그레이션의 범위를 넘는다.
2. 방금 교체·검증을 마친 직후다. 검증된 상태를 한 번은 남겨두는 편이 낫다.

**단계적 정리를 제안한다.**

| 순위 | 대상 | 근거 |
|---|---|---|
| 1 | `bqa/enhanced_backtester.py` — **삭제 또는 `RESULTS_FILE` 쓰기 제거** | 진입점이 없는데 실행되면 라이브 설정을 덮어쓴다(N4). 삭제가 어렵다면 최소한 쓰기 부분만이라도 막을 것을 권한다. **위험 대비 효용이 가장 나쁘다** |
| 2 | `futures_trader/backtester.py` — 삭제, `run_backtester.bat`도 함께 | `bqa/backtester.py`와 200행 중복(N6)인데 cwd에 따라 다른 DB를 읽어(N5) **같은 코드가 다른 답을 낸다**. 혼란만 만든다 |
| 3 | `bqa/backtester.py` — 존치 | 셋 중 유일하게 정식 메뉴 진입점이 있다. 남긴다면 이것 하나 |

덧붙여, 이 백테스터들이 쓰는 볼린저·RSI가 라이브와 다르다는 점(N1/N2)은 **이 파일들의 결과를
라이브 판단 근거로 삼으면 안 된다**는 뜻이기도 하다. 존치하든 삭제하든 그 사실은 파일 상단
주석으로 남겨두었다.

---

## 8. 라이브 이식 단계에서 주의할 지점

> 아래는 이번 작업에서 **하지 않은 것**이며, 향후 `era/era_order_manager.py`에 `indicators.py`를
> 이식할 때 반드시 결정해야 할 항목이다.

1. **ddof를 어느 쪽으로 통일할지 먼저 정할 것.**
   라이브는 ddof=1, 이 백테스터들은 ddof=0이다. 라이브에 `indicators.bollinger_series`를 넣을 때
   기본값(ddof=1)을 그대로 쓰면 **라이브 동작은 안 바뀌지만**, 이 백테스터들과의 결과 비교는
   여전히 성립하지 않는다. "백테스트와 라이브가 같은 밴드를 쓴다"고 착각하지 말 것.

2. **`ta_` 접두 함수를 라이브에 쓰지 말 것.**
   `ta_rsi_series` 등은 구 백테스터의 동작을 박제하려고 만든 것이다. 라이브에는 표준판
   (`wilder_rsi`, `bollinger` 등)을 써야 한다. `__all__`에는 올라가 있으니 import 시 주의.

3. **`ta_atr_series`의 워밍업 0.0 처리(N8)를 그대로 옮기지 말 것.**
   라이브에서 ATR이 0으로 나오면 포지션 사이징 분모가 되어 위험하다. 표준 ATR을 쓰고
   워밍업은 NaN으로 두는 편이 안전하다.

4. **`enhanced_backtester`의 미래참조(당봉 포함 `bb_width`/`is_squeeze`)를 이식하지 말 것.**
   이번에 보존한 것은 "동작 무변경 리팩터링"이라는 이 작업의 성격 때문이지, 옳아서가 아니다.
   라이브에 스퀴즈 필터를 넣는다면 `closes[...:i]` 규약(마이그레이션 3번에서 확인된 깨끗한 패턴)을
   따라야 한다.

5. **`config/active_strategy.json` 쓰기 경로를 감사할 것.**
   백테스터가 라이브 설정을 직접 쓰는 구조(N4)는 `enhanced_backtester` 말고
   `bqa/batch_optimizer.py`에도 같은 상수가 있다. 이식 전에 "누가 이 파일을 쓰는가"를
   한 번 정리해두는 편이 좋다.

6. **골든 하네스는 재사용 가능하다.**
   `golden_bt_harness`는 훅 문자열만 바꾸면 다른 백테스터에도 붙는다. 라이브 이식 시에는
   같은 방식(base 캡처 → 교체 → new 캡처 → 동일 직렬화 후 대조 → 음성 대조)을 쓰되,
   **ERA를 기동하지 않는 리플레이 경로**로만 캡처해야 한다.

---

## 9. 안전 조건 확인

| 조건 | 상태 |
|---|---|
| ERA 정지 유지 | ✅ `era/era.pid` **파일 자체가 없음** (지시에는 "비어 있음"이었으나 실제로는 부재 — 기동한 적 없음) |
| `era/era_order_manager.py` 무수정 | ✅ mtime 08-12 20:38 (세션 시작 21:52 이전) |
| `bqa/kalman_backtester.py` 무수정 | ✅ mtime 08-12 17:26 |
| `system_stopped.flag` | ✅ 존재 (10:33) |
| `emergency_kill.flag` | ✅ 존재 (10:43) |
| `run_era.bat` STOP-GUARD | ✅ 4건 유지 |
| `era/auto_reconnect_era.bat` STOP-GUARD | ✅ 4건 유지 |
| `config/active_strategy.json` | ✅ md5 불변 + 백업 생성 |
| TCA · KIS 야간수집기 | ✅ 미접촉 |

---

## 10. 산출물

**수정**
- `indicators.py` (+95행, §9 ta 호환 변형)
- `bqa/backtester.py`, `bqa/enhanced_backtester.py`, `futures_trader/backtester.py`

**백업** (모두 `.bak_20260812_215222`)
- `indicators.py` / 3개 백테스터 / `config/active_strategy.json`

**신규 도구**
- `scratch/test_ta_compat_20260812.py` — ta 호환 검증 (양성 10 · 음성 5)
- `scratch/golden_bt_harness_20260812.py` / `scratch/golden_bt_compare_20260812.py`
- `scratch/golden_enh_harness_20260812.py` / `scratch/golden_enh_compare_20260812.py`

**골든 데이터** — `scratch/golden_mig4/` (BASE/NEW 6개, 약 19MB)

**조사 기록** — `scratch/mig4_step1_findings_20260812.md`
