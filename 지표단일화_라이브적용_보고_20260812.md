# 지표 단일화 — 라이브 적용 보고

작성: 2026-08-12 20:45 KST
대상: `era/era_order_manager.py` (실계좌 매매 엔진)
결과: **적용 완료 · 검증 통과 · ERA 정지 상태 유지**

---

## 0. 한 줄 요약

교체본 `era_B.py`를 라이브에 적용했고, **적용된 라이브 파일 자체를** 백업본과 다시 대조해
종일·웜스타트 양쪽 모두 **비트 단위 완전 일치**를 확인했다. ERA는 기동하지 않았다.

---

## 1. 적용 내역

| 항목 | 값 |
|---|---|
| 적용 전 해시 (A) | `0ee30d3d2f2ab5206873ba52860d15742162ba7e9a7412e6fe6fdffc77db8e3c` |
| 적용 후 해시 (B) | `48f3bc691ac08677b5791986b1c61abbfc2b6e3d0aaf99c2259ca0c83d8098b7` |
| 백업 파일 | `era/era_order_manager.py.bak_before_indicators_live_20260812_203829` |
| 백업 해시 | `0ee30d3d…8e3c` (= `era_A.py`와 바이트 동일, `cmp` 확인) |
| 파일 크기 | 489,151 B → 490,806 B |
| 문법 검사 | `py_compile` 통과 (Python 3.10.12 / 라이브 venv32 = 3.10.11) |
| `indicators.py` 해시 | `0bb8136f6ef526deb45c6125774fee431a3c64fa91032bc9dcaba7190c80ae90` |

라이브 파일은 `scratch/replay/variants/era_B.py`와 **바이트 동일**하다 (`cmp` 확인).
즉 A/B 대조를 통과한 그 파일이 그대로 올라갔고, 복붙 과정의 변형은 없다.

---

## 2. 적용 전 확인 (1단계)

### 2.1 원본 무결성 — 통과

`era/era_order_manager.py` = `era_A.py` **바이트 동일**. A/B 대조 이후 제3자 수정 없음.

### 2.2 diff 범위 — 통과 (지표 계산부 한정)

총 9개 훅, +78 / −78 라인. 전체 내역:

| # | 위치 | 내용 | 분류 |
|---|---|---|---|
| 1 | L10 | `sys.path` 루트 추가 + `import indicators as ind` | 인프라 |
| 2 | L858 | TR 계산 → `ind.true_range`, Kalman ATR 루프 → `ind.kalman_atr` | 지표 |
| 3 | L980 | BB(20,2) → `ind.bollinger_series`, 밴드폭·스퀴즈·MA200 → `ind.*` | 지표 |
| 4 | L1128 | 칼만 평활·std_error 트림 → `ind.kalman_residual_std` | 지표 |
| 5 | L1171 | 15분봉 장기 칼만 루프 → `ind.kalman_path` | 지표 |
| 6 | L1644 | `_sar_tick()` 헬퍼 **신설** (SAR 값 갱신만 위임) | 지표 |
| 7 | L3725 | 전일 Range → `ind.prev_session_range` | 지표 |
| 8 | L4454 | LONG SAR 인라인 갱신 → `self._sar_tick()` | 지표 |
| 9 | L4661 | SHORT SAR 인라인 갱신 → `self._sar_tick()` | 지표 |

**전략 로직·비용 모델·진입/청산 조건 변경 0건.** 훅 8·9에서 방향 전환(`sar_bull`)과
청산 판정(`current_price <= sar_value or pnl_pt <= -sl_limit`)은 호출부에 그대로 남아 있고,
`_sar_tick`은 값 갱신만 한다. 클램프 적용 → EP/AF 갱신 **순서**도
`SarState._advance`가 원본과 동일하게 재현한다 (bull=`min(clamp)`, bear=`max(clamp)`).

### 2.3 측정 원장 훅 — 통과

`[측정원장 훅 A~D]` 주석 4개, `_ledger(` 참조 5개. **A·B 양쪽 동일 개수·동일 내용.**
교체본은 오전 원장 추가 **이후** 버전을 베이스로 하고 있다.

### 2.4 `indicators.py` 경로 — 통과 (단, 배포 관련 경고 있음 → §5)

```
__file__          = <root>/era/era_order_manager.py
_indicators_root  = <root>                 ← dirname(dirname(__file__))
indicators.py 존재 = True, import OK, 경로일치 True
```

`import sys`가 파일 2행에 있어 17행의 `sys.path` 조작보다 앞선다 — NameError 없음.
라이브 venv32 = **Python 3.10.11 / numpy 1.24.4 / pandas 2.0.3**.
`indicators.py`는 `from __future__ import annotations`, `dataclass`, `typing.Literal`,
`numpy`만 쓴다 (3.10 전용 문법 없음). 호환 문제 없음.

---

## 3. 적용 후 검증 (3단계)

### 3.1 하네스 재대조 — **적용된 라이브 파일 자체를 대조**

라이브 파일을 `variants/LIVE_applied.py`로 복사(해시 `48f3…8b7` 일치)해 하네스에 태우고,
백업본과 바이트 동일한 `era_A`를 기준선으로 다시 돌렸다.

| 실행 | 봉/틱 | bar 필드 | tick 필드 | init | 주문 동일 | 체결 동일 | 판정 |
|---|---|---|---|---|---|---|---|
| 종일 (cold) | 82봉 / 2,075틱 | **0** | **0** | **0** | True | True | 탐지=아니오 |
| 웜스타트 13:00 (warm) | 31봉 / 800틱 | **0** | **0** | **0** | True | True | 탐지=아니오 |

`compare.py`는 값을 `float(x)` 전정밀도로 기록하고 `!=`로 **완전 일치** 비교한다
(허용오차 없음). 따라서 위 0은 반올림 후 0이 아니라 **비트 단위 0**이다.

보고된 "구조" 항목(예: `errors`/`kf_prices`가 A만, `_mid`/`_up`/`_lo`가 B만 존재)은
중간 변수 이름이 바뀐 데서 나오는 것이고 값 대조 대상이 아니다. `~verdict.lines_executed`
등 `info` 항목은 라인 번호 의존이라 판정에서 제외된다.

> **이 검증이 잡는 것**: "교체본이 통과했다"가 아니라 "**지금 디스크에 있는 라이브 파일이**
> 통과한다". 복사 사고·부분 붙여넣기·인코딩 변형이 있었다면 여기서 걸렸다.

### 3.2 shim 인스턴스화 — 통과

`run_shim_check.py` 실행 결과 (`out/shim_check_report.json`):

- `copy_integrity.live_sha256` = **`48f3bc69…8b7`** → **적용된 B 파일**이 검사 대상이었음이 증명됨
- `byte_identical: true`, `instantiation.ok: true`
- 속성 235개, 타이머 17개 등록, **init 중 주문 시도 0건**
- 주문 차단 검증: 기록된 주문 의도 6건, **실 API 호출 0건**
- 정적 추적: `dynamicCall` 155개 전부 수신자 `self.kiwoom`, **우회 경로 0건**
- 가드: **네트워크 0건, 위반 0건**

인스턴스화가 성공했다는 사실 자체가 `import indicators as ind`의 성공을 의미한다 —
이 import는 **모듈 최상위에 있고 try/except로 감싸져 있지 않다.**

### 3.3 `indicators` import 실패가 조용히 넘어가지 않는가 — 확인

지표 함수 3개는 확실히 예외를 삼킨다. 삼키는 지점과 그때 나오는 문자열:

| 라인 | 로그 문자열 | 실패 시 거동 |
|---|---|---|
| 893 | `[AMATS 파생 최적화] 동적 익손절 계산 에러` | 기존 ATR 값 유지 |
| 1022 | `[ERA BB 필터 오류]` | BB·MA200 이전 값 유지 |
| 1176 | `[ERA 칼만] 장기 추세 필터 연산 에러 (NEUTRAL 기본 설정)` | 추세 NEUTRAL |
| 1229 | `[ERA 칼만] 실시간 타점 연산 중 에러 발생` | 타점 이전 값 유지 |

**그러나 `import indicators`는 이 try 블록 밖, 모듈 최상위에 있다.**
`indicators.py`가 사라지면 `era_order_manager` 모듈 import 자체가 실패하고
`leader_order_manager.py`가 기동하지 못한다 — **조용히 틀린 값으로 도는 게 아니라
아예 안 뜬다.** 실패 방향이 안전한 쪽이다. 다만 `py_compile`은 import를 실행하지 않으므로
`run_era.bat`의 기동 전 문법 검사는 이걸 잡지 못한다. 콘솔/로그에서 봐야 한다.

### 3.4 시스템 안전 조건 — 전부 유지

| 항목 | 상태 |
|---|---|
| `system_stopped.flag` | 존재 (2026-08-12 10:33:45) — 그대로 |
| `emergency_kill.flag` | 존재 (2026-08-12T10:44:00) — 그대로 |
| `run_era.bat` STOP-GUARD | 4개 마커 잔존 |
| `era/auto_reconnect_era.bat` STOP-GUARD | 4개 마커 잔존 |
| `config/active_strategy.json` | mtime 08-08 05:01 — **미변경** |
| `era/futures_exit_state.json` | mtime 08-12 10:33 — **미변경** |
| `tca/system_status_futures.json` | mtime 08-12 10:36 — **미변경** |
| 주문 API 호출 | 0건 |
| 증권사/키움 네트워크 접속 | 0건 |
| ERA 기동 | **하지 않음** |
| TCA · KIS 야간수집기 | 건드리지 않음 |

---

## 4. 되돌리는 절차

문제 발생 시 관리자 명령 프롬프트에서 (`C:\Antigravity\AI_T_Agent` 기준):

```bat
copy /Y "era\era_order_manager.py.bak_before_indicators_live_20260812_203829" "era\era_order_manager.py"
```

되돌린 뒤 확인:

```bat
certutil -hashfile "era\era_order_manager.py" SHA256
:: 0ee30d3d2f2ab5206873ba52860d15742162ba7e9a7412e6fe6fdffc77db8e3c 가 나와야 한다

"venv32\Scripts\python.exe" -m py_compile "era\era_order_manager.py"
```

> **`git checkout -- era\era_order_manager.py` 는 쓰지 말 것.**
> `run_era.bat`의 FATAL 메시지가 그 명령을 안내하지만, 이 파일은 git HEAD 기준으로
> 이미 수정 상태(`M`)라 그 명령은 지표 단일화뿐 아니라 **측정 원장 훅까지 통째로 날린다.**
> 반드시 위 `.bak` 백업으로 되돌려야 한다.

---

## 5. 재기동 전에 반드시 알아야 할 것 (⚠️ 결정 필요)

### 5.1 `indicators.py`가 git에 등록되어 있지 않다 — 가장 중요

```
 M era/era_order_manager.py
?? indicators.py        ← untracked
```

`run_era.bat`은 기동할 때마다 `git pull origin main`을 실행한다.
untracked 파일이라 `git pull` 자체가 지우지는 않지만:

- **다른 경로/머신에 git으로 배포하면 `indicators.py`가 따라가지 않는다** → 그쪽 ERA는 기동 실패
- `git clean -fd`를 한 번이라도 돌리면 **삭제된다** → ERA 기동 실패
- 2026-07-31에 클라우드 동기화가 `era_order_manager.py`를 덮어써 장중 몇 시간을 날린 전례가
  있는데, 이제 **파일 2개가 짝을 이뤄야만 도는 구조**가 되어 그 리스크가 커졌다

권장 조치 (재기동 전):

```bat
git add indicators.py
git commit -m "feat: single-source indicator module for live engine"
```

`era/era_order_manager.py`의 커밋 여부는 사용자 판단 사항이다.

### 5.2 남겨진 정리 대상 파일 2개

샌드박스 권한 문제로 삭제하지 못했다. 수동 삭제 권장 (둘 다 `scratch/` 아래라 매매에 영향 없음):

- `scratch/replay/indicators.py` — shim 검사용으로 루트에서 복사한 사본. **원본과 갈라질 수 있으니 지우는 편이 낫다**
- `scratch/replay/variants/era_A.py.PROTECT_20260812` — `era_A.py` 보호용 임시 백업

> 참고: `run_shim_check.py`는 실행할 때마다 **라이브 파일을 `variants/era_A.py`에 덮어쓴다.**
> 이번에 A 기준선이 날아가는 걸 막으려고 미리 백업 후 복원했고, `era_A.py`가
> `0ee30d3d…8e3c`로 정상 복구된 것을 확인했다. 앞으로 이 스크립트를 돌릴 때 같은 주의가 필요하다.

### 5.3 사소한 거동 차이 1건 (실질 도달 불가)

`std_error` 폴백 조건이 `pd.isna(s) or s <= 0` → `not np.isfinite(s) or s <= 0`으로 바뀌었다.
`inf`도 이제 0.5로 폴백된다. 유한한 잔차의 표준편차가 `inf`가 될 수 없으므로 실질적으로
도달 불가능한 경로이고, A/B 대조에서도 차이가 나오지 않았다.

---

## 6. 재기동 절차 (사용자가 직접 결정·실행)

**저는 기동하지 않았습니다.** 아래는 켤 때의 순서입니다.

### 6.1 순서

1. **`emergency_kill.flag` 삭제** — ERA는 이 플래그를 감지하면 **전량 청산 후 스스로 종료**한다.
   지우지 않으면 켜자마자 죽는다.
   ```bat
   del "emergency_kill.flag"
   ```
2. **`system_stopped.flag` 삭제** — `watchdog_check.bat`이 이 파일이 있으면 즉시 exit 한다.
   지우면 워치독 감시가 재개되고, 2분마다 `schtasks /run /tn "AMATS ERA Reconnect"`를 건다.
   ```bat
   del "system_stopped.flag"
   ```
3. **STOP-GUARD 해제** — 파일을 고치지 않는 1번 방법을 권한다.
   ```bat
   setx AMATS_ALLOW_ERA 1
   :: 또는 해당 세션만: set AMATS_ALLOW_ERA=1
   ```
   `run_era.bat`과 `era/auto_reconnect_era.bat` **양쪽 모두** 이 환경변수를 본다.
   블록을 지우는 방법(2번)이나 `.bak_before_stopguard_20260812_104254` 복원(3번)도 있지만,
   되돌리기가 번거로우니 환경변수 쪽이 낫다.
4. **기동** — 둘 중 하나
   ```bat
   run_era.bat
   :: 또는 스케줄러 경로로
   schtasks /run /tn "AMATS ERA Reconnect"
   ```
   `run_era.bat`은 기동 전 `git pull` → `py_compile` 자가검사 → `venv32` 파이썬으로
   `era\leader_order_manager.py`를 띄운다.

### 6.2 기동 직후 눈으로 확인할 로그

**나오면 안 되는 것** (`era/era_order_manager.log` 및 콘솔):

| 문자열 | 의미 |
|---|---|
| `ModuleNotFoundError: No module named 'indicators'` | 루트 `indicators.py` 유실 → 기동 자체 실패 |
| `[ERA BB 필터 오류]` | BB·밴드폭·MA200 계산 실패 → **이전 값으로 계속 돈다** |
| `[ERA 칼만] 실시간 타점 연산 중 에러 발생` | 칼만 타점 실패 → 타점 이전 값 유지 |
| `[ERA 칼만] 장기 추세 필터 연산 에러 (NEUTRAL 기본 설정)` | 15분봉 추세 실패 → NEUTRAL 고정 |
| `[AMATS 파생 최적화] 동적 익손절 계산 에러` | Kalman ATR 실패 → SL/TP 이전 값 유지 |

위 4개는 전부 **조용히 이전 값을 유지**하므로, 나오는데 놓치면 낡은 지표로 실매매가 돈다.
첫 갱신 주기(5분) 동안 반드시 확인할 것.

**나와야 하는 것**:

```
[ERA BB 필터] 갱신 완료 | 중심선=... | 밴드폭=...% (임계=...%) | MA200=... 직전종가=... → ...
[ERA 칼만 주간] 타점 설정 완료 | 코드=... | KF=... | std_err=...pt | band=...pt | LONG=... | SHORT=...
[AMATS 파생 최적화] 선물(...) 동적 Kalman ATR 적용 완료: Kalman ATR=...pt
```

### 6.3 지표 값 정상 범위 (2026-08-12 정지 직전 실측 기준)

| 지표 | 최근 실측값 | 정상 판단 기준 |
|---|---|---|
| BB 중심선 | 1,009.83 ~ 1,011.87 | 현재 지수 ±수 pt 안. 지수와 동떨어지면 이상 |
| 밴드폭 | 2.04 ~ 2.15% | 대략 0.5 ~ 4%. 임계(25% 분위수) = 1.22% |
| MA200 | 984.74 ~ 985.19 | 중심선보다 낮고, 봉마다 0.2pt 안팎으로 완만히 움직임 |
| 직전 종가 | 1,014.38 ~ 1,019.30 | MA200과의 대소가 방향필터(상승=LONG만)를 정한다 |
| `std_err` | 1.63 ~ 1.73pt | 1 ~ 5pt. **0.5pt가 뜨면 폴백이 걸린 것** — 계산 실패 신호 |
| `band` | 0.98 ~ 1.04pt | `std_err × futures_kf_mult` |
| KF (칼만 평활가) | 973.46 ~ 973.57 | 현재가 ±수 pt |
| Kalman ATR | **73.93pt** | §7 참고 — 값 자체는 A/B 동일하나 별도 검토 필요 |

특히 **`std_err`가 정확히 `0.50`으로 찍히면** `kalman_residual_std`의 폴백이 걸린 것이므로
계산 경로에 문제가 있다는 뜻이다. 값이 0.5인지 눈으로 확인할 것.

### 6.4 이상 시 즉시 정지

```bat
stop_system.bat
:: 또는 즉시
echo %date% %time% > "emergency_kill.flag"
echo %date% %time% > "system_stopped.flag"
```
그 다음 §4의 롤백 명령.

---

## 7. 관찰 사항 — Kalman ATR 73.93pt

`Kalman ATR=73.93pt → 손절=73.93pt / 익절=147.86pt`가 로그에 남아 있다.
지수가 1,010선인데 ATR이 73.93pt면 약 7.3%로, 일간 변동성치고 커 보인다.
손절 폭이 그대로 73.93pt가 되므로 사이징에 직결된다.

**이번 변경이 만든 문제는 아니다** — A/B 대조에서 값 차이 0으로 확인된, 적용 전부터 있던 거동이다.
남은 작업의 **손절 캡·손실 한도 적용**이 정확히 이 지점을 다루는 항목이므로,
재기동 전에 캡을 먼저 넣을지 결정하는 게 좋겠다.

---

## 8. 남은 작업

| # | 항목 | 상태 | 비고 |
|---|---|---|---|
| 1 | 미사용 백테스터 3종 마이그레이션 | 미착수 | 실매매 영향 없음. 지표 불일치가 남아 있는 마지막 지점 |
| 2 | SAR 봉 전환 (`on_tick` → `on_bar`) | 미착수 | 현재 라이브는 틱 기반. 회전율이 백테스트의 **18배**(22건/일 vs 1.2건/일). `indicators.SarState.on_bar`는 이미 준비돼 있고 호출부 한 줄만 바꾸면 되지만, **거동이 바뀌는 변경**이라 별도 A/B가 필요하다 |
| 3 | 손절 캡·손실 한도 적용 | 미착수 | §7과 직결 |
| 4 | 갭 처리 방식 결정 (A / B / C1) | 미결정 | `indicators.FeedSpec`에 세 방식이 파라미터로 열려 있고 아직 어느 것도 고정하지 않음 |
| 5 | `indicators.py` git 등록 | **미조치** | §5.1 — 재기동 전 처리 권장 |
| 6 | `scratch/replay` 잔여 파일 2개 삭제 | 미조치 | §5.2 |

---

## 9. 재기동 전 사용자 결정 사항

1. **`indicators.py`를 git에 커밋할 것인가** (§5.1) — 하지 않으면 배포·복구 경로에서 유실 위험
2. **손절 캡을 먼저 넣고 켤 것인가** (§7) — 현재 SL 73.93pt / TP 147.86pt로 돌게 된다
3. **SAR 봉 전환을 이번에 같이 할 것인가** (§8-2) — 회전율 18배 차이. 다만 거동이 바뀌므로
   같이 켜면 문제가 생겼을 때 원인 분리가 안 된다. **이번엔 지표 단일화만 켜고 관찰하는 쪽을 권한다**
4. **STOP-GUARD 해제 방식** — 환경변수(권장) / 블록 삭제 / `.bak` 복원
5. **언제 켤 것인가** — 장 시작 전 켜서 첫 지표 갱신 로그를 눈으로 확인할 수 있는 시간대 권장

---

## 부록 A. 검증 증거 파일

| 파일 | 내용 |
|---|---|
| `era/era_order_manager.py.bak_before_indicators_live_20260812_203829` | 적용 직전 원본 (해시 `0ee30d3d…8e3c`) |
| `scratch/replay/variants/LIVE_applied.py` | 적용된 라이브 파일 사본 (해시 `48f3bc69…8b7`) |
| `scratch/replay/out/LIVE_applied/snap_20260811_OHLC_t24.jsonl` | 종일 스냅샷 |
| `scratch/replay/out/LIVE_applied/snap_20260811_OHLC_t24_s130000.jsonl` | 웜스타트 스냅샷 |
| `scratch/replay/out/compare/cmp_LIVE_cold.json` | 종일 대조 결과 |
| `scratch/replay/out/shim_check_report.json` | shim 인스턴스화 검사 (`copy_integrity` = `48f3…8b7`) |
| `지표단일화_교체본B_AB대조_검증보고_20260812.md` | 선행 A/B 대조 보고 |

## 부록 B. 실행한 명령 요약

```bash
# 1단계
sha256sum era/era_order_manager.py scratch/replay/variants/era_{A,B}.py
cmp era/era_order_manager.py scratch/replay/variants/era_A.py
diff -u scratch/replay/variants/era_A.py scratch/replay/variants/era_B.py
grep -c "측정원장 훅" era/era_order_manager.py scratch/replay/variants/era_B.py

# 2단계
cp -p era/era_order_manager.py era/era_order_manager.py.bak_before_indicators_live_20260812_203829
cp scratch/replay/variants/era_B.py era/era_order_manager.py
python3 -m py_compile era/era_order_manager.py

# 3단계
cp era/era_order_manager.py scratch/replay/variants/LIVE_applied.py
python3 run_set.py --variants era_A LIVE_applied --modes cold --baseline era_A
python3 run_set.py --variants era_A LIVE_applied --modes warm
python3 compare.py --a out/era_A/snap_*_t24_s130000.jsonl --b out/LIVE_applied/snap_*_t24_s130000.jsonl
python3 run_shim_check.py
```
