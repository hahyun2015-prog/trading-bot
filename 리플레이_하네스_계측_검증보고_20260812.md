# 리플레이 하네스 — 계측·스트림 공급 구축 보고

작성: 2026-08-12 / 범위: **계측과 스트림 공급까지.** A/B 대조·음성 대조는 다음 세션
전제: 라이브 원본 3개 파일(`era/era_order_manager.py`, `indicators.py`, `bqa/kalman_backtester.py`) 무수정. ERA 미기동. 주문 API 호출 0건.

---

## 0. 결론

세 가지가 다 됐고, 하나는 예상대로 안 맞았다.

| 항목 | 결과 |
|---|---|
| 스트림 공급기(증분 구축 + 틱 근사) | 완성. 8/11·8/12 전 구간 리플레이 성공 |
| 지표·판정·주문 계측 | 완성. 라이브 코드 무수정으로 봉경계 전/후·틱·게이트·주문 전부 기록 |
| 미완성 봉 5지점 취급 검증 | **5/5 전부 관측.** 14개 실행 조합 전부에서 동일 |
| 조기반환 게이트 기록 | 기록됨. 8/11 기준 **틱의 50%가 무증상 차단** |
| 라이브 로그와의 일치 | **크게 다름.** 8/11 진입 라이브 22건 vs 리플레이 3건(4틱/봉) |

마지막 항목의 원인은 조사해서 규명했다 — **스트림 결함이 아니라 틱 해상도 한계**다. 근거는 3-3에 있다.

---

## 1. 만든 것

```
scratch/replay/
  clock.py            # 리플레이 클럭 + datetime.now()/time.time() 동결
  stream.py           # 시드 DB 증분 구축 + 봉→틱 근사
  instrument.py       # 게이트 지도(AST) + settrace 추적기 + read_sql 인터셉트
  replay_harness.py   # 본체 (유일한 실행 진입점)
  analyze.py          # 스냅샷 → 체크리스트·게이트 통계 자동 대조
  qt_shim.py, guard.py  # 직전 세션 산출물, 그대로 사용
  out/era_A/
    snap_<날짜>_<틱순서>_t<틱수>.jsonl   # 스냅샷
    report_<...>.json                    # 실행 리포트 + 가드 결과
    analysis_all.json                    # 체크리스트 자동 판정
```

실행:

```
python3 replay_harness.py --variant era_A --date 20260811 --tick-order OHLC --ticks-per-bar 24
python3 analyze.py out/era_A/snap_*.jsonl
```

### 1-1. 직전 세션이 남긴 결정 사항 처리

| 결정 | 처리 | 결과 |
|---|---|---|
| `trade_ledger` 정상 로드 | `era/trade_ledger.py`를 변이본 디렉터리로 복사하고 `sys.path`에 추가 | **정상 로드됨.** 스텁으로 대체하지 않았다. 원장 파일은 `workspace_root` 리디렉션으로 샌드박스 안에 생성 |
| `datetime.now()` 고정 | `era_X.datetime`을 `FrozenDatetime` 서브클래스로 교체 | 적용. 서브클래스로 만들어 `strptime`·`isinstance`·생성자 경로가 안 깨짐 |
| 무증상 조기반환 게이트 계측 | AST 게이트 지도 + `sys.settrace` 라인 추적 | 적용. `_process_day_tick`의 return 43곳 + 낙하지점 전부 지도화 |

**추가로 발견해서 처리한 것 — `time.time()`도 동결이 필요했다.**
재진입 쿨다운(방향무관 90초, LONG 301초)과 타임스톱이 `datetime`이 아니라 `time.time()`을 쓴다(`_is_reentry_allowed` L1744~). 이걸 놔두면 리플레이 안에서 경과시간이 실시간 기준 ≈0초로 계산되어 **첫 청산 이후 모든 재진입이 차단**된다. 하루 22건이 1건으로 끝난다. `clock.freeze()`가 `time.time`을 프로세스 전역으로 교체하고 종료 시 `thaw()`로 복원한다.

---

## 2. 스트림 공급기

### 2-1. DB를 통째로 주지 않는다

지시대로 **시간에 따라 증분 구축**했다.

1. 실 `futures_data.db`를 로컬 디스크로 1회 스냅샷 복사(읽기 전용). 마운트 폴더에서 sqlite가 간헐적으로 `disk I/O error`를 냈고, 부수적으로 TCA·야간수집기가 실 DB에 쓰는 동안에도 리플레이 입력이 고정된다.
2. 그 스냅샷에서 **`date < 리플레이 시작` 인 행만** 변이본 DB로 옮긴다. 8/11 리플레이의 시드는 3,506봉(2026-02-13 ~ 2026-08-10).
3. **당일 봉은 시드에 넣지 않는다.** 라이브 코드가 틱에서 직접 만들어 `_flush_ohlcv_buffer()`로 써 넣는다.
4. KIS 야간수집 코드(`A05608`/`A01609`)는 5분마다 `date <= 리플레이 클럭` 인 행만 증분 공개한다. 통째로 주면 칼만 주간 타점의 야간 병합이 미래 데이터를 보게 된다.

**이 방식이 미완성 봉 문제를 실제로 드러냈다.** 봉 경계 스냅샷의 `incomplete_bar_in_buffer` 필드에 매번 `v=1`(틱 1개), `o=h=l=c`인 봉이 찍힌다 — 설계안 1-2가 예측한 그대로다.

### 2-2. 틱 근사

- 1분봉이 있는 구간은 1분에 4틱, 5분봉만 있는 구간은 5분에 4틱. 8/11·8/12는 1분봉이 2026-08-04까지만 있어 **전 구간 5분봉 근사**다.
- 틱 시각은 봉 구간을 등분한 지점(마지막 틱도 구간 안에 머문다 — 라이브의 `period_str = minute//5*5`와 어긋나지 않게).
- 순서 두 가지: `O,H,L,C` / `O,L,H,C`.
- `--ticks-per-bar`로 밀도를 올릴 수 있다(waypoint 사이 선형 보간). **정확도를 높이는 게 아니라 밀도 민감도를 재기 위한 것**이다 — 3-3의 근거가 된다.

### 2-3. 이벤트 순서

`_on_receive_real_data` → `_handle_real_data`부터 라이브 경로를 그대로 탄다. `GetCommRealData`가 가짜 kiwoom을 통해 현재 틱 가격을 돌려준다.

- `QTimer.singleShot(0, ...)`는 **틱 처리 종료 직후** 실행 — 라이브의 이벤트루프 양보와 같은 순서
- `QTimer.singleShot(30000, ...)`(주문락 타임아웃)는 리플레이 클럭에 예약
- `futures_atr_timer`(15분, 지점 C)는 리플레이 클럭 기준으로 발화. 8/11 하루에 28회 발화
- 나머지 주식·연결감시 타이머는 등록만 하고 발화시키지 않음(결정성)

### 2-4. 체결 시뮬레이션 — 없으면 리플레이가 하루 1건에서 멈춘다

`_execute_futures_direct`는 주문만 보내고 **포지션을 만들지 않는다.** `futures_positions`가 생기고 `futures_order_locked`가 풀리는 것은 체잔 콜백(L6704~6782)뿐이다. 콜백을 재현하지 않으면 진입 1건 후 락이 영원히 걸린다.

그래서 주문이 나가면 가짜 kiwoom의 `GetChejanData` 응답(FID 913/302/9001/9203/910/911/905)을 채우고 **라이브의 `_handle_chejan_data`를 그대로 호출**한다. 체결가는 그 틱 가격(즉시체결 가정). 포지션 생성·락 해제·재진입 필터용 청산가 기록이 전부 라이브 코드 안에서 일어난다.

---

## 3. 계측

### 3-1. 기법 — 라이브 코드를 못 고치므로

**기법 A: `sys.settrace` 라인 추적.**
`_process_day_tick` 프레임에만 지역 트레이서를 붙여 실행 라인을 모으고, return 이벤트에서 **어느 줄로 빠져나갔는지**를 잡는다. 그 라인 번호를 AST로 자동 생성한 게이트 지도에 대조해 이름을 얻는다. 라인 번호를 하드코딩하지 않으므로 라인이 밀리는 변이본(B)에도 그대로 쓸 수 있다.

여기에 **낙하지점(FALLTHROUGH) 지도**를 추가했다. `return`을 안 지나고 함수가 그냥 끝나는 경우가 오히려 더 많기 때문이다. 대표적으로

```python
if not self.enable_reentry_filter or self._is_reentry_allowed("LONG", ...):
        ... 진입 ...
# ← False면 여기서 함수 끝. return 문도 없고 로그도 없다.
```

이게 8/11 리플레이에서 **가장 많이 걸린 차단 지점**이다(2,075틱 중 668틱).

**기법 B: `pandas.read_sql` 인터셉트.**
각 지표 함수가 실제로 읽어간 봉 구간을 기록한다. BB 쿼리는 `SELECT close ...`라 date 컬럼이 없으므로, 같은 커넥션에 `SELECT date, close ...` 동반 쿼리를 한 번 더 던져 봉 타임스탬프를 복원한다. **이게 없으면 BB가 미완성 봉을 포함하는지를 계측이 전혀 볼 수 없다.**

지표 함수의 return 시점 지역변수(`daily`, `rows`, `last_row`, `_prev_ma` 등)도 함께 포획한다. 일봉 ATR이 오늘 일봉을 잘라냈는지는 `daily.index[-1]`로만 확인 가능하다.

### 3-2. 스냅샷 종류

| kind | 시점 | 내용 |
|---|---|---|
| `bar` | 봉 경계, `_refresh_futures_targets_deferred` **직전·직후 두 번** | 지표 스칼라 전량(pre/post), `incomplete_bar_in_buffer`, `input_provenance`(각 지표의 df 첫/마지막/끝에서둘째 봉 타임스탬프), `captured_locals` |
| `tick` | 매 틱 | 가격, SAR(value/ep/af/bull), 세션 고저, 포지션, **게이트 판정**(exit_line·게이트명·무증상여부·표식 도달 여부) |
| `order` | 주문 의도 | 시각·방향·가격·수량·`SendOrderFO` 인자·차단 주체 |
| `fill` | 체결 시뮬 | 체결가·수량·구분·직후 상태 |
| `atr_timer` | 15분 ATR 타이머 | 지점 C의 pre/post + 일봉 provenance |
| `session_init` | 세션 시작 | prev_range·ATR 초기 산출 |

`df_last_date`는 지시대로 전부 기록했다. 봉 경계마다 `input_provenance`에 지표별로 `input_first_date` / `input_last_date` / `input_2nd_last_date`가 남는다.

---

## 4. 미완성 봉 취급 체크리스트 — 검증 결과

**14개 실행 조합(2일 × 2틱순서 × 5밀도) 전부에서 5/5 관측.**

| 지점 | 기대 | 관측 (8/11, 봉경계 82회) | 판정 |
|---|---|---|---|
| 칼만 평활·std_error·15분추세 | 포함 | 입력 df 마지막 봉 == 진행 중 봉 : **82/82** | OK |
| BB 중심선·밴드폭·스퀴즈 | 포함 | BB 입력 마지막 봉 == 진행 중 봉 : **82/82** | OK |
| MA200 방향필터 | 제외 | MA 참조 봉 != 진행 중 봉 : **82/82** | OK |
| 일봉 ATR | 제외 | 일봉 마지막 < 오늘(리플레이 클럭) : **28/28** | OK |
| prev_range | 제외 | 채택일 != 오늘 : **1/1** (8/11 → 8/10 채택) | OK |

### 4-1. 핵심 — 같은 함수 안에서 BB와 MA의 기준이 다른 것이 드러나는가

**드러난다. 82/82 봉 전부에서 두 값이 서로 다른 봉을 참조한다.**

`update_bb_psar_filters` 한 번 호출의 스냅샷 예시(8/11):

| 진행 중 봉 | BB 사용 봉 `iloc[-1]` | MA 사용 봉 `iloc[-2]` | BB행 종가 | MA행 종가 | bb_mid | MA200 |
|---|---|---|---|---|---|---|
| 20260811085000 | **20260811085000** | 20260811084500 | 967.00 | 967.16 | 976.483 | 979.411 |
| 20260811085500 | **20260811085500** | 20260811085000 | 966.64 | 966.66 | 975.756 | 979.308 |
| 20260811090000 | **20260811090000** | 20260811085500 | 970.14 | 970.08 | 975.430 | 979.245 |
| 20260811090500 | **20260811090500** | 20260811090000 | 971.06 | 971.10 | 974.994 | 979.180 |

BB가 쓰는 행의 타임스탬프는 버퍼의 진행 중 봉과 **정확히 일치**하고, MA가 쓰는 행은 항상 한 봉 전이다. 두 종가가 실제로 다른 봉도 82봉 중 75봉(나머지 7봉은 우연히 값이 같음).

즉 **설계안 1-2가 지적한 자리를 계측이 숫자로 집어낸다.** `indicators.py`의 `Window.closed_upto`로 BB를 그대로 옮기면 이 스냅샷에서 `BB_사용봉`이 한 칸 밀리므로 즉시 잡힌다.

### 4-2. 일봉 ATR과 prev_range

- ATR: 8/11 리플레이 내내 `atr_daily_last_date = 20260810`으로 고정. 오늘(8/11) 일봉은 5분봉 입력에 들어 있으나 `daily.iloc[:-1]`로 잘려 나간다. 일봉 개수 38개.
- prev_range: 후보일 `[20260810, 20260807, 20260806]` 중 오늘이 아닌 `20260810` 채택, 31.56pt.
  8/12 리플레이에서는 `20260811` 채택, **38.22pt** — 라이브 로그의 `전일 Range 로드 완료: 38.22pt (조회코드: A0568000, 날짜: 20260811)`와 **정확히 일치**한다.

### 4-3. 세션 초기값 대조 (8/12)

| 항목 | 라이브 로그 | 리플레이 | 차이 |
|---|---|---|---|
| prev_range | 38.22pt (20260811) | 38.22pt (20260811) | 0 |
| Kalman ATR | 73.93pt | 73.94pt | 0.01pt |
| 손절/익절 | 73.93 / 147.86 | 73.94 / 147.89 | 0.01 / 0.03 |

ATR 0.01pt 차이는 라이브의 계산 시점 DB 상태(그 순간까지 쌓인 당일 봉)와 리플레이 시드의 미세한 차이에서 나온다. **이 크기의 차이는 off-by-one이 아니다** — off-by-one이면 일봉 하나가 통째로 빠져 수 pt 단위로 벌어진다. 다음 세션의 A/B 대조에서 1e-9 문턱을 쓰기 전에, 이 0.01pt가 어디서 오는지는 A vs A2 양성대조(P0)로 먼저 걸러야 한다.

---

## 5. 조기반환 게이트 — 실제로 기록되는가

기록된다. 8/11 (24틱/봉, 2,075틱) 분포:

| 틱수 | 무증상 | 게이트 |
|---:|:---:|---|
| 668 | **예** | 재진입필터 미통과 (`_is_reentry_allowed` False → 낙하) |
| 428 | 아니오 | 포지션 보유 중 (신규 진입 불가) |
| 220 | 아니오 | 이평선방향(LONG차단) |
| 200 | **예** | 진입종료시각 `entry_end` (15:00 이후) |
| 115 | 아니오 | BB 스퀴즈 |
| 106 | **예** | 돌파 없음 (`target_short`도 미달) |
| 100 | 아니오 | std_error < 1.5 |
| 93 | 아니오 | BB 방향성(LONG차단) |
| 55 | 아니오 | 08:45 강제청산 구간 |
| 50 | **예** | 09:10 이전 |
| 20 | **예** | 시초가 미설정 |
| 10 | 아니오 | 진입 주문 집행 후 종료 |
| 10 | 아니오 | SAR 청산 조건 |

**무증상 차단이 1,044틱 — 전체의 50.3%.** 라이브 로그만 봐서는 이 절반이 왜 진입하지 않았는지 알 수 없다. 가장 큰 668틱(재진입필터)은 `_process_day_tick` 입장에서 return 문조차 없이 그냥 함수가 끝나는 자리다.

`_process_day_tick`의 return 43곳을 전부 지도화했고, 그 중 소스상 무증상(로그·`_note_entry_block` 동반 없음)이 10곳이다. 여기에 낙하지점까지 더해 계측한다.

---

## 6. 라이브 로그와의 대조 — 크게 다르고, 원인은 규명했다

### 6-1. 결과

라이브 실측(era 로그, `[주간선물 주문]` 라인 기준):
- **8/11: 진입 22건 / 청산 22건, 전부 LONG, 15계약**
- **8/12: 진입 5건 / 청산 5건, 전부 LONG, 1계약** (10:35 ERA 정지)

리플레이(전부 LONG, 방향은 일치):

| 틱/봉 | 8/11 총틱 | 8/11 진입 | 8/12 총틱 | 8/12 진입 |
|---:|---:|---:|---:|---:|
| 4 (O,H,L,C) | 332 | 3 | 92 | 2 |
| 12 | 1,079 | 7 | 299 | 3 |
| 24 | 2,075 | 10 | 575 | 2 |
| 48 | 4,067 | 14 | 1,127 | 7 |
| 96 | 8,051 | 16 | 2,231 | 6 |
| **라이브** | 수만 틱 | **22** | 수천 틱 | **5** |

### 6-2. 원인 — 스트림 결함이 아니라 틱 해상도

세 가지 근거.

**(1) 진입 건수가 틱 밀도에 단조 증가하며 라이브 값으로 수렴한다.** 8/11: 3 → 7 → 10 → 14 → 16 (라이브 22). 스트림에 구조적 결함이 있다면 밀도를 올려도 특정 값에 갇히거나 발산한다. 매끄럽게 수렴한다는 것은 공급 자체는 맞고 **해상도만 모자라다**는 뜻이다.

**(2) 지점 D(SAR)가 봉이 아니라 틱 기반이다.** 설계안 1-2에서 확인한 대로 `sar_ep`가 봉 고저가 아니라 `current_price`로 갱신되고 AF도 틱마다 오른다. 4틱/봉이면 AF가 라이브 대비 수십 배 느리게 상승해 SAR이 가격을 훨씬 늦게 따라붙는다. 결과적으로 **청산이 늦어지고 → 보유시간이 길어지고 → 하루 진입 횟수가 줄어든다.**

실측 평균 보유시간(8/11): 4틱/봉 = **57.1분**, 24틱/봉 = **8.8분**.
라이브 로그에는 시각이 찍혀 있지 않아 직접 측정할 수 없으나, 진입 22건이 09:10~15:00(350분)에 들어갔고 매 청산마다 방향무관 90초 + LONG 301초 쿨다운이 붙으므로 사이클당 최소 약 6.5분의 공백이 강제된다 → **라이브 보유시간은 약 9분대**로 추정된다. 24틱/봉의 8.8분과 거의 일치한다. 이 방향의 정황 증거가 하나 더 있는 셈이다.

**(3) 가격 수준과 방향은 맞는다.** 8/12 리플레이 진입가 1011.20 / 1017.14 vs 라이브 1010.94 / 1013.08 / 1015.38 / 1016.66 / 1019.36. 같은 가격대에서 같은 방향(전부 LONG)으로 진입하며, 리플레이는 그 중 일부만 잡는다. 없는 자리에서 진입하거나 반대 방향으로 가는 일은 없었다.

**(4) 봉 내부 경로 가정이 실제로 결과를 바꾼다.** OHLC vs OLHC (24틱/봉, 8/11): 2,075틱 중 SAR 값이 다른 틱 1,624개, 게이트 판정이 다른 틱 653개, 주문 시각·가격 전부 다름(진입 건수는 우연히 둘 다 10). 즉 H/L 순서를 모르는 것이 SAR 궤적과 진입 시각에 직접 영향을 준다 — 설계안 3-1이 예측한 그대로다.

### 6-3. 이 차이가 A/B 대조를 약화시키는가 — 아니다

하네스는 A와 B에 **완전히 동일한 합성 스트림**을 준다. 따라서 A≠B는 근사와 무관하게 코드 차이에서만 나온다. 근사가 제한하는 것은 "라이브가 실제로 그렇게 움직였는가"이지 "교체 전후가 같은가"가 아니다.

다만 **한 가지 실질적 제약**이 생겼다. 4틱/봉으로는 8/11 하루에 진입이 3건뿐이라 주문 시퀀스 대조의 표본이 너무 얇다. 다음 세션의 A/B 대조는 **24틱/봉 이상**을 기본으로 잡기를 제안한다(8/11 기준 진입 10건, 실행 5초). 지표 대조는 봉 경계 기반이라 틱 밀도와 무관하게 82개 표본이 확보된다.

---

## 7. 안전 확인

14회 실행 전부에서:

| 항목 | 결과 |
|---|---|
| 가드 위반 | **0건** |
| 실 API 주문 호출 | **0건** (모든 `SendOrderFO`가 `FakeQAxWidget.dynamicCall`에서 기록 후 0 반환) |
| 네트워크 접속 시도 | **0건** |
| 서브프로세스 실행 | **0건** |
| 샌드박스 밖 파일 쓰기 | 0건 (리포트에 잡힌 64건은 전부 파이썬 라이브러리 `__pycache__` 생성) |
| 라이브 원본 수정 | 없음. `era_order_manager.py` SHA-256 바이트 동일 확인 |
| 실 DB 접근 | 읽기 전용 스냅샷 복사 1회. `mode=ro` 외 연결 없음 |
| `config/active_strategy.json` 등 보호파일 | 무접촉. 변이본은 샌드박스 사본만 읽음 |
| ERA 기동 | 없음. `era/era.pid` 무접촉. Kiwoom OCX 미생성 |

라이브 코드가 쓰는 `futures_exit_state.json`, `system_status_futures.json`, 원장 파일은 전부 `workspace_root` 리디렉션으로 `/tmp/replay_ws/era_A/ws/` 아래에 생성됐다.

---

## 8. 남은 한계와 다음 세션 준비

### 8-1. 이번 계측이 못 보는 것

- 실제 분 내 틱 경로(원시 틱 미저장). SAR EP/AF 궤적과 진입 시각은 근사
- 틱 간격·빈도. `_is_sustained_tick_move`, 이상치 필터, 락아웃은 표면만 탐
- 체결가·슬리피지·부분체결(즉시체결 가정)
- Kiwoom TR 콜백, 잔고 동기화, 미체결 관리
- 주식·연결감시 타이머 경로(의도적으로 미발화)
- 수량: 결정성을 위해 `futures_fixed_qty=1` 주입. 일일손실한도가 `pct=0`으로 비활성이라 게이트 판정에는 영향 없음

### 8-2. 다음 세션에 넘기는 것

1. **P0 양성대조 먼저.** `era_A` vs `era_A2`(바이트 동일 복사본). 전 항목 0 차이가 나와야 한다. 8/12 ATR의 0.01pt 차이가 하네스 노이즈인지 라이브-리플레이 입력 차이인지는 P0에서 갈린다
2. **N1~N6 음성대조.** 특히 N1(BB `iloc[-1]`→`iloc[-2]`)은 4-1 표의 `BB_사용봉` 필드가 그대로 잡아낼 것이다
3. **틱 밀도 24 이상**을 기본값으로
4. **래그 탐지기**(shift ∈ {-2..+2}) 미구현. 대조 스크립트와 함께 다음 세션에서

### 8-3. 판단이 필요한 것

설계안 4부 3번(SAR 의미 차이)에 대한 답이 아직 없다. **지점 D를 봉 기반으로 바꾸면 하네스는 반드시 FAIL을 낸다.** 6-2에서 확인했듯 SAR은 틱 밀도에 극도로 민감해서, 봉 기반으로 바꾸면 진입·청산 시각이 통째로 달라진다. `indicators.py`의 `SarState.on_tick`으로 보존할지, 바꾸고 그 FAIL을 "의도된 변경"으로 승인할지 미리 정해 주시면 B 제작에 바로 반영합니다.
